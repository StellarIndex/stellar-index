---
galaxy_info:
  role_name: haproxy
  author: ratesengine
  description: >-
    HAProxy + keepalived api-tier load balancer per
    docs/architecture/ha-plan.md §3.1. Two LB hosts share a VIP
    via VRRP; HAProxy fronts the ratesengine-api pool with /readyz
    health checks.
  license: Apache-2.0
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble

dependencies: []
