# Cross-File Interaction Ledger

Use this ledger for material coupling. IDs start at `XFI-0001`.

## Register

| ID | Status | Interaction | Source Files | Target Files | Contract | Failure Modes | Tests/Evidence | Findings |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| XFI-0001 | open | R1 firewall design -> installed host firewall -> external exposure | `configs/ansible/roles/archival-node/tasks/11-firewall.yml`; `configs/ansible/roles/archival-node/templates/nftables.conf.j2`; `configs/ansible/roles/archival-node/defaults/main.yml`; live `/etc/nftables.conf` | R1 `nftables`, `ufw`, `ss`, public TCP reachability | nftables is enabled, UFW disabled, input policy default-drop, and only explicitly justified public ports are reachable from the internet; any captive-core ingress policy matches source config and runbooks. | Live R1 now blocks the reviewed internal-service ports externally under active nftables/default-drop, but `/etc/nftables.conf` still explicitly accepts `{11625,11626,11725,11726}` and `stellar-core` listens on `0.0.0.0:11726`; a refreshed public probe to `11726/tcp` on 2026-05-13 still succeeds even though the repo template comments say captive cores dial out rather than accept inbound. | R1-0005; R1-0006; R1-0008; R1-0024; EV-0019; EV-0113; EV-0172 | F-1201 |
| XFI-0002 | fixed | API route table -> OpenAPI source -> generated reference -> generated TS types -> frontend/status/examples/smoke scripts | `internal/api/v1/server.go`; `openapi/rates-engine.v1.yaml` | `docs/reference/api/rates-engine.v1.yaml`; `web/explorer/src/api/types.ts`; `web/explorer/**`; `web/status/**`; `examples/**`; `scripts/dev/audit-public-api.sh`; `scripts/dev/r1-smoke.sh` | Removed HTTP routes must disappear from generated artifacts, active consumers, smoke/audit scripts, machine-facing docs, and production deployment in the same change. | F-1203 closed by wave 31 (2026-05-12) committing the residual `pnpm generate:api` regen so the explorer TS types match the OpenAPI source; running the regen on `HEAD` is now a no-op. F-1204 closed by wave 56 (2026-05-13) — `audit-public-api.sh` migrated to `/v1/assets`, and `llms.txt` rewritten to drop the inline `formerly /v1/coins` parenthetical the audit's literal-string grep was tripping on. | EV-0065; EV-0066; EV-0067 | F-1203; F-1204 |
| XFI-0003 | fixed | Operational timer files -> install/deploy path -> R1 installed timers -> monitoring rules/runbooks | `deploy/systemd/sla-probe.timer`; `deploy/systemd/verify-archive-tier-a.timer`; `deploy/systemd/supply-snapshot.timer`; `deploy/systemd/archive-completeness.timer`; `deploy/monitoring/rules/*.yml`; `docs/operations/runbooks/*` | R1 systemd units/timers; Prometheus textfile metrics | SLA, archive verification, archive completeness, and supply-snapshot timers are installed and produce evidence metrics expected by alert rules and runbooks. | Direct R1 verification now shows the SLA timer path live: `ratesengine-sla-probe.timer` enabled/active, wrapper and binary present, a recent successful service run, and `sla_probe.prom` written. The probe's failing verdict is tracked as `XFI-0097` / `F-1305`; the separate blank external Healthchecks URL channel is tracked as `XFI-0102` / `F-1310`. | R1-0002; R1-0003; R1-0004; R1-0025; R1-0031; R1-0032; EV-0113; EV-0172; EV-0278; EV-0280 | F-1205 |
| XFI-0004 | open | Launch-readiness backlog -> CI/local verify target -> public flip decision | `scripts/ci/verify-launch-ready`; `docs/architecture/launch-readiness-backlog.md`; `Makefile` | local/CI release decision process | A public flip should not proceed while launch readiness rows are red. | Canonical `make verify` passes while refreshed `go run ./scripts/ci/verify-launch-ready` still fails on L4.14-L4.17, L5.6, and L5.8; developers can have a green pre-push gate with known public-launch blockers. | EV-0009; EV-0013; EV-0170; EV-0298; EV-0313 | F-1206 |
| XFI-0005 | open | Web package pins -> CI/dependabot coverage -> hosted dependency alerts -> deployed web apps | `web/explorer/package.json`; `web/dashboard/package.json`; `web/status/package.json`; `.github/workflows/ci.yml`; `.github/dependabot.yml` | Cloudflare Pages builds and public explorer/dashboard/status pages | Web apps should receive automated dependency updates, hosted vulnerability alerting, and CI should fail on critical/high web advisories. | Apps now pin `next@15.5.18`, CI runs `pnpm audit --audit-level high`, Dependabot npm ecosystems exist for all three apps, and refreshed high-level audits report only one moderate advisory per app. Fresh GitHub API evidence still shows vulnerability alerts disabled and Dependabot alerts disabled. | EV-0014; EV-0051; EV-0099; EV-0114; EV-0171; EV-0284; EV-0298; EV-0313; `.github/workflows/ci.yml`; `.github/dependabot.yml` | F-1207 |
| XFI-0006 | open | R1 source health -> API readiness/status -> customer/SLA truth | R1 indexer/aggregator/API logs; Prometheus alerts | `/v1/healthz`; `/v1/readyz`; `/v1/status`; status page probes; runbooks | Health/readiness/status should represent material ingest and latency degradations or deliberately scope themselves to process liveness only. | The served status rollup now reports `overall=degraded` with 10 active incidents. Prometheus shows anomaly-freeze, supply, verify-archive, host-memory, SLA unit-failed, SLO burn, and API p95/p99 latency alerts. Capacity remains tight enough that operational degradation is still material. | R1-0009; R1-0010; R1-0029; R1-0030; R1-0039; R1-0047; EV-0175; EV-0294; EV-0315 | F-1208; F-1209; F-1210; F-1305; F-1311 |
| XFI-0007 | fixed | Status-page implementation -> incident runbook -> customer communication templates | `web/status/**`; `internal/incidents/data/**`; `docs/operations/status-page-setup.md`; `docs/operations/runbooks/sev-status-page-update.md`; `docs/architecture/launch-readiness-backlog.md`; `docs/launch-task-list.md`; `CLAUDE.md`; `deploy/comms/README.md`; `deploy/comms/incident-update.md` | `status.ratesengine.net` incident publication workflow | Operators should have one current, executable status-page path for opening/updating incidents, and customer-comms templates should point to the same incident system. | Wave 126 removed the retired Upptime/cstate/status-repo model from the active orientation, launch, and comms surfaces; the shipped `web/status` Cloudflare Pages app is now the only claimed status-page implementation in the reviewed active prose. | EV-0021; EV-0178; CMD-0024; CMD-0178 | F-1211 |
| XFI-0008 | fixed | Dashboard key form -> dashboard key handler -> platform API key row -> auth subject -> rate-limit middleware | `web/dashboard/src/app/keys/page.tsx`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/ratelimit.go` | Account tier and billing policy should cap customer-controlled rate limits; runtime enforcement should not accept a self-selected higher paid-tier budget from free accounts. | Current `HEAD` now clamps persisted dashboard-key budgets to `platform.Tier.MaxRateLimitPerMin` before insert, and targeted tests cover the tier ladder, so the original free-account privilege-escalation path no longer reproduces. | EV-0089 | F-1212 |
| XFI-0009 | fixed | Stablecoin fiat proxy -> classic USD peg config -> Stellar DEX quote decimals -> min USD volume gate | `configs/example.toml`; `cmd/ratesengine-aggregator/main.go`; `internal/aggregate/stablecoin.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/storage/timescale/usd_volume_quote_spec.go`; `internal/storage/timescale/trades.go`; `internal/sources/{sdex,soroswap,phoenix,comet,aquarius}/**` | Fiat-USD VWAP source expansion and the manipulation-resistance `aggregate.min_usd_volume` threshold | When classic/SAC USD-pegged Stellar quotes are treated as fiat-USD contributors, the volume gate must use the same quote decimals as the trade insert USD-volume code, or use the stored `usd_volume` value rather than raw quote amounts. | Current source computes USD totals from the source pair before rewrite and applies 7-decimal math for configured classic USD pegs; the dedicated `$10k classic-USDC` regression proves the earlier 10x undercount no longer reproduces. | EV-0024; R1-0011; EV-0116 | F-1213 |
| XFI-0010 | open | Local governance policy -> hosted GitHub settings -> release/deploy workflow trust | `CONTRIBUTING.md`; `CODEOWNERS`; `.github/workflows/{ci,release,deploy,docs-deploy,explorer-deploy,status-page}.yml`; `scripts/ci/lint-actions-pinning.sh`; `docs/operations/deploy-workflow.md` | GitHub branch protection, rulesets, environment protection, Actions policy, release/deploy permissions | Protected main should enforce reviews, signed commits, required checks, and deployment approvals; workflows should restrict third-party action supply-chain risk. | Fresh GitHub API evidence still shows `main.protected=false`, latest main commit `e9e57cb2...` unsigned, all five deployment environments with empty protection rules plus admin bypass, and hosted Actions policy `allowed_actions=all` with `sha_pinning_required=false`. The workflow token default is read-only, but the hosted allow-list/SHA-pinning and deployment-approval controls remain absent. | EV-0025; EV-0026; EV-0104; EV-0176; EV-0284; EV-0298; EV-0313 | F-1214; F-1215; F-1216 |
| XFI-0011 | fixed | SEP-10 challenge generation -> signed challenge verification -> optional replay guard -> JWT issuance -> downstream bearer auth | `cmd/ratesengine-api/main.go`; `internal/auth/sep10/validator.go`; `internal/auth/sep10/redisreplay.go`; `internal/api/v1/auth_sep10.go`; `internal/auth/jwt.go`; `internal/api/v1/middleware/auth.go`; OpenAPI auth docs | A signed SEP-10 challenge should be single-use whenever SEP-10 token issuance is enabled, and startup should fail closed if replay protection cannot be enforced. | Current workspace now fails startup when `auth_mode=sep10` is selected without Redis, eliminating the guard-free deployment mode that made this interaction unsafe. | EV-0096 | F-1217 |
| XFI-0012 | fixed | Public signup form/OpenAPI -> signup handler -> verifier/token email -> API-key verified-state marker -> rate-limit/auth middleware -> deployed config | `web/explorer/src/app/signup/page.tsx`; `openapi/rates-engine.v1.yaml`; `internal/api/v1/{signup,signup_verify,server}.go`; `internal/auth/{signup_tracker,signup_verifier}.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/middleware/*`; R1 `/etc/ratesengine.toml` | Anonymous self-service signup should not let attackers cheaply mint many 1000/min keys, duplicate/idempotency checks must stay atomic, and the verification flow must actually influence runtime access if the product claims it does. | Wave 126 flips `signup_require_email_verification` to true by default. Pre-launch R1 has no consumer traffic; operators must now opt in explicitly if they want immediately usable unverified signup keys. | EV-0028; EV-0099; EV-0127; EV-0143; EV-0144; EV-0145; EV-0146; EV-0165; EV-0172; R1-0021; R1-0026 | F-1218 |
| XFI-0013 | fixed | Stripe webhook -> Redis key manager -> platform billing event/audit stores -> dashboard/Postgres keys and subscriptions | `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go`; `internal/auth/*`; `internal/platform/billing.go`; `internal/platform/postgresstore/*`; `migrations/0027_platform_v1_schema.up.sql`; dashboard key UI | Paid upgrades should update the same account/key/subscription source of truth that dashboard users see, with durable webhook idempotency/audit. | F-1219 closed by wave 55 (2026-05-13). `StripePlatformBridge` gained an `APIKeys platform.APIKeyStore` slot; `applyAccountTierAndKeyUpgrade` calls `upgradePlatformAPIKeys` after the account-tier bump to `ListForAccount` + `Update` every active dashboard-created Postgres key with `RateLimitPerMin < target` up to the new tier's budget. Idempotent + revoked-aware. Production wiring in `cmd/ratesengine-api/main.go` plugs `postgresstore.NewAPIKeyStore(pgStore)` into the bridge. Regression test `TestStripeWebhook_PlatformBridge_LiftsPostgresKeys` proves the four-key fixture (lift below-target, skip already-above-target, skip revoked), and the wave-65/66 metric tests plus runbook make platform-store partial-failure handling explicit. | EV-0030; EV-0053; EV-0107; EV-0108; EV-0112; EV-0130; EV-0142; EV-0165; EV-0168 | F-1219 |
| XFI-0014 | fixed | Release artifact -> deploy workflow -> host binaries -> migrations/schema -> rollback docs | `.github/workflows/release.yml`; `.github/workflows/deploy.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml`; `migrations/*`; `docs/operations/{deploy-workflow,release-process}.md`; `CLAUDE.md`; `docs/operations/r1-deployment-state.md`; `docs/operations/runbooks/fx-history-missing.md` | A tagged deploy that can change code expecting a new schema must also ship/apply the matching migrations, gate readiness on schema compatibility, and document the actual release artifacts/rollback artifacts operators can use. | F-1220 closed when the deploy job began installing the Ansible collection set used by the migration-staging playbook; F-1222 closed when SEV runbooks switched to the actual `.prev-<tag>` binary backup layout; F-1221 closed when active release docs were reconciled to the linux/amd64 binary-only release workflow and dropped GHCR/arm64 claims. | EV-0031; EV-0032; EV-0103; EV-0261; R1-0013 | F-1220; F-1221; F-1222 |
| XFI-0015 | fixed | Source Caddyfile -> deployed Caddyfile -> API remote-IP trust -> public metrics boundary | `configs/caddy/Caddyfile.api`; `configs/caddy/README.md`; `cmd/ratesengine-api/main.go`; `internal/api/v1/middleware/remoteip.go`; R1 `/etc/caddy/Caddyfile`; R1 `/etc/ratesengine.toml` | The deployed edge config should match the reviewed source: Cloudflare client IP should be resolved before forwarding to the API, and `/metrics` should be blocked on the public hostname. | Live R1 now carries the Cloudflare `trusted_proxies` + `client_ip_headers` block, forwards `{client_ip}` downstream, and public `/metrics` returns HTTP 404. | EV-0033; R1-0014; EV-0113 | F-1223 |
| XFI-0016 | fixed | Request middleware remote-IP resolution -> dashboard magic-link/session audit fields -> platform user/token stores | `internal/api/v1/middleware/remoteip.go`; `internal/api/v1/middleware/logger.go`; `internal/api/v1/dashboardauth/handlers.go`; `internal/api/v1/dashboardauth/middleware.go`; `internal/platform/postgresstore/{token_store,user_store}.go`; `migrations/0027_platform_v1_schema.up.sql`; Caddy/API config | Dashboard login, magic-link, and session audit data should record the same resolved client IP that logger/rate-limit/auth use behind a trusted proxy. | F-1224 closed: `dashboardauth/handlers.go::clientIP` now reads `middleware.RemoteIP(r)` (the trusted-proxy-resolved client IP) and falls back to `r.RemoteAddr` only when the middleware didn't resolve an IP. Behind Caddy / Cloudflare the dashboard records the real customer IP for magic-link, session, and audit-log writes. | EV-0034; R1-0014 | F-1224 |
| XFI-0017 | fixed | Stablecoin fiat proxy policy -> CAGG history readers -> chart/history API consistency -> R1 historical XLM/USD behavior | `internal/api/v1/history.go`; `internal/api/v1/chart.go`; `internal/api/v1/vwap.go`; `internal/api/v1/ohlc.go`; `internal/api/v1/price.go`; `internal/storage/timescale/aggregates.go`; `[trades].usd_pegged_classic_assets` config; OpenAPI history/chart docs | Every historical USD price surface for Stellar pairs should apply the same declared stablecoin-fiat fallback or clearly mark why a series is unavailable. | Current source implements and tests `historySinceInceptionStablecoinFallback`, and refreshed R1 proof now shows `native/fiat:USD` since-inception returning the same 10 daily buckets as direct Circle-USDC. The earlier empty-series live drift no longer reproduces. | EV-0035; R1-0015; EV-0116; R1-0019; EV-0140; EV-0166; R1-0022; EV-0173; R1-0027; R1-0038; EV-0292 | F-1225 |
| XFI-0018 | fixed | Dashboard API-key policy fields -> Postgres key/account stores -> runtime auth validator -> request policy middleware/cache path -> rate/quota/permission enforcement -> usage attribution | `migrations/0027_platform_v1_schema.up.sql`; `internal/platform/apikey.go`; `internal/platform/account.go`; `internal/platform/postgresstore/{apikey_store,account_store}.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/auth/apikey_postgres.go`; `internal/auth/apikey_redis.go`; `internal/auth/touch_debouncer.go`; `internal/api/v1/middleware/{keypolicy,usage,monthly_quota,touch_usage}.go`; `internal/api/v1/account.go`; `internal/api/v1/server.go`; `internal/usage/counter.go`; `cmd/ratesengine-api/main.go`; OpenAPI/dashboard types | Customer-visible key restrictions, quota fields, and last-used fields should be enforced by the live request path; separately documented account-usage reporting semantics must stay truthful. | Current committed source now preserves IP/referer/permission policy through Redis cache hits, enforces positive monthly quotas through the request path with per-key/account override propagation, and wires Redis-debounced Postgres `TouchUsage` updates in production. The original enforcement interaction is fixed. The distinct `/v1/account/usage` documentation/model drift remains tracked under `XFI-0051` / `F-1259`, not this closed enforcement thread. | EV-0036; EV-0100; EV-0118; EV-0126; EV-0128; EV-0132 | F-1226 |
| XFI-0019 | fixed | Docker migration image -> migration binary defaults -> SQL migration directory -> self-hosted schema bootstrap | `docker/ratesengine-migrate.Dockerfile`; `cmd/ratesengine-migrate/main.go`; `migrations/*`; `Makefile`; `docker/README.md`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml` | A self-hosted migration image should contain the migration files it defaults to applying, or the image/docs/smoke tests should require and verify the expected bind mount/path. | F-1227 closed: `docker/ratesengine-migrate.Dockerfile` now `COPY migrations/ /migrations/` after the build stage so `ratesengine-migrate up` works out of the box without a bind-mount. Verified live on `HEAD`. | EV-0037 | F-1227 |
| XFI-0020 | fixed | API HTTP server timeout -> SSE stream handlers -> docs/customer demos -> R1 streaming behavior | `cmd/ratesengine-api/main.go`; `internal/api/streaming/handler.go`; `internal/api/v1/{price_stream,price_tip_stream,observations_stream}.go`; `openapi/rates-engine.v1.yaml`; `docs/operations/{cdn-setup,customer-demo-script}.md`; R1 API service | SSE endpoints should support long-lived client connections with heartbeat writes and reconnect semantics measured in minutes or hours, not an absolute 30s response deadline. | Source clears the SSE request write deadline through `ResponseController`, and refreshed R1 proof shows both loopback and public tip streams staying open until a 68-second client timeout while emitting frames/keepalives. The earlier 30-second server reset no longer reproduces. | EV-0038; R1-0016; EV-0119; R1-0020; EV-0141; EV-0166; R1-0023; EV-0173; R1-0028; R1-0036; EV-0289 | F-1228 |
| XFI-0021 | fixed | CDN verification script -> API query parameter contract -> streaming cache headers -> operator CDN signoff | `scripts/dev/verify-cdn.sh`; `internal/api/v1/{price,price_tip,price_tip_stream}.go`; `internal/api/streaming/handler.go`; `openapi/rates-engine.v1.yaml` | CDN verification should hit valid representative endpoints and assert the headers those handlers actually promise. | F-1229 closed: `scripts/dev/verify-cdn.sh` now uses the handler-required `asset=` / `quote=` params and asserts the actual SSE `Cache-Control: no-cache` directive (was probing `base=` and asserting `no-store`). | EV-0039 | F-1229 |
| XFI-0022 | open | RFP historical retention target -> backfill command -> CAGG history endpoint -> R1 deployed data depth | `docs/freighter-rfp.md`; `docs/architecture/coverage-matrix.md`; `cmd/ratesengine-ops/backfill.go`; `internal/api/v1/history.go`; `internal/storage/timescale/aggregates.go`; R1 Timescale/API | Public historical APIs should meet at least the stated one-year retention target for core Stellar pairs, or expose an explicit completeness/backfill status so clients do not mistake the deployment window for inception. | Refreshed R1 direct XLM/USDC and USD-fallback since-inception daily series now start at 2026-02-12 and return 84 points, but that is still short of one year/inception and both series have a daily gap from 2026-04-26 to 2026-05-03; endpoint provides no completeness warning. | EV-0040; R1-0017; EV-0173; R1-0027; R1-0041; EV-0300 | F-1230 |
| XFI-0023 | fixed | Branch protection assumptions -> canonical CI trigger policy -> direct-main push behavior -> release confidence | `.github/workflows/ci.yml`; `.github/workflows/api-audit.yml`; `CONTRIBUTING.md`; GitHub branch/environment settings | If canonical CI is PR-only, hosted branch protection must enforce PR merges; otherwise main pushes must run the full canonical gate. | Current `ci.yml` now runs on pushes to `main`, so direct code pushes no longer bypass the canonical workflow even while branch protection remains separately unresolved. | EV-0041; EV-0025; EV-0099 | F-1231 |
| XFI-0024 | fixed | Stablecoin catalogue/asset detail price -> `/v1/price` fallback chain -> batch price rows -> customer SDK parity | `internal/api/v1/price.go`; `internal/api/v1/assets.go`; `internal/api/v1/assets_f2.go`; `internal/api/v1/assets_coin_extension.go`; `openapi/rates-engine.v1.yaml`; R1 price/assets endpoints | A canonical stablecoin asset with a displayed USD price on asset detail should also price consistently through single and batch price APIs. | F-1232 closed: `internal/api/v1/price.go` peg-to-self short-circuit returns `1.0` when the requested asset IS one of the configured USD pegs (instead of querying for `<peg>/<peg>` which has no observations). Regression test: `TestPrice_StablecoinFiatProxy_PegItselfReturnsOne`. | EV-0042; R1-0018 | F-1232 |
| XFI-0025 | fixed | SDEX legacy XDR claim atoms -> op decoder adapter -> dispatcher metrics -> backfill historical coverage claims | `internal/sources/sdex/decode.go`; `internal/sources/sdex/dispatcher_adapter.go`; `internal/dispatcher/dispatcher.go`; `internal/pipeline/processor.go`; `cmd/ratesengine-ops/backfill.go`; `docs/discovery/protocol-versions.md`; `docs/discovery/dexes-amms/sdex.md`; `internal/sources/sdex/README.md` | Historical replay of classic SDEX should decode every claim-atom variant used in the targeted ledger range, or fail/count visibly when a range is outside supported protocol coverage. | Current committed code now decodes `ClaimAtomTypeV0` into regular order-book trade rows by deriving the seller G-strkey from raw Ed25519 bytes, and the targeted SDEX package test passes. The original V0-drop defect no longer reproduces. | EV-0044; EV-0105 | F-1233 |
| XFI-0026 | fixed | Upstream oracle feed vectors -> canonical asset allow-lists -> decoder partial skips -> dispatcher metrics -> oracle coverage dashboards | `internal/sources/reflector/decode.go`; `internal/sources/redstone/decode.go`; `internal/sources/band/decode.go`; `internal/canonical/asset_*.go`; `internal/dispatcher/dispatcher.go`; `internal/dispatcher/statsflush/flusher.go`; `docs/operations/runbooks/decode-errors.md`; `docs/architecture/explorer-data-inventory.md` | When an upstream oracle starts publishing a feed the product does not yet canonicalize, operators should see an unknown-symbol/feed coverage signal even if other assets in the same event decode successfully. | F-1234 closed: all three oracle decoders (`reflector`, `band`, `redstone`) now increment `obs.SourceUnknownSymbolsTotal{source=…}` when a feed inside a mixed batch isn't in the canonical allow-list. Operators alert on the metric to detect upstream coverage drift before silently missing assets. | EV-0045 | F-1234 |
| XFI-0027 | fixed | External websocket frames -> parser errors -> source metrics/runbooks -> source freshness alerts | `internal/sources/external/{binance,kraken,bitstamp,coinbase}/streamer.go`; `internal/sources/external/*/parse.go`; `internal/sources/external/runner.go`; `internal/obs/metrics.go`; `internal/sources/external/README.md`; `docs/operations/runbooks/decode-errors.md`; monitoring rules | Streamer parse/schema failures should be visible as per-source decode or parser-error metrics so vendor schema drift is diagnosable before it becomes only a freshness gap. | Current `HEAD` increments `SourceDecodeErrorsTotal` in all four streamer parse-error branches while preserving reconnect/continue behavior. | EV-0098 | F-1235 |
| XFI-0028 | fixed | Ingestion cursors -> supply snapshot ledger -> component at-or-before readers -> asset supply API/market cap | `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/supply/{refresher,lcm_reader,config_reader,storage_classic_reader,storage_sep41_reader,xlm}.go`; `internal/storage/timescale/{account_observations,classic_supply_observations,sep41_supply_events,supply}.go`; asset detail/stat readers | A supply snapshot ledger should represent the ledger through which every required component observer has complete data, or the snapshot should carry component freshness/lag metadata. | F-1236 closed by wave 60 (2026-05-13). Operator-opt-in strict-freshness gate: new `WithStrictFreshnessRequired(strict bool)` option on the Refresher + new `[supply].strict_freshness_required` config flag close the surviving zero-value escape hatch. Snapshots arriving with `MinComponentLedger == 0` (static-XLM fallback or transiently-failing freshness producer) are rejected with `OutcomeKindMissingFreshness` rather than published. Default false preserves backwards compat; operators flip true post-launch once every producer is wired + every reader is shown to never fail-open under steady-state load. Wired through all three Refresher construction sites (classic / SEP-41 / XLM). Three tests pin strict-rejects-zero, strict-accepts-anchored, default-off-permissive. | EV-0047; EV-0106; EV-0108; EV-0109; EV-0110; EV-0111; EV-0112; EV-0123; EV-0134; EV-0155 | F-1236 |
| XFI-0029 | fixed | Verified currency IDs -> aggregator pair construction -> CoinMarketCap API query -> stored oracle update identity | `internal/currency/data/seed.yaml`; `internal/currency/verified.go`; `cmd/ratesengine-indexer/main.go`; `cmd/ratesengine-ops/main.go`; `internal/sources/external/coinmarketcap/poller.go`; `internal/sources/external/coinmarketcap/poller_test.go` | CMC updates should be keyed to the verified currency's CMC identity when available, not whichever coin CMC returns first for an ambiguous ticker. | F-1237 closed by wave 58 (2026-05-13). Three deltas: (1) the ops `verify-external` path now mirrors the indexer/aggregator wiring — `currency.LoadEmbedded()` + `p.CMCIDs = cat.CoinMarketCapIDs()` so the verifier queries CMC by `id=<numeric>` instead of the ambiguous `symbol=`; (2) the poller resolves the response back to canonical assets when CMC keys the response map by numeric ID — falls back to `coin.Symbol` (the ticker) when the map key isn't a known ticker; (3) regression test `TestPollOnce_IDModeUsesNumericIDs` captures the request URL params (`id=` present, `symbol=` absent) and proves the response round-trips back to XLM/BTC canonical assets. | EV-0048; EV-0102 | F-1237 |
| XFI-0030 | fixed | API Redis optionality -> streaming Hub construction -> Redis pub/sub subscriber -> startup availability | `cmd/ratesengine-api/main.go`; `internal/api/streaming/redispub/subscriber.go`; `docs/reference/config/README.md`; `configs/example.toml`; stream endpoint docs | Redis-dependent features should be disabled or degraded independently when Redis is absent; a Redis-less API should still boot if configured that way, and closed-bucket streaming should return heartbeats/503/degraded status rather than fail startup. | Current workspace now gates subscriber construction on `rdb != nil && hub != nil`, so Redis-less startup no longer attempts `NewSubscriber(nil, ...)`. | EV-0096 | F-1238 |
| XFI-0031 | fixed | Ops WASM audit flags -> ledgerstream worker counters -> long-running audit artifact completion | `cmd/ratesengine-ops/main.go`; `cmd/ratesengine-ops/wasm_extract.go`; `cmd/ratesengine-ops/wasm_history_test.go`; `cmd/ratesengine-ops/wasm_extract_test.go`; `docs/operations/wasm-audits/**` | Operator progress knobs should be validated at flag-parse time or handled consistently in both in-loop and completion counters, so long-running WASM audit/extraction jobs finish cleanly with their JSON/WASM artifacts. | F-1239 closed: both `wasm_extract.go` and the `wasm-history` walker in `cmd/ratesengine-ops/main.go` now branch on `progressEvery == 0` BEFORE computing `workerScanned % progressEvery`, eliminating the divide-by-zero panic at completion. | EV-0055 | F-1239 |
| XFI-0032 | fixed | Go toolchain pin -> CI/release binary builds -> Dockerfile image builds -> self-hosted binary equivalence | `go.mod`; `.github/workflows/{ci,release,release-validate}.yml`; `docker/ratesengine-*.Dockerfile`; `docker/README.md`; `Makefile` | Self-hosted container images should build with the same reviewed Go toolchain as CI/release, or document and test any deliberate toolchain skew. | F-1240 closed: all six `docker/ratesengine-*.Dockerfile` files now use `FROM golang:1.25-alpine`, matching `go.mod`'s `go 1.25.10` and the CI/release toolchain pin. The docs' binary-equivalence claim is now true. | EV-0057 | F-1240 |
| XFI-0033 | fixed | Migration SQL growth -> operator migration index -> deploy/schema reasoning during incidents | `migrations/0016_*.sql` through `migrations/0030_*.sql`; `migrations/README.md`; deploy and runbook users of migration numbering | The migration README should remain a faithful operator index of the shipped schema families so deploy reviews, outage triage, and backfill/schema investigations can reason from the same migration set the binaries actually expect. | F-1241 closed: `migrations/README.md` now documents every migration 0007 through 0030 with one-line rationale and links, including the F-1205 follow-up `0030_asset_supply_history_unique_constraint`. Operators reviewing a deploy or triaging an incident reason from the same set the binaries expect. | EV-0058; EV-0059 | F-1241 |
| XFI-0034 | fixed | Aggregator contribution mirror -> Timescale contribution row shape -> future source-transparency API/UI | `cmd/ratesengine-aggregator/{main,contribution_sink}.go`; `internal/aggregate/{vwap,orchestrator}/`; `internal/storage/timescale/price_source_contributions.go`; `migrations/0026_create_source_contributions_and_sdex_offers.up.sql` | Once contribution rows are durably persisted, fields promised by the schema/commentary should either be populated or explicitly deferred before history accumulates unusable holes. | Current committed code carries per-trade USD attribution by stable `Trade.ID()`, filters trades first, then sums only surviving per-source USD values before persisting `volume_usd`. The specific pre-filter/post-filter mismatch no longer reproduces. | EV-0060; EV-0103; EV-0104; EV-0105 | F-1242 |
| XFI-0035 | fixed | Trade insert -> classic asset registry upsert -> asset/issuer catalogue ranking and freshness | `internal/storage/timescale/trades.go`; `internal/storage/timescale/asset_registry.go`; `internal/storage/timescale/{coins,issuers}.go`; `internal/api/v1/{assets,issuers}.go`; `test/integration/asset_registry_replay_test.go` | Repeated observations for a classic asset should update first/last-seen bounds and observation counts monotonically, and duplicate trade replays should not mutate registry counters when no new trade row is stored. | F-1243 closed by wave 64 (2026-05-13). Wave 47 replaced the process-lifetime registry skip cache with a 60-second TTL; wave 51 added the `RowsAffected()==0` early return before registry mutation on duplicate trade inserts. Wave 64 closes the evidence half: new `ResetAssetRegistryDedupeForTest` helper + `test/integration/asset_registry_replay_test.go` proves the end-to-end counter contract under the simulated-process-restart shape (cold dedupe cache, replay through `InsertTrade`). Three scenarios pin the contract; test passes against testcontainers-go Timescale on current head. | EV-0062; EV-0117; EV-0135; EV-0149; EV-0158; EV-0167 | F-1243 |
| XFI-0036 | fixed | Dashboard webhook create path -> persisted secret field -> delivery worker signing contract -> architecture security promises | `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `migrations/0027_platform_v1_schema.up.sql`; `docs/architecture/platform-spec.md`; `openapi/rates-engine.v1.yaml` | Secret naming/docs/storage semantics must agree: either persist an intentionally recoverable signing key with a true reviewed at-rest protection boundary, or the documentation must not promise application-layer encryption that does not ship. | Wave 54 closes the remaining contradiction. Domain/API/store comments and the architecture spec now agree that customer webhook signing keys are recoverable `bytea` values read back by the delivery worker; the spec no longer promises sealed-box protection for that surface. | EV-0068; EV-0117; EV-0125; EV-0138; EV-0154; EV-0159; EV-0161; EV-0163 | F-1244 |
| XFI-0037 | fixed | Customer-configured webhook URL -> delivery worker outbound HTTP client -> deployment private network boundary | `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/customerwebhook/worker.go`; `internal/customerwebhook/ssrf.go`; `cmd/ratesengine-api/main.go`; webhook tests | User-controlled webhook destinations must not become an SSRF primitive against private/control-plane networks; validation and request execution should enforce the same egress policy. | Current workspace rejects internal/private/reserved destinations at registration, re-resolves before dial, and disables redirect following. | EV-0096 | F-1245 |
| XFI-0038 | fixed | API-design lifecycle docs -> shipped OpenAPI routes -> operational runbooks -> customer feature truth | `docs/reference/api-design.md`; `docs/reference/api/rates-engine.v1.yaml`; `internal/api/v1/dashboardwebhooks/doc.go`; `cmd/ratesengine-api/main.go`; `docs/operations/runbooks/customer-webhook-delivery-failing.md` | A closed design question must not continue to deny a feature that has already shipped across API, worker, and runbook surfaces. | `docs/reference/api-design.md` now says webhook callbacks shipped and explains their relationship to SSE. | EV-0096 | F-1246 |
| XFI-0039 | fixed | API process count / multi-region rollout -> webhook worker startup -> pending-delivery row selection -> customer callback delivery semantics | `cmd/ratesengine-api/main.go`; `internal/customerwebhook/worker.go`; `internal/platform/postgresstore/webhook_store.go`; `docs/architecture/r2-r3-bringup.md` | A delivery row should be atomically claimed or leased so one logical customer callback is emitted once per scheduled attempt even when multiple API processes are alive. | Current `HEAD` claims rows with `FOR UPDATE SKIP LOCKED` and leases them by advancing `next_attempt_at` before the worker performs network I/O, so competing workers no longer select the same due row in parallel. | EV-0098 | F-1247 |
| XFI-0040 | fixed | Dashboard quota promise -> handler pre-check -> Postgres insert path -> schema-level enforcement | `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/postgresstore/webhook_store.go`; `migrations/0027_platform_v1_schema.up.sql`; `test/integration/platform_postgres_stores_test.go` | An advertised per-account webhook ceiling must remain true under concurrent create requests, which requires one atomic enforcement point across read and write. | Current code wraps the store create in a transaction plus account-scoped advisory lock, and after wave 46 cleared the migration blocker the isolated concurrent-cap integration proof runs green. | EV-0074; EV-0098; EV-0121; EV-0147 | F-1248 |
| XFI-0041 | fixed | Incident/anomaly/divergence event sources -> webhook subscription fan-out -> pending-delivery queue -> customer callback worker | `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/emit_incident.go`; `internal/customerwebhook/fanout.go`; `internal/storage/timescale/freeze_events.go`; `internal/divergence/worker.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/dashboardwebhooks/doc.go`; `openapi/rates-engine.v1.yaml`; `docs/operations/runbooks/customer-webhook-delivery-failing.md` | A customer-facing callback feature needs a live producer path that materializes event rows before the delivery worker can do useful work. | Current committed code now has production enqueue paths for all four declared event families: `anomaly.freeze`, `divergence.firing`, `incident.sev1`, and `incident.resolved`. The incident pair is operator-triggered through `ratesengine-ops emit-incident`, matching the embedded incident-corpus workflow recorded in the register. | EV-0076; EV-0105; EV-0106 | F-1249 |
| XFI-0042 | fixed | Freeze marker refreshes -> durable freeze-event insert -> open-row uniqueness -> anomaly timeline/recovery reads | `internal/aggregate/freeze/freeze.go`; `internal/storage/timescale/freeze_events.go`; `test/integration/freeze_events_test.go` | Concurrent refreshes for one still-firing pair must preserve a single open durable row until recovery closes it. | F-1250 closed: `freeze_events.go::RecordFreeze` runs the check + insert inside a transaction guarded by `pg_advisory_xact_lock(pairKey)` keyed on a stable hash of (asset, quote). Concurrent callers serialise through one critical section so only one open durable row exists per still-firing pair. | EV-0079 | F-1250 |
| XFI-0043 | fixed | Trade timestamp -> FX resolver SQL bound -> freshness policy -> stored `trades.usd_volume` -> downstream volume surfaces | `cmd/ratesengine-indexer/main.go`; `internal/storage/timescale/usd_fx_resolver.go`; `internal/storage/timescale/trades.go`; `test/integration/usd_fx_resolver_test.go` | FX-derived USD enrichment should evaluate anchor freshness against the trade-time query, not the wall-clock time when a delayed or historical insert happens. | Current source carries the trade-time freshness comparison, NUMERIC-text trimming, and unambiguous freshness sentinels; after wave 46 cleared migration bootstrap, the DB-backed FX integration proof runs green end to end. | EV-0080; EV-0102; EV-0139; EV-0147 | F-1251 |
| XFI-0044 | fixed | Multi-region runbook pre-flight -> repository command surface -> actual consistency verification path | `docs/operations/multi-region-cutover.md`; `scripts/dev/verify-cross-region.sh`; `Makefile` | A cutover checklist should name an executable, supported command for the consistency gate it requires. | F-1252 closed: `Makefile::verify-cross-region` exists and shells out to `scripts/dev/verify-cross-region.sh`. An operator following the cutover runbook gets the consistency check the docs promised. | EV-0082 | F-1252 |
| XFI-0045 | fixed | Redis ACL server hardening -> rendered application TOML -> Redis client username -> runtime Redis-backed features | `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/templates/redis.conf.j2`; `configs/ansible/roles/archival-node/templates/ratesengine.toml.j2`; `internal/config/config.go`; `internal/storage/redisclient/redisclient.go` | When the server disables the legacy default user, generated client config must move to the named ACL user in the same rollout. | Current Ansible rendering writes `redis_username = "{{ redis_acl_username | default('ratesengine') }}"` whenever `redis_acl_lockdown` is enabled, matching the named ACL user contract consumed by `redisclient.Build`. | EV-0098 | F-1253 |
| XFI-0046 | fixed | Redis ACL key allow-list -> live Redis namespace builders -> API/auth/cache runtime features | `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `internal/ratelimit/bucket.go`; `internal/cachekeys/keys.go`; `internal/auth/signup_tracker.go`; `internal/auth/sep10/redisreplay.go`; `internal/usage/counter.go`; `cmd/ratesengine-api/main.go`; `docs/adr/0007-redis-cache-schema.md` | ACL lockdown must allow the exact key families current binaries read/write; server-side hardening cannot silently deny core runtime namespaces. | Current ACL rendering now permits `rl:*`, `sub:*`, `signup:email:*`, `sep10:seen:*`, `usage:*`, `assets:list:*`, and `markets:list:*`, closing the stale/missing namespace drift from the earlier pass. | EV-0098 | F-1254 |
| XFI-0047 | fixed | Multiple valid magic links -> callback token consume -> user absence check -> account create -> user unique constraint | `internal/api/v1/dashboardauth/handlers.go`; `internal/platform/postgresstore/token_store.go`; `internal/platform/postgresstore/account_store.go`; `internal/platform/postgresstore/user_store.go`; `migrations/0027_platform_v1_schema.up.sql` | First-login provisioning for one email should be transactional or idempotent so parallel valid callbacks converge on one account/user outcome without leaking orphan rows. | F-1255 closed: Redis-SETNX-backed `EmailLocker` seam wired through `dashboardauth.Config`. The losing callback short-circuits before `Account.Create`, polls briefly for the winner's user, and never inserts a speculative-account row. Redis-less deployments fall back to the Suspend-on-conflict path as defence in depth. Tests pin the no-speculative-account assertion + miniredis acquire/release/TTL round-trip. `signup:lock:*` added to the Redis ACL allow-list. | EV-0086; EV-0087; EV-0102 | F-1255 |
| XFI-0048 | fixed | Dashboard key form copy -> OpenAPI create-key contract -> tier-cap implementation -> created key response | `web/dashboard/src/app/keys/page.tsx`; `openapi/rates-engine.v1.yaml`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/account.go` | Customer-facing rate-limit controls and API docs should explain the actual tier-specific persisted budget semantics. | The backend clamp, OpenAPI tier ladder, and wave-48 dashboard hint now agree on the effective persisted budget model. The UI still allows typing up to the global ceiling, but its inline copy explicitly states lower tiers silently clamp on save, matching the server response semantics. | EV-0090; EV-0150 | F-1256 |
| XFI-0049 | fixed | Dashboard key-quota promise -> handler active-count pre-check -> Postgres insert path -> schema-level enforcement | `internal/api/v1/dashboardkeys/handlers.go`; `internal/api/v1/dashboardkeys/handlers_test.go`; `internal/platform/postgresstore/apikey_store.go`; `test/integration/platform_postgres_stores_test.go` | An advertised active-key/account cap must remain true under concurrent create requests, which requires one atomic enforcement point across read and write. | Current code wraps capped creates in a transaction plus account-scoped advisory lock and adds a concurrent cap integration scenario. Wave 53 repairs the fixture's invalid ID/prefix shapes, and the full Postgres-store integration now passes through the intended quota winners/losers assertions. | EV-0092; EV-0103; EV-0121; EV-0148; EV-0152; EV-0156; EV-0157; EV-0160; EV-0162 | F-1257 |
| XFI-0050 | fixed | Redis optionality -> usage counter construction -> usage middleware nil handling -> account usage reader | `cmd/ratesengine-api/main.go`; `internal/api/v1/middleware/usage.go`; `internal/usage/counter.go`; `internal/api/v1/server.go` | Optional Redis must either omit usage tracking entirely or make every usage-dependent adapter nil-safe; it must not leave a route-specific nil dependency behind. | F-1258 closed by wave 33 (2026-05-12): API startup now wires `UsageReader: usageReaderOrNil(usageCounter)` instead of the unconditional `usageReaderAdapter{c:usageCounter}`. The helper returns a typed-nil `v1.UsageReader` when the counter is nil; the `/v1/account/usage` handler short-circuits on `usageReader == nil` with an empty list, so Redis-less deployments degrade cleanly instead of nil-deref'ing on `Read`. | EV-0094; EV-0103 | F-1258 |
| XFI-0051 | fixed | Live usage counter wiring -> `/v1/account/usage` handler -> OpenAPI/reference docs -> explorer/product expectations | `cmd/ratesengine-api/main.go`; `internal/api/v1/account.go`; `internal/api/v1/server.go`; `openapi/rates-engine.v1.yaml`; `docs/reference/api/rates-engine.v1.yaml`; `docs/reference/api-design.md`; `examples/postman/rates-engine.postman_collection.json`; `docs/architecture/explorer-data-inventory.md` | Published docs should describe the usage endpoint's actual data semantics on the current runtime path. | Current tracked artifacts now agree: source OpenAPI, handler comments, API-design docs, architecture inventory, generated reference YAML, and the canonical tracked Postman collection all describe live Redis-backed usage plus `/readyz` degradation. The stale `docs/reference/api/postman-collection.json` residual from the earlier pass is not tracked and is absent in the current tree. | EV-0095; EV-0103; EV-0153; EV-0159 | F-1259 |
| XFI-0052 | fixed | USD-volume quote normalization -> class/outlier filtering -> min-volume manipulation gate -> VWAP publication | `internal/aggregate/orchestrator/orchestrator.go`; `internal/aggregate/outliers.go`; `configs/example.toml`; R1 `[aggregate].min_usd_volume` posture | A manipulation-resistance threshold documented as post-class/post-outlier must compare against the filtered survivor set, not volume discarded before publication. | Current committed code now computes `survivorUSDVolume(trades, tradeUSD)` after filtering and feeds that survivor-only value into `dropForMinUSDVolume`; the regression test cited under `F-1260` pins the formerly vulnerable case. | EV-0105 | F-1260 |
| XFI-0053 | fixed | `asset_supply_history` compression policy -> migration `0030` constraint swap -> migration runner/integration bootstrap -> future R1 schema advance | `migrations/0005_create_asset_supply_history.up.sql`; `migrations/0030_asset_supply_history_unique_constraint.up.sql`; `migrations/README.md`; `internal/storage/timescale/supply.go`; `test/integration/platform_postgres_stores_test.go`; live R1 schema state | Constraint-changing migrations on compressed Timescale hypertables must follow a supported operational sequence so bootstrap, deploy, and writer expectations stay aligned. | Wave 46 rewrites `0030` to decompress chunks, disable compression around the constraint swap, and restore compression settings afterward. Fresh migration round-trip now passes; the former Timescale `0A000` bootstrap failure no longer reproduces on current head. | EV-0120; EV-0137; EV-0147 | F-1261 |
| XFI-0054 | fixed | Dashboard key-create request -> platform APIKey zero values -> Postgres create/update arg marshalling -> schema NOT NULL array columns | `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/apikey.go`; `internal/platform/postgresstore/apikey_store.go`; `migrations/0027_platform_v1_schema.up.sql`; `web/dashboard/src/lib/api.ts`; `test/integration/platform_postgres_stores_test.go` | A dashboard key-create request that omits optional allowlists should persist empty arrays, not convert nil slices into SQL NULL and 500 before the advertised workflow completes. Shared store writers should preserve the same invariant on later updates. | Wave 50 wraps both create and update `referer_allowlist` writers through `nonNilStringArray`, so nil slices persist as `'{}'` rather than SQL NULL. Focused package tests pass and the full integration now advances past the former NULL failure; its remaining blocker is the separate malformed-ID fixture tracked as `F-1263`. | EV-0148; EV-0152; EV-0156 | F-1262 |
| XFI-0055 | fixed | Concurrent API-key quota proof -> fixture-generated IDs/prefixes -> migration 0027 identity constraints -> advisory-lock closure evidence | `test/integration/platform_postgres_stores_test.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/api/v1/dashboardkeys/handlers.go` | A closure-grade DB concurrency test must generate production-valid records so failures speak to the invariant under test rather than to unrelated schema validation. | Wave 53 now generates both schema-valid `kid_<hex>` IDs and `rek_<8hex>` stored prefixes. The full Postgres-store integration reaches the quota assertions and passes, so the proof fixture no longer masks the advisory-lock invariant. | EV-0157; EV-0160; EV-0162 | F-1263 |
| XFI-0056 | fixed | R1 observability listener binds -> nftables ingress policy -> operator access docs | `configs/prometheus/README.md`; `configs/loki/README.md`; live R1 `ss -ltnp`; live `/etc/nftables.conf`; external reachability probes | Operator docs must distinguish "service listens on-host" from "service is publicly reachable" after firewall posture changes. | Current workspace docs now say the listeners remain on-host while external ingress is blocked by default-drop nftables and preserve SSH tunnelling as the supported operator path. | EV-0181; EV-0191; CMD-0181; CMD-0191 | F-1264 |
| XFI-0057 | fixed | Alertmanager severity ladder -> runnable configs/operator docs -> shipped Prometheus design note | `configs/alertmanager/README.md`; `configs/alertmanager/alertmanager.r1.yml`; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2`; `docs/architecture/prometheus-ansible-role-design-note.md`; `docs/operations/severity-ladder.md` | Every maintained monitoring doc should describe the same severity vocabulary as the actual Alertmanager templates. | Current workspace docs and templates all describe the shared `page` / `ticket` / `informational` ladder; the design note now includes the same routes and records the historical correction. | EV-0182; EV-0184; EV-0191; EV-0204; EV-0206; CMD-0182; CMD-0184; CMD-0191; CMD-0204; CMD-0206 | F-1265 |
| XFI-0058 | fixed | Tracked Ansible playbook inventory -> role-level operator docs -> executable bring-up commands | `configs/ansible/playbooks/{archival-node,deploy-binary,monitoring}.yml`; `configs/ansible/README.md`; `configs/ansible/roles/{haproxy,loki,patroni,prometheus,redis-sentinel}/README.md` | Deployment docs should reference only tracked playbooks, or clearly mark missing orchestration entrypoints as not yet landed. | Current workspace docs now satisfy that rule. The remaining HAProxy, Loki, and Patroni role docs label absent cluster playbooks as backlog-only commented examples, and Prometheus points at tracked `playbooks/monitoring.yml`. | EV-0184; EV-0191; EV-0199; EV-0203; EV-0210; CMD-0184; CMD-0191; CMD-0199; CMD-0203; CMD-0210 | F-1266 |
| XFI-0059 | fixed | Healthchecks installer/env template -> SLA-probe timer -> operator setup docs/hardening guide | `configs/healthchecks/install.sh`; `configs/healthchecks/README.md`; `configs/healthchecks/ratesengine-sla-probe.timer`; `docs/operations/pre-launch-hardening.md` | Once the SLA-probe heartbeat is installed/enabled, every setup path must instruct operators to create and populate the same fifth external check. | README, installer comments, and pre-launch hardening now all describe the five-check contract and include `HEALTHCHECKS_URL_SLA_PROBE`. | EV-0186; EV-0191; CMD-0186; CMD-0191 | F-1267 |
| XFI-0060 | fixed | R1 overlay README copy target -> Prometheus `rule_files` include path -> alert availability after reload | `configs/prometheus/rules.r1/README.md`; `configs/prometheus/prometheus.r1.yml` | Operator copy instructions and the active Prometheus include path must name the same directory. | The README now copies into `/etc/prometheus/rules.r1/` and explicitly states that path matches `prometheus.r1.yml`. | EV-0188; EV-0191; EV-0194; CMD-0188; CMD-0191; CMD-0194 | F-1268 |
| XFI-0061 | fixed | WASM audit-input README -> curated contracts YAML schema -> future audit refresh workflow | `configs/audit/README.md`; `configs/audit/wasm-walk-contracts.yaml`; `docs/operations/wasm-audits/r1-walk-2026-05-01.md` | Audit-tooling docs should describe the exact curated schema future auditors are expected to preserve and refresh. | The README now documents the live eight-source shape and says `_unattributed` was intentionally removed in the 2026-05-01 cleanup, matching the YAML. | EV-0190; EV-0194; CMD-0190; CMD-0194 | F-1269 |
| XFI-0062 | fixed | Cloudflare edge trust -> Caddy trusted-proxy resolution -> API immediate-peer trust -> operator hardening/docs | `configs/caddy/README.md`; `docs/operations/pre-launch-hardening.md`; `docs/adr/0025-caddy-cloudflare-trusted-proxy.md`; `internal/config/config.go`; `docs/reference/config/README.md` | Docs must keep the Cloudflare trust boundary at Caddy unless the architecture is intentionally redesigned; API `trusted_proxy_cidrs` should describe immediate peers only. | Current workspace docs now match ADR-0025 and the config source: Cloudflare CIDRs are handled in Caddy's `trusted_proxies`, while the API keeps trusting only the immediate Caddy peer. | EV-0193; EV-0198; CMD-0193; CMD-0198 | F-1270 |
| XFI-0063 | fixed | Redis Sentinel render -> operator/runbook auth commands -> Go FailoverClient SentinelPassword -> HA Redis viability | `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`; `configs/ansible/roles/redis-sentinel/README.md`; `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `docs/operations/runbooks/redis-master-down.md`; `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md`; `internal/storage/redisclient/redisclient.go`; go-redis v9 Sentinel auth code | The rendered Sentinel listener must implement the same auth contract that clients, operators, and runbooks assume. | Current workspace rendering now emits `requirepass {{ redis_password }}` for Sentinel listener auth while preserving `sentinel auth-pass` for Redis-primary auth, matching the client/runbook contract already audited. | EV-0196; EV-0198; CMD-0196; CMD-0198 | F-1271 |
| XFI-0064 | fixed | Redis ACL mode flag -> rendered exporter auth contract -> ACL-file emission -> Prometheus scrape target -> Redis alert continuity | `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/redis-sentinel/templates/redis.conf.j2`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/tasks/03-redis-configure.yml`; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | Exporter auth must be valid in both supported Redis modes: legacy default-user requirepass when lockdown is off, and named ACL-user auth when lockdown is on. | Current workspace source now branches `-redis.user=redis_exporter` on the same `redis_acl_lockdown | bool` condition that controls ACL-file emission, preserving valid exporter auth in both modes. | EV-0197; EV-0200; EV-0211; CMD-0197; CMD-0200; CMD-0211 | F-1272 |
| XFI-0065 | fixed | Shipped Sentinel implementation -> architecture/role docs -> drill-time verification command | `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `configs/ansible/roles/redis-sentinel/README.md`; `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md`; `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`; `internal/storage/redisclient/redisclient.go` | Shipped architecture/drill docs must reflect the package and auth contract the repository now implements. | The drill command now matches the authenticated listener, but both the shipped design note and the role README still describe future `internal/cachekeys` FailoverClient work even though `internal/storage/redisclient` already owns the live Sentinel path. | EV-0201; EV-0208; EV-0212; CMD-0201; CMD-0208; CMD-0212 | F-1273 |
| XFI-0066 | fixed | Shipped HAProxy role docs -> companion incident runbook reference -> tracked runbook inventory | `configs/ansible/roles/haproxy/README.md`; `docs/architecture/haproxy-ansible-role-design-note.md`; `docs/operations/runbooks/` | A shipped role that says a named incident runbook is the companion mitigation path should point at a tracked runbook or clearly mark it as not landed. | Current workspace docs now redirect to tracked `api-down.md` guidance and explicitly record that the older `api-pod-down.md` name was nonexistent. | EV-0207; EV-0213; CMD-0207; CMD-0213 | F-1274 |
| XFI-0067 | fixed | HAProxy backend health-check path -> API `/v1/readyz` dependency set -> Redis outage contract -> customer-visible routing availability | `configs/ansible/roles/haproxy/defaults/main.yml`; `configs/ansible/roles/haproxy/templates/haproxy.cfg.j2`; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `docs/architecture/ha-plan.md`; `docs/operations/runbooks/{redis-master-down,api-down}.md` | A dependency documented as fail-open/degraded should not automatically remove every API backend from the public edge unless the outage contract explicitly says so. | Runtime is source-closed: Redis is now a non-critical `ReadyChecker`, `/v1/readyz` returns 200 with `status="degraded"` for Redis-only failure, and a regression test pins the contract. Residual HAProxy/HA documentation drift is split into XFI-0076 / F-1284. | EV-0214; EV-0227; CMD-0214; CMD-0227 | F-1275 |
| XFI-0068 | fixed | Alert-rule job-label families -> operator-facing PromQL examples -> metrics-source commentary | `configs/ansible/roles/prometheus/templates/prometheus.yml.j2`; `configs/prometheus/prometheus.r1.yml`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/alerts-catalog.md`; `docs/operations/runbooks/{api-down,api-5xx,api-latency,sla-probe-p95-breach}.md`; `internal/obs/metrics.go` | Maintained query examples should target the real selector families current rules and scrape configs use. | Operator docs now use the current `ratesengine[_-]api` / deployment-specific selectors, but `internal/obs/metrics.go` still publishes a stale `job="api"` selector comment. | EV-0215; EV-0229; CMD-0215; CMD-0229 | F-1276 |
| XFI-0069 | fixed | Runbook source breadcrumb -> tracked readiness-handler implementation path | `docs/operations/runbooks/api-down.md`; `internal/api/v1/server.go` | A source citation in an incident runbook should resolve to a tracked implementation file. | Current `api-down.md` cites `internal/api/v1/server.go::handleReadyz` and records the earlier nonexistent `healthz.go` reference as corrected. | EV-0216; EV-0229; CMD-0216; CMD-0229 | F-1277 |
| XFI-0070 | fixed | HA-role nftables drop-ins -> repo default-drop base chain -> actual ingress reachability/security semantics | `configs/ansible/roles/{haproxy,redis-sentinel,prometheus}/tasks/06-firewall.yml`; `configs/ansible/roles/loki/tasks/server-05-firewall.yml`; `configs/ansible/roles/patroni/tasks/10-firewall.yml`; `configs/ansible/roles/archival-node/templates/nftables.conf.j2`; related HA role docs/design notes | Role firewall tasks must implement one deterministic policy model: either enforce their own restrictions or compose predictably with the repo's default-deny base chain. | The current remediation changed the drop-ins to `priority -100`, but the same flaw remains: an `accept` verdict in an earlier base chain is not final if the later priority-0 default-drop chain drops the packet. Without the default-drop chain, the role chains still have `policy accept`, so they do not enforce a deny posture themselves. | EV-0217; EV-0224; CMD-0217; CMD-0224 | F-1278 |
| XFI-0071 | fixed | Patroni first-run firewall task ordering -> drop-in directory existence -> clean-host role execution | `configs/ansible/roles/patroni/tasks/10-firewall.yml` | A clean-host role run must create parent directories before writing files beneath them. | Current source creates `/etc/nftables.d/` before writing `/etc/nftables.d/40-patroni.conf`. | EV-0218; EV-0224; CMD-0218; CMD-0224 | F-1279 |
| XFI-0072 | fixed | Patroni etcd version pin -> checksum variable/defaults -> documented role inventory path -> clean-host DCS bootstrap | `configs/ansible/roles/patroni/tasks/02-etcd-install.yml`; `configs/ansible/roles/patroni/defaults/main.yml`; `configs/ansible/roles/patroni/README.md` | A pinned upstream binary download must have a real checksum supplied by defaults or a documented required variable. | Source now preflight-validates `etcd_release_sha256` and defaults comment the requirement, but the role README still omits the required checksum from prerequisites and sample inventory. | EV-0220; EV-0224; CMD-0220; CMD-0224 | F-1280 |
| XFI-0073 | fixed | Patroni monitoring script dependencies -> role package install -> node_exporter textfile metrics availability | `configs/ansible/roles/patroni/tasks/11-monitoring.yml`; `configs/ansible/roles/patroni/tasks/05-patroni-install.yml`; `configs/ansible/roles/patroni/README.md`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | A textfile scraper installed by a role must have its runtime dependencies installed or declared. | Current source installs `jq` in the Patroni package task before installing the textfile scraper that uses it. | EV-0221; EV-0224; CMD-0221; CMD-0224 | F-1281 |
| XFI-0074 | fixed | Patroni DR restore configuration -> pgBackRest command render -> README/design-note recovery promise | `configs/ansible/roles/patroni/defaults/main.yml`; `configs/ansible/roles/patroni/tasks/08-patroni-bootstrap.yml`; `configs/ansible/roles/patroni/README.md`; `docs/architecture/patroni-ansible-role-design-note.md` | Documented point-in-time restore targets must be passed to the restore command or rejected. | Current source validates `latest`/`immediate`/`time:<timestamp>` and maps them to the corresponding pgBackRest restore flags. | EV-0222; EV-0224; CMD-0222; CMD-0224 | F-1282 |
| XFI-0075 | fixed | Timescale primary-down runbook -> shipped Patroni/etcd endpoint scheme and DCS shape -> incident diagnosis commands | `docs/operations/runbooks/timescale-primary-down.md`; `configs/ansible/roles/patroni/templates/etcd.conf.j2`; `configs/ansible/roles/patroni/templates/patroni.yml.j2`; `configs/ansible/roles/patroni/tasks/04-etcd-systemd.yml`; `configs/ansible/roles/patroni/tasks/01-preflight.yml`; `docs/architecture/patroni-ansible-role-design-note.md` | The runbook's direct etcd commands must match the role's protocol, namespace/scope, and quorum size. | The runbook uses `https://etcd-*.internal:2379`, probes `/ratesengine/leader`, and expects 3 of 5 members; the role renders HTTP etcd URLs, Patroni namespace `/service/` plus per-cluster scope, and exactly three postgres-cluster hosts. | EV-0225; CMD-0225 | F-1283 |
| XFI-0076 | fixed | API readiness criticality -> HAProxy role docs/template comments -> HA architecture failure matrix | `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/server_test.go`; `configs/ansible/roles/haproxy/{defaults/main.yml,templates/haproxy.cfg.j2,README.md}`; `docs/architecture/ha-plan.md` | Operator-facing HA docs must describe the current critical/non-critical readiness split. | Runtime now treats Redis-only ready-check failure as HTTP 200 + `status="degraded"`, but HAProxy defaults/template comments, role README, and HA plan still say `/v1/readyz` requires Redis reachability or goes false during Redis failover. | EV-0227; CMD-0227 | F-1284 |
| XFI-0077 | fixed | Monitoring binary version pins -> upstream archive downloads -> checksum variables/docs -> host observability binaries | `configs/ansible/roles/loki/tasks/server-02-install.yml`; `configs/ansible/roles/loki/tasks/agent-01-install.yml`; `configs/ansible/roles/prometheus/tasks/02-install.yml`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/README.md` | Production roles that install upstream observability binaries must authenticate the exact release archive they install, and the documented clean inventory path must provide those digests. | Install tasks now assert and use required checksum variables for all four archives, but role READMEs/defaults still do not document those variables as required inventory inputs. | EV-0231; EV-0241; CMD-0231; CMD-0241 | F-1285 |
| XFI-0078 | fixed | Loki S3 credential variable names -> systemd unit environment -> Loki S3 backend -> MinIO log chunks | `configs/ansible/roles/loki/templates/loki.service.j2`; `configs/ansible/roles/loki/templates/loki-config.yaml.j2`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md` | The service manager must pass real AWS-compatible credential values to Loki, not literal indirection strings, before Loki writes chunks to S3-compatible MinIO. | Current source removes the literal `Environment=AWS_ACCESS_KEY_ID=${...}` / `AWS_SECRET_ACCESS_KEY=${...}` assignments and documents direct `AWS_ACCESS_KEY_ID=` / `AWS_SECRET_ACCESS_KEY=` values in `/etc/default/loki`. | EV-0232; EV-0238; CMD-0232; CMD-0238 | F-1286 |
| XFI-0079 | fixed | Prometheus listener defaults -> systemd bind addresses -> generated self-scrape/Alertmanager targets -> alert fanout | `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/templates/prometheus.service.j2`; `configs/ansible/roles/prometheus/templates/alertmanager.service.j2`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2`; `configs/ansible/roles/prometheus/README.md` | The address family Prometheus uses for self-scrape and Alertmanager delivery must match the address family the services bind. | Current source binds Prometheus/Alertmanager HTTP listeners to `0.0.0.0:9090/9093` and opens 9090/9093/9094 to internal CIDRs, matching the generated private-IP targets. Residual README wording drift is tracked under XFI-0082/F-1290. | EV-0234; EV-0241; CMD-0234; CMD-0241 | F-1287 |
| XFI-0080 | fixed | Prometheus TSDB retention defaults -> preflight disk gate -> README capacity prerequisite -> launch monitoring durability | `configs/ansible/roles/prometheus/tasks/01-preflight.yml`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/README.md` | A documented production capacity prerequisite should be enforced by the role before installing a 30-day TSDB workload. | Current source uses `df --output=avail -B1 /var` and a blocking assert against 20 GB; the soft-fail `ignore_errors` path is gone. | EV-0236; EV-0241; CMD-0236; CMD-0241 | F-1288 |
| XFI-0081 | fixed | Loki storage prerequisites -> server preflight -> MinIO chunk bucket/local index capacity -> incident log retention | `configs/ansible/roles/loki/tasks/server-01-preflight.yml`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md`; `docs/architecture/loki-ansible-role-design-note.md` | The role's documented durable-storage prerequisites should be checked before starting Loki. | Defaults/README/design note claim or require MinIO bucket/access and 50 GB local capacity checks, but the server preflight only verifies OS, inventory groups, time sync, and user creation. | EV-0239; CMD-0239 | F-1289 |
| XFI-0082 | fixed | Prometheus listener/firewall change -> role README access model -> operator security assumptions | `configs/ansible/roles/prometheus/README.md`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/tasks/06-firewall.yml` | Operator docs should describe whether 9090/9093 are loopback-only or internal-network listeners and which ports the role opens. | Defaults now bind 9090/9093 on all interfaces and the firewall task opens 9090/9093/9094 to internal CIDRs, but the README still says loopback-only UI access and 9094-only firewall. | EV-0242; CMD-0242 | F-1290 |
| XFI-0083 | fixed | Archival-node executable download pins -> Ansible `get_url` checksum enforcement -> privileged host binaries | `configs/ansible/roles/archival-node/tasks/09-minio.yml`; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/ansible/roles/archival-node/defaults/main.yml`; archival-node bring-up docs | Production archival-node roles should authenticate exact upstream artifacts before installing binaries into `/usr/local/bin`, and the documented inventory path should expose every required digest variable. | Install tasks now assert and use SHA-256 variables for MinIO, `mc`, and node_exporter; `mc` now uses a versioned archive URL. Remaining gap: defaults/operator docs do not list the required digest/version variables. | EV-0244; EV-0247; CMD-0244; CMD-0246 | F-1291 |
| XFI-0084 | fixed | ADR-0001 Horizon ban -> active incident runbooks -> operator diagnosis dependency choices | `docs/adr/0001-horizon-deprecated.md`; `docs/operations/runbooks/core-lag.md`; `README.md`; `CLAUDE.md` | Active runbooks should preserve the no-Horizon architecture boundary or explicitly document an accepted exception. | `core-lag.md` now tells operators to cross-check via stellar.expert/explorer or a public stellar-rpc endpoint and notes that the prior Horizon wording was removed for ADR-0001. | EV-0245; EV-0247; CMD-0244; CMD-0246 | F-1292 |
| XFI-0085 | fixed | Redis exporter version pin -> Ansible download/extract -> `/usr/local/bin/redis_exporter` -> Prometheus Redis HA scrape | `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/README.md`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | Redis HA exporter binaries should be authenticated before installation, and the required digest should be part of the role inventory contract. | Current source verifies the exporter archive with `redis_exporter_release_sha256`, but defaults/README still omit that required digest variable and checksum source. | EV-0249; EV-0255; CMD-0248; CMD-0252 | F-1293 |
| XFI-0086 | fixed | CI external tool versions -> curl-to-tar installer steps -> trusted lint/security verdicts | `.github/workflows/ci.yml`; `scripts/ci/lint-actions-pinning.sh`; repository workflow permissions | CI should not trust unauthenticated external binaries for rule validation or secret scanning. | Current CI verifies both Prometheus and gitleaks release archives with required SHA-256 secrets before extraction; repo workflow tokens remain read-only. | EV-0250; EV-0252; EV-0255; CMD-0248; CMD-0250; CMD-0252 | F-1294 |
| XFI-0087 | fixed | Redis exporter auth env -> systemd unit file permissions -> Redis/Sentinel credential boundary -> Prometheus Redis scrape | `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | Redis exporter credentials should be stored in a restricted secret file or separate ACL credential, not in a world-readable systemd unit. | The role writes `Environment=REDIS_PASSWORD={{ redis_password }}` into `/etc/systemd/system/redis_exporter.service` with mode `0644`, exposing the shared Redis/Sentinel password to local unit-file readers. | EV-0253; CMD-0250 | F-1295 |
| XFI-0088 | fixed | Workflow permission block -> release/deploy job credential surface -> future OIDC relying-party trust | `.github/workflows/release.yml`; `.github/workflows/deploy.yml`; `.github/workflows/api-docs.yml`; `docs/operations/release-process.md`; GitHub Actions environment settings | Release/deploy workflows should not be able to mint GitHub OIDC tokens unless a current signing, attestation, or cloud-auth step consumes them. | `api-docs.yml` legitimately uses `id-token: write` for Pages deploy, but `release.yml` and `deploy.yml` grant `id-token: write` only for future keyless signing/attestation and contain no OIDC consumer today. | EV-0257; CMD-0254 | F-1296 |
| XFI-0089 | fixed | Deploy host secret -> SSH known_hosts setup -> Ansible root deploy -> binary swap/rollback | `.github/workflows/deploy.yml`; `docs/operations/deploy-workflow.md`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml`; `docs/operations/r1-deployment-state.md` | The production deploy workflow should fail closed when the pinned host-key secret is absent. | The docs treat `<REGION>_SSH_KNOWN_HOSTS` as the MITM control, but the workflow accepts an unset secret and trusts a live `ssh-keyscan` result for the same deploy. | EV-0259; CMD-0256 | F-1297 |
| XFI-0090 | fixed | Manual workflow_dispatch input -> shell script parsing -> release/deploy secrets and artifact publication | `.github/workflows/deploy.yml`; `.github/workflows/release.yml`; `.github/workflows/explorer-deploy.yml`; `.github/workflows/status-page.yml`; `.github/workflows/docs-deploy.yml`; `docs/operations/{deploy-workflow,release-process}.md` | Manual inputs should be treated as untrusted data until validated and should not be interpolated into shell source. | Deploy and release workflows embed free-form dispatch inputs directly into `run:` scripts before validation; other manual deploy workflows use choice inputs or env-passed values and do not show the same shell-source injection surface. | EV-0263; CMD-0260 | F-1298 |
| XFI-0091 | fixed | Deploy workflow tooling install -> Ansible collection resolver -> production playbook modules -> root SSH host mutation | `.github/workflows/deploy.yml`; `configs/ansible/requirements.yml`; `configs/ansible/README.md`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml` | Production deploy dependencies should be pinned and reproducible from the repo revision. | The deploy workflow resolves mutable PyPI/Galaxy dependency ranges during the deploy itself, including `ansible-core==2.18.*` and lower-bound collection ranges. | EV-0265; CMD-0262 | F-1299 |
| XFI-0092 | fixed | Healthchecks SLA wrapper -> systemd sandbox -> node_exporter textfile -> Prometheus SLA alerts | `configs/healthchecks/sla-probe.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `cmd/ratesengine-sla-probe/main.go`; `deploy/monitoring/rules/sla-probe.yml`; `docs/operations/sla-probe.md`; `docs/operations/runbooks/sla-probe-stale.md` | SLA evidence timers should either be able to write the configured textfile metric or omit the textfile flag. | Current Healthchecks SLA service grants the node_exporter textfile collector path through `ReadWritePaths` and joins the `ratesengine` group that owns the directory, closing the sandbox write conflict. | EV-0267; EV-0273; CMD-0264; CMD-0270 | F-1300 |
| XFI-0093 | fixed | Aggregator metrics listener -> R1 scrape target -> Healthchecks heartbeat -> P1 runbook diagnosis | `cmd/ratesengine-aggregator/main.go`; `configs/prometheus/prometheus.r1.yml`; `configs/healthchecks/heartbeat.sh`; `docs/operations/runbooks/aggregator-silent.md`; `configs/ansible/roles/prometheus/README.md` | Operator diagnostics should query the same metrics endpoint that Prometheus and Healthchecks use for the deployed aggregator topology. | The aggregator shifts a default single-host listener collision to `9465`, and R1 Prometheus/Healthchecks scrape `9465`, but the P1 aggregator-silent runbook tells responders to inspect `localhost:9464`, which is the indexer scrape endpoint on R1/default co-located deployments. | EV-0269; CMD-0266 | F-1301 |
| XFI-0094 | fixed | Healthchecks smoke installer -> copied smoke script -> wrapper error handling -> external API-surface heartbeat | `configs/healthchecks/install.sh`; `configs/healthchecks/smoke.sh`; `configs/healthchecks/ratesengine-smoke.service`; `configs/healthchecks/README.md`; `scripts/dev/r1-smoke.sh` | A failed or missing smoke probe executable should produce a Healthchecks failure and visible local service failure rather than a successful no-op. | The wrapper exits `0` before calling `/fail` when `SMOKE_SCRIPT` is missing or not executable, so an install/path/permission break can silently disable the 5-minute API-surface smoke check. | EV-0271; CMD-0268 | F-1302 |
| XFI-0095 | fixed | SLA probe binary deploy -> Healthchecks wrapper -> external SLA heartbeat -> Prometheus SLA evidence | `configs/healthchecks/sla-probe.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/install.sh`; `cmd/ratesengine-sla-probe/main.go`; `docs/operations/sla-probe.md` | A missing or non-executable SLA probe binary should fail the Healthchecks SLA check and local service state. | The wrapper exits `0` before calling `/fail` when `PROBE_BIN` is missing or not executable, so a binary deploy/path/permission break can silently disable the Healthchecks SLA proof. | EV-0274; CMD-0271 | F-1303 |
| XFI-0096 | fixed | Pre-launch hardening guide -> Healthchecks EnvironmentFile edit -> timer restart -> external SLA heartbeat activation | `docs/operations/pre-launch-hardening.md`; `configs/healthchecks/README.md`; `configs/healthchecks/install.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/ratesengine-sla-probe.timer` | Operator apply steps that add Healthchecks URLs should restart every timer whose service reads that EnvironmentFile. | The hardening guide lists the SLA URL but restarts only heartbeat and smoke timers; the canonical Healthchecks docs include `ratesengine-sla-probe.timer`, and the SLA service reads the URL from the same EnvironmentFile. | EV-0276; CMD-0273 | F-1304 |
| XFI-0097 | open | Live SLA probe timer -> API key tier -> API availability/freshness observations -> textfile verdict -> SLA alert/runbook/status truth | R1 `ratesengine-sla-probe.timer`; `/usr/local/bin/ratesengine-sla-probe`; `/var/lib/node_exporter/textfile_collector/sla_probe.prom`; `cmd/ratesengine-sla-probe`; `configs/healthchecks/*`; SLA alert rules and runbooks | RFP freshness/latency/availability proof and customer launch readiness | A live SLA probe run should produce `ratesengine_sla_probe_unit_failed 0` when the service is within the documented latency/freshness/availability targets, or the failure should be visible as an active launch/SLA blocker with a trustworthy cause. | The active R1 timer writes `sla_probe.prom`, but refreshed live evidence still reports `ratesengine_sla_probe_unit_failed 1`. Freshness is now below target (`18.236s`), but the deployed probe lacks `RATESENGINE_PROBE_API_KEY`, the canonical Healthchecks env template omits that required key, and manual probe runs fail data-endpoint availability by hitting the anonymous rate limit. | R1-0032; R1-0039; R1-0044; EV-0280; EV-0294; EV-0309; EV-0319; CMD-0277; CMD-0291; CMD-0306; CMD-0315 | F-1305 |
| XFI-0098 | fixed | API price response stale flag -> price staleness metric producer -> Prometheus alert -> price-stale runbook/status incidents | `internal/api/v1/price.go`; `internal/obs/metrics.go`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/runbooks/price-stale.md`; R1 API/Prometheus | Every documented freshness alert should be backed by a live metric producer, and stale customer-visible price responses should become visible in alert/status workflows. | Source now contains an aggregator-side bounded producer for configured pairs, and direct R1 verification shows `ratesengine_price_staleness_seconds` samples in live aggregator metrics plus Prometheus for BTC/ETH/XLM/native. The alert expression now has a real series source. | R1-0033; R1-0035; R1-0037; EV-0282; EV-0287; EV-0290; CMD-0279; CMD-0284; CMD-0287 | F-1306 |
| XFI-0099 | fixed | SLA probe textfile -> node_exporter textfile collector -> Prometheus SLA rules/status | `/var/lib/node_exporter/textfile_collector/sla_probe.prom` on R1; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/prometheus/prometheus.r1.yml`; `configs/prometheus/rules.r1/sla-probe.yml`; `deploy/monitoring/rules/sla-probe.yml`; `docs/operations/sla-probe.md`; `docs/operations/runbooks/sla-probe-stale.md` | Textfile-producing operational proof must be scraped into Prometheus before alert rules and status rollups can rely on it. | Direct R1 verification now shows node_exporter running with the textfile collector directory flag, node_exporter exposing `ratesengine_sla_probe_*`, and Prometheus returning SLA probe verdict/freshness samples. | R1-0034; R1-0035; EV-0286; EV-0287; CMD-0283; CMD-0284 | F-1307 |
| XFI-0100 | fixed | Customer-visible price freshness -> aggregator staleness metric producer -> Prometheus price-stale alert -> SLA/status truth | `internal/api/v1/price.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/obs/metrics.go`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/runbooks/price-stale.md`; R1 API/aggregator/Prometheus | The staleness gauge and customer-visible stale flag must have documented, distinct semantics so operators do not confuse fallback degradation with aggregator-write staleness. | `F-1308` is closed as falsified: `flags.stale=true` can mark documented fallback/degradation while `ratesengine_price_staleness_seconds` tracks aggregator-write staleness. The local source patch mirrors `crypto:XLM` and `native` labels, and the remaining live SLA probe failure is tracked under `XFI-0097` / `F-1305`. | R1-0040; R1-0043; EV-0296; EV-0304; CMD-0293; CMD-0301 | F-1308 |
| XFI-0101 | fixed | SLA freshness alert -> runbook diagnosis commands -> live Healthchecks unit/cache-key topology | `docs/operations/runbooks/sla-probe-freshness-breach.md`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/ratesengine-sla-probe.timer`; `deploy/systemd/sla-probe.service`; `internal/cachekeys/keys.go`; `internal/api/v1/price.go`; R1 systemd/Redis | Runbooks for a live freshness breach should name the unit and cache keys that are actually active on R1, so responders can bisect API/Redis/Postgres freshness quickly. | The freshness-breach runbook now tells responders to inspect `ratesengine-sla-probe.service` and the actual native/USD VWAP keys `vwap:native:fiat:USD:{300,3600,86400}` instead of the failed legacy unit and empty `price:native:fiat:USD` key. | R1-0042; EV-0302; EV-0308; CMD-0299; CMD-0305 | F-1309 |
| XFI-0102 | open | Healthchecks URL env file -> heartbeat/smoke/SLA wrappers -> external pass/fail monitoring channel | R1 `/etc/default/ratesengine-healthchecks`; `configs/healthchecks/heartbeat.sh`; `configs/healthchecks/smoke.sh`; `configs/healthchecks/sla-probe.sh`; `configs/healthchecks/README.md`; `docs/operations/pre-launch-hardening.md`; R1 heartbeat/smoke/SLA timers | Every configured Healthchecks timer should either ping an external check or be explicitly retired in favor of another independent monitoring channel. | R1 has all five Healthchecks timers running locally, but every `HEALTHCHECKS_URL_*` entry is empty, so the wrappers skip external pass/fail pings for indexer, aggregator, API heartbeat, smoke, and SLA probe. | R1-0045; EV-0310; CMD-0307 | F-1310 |
| XFI-0103 | open | API route latency -> Prometheus API latency alerts -> `/v1/status` incidents -> launch/SLA truth | R1 API runtime; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/runbooks/api-latency.md`; `/v1/status` | Latency alerts should reflect customer-facing API performance and stay visible as launch blockers until cleared. | R1 currently fires `ratesengine_api_latency_p95_high`, `ratesengine_api_latency_p99_high`, and `ratesengine_slo_latency_burn_slow`; `/v1/status` is degraded with the p95/p99 alerts included in its active incidents. | R1-0047; EV-0315; CMD-0311 | F-1311 |
| XFI-0104 | open | Pinned Galexie version -> Ansible build guard -> installed archival exporter binary -> R1/R2/R3 ledger archive semantics | `configs/ansible/roles/archival-node/defaults/main.yml`; `configs/ansible/roles/archival-node/tasks/07-galexie.yml`; `VERSIONS.md`; `docs/operations/r1-deployment-state.md`; Galexie systemd unit | A version bump in the audited Galexie pin should deterministically rebuild and install that version, or fail before production continues on an old binary. | The role checks `galexie_version` against `/usr/local/bin/galexie version`, but the build command also has `creates: /root/go/bin/stellar-galexie`; once any artifact exists, a later version bump can skip `go install` and copy the stale binary. | EV-0317; CMD-0313 | F-1312 |
| XFI-0105 | open | SLA probe operations docs -> active Healthchecks unit/env/wrapper -> live incident diagnosis | `docs/operations/sla-probe.md`; `docs/operations/alerts-catalog.md`; `docs/operations/runbooks/sla-probe-stale.md`; `configs/prometheus/rules.r1/sla-probe.yml`; `docs/architecture/coverage-matrix.md`; `configs/healthchecks/README.md`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/sla-probe.sh`; R1 `ratesengine-sla-probe.timer` | Maintained SLA procedures should name the deployed unit, env file, and failure semantics. | Multiple active SLA docs still point at retired `deploy/systemd/sla-probe.*`, `sla-probe.timer`, `/etc/default/sla-probe`, or `sla-probe.service`, while the live path is `ratesengine-sla-probe.*`, `/etc/default/ratesengine-healthchecks`, and a wrapper that exits service-success while surfacing verdict through Healthchecks/textfile metrics. | EV-0319; EV-0320; CMD-0315; CMD-0316 | F-1313 |
| XFI-0106 | open | Release artifact matrix -> default deploy binary set -> fresh-host bootstrap binary loop -> Healthchecks SLA wrapper/timer | `.github/workflows/release.yml`; `.github/workflows/deploy.yml`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml`; `configs/healthchecks/install.sh`; `docs/operations/{deploy-workflow,release-process,archival-node-bringup}.md`; R1 `ratesengine-sla-probe.timer` | A repo-managed timer should run the binary version produced and deployed by the same release path, or the deploy should fail before SLA evidence is trusted. | Release publishes `ratesengine-sla-probe` and Healthchecks enables the SLA timer, but bootstrap omits the probe from its build/install loops, default deploy omits it from `binaries`, and deploy docs do not warn that CLI binaries use different verification and are excluded from the default service deploy. | EV-0322; CMD-0318 | F-1314 |

## Required Interaction Classes

| Class | Status | Notes |
| --- | --- | --- |
| binary -> config -> package | in_progress | Core API/indexer/aggregator observed on R1; full per-binary closure still pending |
| config example -> config parser -> deploy template | in_progress | `configs/example.toml`, Ansible, systemd; secret-boundary negative evidence recorded |
| workflow -> script -> artifact | in_progress | CI, OpenAPI generation, web build, dependency audit, release/deploy controls, and hosted GitHub settings reviewed; remaining workflow branches pending |
| Dockerfile -> binary -> runtime flags/env | in_progress | Migration image/default path failure recorded; remaining image env/entrypoint checks pending |
| migration -> Timescale store -> integration test | in_progress | Migration inventory mapped; store/test trace pending |
| decoder -> event type -> pipeline sink -> store | in_progress | SDEX legacy claim-atom/drop path recorded; remaining source decoders pending |
| external adapter -> normalized trade -> aggregation | todo | Every external source |
| aggregator -> Redis/Timescale -> API handler | in_progress | Price/VWAP route and stablecoin proxy reviewed; decimal undercount remains tracked as F-1213, while the formerly stale pre-filter min-volume gate is now fixed under F-1260. |
| API handler -> OpenAPI -> Go client -> frontend client | in_progress | Removed coins/currencies routes produced concrete drift findings |
| metric -> alert rule -> runbook -> service | in_progress | R1 alerts and missing timer evidence recorded |
| web route -> API hook -> UI state -> SEO/headers | in_progress | Removed endpoint consumers, status probes, dashboard key budget bypass, and signup abuse path recorded |
| docs claim -> code path -> tests/runtime | in_progress | RFP, proposal, ADRs, runbooks; status-page closure drift plus monitoring-doc/live-firewall drift now recorded |
