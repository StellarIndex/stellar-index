# Evidence Log

| ID | Date | Claim Or Observation | Source Refs | Notes |
| --- | --- | --- | --- | --- |
| EV-0001 | 2026-05-11 | Audit plan anchored to commit `80c57e38eeee729ec2d879d54286419206cee864`; worktree was clean before creating this audit directory. | `git rev-parse HEAD`; `git status --short` | Planning evidence only |
| EV-0002 | 2026-05-11 | Initial tracked application inventory before this audit directory contained `1,747` files from `git ls-files`. | `git ls-files \| wc -l`; [inventory/file-coverage.tsv](../inventory/file-coverage.tsv) | Inventory rows start as `todo`; not reviewed yet |
| EV-0003 | 2026-05-11 | Second-pass plan reconciliation verified the generated inventory has one row for each tracked file in scope. | CMD-0004 | `tracked=1747`, `rows=1747` |
| EV-0004 | 2026-05-11 | Third-pass cold-audit check found all generated inventory rows remain `todo`, which is intentional for a plan-only artifact with no imported findings. | CMD-0005; [05-findings-register.md](../05-findings-register.md) | Prevents false completion claims |
| EV-0005 | 2026-05-12 | Go package topology resolves with `go list ./...`; local sandbox cannot write the Go module stat cache under `/Users/ash/go/pkg/mod/cache/download`, but that did not block package discovery. | CMD-0007 | Environment caveat |
| EV-0006 | 2026-05-12 | Full Go unit suite passes when allowed to bind local test ports; sandbox-only run failed on localhost bind restrictions. | CMD-0008 | Test failures from first pass were environmental |
| EV-0007 | 2026-05-12 | `scripts/ci/lint-docs.sh` passed even while regenerated API reference and TS API types had uncommitted drift. | CMD-0009; CMD-0012; CMD-0013 | Supports F-1203 |
| EV-0008 | 2026-05-12 | OpenAPI URL-discipline linter passed for `openapi/rates-engine.v1.yaml`. | CMD-0010 | No URL-discipline finding from this pass |
| EV-0009 | 2026-05-12 | Launch readiness gate reports the engineering surface is not ready; cross-region R2/R3/DNS/Patroni, external security review, region-failover chaos, and finalisation rows are still red. | CMD-0011; `scripts/ci/verify-launch-ready` | Supports F-1206 |
| EV-0010 | 2026-05-12 | Regenerating API reference changed `docs/reference/api/rates-engine.v1.yaml`; stale generated content included removed `/currencies` and `/coins` path definitions before regeneration. | CMD-0012; `git diff -- docs/reference/api/rates-engine.v1.yaml` | Supports F-1203 |
| EV-0011 | 2026-05-12 | Regenerating explorer OpenAPI types changed `web/explorer/src/api/types.ts`; stale generated content included removed `/currencies`, `/currencies/{ticker}`, `/coins`, and `/coins/{slug}` path types before regeneration. | CMD-0013; `git diff -- web/explorer/src/api/types.ts` | Supports F-1203 |
| EV-0012 | 2026-05-12 | Active source references still call or publish removed API endpoints: `web/status/src/app/page.tsx` probes `/v1/coins`, `web/explorer/src/app/HomeCurrencies.tsx` calls `/v1/currencies`, converter/embed/asset components call `/v1/currencies`, and `examples/curl/04-coins.sh` calls `/v1/coins`. | CMD-0014; `internal/api/v1/server.go`; `internal/api/v1/coins.go`; `internal/api/v1/currencies.go` | Supports F-1202/F-1204 |
| EV-0013 | 2026-05-12 | `make verify` passed: fmt, vet, lint, docs, import lint, OpenAPI URL lint, race tests, integration build, and explorer/dashboard/status typecheck/lint/build. Explorer lint emitted one non-fatal `react-hooks/exhaustive-deps` warning in `OraclesView.tsx`. | CMD-0015 | Confirms current local gate misses some audit findings |
| EV-0014 | 2026-05-12 | `pnpm audit --audit-level moderate` fails in all three web apps. Each pins `next@15.0.4` and reports 27 advisories: 2 critical, 8 high, 13 moderate, 4 low. | CMD-0016; `web/explorer/package.json`; `web/dashboard/package.json`; `web/status/package.json` | Supports F-1207 |
| EV-0015 | 2026-05-12 | Local gitleaks scan passed: 2,067 commits, about 24.97 MB scanned, no leaks found. | CMD-0017; `.gitleaks.toml` | No secret leak finding from this pass |
| EV-0016 | 2026-05-12 | Static hostile-pattern sweep identified a review queue including production panics, a skipped dispatcher fixture test, TODOs in Ansible/script areas, and many `int64` occurrences requiring amount-vs-counter classification. | CMD-0018 | Inputs to continued per-file audit |
| EV-0017 | 2026-05-12 | Migration inventory contains 28 numbered migration families through `0028_create_fx_quotes`, with Timescale hypertables/materialized views, platform schema, and destructive down migrations. | CMD-0019; `migrations/` | Inputs to W12 |
| EV-0018 | 2026-05-12 | Tracked file topology is dominated by `internal` (651), `docs` (421), `web` (199), `configs` (163), `migrations` (57), `deploy` (41), `cmd` (36), `test` (89). | CMD-0020 | Inputs to coverage reconciliation |
| EV-0019 | 2026-05-12 | External TCP probes from the auditor workstation succeeded against R1 ports 9000, 9001, 9090, 3100, 9080, 9100, 6061, and 11726. | CMD-0021; R1-0008 | Supports F-1201 |
| EV-0020 | 2026-05-12 | Public API audit and smoke scripts both pass against `https://api.ratesengine.net`, but their passing checks include legacy `/v1/coins` and `/v1/currencies` routes that current local source no longer mounts. | CMD-0022; CMD-0023; `scripts/dev/audit-public-api.sh`; `scripts/dev/r1-smoke.sh` | Supports F-1202/F-1204 |
| EV-0021 | 2026-05-12 | Status-page operational docs disagree with the shipped implementation. `deploy/status-page/README.md` says the page is `web/status` on Cloudflare Pages, but `docs/operations/status-page-setup.md`, `docs/operations/runbooks/sev-status-page-update.md`, and `deploy/comms/*` still direct operators to Upptime or removed `deploy/status-page/cstate/**` paths; `find deploy/status-page -maxdepth 3 -type d` returned only `deploy/status-page`. | CMD-0024; `deploy/status-page/README.md`; `docs/operations/status-page-setup.md`; `docs/operations/runbooks/sev-status-page-update.md`; `deploy/comms/README.md`; `deploy/comms/incident-update.md` | Supports F-1211 |
| EV-0022 | 2026-05-12 | The local `configs/ansible/inventory/r1.secrets.yml` vault file is not tracked and is ignored by `.gitignore:64` (`/configs/ansible/inventory/r*.yml`); `.gitignore:56-57` also ignores `*.secrets.yml` and `*.secrets.yaml`. | CMD-0025; `.gitignore`; `configs/ansible/inventory/r1.secrets.yml` | Negative evidence: no tracked-vault-secret finding from this pass |
| EV-0023 | 2026-05-12 | Dashboard key creation lets a logged-in owner/admin/member choose `rate_limit_per_min` up to 100,000/min and stores that value on the key. `auth.PostgresAPIKeyValidator` returns it as `Subject.RateLimitPerMin`, and `middleware.RateLimitBySubject` uses positive values as the effective per-key limit. The dashboard UI exposes the same input with `max={100000}`. | CMD-0026; `web/dashboard/src/app/keys/page.tsx`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/ratelimit.go` | Supports F-1212 |
| EV-0024 | 2026-05-12 | `aggregate.EnableStablecoinFiatProxy` expands `XLM/fiat:USD` to classic/SAC USD-pegged Stellar quotes from `[trades].usd_pegged_classic_assets`, but `dropForMinUSDVolume` calls `windowUSDVolume`, which sums `QuoteAmount` and divides by `100_000_000`. The storage USD-volume path documents and implements classic/SAC USD-peg quotes as 7-decimal values. Therefore a true $10,000 classic-USDC window is read as $1,000 by the min-volume gate when proxy expansion and the default `aggregate.min_usd_volume=10000` are both active. R1 currently sets `enable_stablecoin_fiat_proxy=true` and `min_usd_volume=0`, which avoids the drop but disables the manipulation threshold. | CMD-0027; CMD-0028; `internal/aggregate/orchestrator/orchestrator.go`; `internal/aggregate/stablecoin.go`; `internal/storage/timescale/usd_volume_quote_spec.go`; `internal/storage/timescale/trades.go`; `configs/example.toml`; R1-0011 | Supports F-1213 |
| EV-0025 | 2026-05-12 | Hosted GitHub controls do not enforce the local governance claims. `main` reports `protected=false`; branch protection and repository rulesets return private-tier feature errors; the `r1`, `explorer-production`, `docs-production`, `status-production`, and `github-pages` environments all have empty `protection_rules` and `can_admins_bypass=true`; repository Actions settings allow all actions and do not require SHA pinning. | CMD-0029; GitHub API | Supports F-1214/F-1215/F-1216 |
| EV-0026 | 2026-05-12 | Local governance/release docs require green CI, CODEOWNER review, and signed commits, and `CONTRIBUTING.md` explicitly says branch protection enforces signed commits. `deploy.yml` has an `environment: r1` but its comment says required reviewers are optional/empty by default. Workflows use third-party reusable actions by version tags rather than immutable SHAs while hosted Actions settings allow all actions and `sha_pinning_required=false`. | CMD-0030; `CONTRIBUTING.md`; `CODEOWNERS`; `.github/workflows/*.yml`; `docs/operations/deploy-workflow.md` | Supports F-1214/F-1215/F-1216 |
| EV-0027 | 2026-05-12 | Initial SEP-10 pass identified replay risk and confirmed R1 currently returns 503 because SEP-10 is not configured. A later current-source pass found Redis replay-guard support and supersedes the earlier "no used-challenge store exists" wording; the remaining issue is that replay protection is optional/guard-free when Redis is absent. | CMD-0031; CMD-0032; CMD-0057; `internal/auth/sep10/validator.go`; `internal/auth/sep10/redisreplay.go`; `internal/api/v1/auth_sep10.go`; R1-0012 | Superseded by EV-0053; still supports updated F-1217 |
| EV-0028 | 2026-05-12 | Public `/v1/signup` mints an immediately usable Starter API key at 1000 req/min from an email string with no email verification or challenge. Duplicate detection is optional (`Signups` nil still succeeds twice by test), and the Redis duplicate guard is lookup -> create key -> SETNX mark, so concurrent same-email signups can mint multiple keys even when Redis is wired. The public signup page advertises Starter as free at 1000 req/min per key. | CMD-0031; `internal/api/v1/signup.go`; `internal/api/v1/signup_test.go`; `internal/auth/signup_tracker.go`; `web/explorer/src/app/signup/page.tsx`; `openapi/rates-engine.v1.yaml` | Supports F-1218 |
| EV-0029 | 2026-05-12 | Negative evidence for the cache-control concern: `policyForPath` explicitly covers SEP-10 auth as `private, no-store` and defaults unknown routes to `private, no-store`. Therefore dashboard auth (`/v1/auth/login`, `/v1/auth/callback`, `/v1/auth/logout`), dashboard keys, signup, Stripe webhook, and methodology do not fall through to public caching. SSE handlers and incidents atom also set their own cache-control headers. | CMD-0031; `internal/api/v1/middleware/cachecontrol.go`; `internal/api/v1/middleware/cachecontrol_test.go`; `internal/api/v1/dashboardauth/handlers.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/api/v1/incidents.go`; `internal/api/v1/price_stream.go` | Negative evidence; no cache-control finding from this cold pass |
| EV-0030 | 2026-05-12 | Initial Stripe pass identified that paid upgrades are split from the platform account/subscription/dashboard-key source of truth. Later current-source review found Postgres `stripe_event_log` dedupe and audit wiring, so the earlier "no implementation/use" wording is superseded. The remaining issue is that the webhook still upgrades Redis keys by `client_reference_id` and does not update platform subscriptions, accounts, or Postgres dashboard keys; no-key and failed-upgrade outcomes can still be acknowledged. | CMD-0033; CMD-0057; `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go`; `internal/platform/billing.go`; `internal/platform/postgresstore/billing_store.go`; `migrations/0027_platform_v1_schema.up.sql` | Superseded by EV-0053; still supports updated F-1219 |
| EV-0031 | 2026-05-12 | Tagged production deploys can restart binaries without shipping or applying matching migrations. `.github/workflows/deploy.yml` downloads release binaries and calls `configs/ansible/playbooks/deploy-binary.yml`, whose per-binary task backs up, swaps, restarts, and probes binaries only. Migration sync and `ratesengine-migrate up` exist in the initial archival-node role, while the FX-history runbook explicitly documents that releases adding migrations currently require operator-side migration file copy/apply actions. R1 currently reports schema version 28 dirty=false, but the deploy path itself has no schema gate, no migration artifact download, and no ready check comparing binary expectation to `schema_migrations`. | CMD-0034; CMD-0035; `.github/workflows/deploy.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml`; `docs/operations/runbooks/fx-history-missing.md`; R1-0013 | Supports F-1220 |
| EV-0032 | 2026-05-12 | Release/deploy operator docs are stale relative to the current workflows. `.github/workflows/release.yml` explicitly says container images are not built or pushed, but `docs/operations/deploy-workflow.md` and `docs/operations/release-process.md` still describe GHCR image pushes. `docs/operations/release-process.md` also tells operators to find previous release directories under `/opt/ratesengine/release-<tag>/`, while the actual deploy task keeps at most five backups as `/usr/local/bin/<binary>.prev-<tag>` and version sidecars under `/var/lib/ratesengine/deployed-versions`. | CMD-0034; `.github/workflows/release.yml`; `docs/operations/deploy-workflow.md`; `docs/operations/release-process.md`; `configs/ansible/tasks/deploy-one-binary.yml` | Supports F-1221/F-1222 |
| EV-0033 | 2026-05-12 | R1's live Caddy configuration is stale relative to the repo and publicly exposes API metrics. Source `configs/caddy/Caddyfile.api` contains a Cloudflare `trusted_proxies`/`client_ip_headers` block, forwards `{client_ip}`, and returns 404 for `/metrics`; R1 `/etc/caddy/Caddyfile` lacks that block, forwards `X-Real-IP` and `X-Forwarded-For` as `{remote_host}`, and has no `/metrics` guard. External `curl -skI https://api.ratesengine.net/metrics` returned HTTP 200 and the body included Go runtime metrics, per-route latency histograms/counters, cache counters, stream subscriber counters, and other internal metric names. | CMD-0036; CMD-0037; `configs/caddy/Caddyfile.api`; `configs/caddy/README.md`; `internal/api/v1/middleware/remoteip.go`; R1-0014 | Supports F-1223 |
| EV-0034 | 2026-05-12 | Dashboard magic-link and session IP capture bypasses the middleware-resolved client IP. The request middleware can resolve trusted `X-Forwarded-For` into context via `RemoteIPFrom`, but `internal/api/v1/dashboardauth/handlers.go` defines its own `clientIP(r)` that parses `r.RemoteAddr`. Behind Caddy, `r.RemoteAddr` is the local proxy peer, so `magic_link_tokens.requested_ip`, session `ip_first_seen`/`ip_last_seen`, and `TouchSession` updates record loopback/proxy IP rather than the customer IP. Migration 0027 says these fields drive new-login/new-country security signals. | CMD-0038; `internal/api/v1/dashboardauth/handlers.go`; `internal/api/v1/dashboardauth/middleware.go`; `internal/api/v1/middleware/remoteip.go`; `internal/platform/postgresstore/token_store.go`; `internal/platform/postgresstore/user_store.go`; `migrations/0027_platform_v1_schema.up.sql`; R1-0014 | Supports F-1224 |
| EV-0035 | 2026-05-12 | Historical XLM/USD surfaces are inconsistent because `/v1/history/since-inception` lacks the stablecoin-fiat fallback implemented on chart/VWAP/TWAP/OHLC/price paths. Source `handleHistorySinceInception` calls `HistoryPoints` on the literal pair and writes the result directly. `handleChart` retries `X/fiat:USD` against configured USD-pegged classics through `chartStablecoinFallback`, and the raw-window endpoints have equivalent fallback helpers. On R1, `native/fiat:USD` since-inception returned empty points while `native/fiat:USD` chart returned daily points with `triangulated=true`, and direct `native/USDC-GA5Z...` since-inception history returned populated daily points. | CMD-0039; `internal/api/v1/history.go`; `internal/api/v1/chart.go`; `internal/api/v1/vwap.go`; `internal/api/v1/ohlc.go`; `internal/api/v1/price.go`; R1-0015 | Supports F-1225 |
| EV-0036 | 2026-05-12 | Dashboard-created API keys expose policy fields that are not enforced by the runtime request path. Migration 0027 and `platform.APIKey` include `monthly_quota`, `permissions`, `ip_allowlist`, `referer_allowlist`, and last-used columns. Dashboard create/list accepts and returns those fields. `PostgresAPIKeyValidator.Lookup` receives only the plaintext key, checks hash/revocation/expiry/account status, and returns an `auth.Subject` with identity/tier/key/rate-limit fields; the subject type and auth middleware do not carry request IP, referer, permissions, monthly quota, or usage updates. `APIKeyStore.TouchUsage` exists, but the repo search found no production caller. | CMD-0040; `migrations/0027_platform_v1_schema.up.sql`; `internal/platform/apikey.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/auth.go`; `openapi/rates-engine.v1.yaml`; `web/dashboard/src/lib/api.ts` | Supports F-1226 |
| EV-0037 | 2026-05-12 | The self-hosted `ratesengine-migrate` container cannot apply bundled migrations out of the box. The binary defaults to `-migrations migrations`, and the runtime Dockerfile copies only `/usr/local/bin/ratesengine-migrate` into a distroless image, not `migrations/*.sql`. The Docker smoke target only runs each image with `--help`, so it cannot catch the missing directory. The production Ansible role works around the same requirement by syncing migrations to `/usr/local/share/ratesengine/migrations` and invoking the binary with `-migrations /usr/local/share/ratesengine/migrations`; Docker docs do not provide an equivalent mount/copy instruction. | CMD-0041; `cmd/ratesengine-migrate/main.go`; `docker/ratesengine-migrate.Dockerfile`; `Makefile`; `docker/README.md`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml` | Supports F-1227 |
| EV-0038 | 2026-05-12 | R1 SSE streams are capped at about 30 seconds by the API server timeout. `cmd/ratesengine-api` sets `http.Server.WriteTimeout` to `30 * time.Second`; Go documents that timeout as a response-write deadline reset when a new request's headers are read, not a handler-controlled per-write deadline. The streaming handlers and docs expect long-lived SSE with 15s heartbeats and customer demos that run for 60 seconds or longer. A loopback R1 probe of `/v1/price/tip/stream?asset=native&quote=fiat:USD` emitted `tip_update` events at 0, 5, 10, 15, 20, and 25 seconds, then ended with `elapsed 30`. | CMD-0042; `cmd/ratesengine-api/main.go`; `internal/api/streaming/handler.go`; `internal/api/v1/price_tip_stream.go`; `internal/api/v1/observations_stream.go`; `internal/api/v1/price_stream.go`; `docs/operations/customer-demo-script.md`; R1-0016 | Supports F-1228 |
| EV-0039 | 2026-05-12 | The CDN verification script does not exercise the API surfaces it claims to verify. `scripts/dev/verify-cdn.sh` calls `/v1/price?base=native&quote=fiat:USD` and `/v1/price/tip/stream?base=native&quote=fiat:USD`, but price/tip handlers and OpenAPI use `asset`, not `base`, for those endpoints. R1 returns 400 `missing-asset` for the script's SSE URL. The same script expects the SSE cache header to contain `no-store`, while the SSE writer deliberately sets `Cache-Control: no-cache`. | CMD-0043; `scripts/dev/verify-cdn.sh`; `internal/api/v1/price_tip.go`; `internal/api/v1/price_tip_stream.go`; `internal/api/streaming/handler.go`; `openapi/rates-engine.v1.yaml` | Supports F-1229 |
| EV-0040 | 2026-05-12 | R1's direct XLM/USDC since-inception series is not actually since inception or even one year. The API returned nine daily buckets for `native` quoted by Circle USDC, with first point `2026-05-03T00:00:00Z` and last point `2026-05-11T00:00:00Z`. The Freighter RFP asks for historical retention of at least one year and ideally since inception; the project coverage matrix calls the target ledger 2 -> today and identifies `cmd/ratesengine-ops backfill` as the backfill path. The handler itself returns whatever closed buckets exist; it does not expose a completeness marker warning clients that the deployment's historical backfill is incomplete. | CMD-0044; `docs/freighter-rfp.md`; `docs/architecture/coverage-matrix.md`; `internal/api/v1/history.go`; `cmd/ratesengine-ops/backfill.go`; R1-0017 | Supports F-1230 |
| EV-0041 | 2026-05-12 | The canonical GitHub `ci` workflow is not a post-merge safety net. `.github/workflows/ci.yml` triggers on `pull_request` only and comments that push-to-main was removed to reduce duplicate CI burn. The only push-to-main workflow observed for the endpoint-removal change was `api-audit`, whose trigger is path-limited and runs the public API smoke script rather than lint, tests, web builds, govulncheck, gitleaks, import checks, or monitoring-rule validation. Earlier hosted-control evidence shows `main.protected=false`, so the PR-only assumption is not enforceable. | CMD-0045; `.github/workflows/ci.yml`; `.github/workflows/api-audit.yml`; EV-0025; GitHub API recent workflow runs | Supports F-1231 |
| EV-0042 | 2026-05-12 | Core stablecoin USD price behavior is inconsistent across API surfaces. On R1, `/v1/assets/USDC-GA5Z...` returned `price_usd:"0.9999838427"`, while `/v1/price?asset=USDC-GA5Z...&quote=fiat:USD` returned 404 and both GET and POST `/v1/price/batch` returned `data:[]` for the same asset/quote. Source explains the miss: `tryStablecoinFiatProxy` walks declared USD pegs but skips a peg when `peg.Equal(asset)`, so the canonical Circle USDC asset itself cannot be priced as USD through the price endpoint unless a literal `USDC/fiat:USD` row or separate coin overlay path is used. Asset detail can still have a coin-overlay `price_usd`, creating cross-surface disagreement. | CMD-0046; `internal/api/v1/price.go`; `internal/api/v1/assets.go`; `internal/api/v1/assets_f2.go`; `internal/api/v1/assets_coin_extension.go`; `openapi/rates-engine.v1.yaml`; R1-0018 | Supports F-1232 |
| EV-0043 | 2026-05-12 | Post-update docs lint passed after adding findings through F-1232 and remediation items through R-1230. | CMD-0047; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0044 | 2026-05-12 | The SDEX source cannot fulfill ledger-2/genesis historical coverage for old classic DEX fills. `decodeClaimAtom` returns `ErrUnknownClaimAtomType` for `ClaimAtomTypeV0`, the legacy raw-Ed25519 claim-atom shape used by old protocol ledgers. The dispatcher contract would count decoder errors only when `OpDecoder.Decode` returns an error, but `internal/sources/sdex.Decoder.Decode` catches each `decodeClaimAtom` error and `continue`s, returning success with fewer or zero outputs. Therefore replaying old SDEX ledgers silently drops V0 claim atoms and does not increment `ratesengine_source_decode_errors_total{source="sdex"}`. This contradicts the source README/backfill claim that SDEX trades go back to genesis. | CMD-0048; `internal/sources/sdex/decode.go`; `internal/sources/sdex/dispatcher_adapter.go`; `internal/dispatcher/dispatcher.go`; `internal/pipeline/processor.go`; `cmd/ratesengine-ops/backfill.go`; `internal/sources/sdex/README.md`; `docs/discovery/dexes-amms/sdex.md`; `docs/discovery/protocol-versions.md` | Supports F-1233 |
| EV-0045 | 2026-05-12 | Oracle decoders have no visible metric for partial coverage misses. Reflector skips unknown `Asset::Other(Symbol)` entries inside `sdkDecodeUpdateBody`; Redstone skips unknown `feed_id` entries inside `decodeWritePrices`; Band skips unknown `symbol_rates` entries inside `decodeRelayArgs`. Their tests pin the mixed-batch behavior: known BTC/ETH entries land while unknown NOTACOIN/BENJI-style entries are omitted. The dispatcher increments `DecodeErrors` only when the decoder returns an error, so a mixed event with at least one known entry returns success and produces no decode-error metric. Existing docs reference decoder stats/decode-errors, but the source has no per-entry unknown-symbol/feed counter or coverage reconciliation against upstream feed lists. | CMD-0049; `internal/sources/reflector/decode.go`; `internal/sources/reflector/decode_test.go`; `internal/sources/redstone/decode.go`; `internal/sources/redstone/decode_test.go`; `internal/sources/band/decode.go`; `internal/sources/band/decode_test.go`; `internal/dispatcher/dispatcher.go`; `docs/operations/runbooks/decode-errors.md`; `docs/architecture/explorer-data-inventory.md` | Supports F-1234 |
| EV-0046 | 2026-05-12 | External CEX stream parse failures are not counted. Binance, Kraken, Bitstamp, and Coinbase stream loops call their parse functions and `continue` on malformed/unknown frame errors, with comments saying the stream should stay up. None of those branches increments `obs.SourceDecodeErrorsTotal` or a source-specific parse-error metric. The only external runner metrics are poller outcomes and last poll success; websocket parser drops are absent. This conflicts with `internal/sources/external/README.md`, which says every external connector contributes to `ratesengine_source_decode_errors_total{source="<venue>"}` and points operators to the decode-errors runbook. | CMD-0050; `internal/sources/external/binance/streamer.go`; `internal/sources/external/kraken/streamer.go`; `internal/sources/external/bitstamp/streamer.go`; `internal/sources/external/coinbase/streamer.go`; `internal/sources/external/runner.go`; `internal/sources/external/README.md`; `internal/obs/metrics.go`; `docs/operations/runbooks/decode-errors.md` | Supports F-1235 |
| EV-0047 | 2026-05-12 | Supply snapshots can be stamped at a fresh ledger while using stale component observations. `supplyAggregatorLedgers.LatestKnownLedger` and `resolveSnapshotLedger` choose the maximum `last_ledger` across all ingestion cursors. Classic, SEP-41, SAC, trustline, claimable, LP, and XLM readers then query `*AtOrBefore` storage methods for that ledger. Those methods return the most recent row at or before the requested ledger; the reader interfaces return only balances/totals, not the component row ledger or freshness. The `Refresher` inserts the computed `Supply` at the max cursor ledger with no per-component lag check. If an observer cursor stalls while another source advances, the snapshot can look current while some supply components are old. | CMD-0051; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/supply/refresher.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_sep41_reader.go`; `internal/supply/lcm_reader.go`; `internal/storage/timescale/classic_supply_observations.go`; `internal/storage/timescale/account_observations.go` | Supports F-1236 |
| EV-0048 | 2026-05-12 | The CoinMarketCap connector has an identity ambiguity despite the catalogue already storing CMC IDs. `internal/currency/data/seed.yaml` includes `coinmarketcap_id` values, and `Catalogue.CoinMarketCapIDs()` exposes them, but `startExternalConnectors` builds one generic `aggregatorPairs` list and passes it to the CMC poller without IDs. `coinmarketcap.PollOnce` queries `symbol=<tickers>` and explicitly takes `coins[0]` when CMC returns multiple coins for a ticker; `TestPollOnce_MultipleCoinsWithSameTicker_TakesFirst` pins that behavior. The source code comment says multiple coins can share a ticker. | CMD-0052; `internal/currency/data/seed.yaml`; `internal/currency/verified.go`; `cmd/ratesengine-indexer/main.go`; `internal/sources/external/coinmarketcap/poller.go`; `internal/sources/external/coinmarketcap/poller_test.go` | Supports F-1237 |
| EV-0049 | 2026-05-12 | Post-update docs lint passed after adding findings through F-1237 and remediation items through R-1235. | CMD-0053; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0050 | 2026-05-12 | Root governance and workflow files were reviewed directly rather than trusted from docs. The pass confirmed the existing governance/release findings remain the material risks: `main` protection and CODEOWNER/signed-commit claims depend on hosted controls, deployment environments are manually dispatched but unprotected, release/deploy workflows publish/swap binaries without migration coupling, generated API docs are trusted as committed artifacts, k6 SLA evidence is manual-only while the schedule is disabled, and workflows use tag-pinned third-party actions. The PR/release templates, `.gitignore`, `.gitleaks.toml`, `.golangci.yml`, `.spectral.yaml`, and `commitlint.config.js` did not produce additional standalone findings in this pass. | CMD-0054; `.github/**`; `CODEOWNERS`; `CONTRIBUTING.md`; `SECURITY.md`; `.gitignore`; `.gitleaks.toml`; `.golangci.yml`; `.spectral.yaml`; `commitlint.config.js` | Inventory closure evidence for reviewed governance/workflow slice |
| EV-0051 | 2026-05-12 | Hosted dependency-security controls are weaker than the repository's dependency automation implies. GitHub reported vulnerability alerts disabled, and the Dependabot alerts API reported Dependabot alerts disabled for the repository. This compounds the existing web dependency gap: Dependabot config covers gomod/actions/docker but not npm/pnpm, CI lacks `pnpm audit`, and hosted alerting is not available as a backstop. | CMD-0055; `.github/dependabot.yml`; `.github/workflows/ci.yml`; GitHub API | Supports F-1207 |
| EV-0052 | 2026-05-12 | Command package tests passed for `./cmd/...`, establishing a current baseline before command-surface inventory closure. | CMD-0056 | Command-surface verification |
| EV-0053 | 2026-05-12 | Current auth/billing wiring differs from the earlier ledger wording. SEP-10 verification now supports a `ReplayGuard`; `cmd/ratesengine-api` rebuilds the validator with `sep10.NewRedisReplayGuard(rdb)` when Redis is configured, and tests cover Redis SETNX replay rejection. The same code still allows a configured SEP-10 validator without Redis, and startup explicitly leaves the validator guard-free when Redis is absent, so `auth_mode=sep10` is not coupled to replay protection. Stripe webhook processing now wires Postgres `BillingStore` for event dedupe and `AuditStore` for audit rows when Postgres is available, but the mutation manager remains `auth.RedisAPIKeyStore`: the webhook looks up keys by `client_reference_id`, updates Redis key rate limits only, does not call `UpsertSubscription`, does not update platform Postgres API keys/accounts, acknowledges no-key paid events with 200, and can return 200 after partial or total key-update failure. | CMD-0057; `cmd/ratesengine-api/main.go`; `internal/auth/sep10/validator.go`; `internal/auth/sep10/redisreplay.go`; `internal/auth/sep10/redisreplay_test.go`; `internal/api/v1/stripe_webhook.go`; `internal/platform/postgresstore/billing_store.go`; `migrations/0027_platform_v1_schema.up.sql` | Supersedes stale portions of EV-0027 and EV-0030; supports updated F-1217/F-1219 |
| EV-0054 | 2026-05-12 | API Redis optionality is broken by closed-bucket stream subscriber wiring. `cmd/ratesengine-api` describes Redis as optional and builds many Redis-backed features behind `if rdb != nil`, but it creates `hub := streaming.NewHub(0)` unconditionally and later gates `redispub.NewSubscriber(rdb, DefaultChannel, hub, ...)` on `hub != nil` rather than `rdb != nil`. `redispub.NewSubscriber` returns `redispub: RedisSubscriber is required` when the Redis client is nil. Config docs say empty `api.streaming.pairs` disables only the producer and clients receive SSE heartbeats, and source comments say Redis-less deployments still serve prices without freeze/cache visibility. In reality, a deliberately Redis-less API deployment fails startup before serving HTTP. | CMD-0058; `cmd/ratesengine-api/main.go`; `internal/api/streaming/redispub/subscriber.go`; `internal/api/streaming/redispub/subscriber_test.go`; `docs/reference/config/README.md`; `configs/example.toml` | Supports F-1238 |
| EV-0055 | 2026-05-12 | Command-surface audit covered all `cmd/**` files and tests. Existing findings account for the material API, indexer, aggregator, migration, release, CMC, supply, stablecoin, SSE, and SLA/runtime interactions. A new ops-tool reliability issue remains: both `ratesengine-ops wasm-history` and `ratesengine-ops extract-wasm-from-galexie` accept `-progress-every 0` because the flag is an unsigned interval, but after the ledger stream finishes they still execute `workerScanned % progressEvery`. The in-loop progress print is guarded by `progressEvery > 0`, so zero is a plausible "disable progress" value, but it panics at completion. The tests cover range splitting, transition merging, WASM extraction entry-bearing changes, and archive verification, but do not cover zero progress intervals. | CMD-0059; `cmd/ratesengine-ops/main.go`; `cmd/ratesengine-ops/wasm_extract.go`; `cmd/ratesengine-ops/wasm_history_test.go`; `cmd/ratesengine-ops/wasm_extract_test.go`; `cmd/ratesengine-ops/verify_archive_chunks.go`; `cmd/ratesengine-sla-probe/*`; `cmd/ratesengine-{api,indexer,aggregator,migrate}/*` | Supports F-1239; inventory closure evidence for `cmd/**` |
| EV-0056 | 2026-05-12 | Post-update docs lint passed after adding command-surface finding F-1239, cross-file interaction XFI-0031, command evidence CMD-0059/CMD-0060, and marking `cmd/**` inventory rows done. | CMD-0060; `./scripts/ci/lint-docs.sh`; [inventory/file-coverage.tsv](../inventory/file-coverage.tsv) | Reconciliation sanity check |
| EV-0057 | 2026-05-12 | Docker image build toolchain is not aligned with the pinned/release Go toolchain. `go.mod` declares `go 1.25.10`; CI and release workflows use `actions/setup-go` with `go-version-file: go.mod`; `docker/README.md` says the builder stage uses `golang:1.25-alpine` and runs the same build invocation as release so locally-built images and CI-released binaries are byte-equivalent at the binary level. All six Dockerfiles actually use `FROM golang:1.26-alpine AS builder`. The migration-image file-copy gap remains separately tracked as F-1227. | CMD-0061; `go.mod`; `.github/workflows/ci.yml`; `.github/workflows/release.yml`; `.github/workflows/release-validate.yml`; `docker/*.Dockerfile`; `docker/README.md`; `Makefile` | Supports F-1240; inventory closure evidence for `docker/**` |
| EV-0058 | 2026-05-12 | The live migration tree now contains 29 dense paired migration families, with matching `.up.sql` / `.down.sql` files from `0001` through `0029`. `migrations/README.md` still documents only `0001` through `0015` in its "Current migrations" table and explicitly tells maintainers to update that table whenever a migration lands. The omission hides newer FX history, platform, source-contribution, router, MEV/TVL, classic-registry, and schema-maintenance migrations from the operator-facing migration index. | CMD-0062; `migrations/README.md`; `migrations/0016_*.sql` through `migrations/0029_*.sql` | Supports F-1241 |
| EV-0059 | 2026-05-12 | The integration migration round-trip test passed against TimescaleDB with the current migration set. The test created the extension, ran `migrator.Up()` across all available migrations, exercised core schema assertions, then completed `migrator.Down()` without residual object failures. | CMD-0063; `test/integration/migrations_test.go` | Positive W12 evidence; no SQL execution finding from this pass |
| EV-0060 | 2026-05-12 | The per-source contribution persistence path is live but permanently omits USD volume. The aggregator binary wires `ContributionSink: newContributionSink(store)` for every orchestrator run. `aggregate.SourceContribution` carries source weight, base volume, quote volume, and trade count; `timescale.PriceSourceContribution` also has `VolumeUSD *float64`, and migration 0026 says `price_source_contributions.volume_usd` powers the source-donut tooltip. The adapter in `cmd/ratesengine-aggregator/contribution_sink.go` forwards asset, quote, bucket, source, weight, and trade count only, leaving `VolumeUSD` nil for every row; the storage writer then inserts SQL `NULL`. No test or alternative production caller populates that column. | CMD-0064; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-aggregator/contribution_sink.go`; `internal/aggregate/vwap.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/storage/timescale/price_source_contributions.go`; `migrations/0026_create_source_contributions_and_sdex_offers.up.sql` | Supports F-1242 |
| EV-0061 | 2026-05-12 | Post-update docs lint passed after adding W12 findings F-1241/F-1242, cross-file interactions XFI-0033/XFI-0034, migration execution evidence, and remediation rows through R-1240. | CMD-0065; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0062 | 2026-05-12 | The classic-asset registry writer suppresses repeat same-process observations before its correctness SQL can execute. `InsertTrade` calls `registerClassicAssetSeen` for both trade sides after every successful trade insert. `assetRegistryDedupe` stores an asset ID after the first write and returns nil on later touches in the same process. That early return happens before the upsert that is supposed to apply `LEAST(first_seen_*)`, `GREATEST(last_seen_*)`, and `observation_count = observation_count + 1`. As a result, repeated trades for an asset during one long-running indexer/backfill process do not advance `last_seen_*`, do not repair out-of-order `first_seen_*`, and do not increment observation counts. Existing integration tests read/seed `classic_assets` directly rather than exercising this writer path. | CMD-0066; `internal/storage/timescale/trades.go`; `internal/storage/timescale/asset_registry.go`; `internal/storage/timescale/{coins,issuers}.go`; `test/integration/issuers_coins_storage_test.go` | Supports F-1243 |
| EV-0063 | 2026-05-12 | The audit control snapshot drifted during execution. The Codex audit directory was originally anchored to commit `80c57e38...` with 1,747 tracked non-Codex-audit files. Current `HEAD` is `6e873cac...`, and `git ls-files ':!:docs/audit-2026-05-12-codex/**'` now reports 1,869 tracked files. The existing Codex `file-coverage.tsv` still has 1,747 rows, so it no longer represents the full current tracked tree. Concrete proof: `migrations/0029_drop_unused_blend_jsonb_gin_indexes.{up,down}.sql` are tracked and present in Git history, but absent from the Codex inventory. Literal every-file closure therefore requires either a frozen target snapshot or an inventory refresh/merge against the new `HEAD`. | CMD-0067; `docs/audit-2026-05-12-codex/01-tracker.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `migrations/0029_drop_unused_blend_jsonb_gin_indexes.{up,down}.sql` | Audit-control evidence; not a product finding |
| EV-0064 | 2026-05-12 | Post-update docs lint passed after adding classic-asset registry finding F-1243 and documenting execution-time audit-scope drift. | CMD-0068; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0065 | 2026-05-12 | Current `HEAD` has already migrated live explorer/status code and the R1 smoke script away from removed `/v1/coins` and `/v1/currencies` routes. Repo search over the formerly affected live files now finds only explanatory comments, while `web/status/src/app/page.tsx` probes `/v1/assets` instead of `/v1/coins`; `scripts/dev/r1-smoke.sh` keeps only commented historical references. Remaining active stale references in the current tree are narrower: `scripts/dev/audit-public-api.sh` still executes five removed legacy-route probes, and `web/explorer/public/llms.txt` still advertises `GET /v1/coins`. | CMD-0069; `web/explorer/src/app/HomeCurrencies.tsx`; `web/explorer/src/app/convert/[from]/[to]/page.tsx`; `web/explorer/src/app/embed/currency/[ticker]/page.tsx`; `web/explorer/src/app/assets/[slug]/AssetConverter.tsx`; `web/status/src/app/page.tsx`; `scripts/dev/r1-smoke.sh`; `scripts/dev/audit-public-api.sh`; `web/explorer/public/llms.txt` | Supersedes stale portions of EV-0012; narrows F-1204 |
| EV-0066 | 2026-05-12 | R1 now matches the current source contract for the legacy endpoint removal. Running `bash scripts/dev/audit-public-api.sh` against `https://api.ratesengine.net` produced 5 failures, each a 404 for `/v1/coins?limit=5`, `/v1/coins/XLM`, `/v1/coins/USDC`, `/v1/currencies`, and `/v1/currencies/EUR`; all other representative checks in the script passed. That means the earlier source-vs-R1 deployment mismatch has cleared, but the audit script itself is stale. | CMD-0069; `scripts/dev/audit-public-api.sh`; live R1 API | Supersedes EV-0020 for deployment mismatch; supports marking F-1202 fixed and keeps F-1204 open in narrowed form |
| EV-0067 | 2026-05-12 | Generated artifact drift still exists on current `HEAD`, but its shape changed. `./scripts/dev/docs-api.sh` leaves no diff in `docs/reference/api/rates-engine.v1.yaml`, while `pnpm generate:api` leaves a 362-line diff in `web/explorer/src/api/types.ts`, adding dashboard webhook route types and updating stale legacy-route wording. The generated reference YAML is current; the explorer TypeScript client artifact is not. | CMD-0070; CMD-0071; `docs/reference/api/rates-engine.v1.yaml`; `web/explorer/src/api/types.ts` | Supersedes the stale details in EV-0010/EV-0011 while keeping F-1203 open |
| EV-0068 | 2026-05-12 | Dashboard webhook signing-secret semantics are internally contradictory and the implementation persists the live HMAC key material. `generateSecret` returns a customer plaintext secret; `HandleCreate` stores `SecretHash: []byte(secret)` with an inline note that the worker reads it verbatim as the HMAC key; `postgresstore.CreateWebhook` inserts those bytes directly into `customer_webhooks.secret_hash`; and `customerwebhook.Worker` signs deliveries with `wh.SecretHash`. By contrast, `platform.CustomerWebhook.SecretHash`, `WebhookStore.RotateWebhookSecret`, handler comments, and OpenAPI prose all describe the field as a hash or claim the plaintext is returned once and never persisted. | CMD-0072; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `migrations/0027_platform_v1_schema.up.sql`; `openapi/rates-engine.v1.yaml` | Supports F-1244 |
| EV-0069 | 2026-05-12 | Dashboard webhook URL validation is insufficient for a network-capable outbound worker. `validateWebhookURL` only checks non-empty, `https://` prefix, and parseability. It does not block loopback, link-local, RFC1918, cluster-local names, DNS rebinding, redirects, or credential-bearing URLs. The delivery worker uses Go's default `http.Client`, which follows redirects unless overridden, and production wiring starts that worker whenever the dashboard webhook store is enabled. Tests reject plain `http://` but have no private-address, redirect, DNS, or rebinding cases. | CMD-0072; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/customerwebhook/worker.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/dashboardwebhooks/handlers_test.go`; `internal/customerwebhook/worker_test.go` | Supports F-1245 |
| EV-0070 | 2026-05-12 | Targeted webhook package tests passed after the security review: `internal/api/v1/dashboardwebhooks` and `internal/customerwebhook` are green; `internal/platform/postgresstore` has no direct unit tests. The findings are therefore design/contract gaps not regressions caught by current test coverage. | CMD-0073 | Positive verification context for F-1244/F-1245 |
| EV-0071 | 2026-05-12 | Post-update docs lint passed after recording dashboard webhook findings F-1244/F-1245 and updating reconciliation ranges. | CMD-0074; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0072 | 2026-05-12 | Webhook callback documentation disagrees across source-of-truth surfaces. `docs/reference/api-design.md` still closes the design question with "Webhook callbacks? Not in v1. Customers who want push use SSE." Current code/spec/runbooks say otherwise: OpenAPI/reference YAML documents `/v1/dashboard/webhooks`, `/v1/dashboard/webhooks/{id}`, and delivery-log routes; `internal/api/v1/dashboardwebhooks` mounts them; `cmd/ratesengine-api/main.go` starts the delivery worker; and `docs/operations/runbooks/customer-webhook-delivery-failing.md` tells operators how to recover failed webhook callbacks. | CMD-0075; `docs/reference/api-design.md`; `docs/reference/api/rates-engine.v1.yaml`; `internal/api/v1/dashboardwebhooks/doc.go`; `cmd/ratesengine-api/main.go`; `docs/operations/runbooks/customer-webhook-delivery-failing.md` | Supports F-1246 |
| EV-0073 | 2026-05-12 | Customer webhook delivery is not concurrency-safe across multiple API workers. `cmd/ratesengine-api/main.go` starts one `customerwebhook.Worker` for every API process that wires the dashboard webhook store. `postgresstore.ListPendingDeliveries` reads due rows with a plain SELECT and no claim/lease or `FOR UPDATE SKIP LOCKED`; `MarkDelivered` and `MarkAttemptFailed` then update by ID after the HTTP POST. The worker package comment acknowledges the omission, says expected concurrency is one operator-run worker, and treats double delivery as "cosmetically harmless." That assumption conflicts with the repo's documented R2/R3 multi-region application rollout and any horizontal API replica pattern. | CMD-0076; `cmd/ratesengine-api/main.go`; `internal/customerwebhook/worker.go`; `internal/platform/postgresstore/webhook_store.go`; `docs/architecture/r2-r3-bringup.md` | Supports F-1247 |
| EV-0074 | 2026-05-12 | The dashboard webhook account quota is raceable. `HandleCreate` first calls `checkQuota`, which loads all existing hooks for the account and rejects only when `len(hooks) >= 10`; it then separately calls `CreateWebhook`. `postgresstore.CreateWebhook` performs a plain insert into `customer_webhooks`, and migration 0027 defines no database-side cap for "at most ten rows per account." The current test proves only sequential rejection after ten rows already exist. Two or more concurrent creates that each observe fewer than ten rows can all proceed and insert, leaving the account above the advertised cap. | CMD-0077; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/postgresstore/webhook_store.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/api/v1/dashboardwebhooks/handlers_test.go` | Supports F-1248 |
| EV-0075 | 2026-05-12 | Post-update docs lint passed after recording webhook quota finding F-1248, remediation R-1246, and cross-file interaction XFI-0040. | CMD-0078; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0076 | 2026-05-12 | The customer-webhook system has no production enqueue path. `platform.WebhookEventType` defines four customer-facing events; the dashboard webhook handlers accept those subscriptions; OpenAPI and reference docs state the worker "fans out one row per (webhook, event)"; `cmd/ratesengine-api/main.go` starts the worker; and the operations runbook explains how to recover failed callback deliveries. However, source search across `cmd`, `internal`, docs, and OpenAPI finds `EnqueueDelivery(` only in the interface/store implementation and handler test fake, with no production producer invoking it. Event constants likewise appear only in platform definitions, validators, and tests. The shipped callback feature therefore has no real event ingress. | CMD-0079; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/dashboardwebhooks/doc.go`; `openapi/rates-engine.v1.yaml`; `docs/reference/api/rates-engine.v1.yaml`; `docs/operations/runbooks/customer-webhook-delivery-failing.md` | Supports F-1249 |
| EV-0077 | 2026-05-12 | Post-update docs lint passed after recording webhook producer-gap finding F-1249, remediation R-1247, and cross-file interaction XFI-0041. | CMD-0080; `./scripts/ci/lint-docs.sh` | Reconciliation sanity check |
| EV-0078 | 2026-05-12 | The Codex audit inventory has now been refreshed and merged to match the current moving repository state. After rerunning `inventory/generate.sh` at current `HEAD`, the tracked tree excluding `docs/audit-2026-05-12-codex/**` and the TSV row count both equal `1,869`. Previously recorded review progress was preserved rather than reset: `done=64`, `in_progress=35`, `todo=1770`. `inventory/area-counts.md` and `inventory/repo-snapshot.md` were regenerated alongside the TSV. This resolves the earlier count mismatch from EV-0063, but does not close bottom-up audit work because the vast majority of file rows remain non-terminal. | CMD-0081; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md` | Audit-control refresh; not a product finding |
| EV-0079 | 2026-05-12 | Freeze-event open-row dedupe is not concurrency-safe. `freeze.Writer.Mark` can call the durable sink repeatedly for the same firing pair; `FreezeEventSink.RecordFreeze` says its idempotency check and insert are transactionally protected, but the code executes a single `INSERT ... SELECT ... WHERE NOT EXISTS (...) ON CONFLICT (asset_id, quote_id, frozen_at) DO NOTHING` statement without a transaction or lock. Because `frozen_at` is generated per call and belongs to the primary key, concurrent callers that both observe no open row can insert separate open rows with distinct timestamps. The integration test `TestFreezeEventSink_IdempotentOpenRow` exercises only three serial calls and therefore does not cover the race. | CMD-0082; `internal/aggregate/freeze/freeze.go`; `internal/storage/timescale/freeze_events.go`; `test/integration/freeze_events_test.go` | Supports F-1250 |
| EV-0080 | 2026-05-12 | Phase-2 FX-derived USD-volume enrichment is time-anchored incorrectly. `cmd/ratesengine-indexer` wires `VWAPUSDFXResolver` whenever classic USD pegs are configured; `tradeUSDVolumeViaFX` asks it for a quote asset USD rate at the trade timestamp. The resolver's SQL lookup is correctly bounded by `bucket <= at`, but staleness is then evaluated with `r.clock().Sub(observedAt) > freshness`, making wall-clock process age decisive instead of the requested trade timestamp. The targeted integration test seeds a trade/VWAP about two hours in the past and calls the resolver at the matching historical timestamp; current code rejects that anchor with `ok=false`, so `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1` fails at `usd_fx_resolver_test.go:72`. The option docs also say `Freshness: 0` disables the check, while the constructor converts zero to the one-hour default. | CMD-0083; `cmd/ratesengine-indexer/main.go`; `internal/storage/timescale/usd_fx_resolver.go`; `internal/storage/timescale/trades.go`; `test/integration/usd_fx_resolver_test.go` | Supports F-1251 |
| EV-0081 | 2026-05-12 | Audit-control sanity remains green after the latest webhook/freeze/FX findings and TSV progress sync. `./scripts/ci/lint-docs.sh` passed, and the refreshed per-file coverage inventory now rolls up to `done=76`, `in_progress=35`, `todo=1758`. | CMD-0084; `./scripts/ci/lint-docs.sh`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv` | Reconciliation sanity check |
| EV-0082 | 2026-05-12 | The multi-region cutover checklist points at a command that does not exist. `docs/operations/multi-region-cutover.md` Stage 5 pre-flight says `make verify-cross-region`; repository search over `Makefile` finds no matching target, while `scripts/dev/verify-cross-region.sh` and `ratesengine-ops cross-region-check` are the actual implemented checks. | CMD-0085; `docs/operations/multi-region-cutover.md`; `scripts/dev/verify-cross-region.sh`; `Makefile` | Supports F-1252 |
| EV-0083 | 2026-05-12 | Redis ACL lockdown is not coupled to rendered client identity. The Redis role's ACL file disables `default` entirely and defines only a passworded `ratesengine` application user. `internal/config.StorageConfig.RedisUsername` and `redisclient.Build` support that username, and reference config docs say it must be set when ACL lockdown is enabled. But the archival-node `ratesengine.toml.j2` storage section renders no `redis_username` field, `configs/example.toml` has no such sample line, and repo search finds no Ansible/application template path that injects it. A lockdown flip therefore changes the server auth contract without changing the generated client config. | CMD-0086; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/templates/redis.conf.j2`; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/archival-node/templates/ratesengine.toml.j2`; `internal/config/config.go`; `internal/storage/redisclient/redisclient.go`; `docs/reference/config/README.md`; `configs/example.toml` | Supports F-1253 |
| EV-0084 | 2026-05-12 | Redis ACL lockdown's key-pattern allow-list is materially stale versus the active runtime keyspace. `users.acl.j2` allows `~ratelimit:*` and `~subscriber:*`, but the current rate limiter and SSE subscriber registry use `rl:*` and `sub:*` (`internal/ratelimit/bucket.go`, `internal/cachekeys/keys.go`, ADR-0007). The same ACL omits key families that are live in current code: `signup:email:*` for duplicate-signup tracking, `sep10:seen:*` for replay protection, `usage:*` for account usage counters, `assets:list:*` for asset catalogue caching, and `markets:list:*` for market catalogue caching. `cmd/ratesengine-api/main.go` wires each of those Redis-backed paths when Redis is available. With ACL lockdown enabled, fixing only `redis_username` still leaves Redis command denials across rate limiting, signup controls, SEP-10 replay protection, usage reporting, SSE presence tracking, and catalogue read-through caching. | CMD-0087; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `internal/ratelimit/bucket.go`; `internal/cachekeys/keys.go`; `internal/auth/signup_tracker.go`; `internal/auth/sep10/redisreplay.go`; `internal/usage/counter.go`; `cmd/ratesengine-api/main.go`; `docs/adr/0007-redis-cache-schema.md` | Supports F-1254 |
| EV-0085 | 2026-05-12 | Audit-control sanity remains green after recording Redis ACL finding F-1254. `./scripts/ci/lint-docs.sh` passed, the current tracked scope and inventory still reconcile at `1,869` rows each, and the per-file inventory now rolls up to `done=88`, `in_progress=38`, `todo=1743`. | CMD-0088; `./scripts/ci/lint-docs.sh`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv` | Reconciliation sanity check |
| EV-0086 | 2026-05-12 | First-login account provisioning is not atomic or idempotent per email. `HandleLogin` can mint multiple distinct still-valid magic-link tokens for one address. `ConsumeMagicLinkToken` is atomic for each token individually, but `HandleCallback` then does `GetUserByEmail`; on not-found it calls `signupNewUser`, which creates an `accounts` row first and only afterward inserts the `users` row. Migration 0027 enforces unique `accounts.slug` and unique `users.email`; on concurrent callbacks for the same new email, both requests can observe no user, one request creates the base slug/account, the other retries with a suffixed slug after `ErrConflict`, and the losing `CreateUser` then trips `users_email_idx`. The handler returns 500 while the second account row remains persisted without its owner user. | CMD-0089; `internal/api/v1/dashboardauth/handlers.go`; `internal/platform/postgresstore/token_store.go`; `internal/platform/postgresstore/account_store.go`; `internal/platform/postgresstore/user_store.go`; `migrations/0027_platform_v1_schema.up.sql` | Supports F-1255 |
| EV-0087 | 2026-05-12 | Existing tests do not close the first-login provisioning race. `go test ./internal/api/v1/dashboardauth ./internal/platform/postgresstore` passed, but the dashboard-auth package uses fakes for its handler tests and the Postgres store package has no direct unit tests. Current integration coverage verifies duplicate email conflicts in isolation, not concurrent same-email callback provisioning that spans token consume -> account create -> user create. | CMD-0090; `internal/api/v1/dashboardauth/handlers_test.go`; `test/integration/platform_postgres_stores_test.go` | Coverage context for F-1255 |
| EV-0088 | 2026-05-12 | Audit-control sanity remains green after recording first-login provisioning finding F-1255. `./scripts/ci/lint-docs.sh` passed, the current tracked scope and inventory still reconcile at `1,869` rows each, and the per-file inventory now rolls up to `done=95`, `in_progress=39`, `todo=1735`. | CMD-0091; `./scripts/ci/lint-docs.sh`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv` | Reconciliation sanity check |
| EV-0089 | 2026-05-12 | The live security claim behind F-1212 is now fixed on current `HEAD`. `dashboardkeys.HandleCreate` still accepts a requested `rate_limit_per_min`, but immediately clamps it through `clampRateLimitToTier(req.RateLimitPerMin, sc.Account.Tier)`. `platform.Tier.MaxRateLimitPerMin` defines the current ceilings as Free 60, Starter 1000, Pro 10,000, Business 60,000, Enterprise 100,000, and `TestHandleCreate_TierClampsRateLimit` verifies both downward clamps and permitted passthrough cases. `go test ./internal/api/v1/dashboardkeys` passed on this state. | CMD-0092; `internal/api/v1/dashboardkeys/handlers.go`; `internal/api/v1/dashboardkeys/handlers_test.go`; `internal/platform/account.go` | Supersedes the open runtime claim in F-1212 |
| EV-0090 | 2026-05-12 | The customer-facing/dashboard docs around key rate limits are now behind the fixed backend. `web/dashboard/src/app/keys/page.tsx` still renders a generic `max={100000}` input and says "Default for free tier is 1000," while current backend tier logic caps Free at 60/min. `openapi/rates-engine.v1.yaml` likewise documents `CreateKeyRequest.rate_limit_per_min` as "Default 1000" with `maximum: 100000`, but the route description never explains tier-specific server-side clamping. Customers can therefore submit a value the UI/API contract implies is acceptable and silently receive a materially smaller persisted budget in the 201 response. | CMD-0092; `web/dashboard/src/app/keys/page.tsx`; `openapi/rates-engine.v1.yaml`; `internal/platform/account.go`; `internal/api/v1/dashboardkeys/handlers.go` | Supports F-1256 |
| EV-0092 | 2026-05-12 | The dashboard API-key account quota is raceable. `HandleCreate` calls `checkQuota`, which loads all keys for the account and counts rows whose `RevokedAt` is zero; only after that check passes does it mint a key and call `APIKeyStore.Create`. Migration 0027 defines indexes for active and all account keys, but no invariant that enforces `<= 25` active rows per account. The current handler test `TestHandleCreate_QuotaEnforced` pre-seeds 25 rows and verifies only serial rejection. Concurrent creates that each observe fewer than 25 active keys can all insert and leave the account above the advertised ceiling. | CMD-0094; `internal/api/v1/dashboardkeys/handlers.go`; `internal/api/v1/dashboardkeys/handlers_test.go`; `internal/platform/postgresstore/apikey_store.go`; `migrations/0027_platform_v1_schema.up.sql` | Supports F-1257 |
| EV-0093 | 2026-05-12 | Audit-control sanity remains green after reconciling dashboard-key behavior. `./scripts/ci/lint-docs.sh` passed, the current tracked scope and inventory still reconcile at `1,869` rows each, and the per-file inventory now rolls up to `done=97`, `in_progress=38`, `todo=1734`. | CMD-0095; `./scripts/ci/lint-docs.sh`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv` | Reconciliation sanity check |
| EV-0094 | 2026-05-12 | Redis-less API deployments are not actually safe for usage tracking despite comments saying they degrade to no-op. `cmd/ratesengine-api/main.go` constructs `usageCounter := usage.New(rdb)` unconditionally, then always passes `middleware.UsageTracker(usageCounter, ...)` into the server. The middleware only skips work when `counter == nil`; it does not inspect whether the embedded Redis client is nil. `usage.Counter.Increment` calls `c.rdb.TxPipeline()` and `Read` calls `c.rdb.MGet(...)` directly. When `redisclient.Build` returns nil, any authenticated request that reaches `UsageTracker` can panic after the wrapped handler returns, contradicting the "Redis optional" startup path and the local comment claiming the middleware degrades cleanly. | CMD-0096; `cmd/ratesengine-api/main.go`; `internal/api/v1/middleware/usage.go`; `internal/usage/counter.go`; `internal/api/v1/server.go` | Supports F-1258 |
| EV-0095 | 2026-05-12 | `/v1/account/usage` is no longer an always-empty placeholder on current `HEAD`, but several published/current docs still say it is. `cmd/ratesengine-api/main.go` wires both `UsageTracker` and `usageReaderAdapter{c: usageCounter}` into `v1.Options`; `internal/api/v1/account.go` reads trailing 30-day data from `s.usageReader` and emits real `UsageRow` values when present. Yet the same file's doc comment still says the endpoint "always returns an empty []"; OpenAPI and generated reference YAML say "Usage-summary placeholder (currently empty)"; `docs/reference/api-design.md`, `docs/architecture/explorer-data-inventory.md`, and the generated Postman collection retain the placeholder description. `go test ./internal/api/v1 ./internal/usage` passed against the current implementation. | CMD-0097; `cmd/ratesengine-api/main.go`; `internal/api/v1/account.go`; `internal/api/v1/server.go`; `openapi/rates-engine.v1.yaml`; `docs/reference/api/rates-engine.v1.yaml`; `docs/reference/api-design.md`; `docs/reference/api/postman-collection.json`; `docs/architecture/explorer-data-inventory.md` | Supports F-1259 |
| EV-0096 | 2026-05-12 | Four earlier findings are fixed in the current shared workspace. `cmd/ratesengine-api/main.go` now returns startup error when `auth_mode=sep10` is selected without Redis, closing the guard-free replay deployment path from F-1217. The same file now gates `redispub.NewSubscriber` on `rdb != nil && hub != nil`, closing the Redis-less startup failure from F-1238. Customer webhook creation now rejects credential-bearing/internal/private/reserved destination hosts during validation, the worker's default HTTP client uses `ssrfGuardedDialContext` plus `CheckRedirect: http.ErrUseLastResponse`, and `internal/customerwebhook/ssrf.go` re-resolves before dialing, closing the outbound SSRF path from F-1245. `docs/reference/api-design.md` now states webhook callbacks shipped and explains the webhook/SSE split, closing F-1246. Targeted package tests passed: `./internal/api/v1/dashboardwebhooks`, `./internal/customerwebhook`, and `./cmd/ratesengine-api`. | CMD-0098; `cmd/ratesengine-api/main.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/customerwebhook/worker.go`; `internal/customerwebhook/ssrf.go`; `docs/reference/api-design.md` | Current-workspace fixes for F-1217/F-1238/F-1245/F-1246 |
| EV-0097 | 2026-05-12 | Audit controls are reconciled again after the repository advanced to `HEAD=9f68ef9aae5aa0ea836d32cc805fc5c63d2e12fd` and `internal/customerwebhook/ssrf.go` entered tracked scope. `./scripts/ci/lint-docs.sh` passed, the current tracked tree and TSV inventory both report `1,870` rows, and the TSV roll-up now reads `done=99`, `in_progress=41`, `todo=1730`. `git status --short` currently shows 15 dirty paths, including non-audit edits outside the audit directory that are being preserved rather than reverted. | CMD-0099; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `docs/audit-2026-05-12-codex/inventory/area-counts.md` | Reconciliation sanity check after live scope drift |
| EV-0098 | 2026-05-12 | The settled Wave 5 remediation commit at `HEAD=7c9e79ae06467090f5272681cd035bc83199b3c2` closes four prior audit claims and exposes one failed partial fix. Binance, Bitstamp, Coinbase, and Kraken websocket streamers now increment `obs.SourceDecodeErrorsTotal` on skipped parse failures, closing F-1235. `postgresstore.ListPendingDeliveries` now claims due rows via `FOR UPDATE SKIP LOCKED` and immediately pushes `next_attempt_at` five minutes forward, closing the duplicate multi-worker callback race in F-1247. ACL-lockdown rendering now writes `redis_username = "{{ redis_acl_username | default('ratesengine') }}"` into the Ansible-owned application TOML and `users.acl.j2` now permits the live `rl:*`, `sub:*`, `signup:email:*`, `sep10:seen:*`, `usage:*`, `assets:list:*`, and `markets:list:*` families, closing F-1253/F-1254. The new webhook-cap store path does not close F-1248: `CreateWebhook` reads `COUNT(*)` inside a CTE and then inserts when `n < max`, but two concurrent statements can share the same PostgreSQL snapshot before either commit, both observe the same below-cap count, and both insert. Targeted packages passed: `./internal/api/v1/dashboardwebhooks`, `./internal/platform/postgresstore` (no direct test files), and all four external CEX streamer packages. | CMD-0100; `internal/sources/external/{binance,bitstamp,coinbase,kraken}/streamer.go`; `internal/platform/postgresstore/webhook_store.go`; `configs/ansible/roles/archival-node/templates/ratesengine.toml.j2`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2` | Fix evidence for F-1235/F-1247/F-1253/F-1254; preservation evidence for F-1248 |
| EV-0099 | 2026-05-12 | The settled Wave 7 remediation commit at `HEAD=27343a46ba3331755839736f3b73a167cce0bf4c` changes three existing audit findings. `.github/workflows/ci.yml` now includes `push: branches: [main]`, so direct code pushes to the currently unprotected default branch once again trigger the canonical workflow; that closes F-1231 while F-1214 remains open for the hosted branch-protection gap itself. The same workflow now runs `pnpm audit --audit-level high` in explorer, dashboard, and status jobs; that narrows F-1207 but does not close it because the apps still pin vulnerable `next@15.0.4`, Dependabot still omits npm/pnpm ecosystems, and hosted vulnerability/Dependabot alerts remain disabled. The signup path now calls `ReserveEmail` before minting and the Redis tracker implements that reservation with `SETNX` plus a five-minute pending TTL, closing the same-email race when Redis tracking is wired. F-1218 remains open because `/v1/signup` still returns a usable plaintext Starter key without email ownership proof and the intentionally supported `signups == nil` path still disables duplicate protection entirely. `go test ./internal/api/v1 ./internal/auth` passed. | CMD-0101; `.github/workflows/ci.yml`; `internal/api/v1/signup.go`; `internal/api/v1/signup_test.go`; `internal/auth/signup_tracker.go` | Fix evidence for F-1231; partial-remediation evidence for F-1207/F-1218 |
| EV-0100 | 2026-05-12 | The shared workspace contains an in-progress `F-1226` remediation slice, but it is not end-to-end safe yet. `cmd/ratesengine-api/main.go` now passes `middleware.KeyPolicy()` into `v1.New`, `internal/api/v1/server.go` inserts that middleware after auth and before rate limiting, and `internal/auth.PostgresAPIKeyValidator` now copies IP allowlists, referer allowlists, and permission entries onto `auth.Subject`. However, the read-through cache path still serializes `APIKeyRecord` without any of those new fields and `cacheLookup` reconstructs a policy-empty subject from that legacy shape, so a Postgres-served first request can be policy-restricted while subsequent Redis cache hits silently bypass the same restrictions. The workspace also still has no monthly-quota request enforcement and no production caller of `APIKeyStore.TouchUsage`. `internal/api/v1/middleware/keypolicy.go` is untracked at this checkpoint, so the scope is active shared-workspace code rather than committed tracked inventory. `go test ./internal/api/v1/middleware ./internal/auth` passed. | CMD-0102; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/middleware/keypolicy.go`; `internal/auth/subject.go`; `internal/auth/apikey_postgres.go`; `internal/auth/apikey_redis.go`; `internal/platform/postgresstore/apikey_store.go` | Partial-remediation evidence that keeps F-1226 open |
| EV-0101 | 2026-05-12 | Audit controls are reconciled again after the Wave 8 policy middleware and its test file landed in tracked scope at `HEAD=8a61c17e3300dc93e36a608571f2dd8485b88c44`. `./scripts/ci/lint-docs.sh` passed, the current tracked tree and TSV inventory both report `1,872` rows, and the TSV roll-up now reads `done=99`, `in_progress=50`, `todo=1723`. The snapshot-status probe showed only the two inventory-maintenance files dirty at that moment, which is expected while this reconciliation is being written. | CMD-0103; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md` | Reconciliation sanity check after Wave 8 tracked-scope growth |
| EV-0102 | 2026-05-12 | The settled Wave 9 commit at `HEAD=82a6052c1060a4bd9ef369b8c796404e485eb3b6` materially narrows three open findings without closing them. CMC identity handling improved: the indexer now passes `catalogue.CoinMarketCapIDs()` into the poller and the poller emits `id=` parameters for mapped tickers. Closure is still not justified because `cmd/ratesengine-ops/main.go` still constructs the CMC poller without IDs, committed CMC tests cover only symbol-shaped responses, and the ID-mode response mapping path remains unproven. FX freshness improved: `VWAPUSDFXResolver` now compares `at.Sub(observedAt)` so the historical VWAP anchor is no longer rejected on wall-clock age. The targeted integration now gets a value and fails later on contract drift instead, reporting `USDPriceAt = "1.08500000000000000000", want "1.085"`; the documented `Freshness: 0` disable behavior still disagrees with the constructor defaulting logic. Dashboard first-login races improved: `signupNewUser` now catches the losing `CreateUser` conflict and reloads the winner user, but the speculative loser account row can still be left orphaned after account creation succeeds before the user uniqueness collision. Package tests passed for `./internal/sources/external/coinmarketcap`, `./internal/storage/timescale`, and `./internal/api/v1/dashboardauth`; tracked scope still reconciles at `1872` rows. | CMD-0104; `cmd/ratesengine-indexer/main.go`; `cmd/ratesengine-ops/main.go`; `internal/sources/external/coinmarketcap/poller.go`; `internal/sources/external/coinmarketcap/poller_test.go`; `internal/storage/timescale/usd_fx_resolver.go`; `test/integration/usd_fx_resolver_test.go`; `internal/api/v1/dashboardauth/handlers.go` | Partial-remediation evidence that preserves F-1237/F-1251/F-1255 |
| EV-0103 | 2026-05-12 | The shared workspace after settled `HEAD=82a6052c...` contains a broader Wave 10 remediation slice, but several claims remain open after adversarial review. Deploy-migration coupling improves on paper: `.github/workflows/deploy.yml` now stages tag-matched migrations and `configs/ansible/playbooks/deploy-binary.yml` runs sync + `ratesengine-migrate up` before binary swap. The actual deploy environment still installs only `ansible-core==2.18.*`, while the new playbook calls `ansible.posix.synchronize`; Ansible's own docs state that module belongs to the separate `ansible.posix` collection, so the workflow/playbook combination is not yet self-contained. Contribution-history USD volume now attempts to persist non-null values, but `USDVolumeTotal` is computed before class/outlier filtering while source weights are computed after filtering, so `volume_usd = total * weight` can misstate the surviving contribution set. Dashboard API-key quota handling now returns `ErrAPIKeyQuotaExceeded`, but the store uses an unlocked `WITH active_count AS (...) INSERT ... WHERE n < max` pattern with the same normal-MVCC race already preserved for webhook quotas. Redis-less usage handling improves the middleware path by omitting a nil-backed counter, yet API wiring still passes `UsageReader: usageReaderAdapter{c:nil}`, and `/v1/account/usage` can therefore dereference nil through `Read`. The source OpenAPI usage prose has started moving, but generated/reference docs and the handler comment remain stale, and the new source text incorrectly says Redis-less degradation is visible on `/v1/healthz` `checks` even though checks are `/readyz`-only. Separately, the workspace's SDEX V0 decoder branch now derives a G-strkey and `go test ./internal/sources/sdex` passes, so `F-1233` should be re-evaluated once that code settles. Targeted package tests and `./scripts/ci/lint-docs.sh` passed. | CMD-0105; `.github/workflows/deploy.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `cmd/ratesengine-aggregator/contribution_sink.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/postgresstore/apikey_store.go`; `cmd/ratesengine-api/main.go`; `internal/usage/counter.go`; `openapi/rates-engine.v1.yaml`; `internal/sources/sdex/decode.go`; `internal/sources/sdex/decode_test.go` | In-flight Wave 10 reconciliation preserving F-1220/F-1242/F-1257/F-1258/F-1259 and flagging F-1233 for recheck |
| EV-0104 | 2026-05-12 | A follow-on shared-workspace review narrows two prior threads. First, `.github/workflows/ci.yml` now adds an `actions-pinning` job backed by `scripts/ci/lint-actions-pinning.sh`. Running the script in both normal mode and `PR_DIFF=1` mode exits zero but warns on 12 existing mutable third-party action tags, so the new gate blocks future regressions without remediating the current repository state or the hosted Actions policy from `F-1216`. Second, the contribution-volume implementation has already moved past the `EV-0103` pre-filter/post-filter mismatch: `fetchForTarget` now emits per-trade USD values keyed by stable `canonical.Trade.ID()`, `flushContributions` sums only the post-filter survivor set by source, and the sink persists `SourceUSDVolume[c.Source]` directly. The specific attribution bug called out in `EV-0103` appears addressed in this later workspace diff, and targeted aggregator/orchestrator tests pass, so `F-1242` should be re-evaluated after that workspace settles or gains dedicated contribution-volume assertions. | CMD-0106; `.github/workflows/ci.yml`; `scripts/ci/lint-actions-pinning.sh`; `cmd/ratesengine-aggregator/contribution_sink.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/canonical/trade.go` | Partial-remediation evidence for F-1216 and recheck marker for F-1242 |
| EV-0105 | 2026-05-12 | The latest reconciliation changes four audit conclusions. First, settled `HEAD=e787c11b...` contains the contribution-volume rewrite: `fetchForTarget` records per-trade USD by stable `Trade.ID()`, filtered survivor trades are the only inputs to `flushContributions`, and `contributionSink` persists `SourceUSDVolume[c.Source]` directly. The specific pre-filter/post-filter attribution defect behind `F-1242` no longer reproduces, and targeted aggregator/orchestrator tests pass. Second, the committed SDEX decoder now handles `ClaimAtomTypeV0` by deriving a G-strkey from raw Ed25519 bytes and ships `TestDecoder_v0ClaimAtom_decodedAsOrderBook`; `go test ./internal/sources/sdex` passes, so the original V0-drop defect behind `F-1233` no longer reproduces. Third, the concurrent webhook workspace adds `ListWebhooksSubscribedTo`, a `customerwebhook.Fanout`, aggregator `anomaly.freeze` wiring, and a post-insert freeze hook; targeted aggregator/customerwebhook/timescale tests pass. That materially narrows `F-1249`, but a repo-wide producer search still finds no production enqueue path for `incident.sev1`, `incident.resolved`, or `divergence.firing`, and the new hook/fan-out slice still lacks the per-event integration and enqueue-success/failure observability promised by the remediation gate. Fourth, the min-USD volume gate remains semantically wrong after the rewrite: `refreshPairWindow` computes `usdVolume` before class/outlier filtering, then later calls `dropForMinUSDVolume(pair, trades, usdVolume)` after the filtered survivor set is known; the function ignores `trades` and compares the stale pre-filter total while `Config.MinUSDVolume` and the function comment both claim post-filter semantics. A window can therefore keep only a thin surviving contribution set after outlier/class drops yet still publish because discarded volume satisfied the threshold earlier. | CMD-0107; `cmd/ratesengine-aggregator/contribution_sink.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/sources/sdex/decode.go`; `internal/sources/sdex/decode_test.go`; `cmd/ratesengine-aggregator/main.go`; `internal/customerwebhook/fanout.go`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/storage/timescale/freeze_events.go` | Fix evidence for F-1233/F-1242, narrowing evidence for F-1249, and support for F-1260 |
| EV-0106 | 2026-05-12 | The Wave 13 webhook work closes another piece of `F-1249` and an unrelated current-worktree supply attempt remains non-closing. At committed `HEAD=60c8a2e4...`, `divergence.ServiceOptions` accepts `OnWarningFired`, `RefreshPair` maintains a per-pair edge latch so only `false -> true` warning transitions invoke the hook, `cmd/ratesengine-aggregator/main.go` maps that hook to `customerwebhook.Fanout.Publish`, and `TestRefreshPair_OnWarningFiredEdgeOnly` passes in `./internal/divergence`. Repo search now finds production enqueue paths for `anomaly.freeze` and `divergence.firing`; only `incident.sev1` and `incident.resolved` remain without producers, and the broader integration/producer-observability gate from `R-1247` is still unmet. Separately, the uncommitted `F-1236` supply-ledger remediation attempt in `cmd/ratesengine-aggregator/main.go` and `cmd/ratesengine-ops/supply.go` does not compile: both patches filter cursors through `c.Name`, but `timescale.Cursor` defines only `Source`, `Sub`, `LastLedger`, and `UpdatedAt`. `go test ./internal/divergence ./cmd/ratesengine-aggregator ./internal/customerwebhook ./internal/storage/timescale ./internal/platform/postgresstore` therefore fails on the aggregator build, and `go test ./cmd/ratesengine-ops` fails on the same undefined field. `./scripts/ci/lint-docs.sh` still passes. | CMD-0108; `internal/divergence/worker.go`; `internal/divergence/worker_test.go`; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/storage/timescale/cursors.go` | Narrows F-1249 to incident producers only; preserves F-1236 as open after a non-building workspace attempt |
| EV-0107 | 2026-05-12 | The current Stripe workspace narrows the shape of `F-1219` but does not close it. `internal/api/v1/stripe_webhook.go` now defines `StripePlatformBridge` plus `applyPlatformSideEffects`, which can resolve an account by Stripe customer ID, call `Billing.UpsertSubscription`, and update `Account.Tier`. However, production wiring in `cmd/ratesengine-api/main.go` still constructs `StripeWebhookConfig` with only `SigningSecret`, Redis `Manager`, Postgres `Events`, and Postgres `Audit`; it never sets `Platform`, so the new bridge is dormant on the live runtime path. Repo search also finds no `StripePlatformBridge` use outside the new file, and existing webhook tests do not exercise the bridge. Even if it were wired, the helper updates account/subscription state only; dashboard-created Postgres API keys still are not lifted alongside Redis-backed legacy keys, which remains a core part of `F-1219`. `go test ./internal/api/v1` passes. | CMD-0109; `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/stripe_webhook_test.go`; `internal/platform/postgresstore/account_store.go`; `internal/platform/postgresstore/billing_store.go` | Preserves F-1219 as open after a partial uncommitted remediation |
| EV-0108 | 2026-05-12 | Wave 15 narrows two findings without closing either. Settled `HEAD=c82c1602...` expands the Stripe webhook again: `internal/api/v1/stripe_webhook.go` now accepts `customer.subscription.updated` and `customer.subscription.deleted`, can refresh subscription period/cancel state through the dormant platform bridge, and can roll an account tier down to Free on deletion. Production API wiring in `cmd/ratesengine-api/main.go` still never sets `StripeWebhookConfig.Platform`, repo search finds no committed bridge use outside the webhook file, and `internal/api/v1/stripe_webhook_test.go` still lacks bridge or `customer.subscription.*` assertions. The same settled head adds `Supply.MinComponentLedger`, `OutcomeKindStaleComponent`, `WithStaleComponentLedgers`, and refresher tests proving that a populated freshness signal can reject stale snapshots. The current dirty `internal/supply/classic.go` / `sep41.go` follow-up threads `comps.MinComponentLedger` into `Supply`, but production storage readers still return no component-ledger signal at all, so real snapshots preserve `MinComponentLedger == 0` and explicitly bypass the new gate. `go test ./internal/api/v1 ./internal/supply ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` passes. | CMD-0110; `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/stripe_webhook_test.go`; `internal/supply/refresher.go`; `internal/supply/refresher_test.go`; `internal/supply/supply.go`; `internal/supply/classic.go`; `internal/supply/sep41.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_sep41_reader.go` | Preserves F-1219/F-1236 after Wave 15 reduces but does not eliminate the gaps |
| EV-0109 | 2026-05-12 | Wave 16 settles the previously in-flight supply computer threading and grows audit scope by one operations document. At `HEAD=65197ec0...`, `ClassicSupplyComponents` and `SEP41SupplyComponents` carry `MinComponentLedger`, and both computers now copy that field into emitted `Supply`, which completes the consumer-side conduit identified in `EV-0108`. The producer side is still absent: `StorageClassicSupplyReader` and `StorageSEP41SupplyReader` continue to construct those component structs without any freshness ledger, and repo search finds no storage-backed `MinComponentLedger` assignment. Real snapshots therefore still arrive at the refresher with zero and bypass the stale-component rejection gate. The same commit adds `docs/operations/github-actions-sha-pinning.md`, increasing tracked non-audit scope from `1,874` to `1,875`; the audit inventory row set and area totals were refreshed to match, leaving the current roll-up at `done=105`, `in_progress=51`, `todo=1719`. `./scripts/ci/lint-docs.sh` passes. | CMD-0111; `internal/supply/classic.go`; `internal/supply/sep41.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_sep41_reader.go`; `docs/operations/github-actions-sha-pinning.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md` | Preserves F-1236, records Wave 16 scope drift, and restores literal inventory parity |
| EV-0110 | 2026-05-12 | The next live `F-1236` workspace slice narrows the defect again without closing it. `internal/storage/timescale/classic_supply_observations.go` now adds `MinClassicComponentLedger`, which calculates the minimum of each non-empty classic component table's latest observed ledger at-or-before the snapshot ledger. `internal/supply/storage_classic_reader.go` extends `ClassicSupplyStore`, queries that value, and fills `ClassicSupplyComponents.MinComponentLedger`; the refreshed fake store in `storage_classic_reader_test.go` keeps the reader contract compiling. Targeted tests pass for `./internal/supply`, `./internal/storage/timescale`, `./cmd/ratesengine-aggregator`, and `./cmd/ratesengine-ops`. This remains non-closing for three independent reasons: the work is still uncommitted, SEP41 and XLM paths have no analogous producer, and the classic reader intentionally swallows freshness-query errors and resets the signal to zero, which re-enters the refresher's documented permissive bypass. | CMD-0112; `internal/storage/timescale/classic_supply_observations.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_classic_reader_test.go`; `internal/supply/storage_sep41_reader.go`; `internal/supply/xlm.go`; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go` | Narrows but preserves F-1236 during the classic-only producer attempt |
| EV-0111 | 2026-05-12 | The repository advanced again to `HEAD=6819e7dc...`, settling the classic-storage producer from `EV-0110`: `MinClassicComponentLedger` is now committed in Timescale and `StorageClassicSupplyReader` now emits a non-zero freshness signal when that query succeeds. The next SEP41 workspace slice is not closure-grade yet. `internal/storage/timescale/sep41_supply_events.go` adds `MinSEP41ComponentLedger`, `internal/supply/storage_sep41_reader.go` extends `SEP41SupplyStore` and fills `SEP41SupplyComponents.MinComponentLedger`, and `cmd/ratesengine-aggregator/main.go` adapts the aggregator store, but `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` exits non-zero because `internal/supply/storage_sep41_reader_test.go`'s `fakeSEP41Store` does not implement the expanded interface. The other three packages pass. XLM remains without any freshness producer, and the SEP41 reader mirrors classic's permissive `err -> 0` fallback. | CMD-0113; `cmd/ratesengine-aggregator/main.go`; `internal/storage/timescale/sep41_supply_events.go`; `internal/supply/storage_sep41_reader.go`; `internal/supply/storage_sep41_reader_test.go`; `internal/supply/xlm.go` | Preserves F-1236 after Wave 17; records the non-building SEP41 follow-up |
| EV-0112 | 2026-05-12 | The repository advanced to `HEAD=fb0b3073...`, settling the SEP41 producer from `EV-0111`: `MinSEP41ComponentLedger` is now committed in Timescale, the SEP41 storage reader and aggregator adapter compile, the fake SEP41 test store implements the expanded interface, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` passes. That narrows `F-1236` to the remaining XLM producer gap plus the intentionally permissive freshness-query error fallback. Separately, the current Stripe workspace adds `invoice.paid` handling and an invoice-period upsert path in `internal/api/v1/stripe_webhook.go`. This does not close `F-1219`: production API wiring in `cmd/ratesengine-api/main.go` still never sets `StripeWebhookConfig.Platform`, repo search still finds no production bridge use, and the dashboard Postgres-key source of truth remains outside the Stripe mutation path. | CMD-0114; `cmd/ratesengine-aggregator/main.go`; `internal/storage/timescale/sep41_supply_events.go`; `internal/supply/storage_sep41_reader.go`; `internal/supply/storage_sep41_reader_test.go`; `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go` | Records Wave 18 supply progress and preserves F-1219 after the live invoice follow-up |
| EV-0113 | 2026-05-12 | Late live-R1 verification materially changes three priority findings. The hardened edge path is now live: `/etc/caddy/Caddyfile` includes the Cloudflare trusted-proxy CIDRs, `client_ip_headers CF-Connecting-IP X-Forwarded-For`, and `{client_ip}` forwarding; public `curl -skI https://api.ratesengine.net/metrics` returns `HTTP/2 404`, closing the concrete `F-1223` exposure. The evidence-timer rollout is partly live: `archive-completeness.timer`, `verify-archive-tier-a.timer`, and `supply-snapshot.timer` are enabled, while `sla-probe.timer` is present but disabled, so `F-1205` narrows rather than closes. Firewall hardening is also partial rather than complete: `nftables` is active, UFW inactive, and public probes to `9000`, `9001`, `9090`, `3100`, `9080`, `9100`, `6061`, and `38563` now time out; however live `/etc/nftables.conf` explicitly accepts `{11625,11626,11725,11726}`, `ss -ltnp` shows `stellar-core` bound on `0.0.0.0:11726`, and public `11726/tcp` connects successfully. That residual source/live drift keeps `F-1201` open. | CMD-0115; R1 `/etc/caddy/Caddyfile`; R1 `/etc/nftables.conf`; R1 `systemctl` timer reads; R1 `nft list ruleset`; R1 `ss -ltnp`; public `curl`/`nc` probes | Narrows F-1201/F-1205 and marks F-1223 fixed on live R1 |
| EV-0114 | 2026-05-12 | The public web dependency-risk posture is now materially better than the original `F-1207` snapshot. Explorer, dashboard, and status all pin `next@15.5.18`; `.github/dependabot.yml` has separate `package-ecosystem: npm` entries for `/web/explorer`, `/web/dashboard`, and `/web/status`; and current `pnpm audit --audit-level high` runs in all three directories each return only `1 vulnerabilities found` with `Severity: 1 moderate`. The remaining open risk is hosted GitHub control posture rather than an active known high/critical Next.js exposure: repository vulnerability alerts are disabled (`HTTP 404`) and Dependabot alerts are disabled (`HTTP 403`). | CMD-0116; `web/explorer/package.json`; `web/dashboard/package.json`; `web/status/package.json`; `.github/dependabot.yml`; hosted GitHub API responses | Narrows F-1207 to hosted alert posture |
| EV-0115 | 2026-05-12 | A late current-head auth/runtime pass preserves two high-priority findings despite passing targeted tests. `PostgresAPIKeyValidator.Lookup` now constructs rich policy-bearing subjects on canonical Postgres reads, and `middleware.KeyPolicy` enforces IP allowlists, referer allowlists, and endpoint permissions. But `cacheStore` still serializes the legacy `APIKeyRecord` shape and `cacheLookup` reconstructs a `Subject` without those policy fields, so cache hits can weaken policy enforcement after a strict cache-miss path; monthly quota and production `TouchUsage` wiring also remain absent. Separately, Redis-less API wiring now avoids the usage middleware panic route, but it still passes `UsageReader: usageReaderAdapter{c: usageCounter}` even when `usageCounter == nil`; `usageReaderAdapter.Read` immediately calls `a.c.Read(...)`, so `/v1/account/usage` can still dereference a nil inner counter. `go test ./internal/api/v1 ./internal/auth ./internal/platform/...` and `go test ./cmd/ratesengine-aggregator ./internal/aggregate/...` pass. | CMD-0117; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/keypolicy.go`; `cmd/ratesengine-api/main.go`; `internal/api/v1/account.go` | Preserves F-1226/F-1258 after direct current-head review |
| EV-0116 | 2026-05-12 | The next market-data parity pass separates one source-fixed defect from one still-live production mismatch. `fetchForTarget` in the orchestrator now computes per-source-pair USD totals before pair rewrites, `usdVolumeForPairPerTrade` uses 7-decimal math for configured classic USD pegs, and `TestTick_MinUSDVolumeFilter/classic USD-pegged proxy: $10k publishes under min=10000` passes, so the original `F-1213` undercount no longer reproduces in current source. R1 still keeps `min_usd_volume = 0`, but the live config comment frames that as an on-chain-only operator posture rather than a workaround for the arithmetic bug. Separately, source now implements `historySinceInceptionStablecoinFallback` and `TestHistorySinceInception_StablecoinFallback` passes, but public R1 still serves an empty `native/fiat:USD` since-inception series while direct Circle-USDC history returns nine daily points. `/etc/ratesengine.toml` shows `enable_stablecoin_fiat_proxy = true` and the Circle USDC classic peg configured; the deployed API marker/version reads `v0.5.0-rc.49`, so `F-1225` remains a live source/runtime reconciliation gap rather than a missing handler implementation. | CMD-0118; `internal/aggregate/orchestrator/orchestrator.go`; `internal/aggregate/orchestrator/orchestrator_test.go`; `internal/api/v1/history.go`; `internal/api/v1/history_test.go`; R1 `/etc/ratesengine.toml`; public history API responses; R1 deployed-version marker | Fixes F-1213 and preserves F-1225 as a live production parity gap |
| EV-0117 | 2026-05-12 | A current-head severe-integrity/security pass preserves `F-1243` and `F-1244`. `internal/storage/timescale/asset_registry.go` still keeps a process-lifetime `assetRegistryDedupe`; repeat touches for the same classic asset return before `registerIssuerSeen` and before the `INSERT ... ON CONFLICT DO UPDATE` that would apply `LEAST`/`GREATEST` and increment `observation_count`. Package tests pass, but there is still no closure-grade integration proving repeated same-process observations keep first/last/count semantics accurate. Separately, webhook secret truthfulness is only partially improved: `platform.CustomerWebhook` now explicitly states `SecretHash` is the literal HMAC key, not a hash, yet `customer_webhooks.secret_hash` still persists those live key bytes, `dashboardwebhooks.webhookDTO` still says the plaintext is shown once and “never persisted,” `WebhookStore.RotateWebhookSecret` still says the returned plaintext is “not stored,” and the delivery worker still signs with `wh.SecretHash` directly. That preserves the storage/docs contract mismatch at the heart of `F-1244`. `go test ./internal/storage/timescale ./internal/platform/postgresstore ./internal/customerwebhook` passes. | CMD-0119; `internal/storage/timescale/asset_registry.go`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `migrations/0027_platform_v1_schema.up.sql` | Preserves F-1243/F-1244 after current-head review |
| EV-0118 | 2026-05-12 | A new shared-workspace `F-1226` patch closes the earlier cache-hit policy shedding slice without closing the whole finding. `internal/auth/apikey_redis.go` now extends `APIKeyRecord` with IP allowlist, referer allowlist, and permission payload fields; `internal/auth/subject.go` adds JSON tags for permission entries; and `internal/auth/apikey_postgres.go` now encodes those fields during cache write-back and decodes them during cache reads, including CIDR parsing back into `netip.Prefix`. `TestPostgresValidator_CacheRoundTripsPolicy` proves a Postgres miss and subsequent Redis hit preserve the same policy surface, and `go test ./internal/auth ./internal/api/v1/middleware` passes. `F-1226` remains open because the runtime still does not enforce `monthly_quota`, and production `TouchUsage` / `last_used_*` updates are still missing. | CMD-0120; `internal/auth/apikey_postgres.go`; `internal/auth/apikey_postgres_test.go`; `internal/auth/apikey_redis.go`; `internal/auth/subject.go` | Narrows F-1226 after cache-parity remediation |
| EV-0119 | 2026-05-12 | `F-1228` is now a source/live runtime mismatch rather than a missing source fix. Current source explicitly clears SSE request write deadlines with `http.NewResponseController(w).SetWriteDeadline(time.Time{})` inside `internal/api/streaming/handler.go`, while `cmd/ratesengine-api/main.go` still keeps the global `http.Server.WriteTimeout = 30s` for ordinary handlers. Middleware comments and wrappers preserve `ResponseController` reachability, and `go test ./internal/api/v1 ./cmd/ratesengine-api` passes. However, an HTTP/1.1 probe launched from R1 against the public tip stream still emits frames only through the first roughly 30 seconds (`as_of` timestamps observed from `21:44:07Z` through `21:44:32Z`) and then terminates before the next expected 5-second frame despite the client allowing 65 seconds. The finding therefore remains open until live R1 proves a stream survives beyond the former 30-second cutoff. | CMD-0121; `cmd/ratesengine-api/main.go`; `internal/api/streaming/handler.go`; `internal/api/v1/price_tip_stream.go`; live R1 SSE probe | Preserves F-1228 as source/live runtime drift |
| EV-0120 | 2026-05-12 | Migration `0030_asset_supply_history_unique_constraint.up.sql` is not executable against the compressed hypertable shape this repository itself creates. `0005_create_asset_supply_history.up.sql` enables Timescale compression and attaches a compression policy; `0030` later drops the existing unique index and attempts `ALTER TABLE asset_supply_history ADD CONSTRAINT ... UNIQUE (...)` without first following the decompress/disable-DDL-restore pattern already demonstrated in `0004_relax_trades_ledger_for_offchain.up.sql`. Fresh integration bootstrap fails immediately with `pq: operation not supported on hypertables that have compression enabled (0A000)`. Live R1 corroborates that this path is still ahead of production rather than already absorbed: `schema_migrations` reports version `28`, `asset_supply_history` reports `compression_enabled=t`, and `pg_indexes` shows only the old unique index plus the ordinary read indexes. | CMD-0122; `migrations/0004_relax_trades_ledger_for_offchain.up.sql`; `migrations/0005_create_asset_supply_history.up.sql`; `migrations/0030_asset_supply_history_unique_constraint.up.sql`; R1 migration/compression/index queries | Supports F-1261 |
| EV-0121 | 2026-05-12 | The current shared workspace materially improves the two quota-race findings but does not yet produce terminal evidence. `WebhookStore.CreateWebhook` now runs the count + insert inside a transaction guarded by `pg_advisory_xact_lock(hashtext('webhook:'||account_id))`; capped `APIKeyStore.Create` mirrors that with a disjoint `apikey:` advisory-lock keyspace. `test/integration/platform_postgres_stores_test.go` adds concurrent cap scenarios for both surfaces, and package-level handler/store tests still compile and pass. The closure proof is blocked by `EV-0120`: the integration harness aborts in migration `0030` before either new concurrent scenario can execute, so `F-1248` and `F-1257` are best classified as `needs_evidence`, not `fixed`. | CMD-0122; `internal/platform/postgresstore/webhook_store.go`; `internal/platform/postgresstore/apikey_store.go`; `test/integration/platform_postgres_stores_test.go` | Evidence-state reconciliation for F-1248/F-1257 |
| EV-0122 | 2026-05-12 | The file-level audit control is back in sync with the moving checkout. After the migration/quota pass, five newly tracked non-audit files were absent from `inventory/file-coverage.tsv` (`cmd/ratesengine-ops/emit_incident.go`, `cmd/ratesengine-ops/emit_incident_test.go`, `internal/api/v1/dashboardauth/signup_lock_test.go`, `internal/auth/signup_email_locker.go`, and `internal/auth/signup_email_locker_test.go`), and migration `0030` itself also lacked explicit reviewed coverage rows. The TSV now includes those files plus refreshed notes for `apikey_store.go`, `webhook_store.go`, `platform_postgres_stores_test.go`, `migrations/README.md`, and `0030_asset_supply_history_unique_constraint.up.sql`. Current reconciliation reports `tracked=1882`, `rows=1882`, with status roll-up `done=105`, `in_progress=54`, `todo=1723`; docs lint remains green. | CMD-0123; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Restores current inventory parity while preserving open review debt |
| EV-0123 | 2026-05-12 | `F-1236` remains open on the native-XLM path even after classic and SEP-41 freshness producers settled. `cmd/ratesengine-aggregator/buildXLMRefresher` and `cmd/ratesengine-ops/supply.go` both construct an `LCMReserveBalanceReader` wrapped by a chain reader that falls back to `ConfigReserveBalanceReader` when any configured SDF reserve account lacks a live observation (`ErrNoObservation`). `XLMComputer.Compute` never populates `Supply.MinComponentLedger`, and the config reader ignores the requested ledger entirely because it is static operator data. The refresher then evaluates `snap.MinComponentLedger > 0` before enforcing stale-component rejection, so these live-to-static fallback snapshots are stamped at the freshest known cursor ledger while the guard is categorically skipped. That is the weakest-proven provenance path in the supply subsystem, and it applies to native XLM rather than a secondary asset family. Targeted tests for supply/timescale/aggregator/ops still pass, confirming the gap is semantic rather than a compilation regression. | CMD-0124; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/supply/refresher.go`; `internal/supply/xlm.go`; `internal/supply/lcm_reader.go`; `internal/supply/config_reader.go` | Strengthens F-1236 with the XLM fallback bypass |
| EV-0124 | 2026-05-12 | The supply-freshness pass is now reflected in the per-file control ledger. `inventory/file-coverage.tsv` was updated for `cmd/ratesengine-aggregator/main.go`, `cmd/ratesengine-ops/supply.go`, `internal/supply/config_reader.go`, `internal/supply/lcm_reader.go`, `internal/supply/refresher.go`, `internal/supply/storage_classic_reader.go`, `internal/supply/storage_sep41_reader.go`, and `internal/supply/xlm.go`. Tracked-file parity remains exact at `1882` rows, with status roll-up `done=105`, `in_progress=60`, `todo=1717`; docs lint passed after the update. | CMD-0125; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after the F-1236 continuation |
| EV-0125 | 2026-05-12 | `F-1244` is not only a naming/docs mismatch; the current explanatory comment overstates the actual protection boundary. `platform.CustomerWebhook` now says the live HMAC key is recoverable and that at-rest protection is the Postgres row's “standard column-encryption posture,” but repository search finds no column encryption, envelope encryption, KMS wrapper, or decrypt-on-read layer for `customer_webhooks.secret_hash`. Migration `0027` defines `secret_hash bytea NOT NULL`, `postgresstore.WebhookStore` reads/writes that column directly, and `customerwebhook.Worker` signs with `wh.SecretHash` bytes directly. The broader architecture docs describe at-rest protection as disk/volume encryption, not column encryption. The interface remains inconsistent too: `WebhookStore.RotateWebhookSecret` says the new plaintext is shown once and “not stored,” while the Postgres implementation returns `not yet implemented` and the persisted current secret is in fact stored recoverably. Targeted webhook/store tests pass, so the gap is semantic/security-contract drift rather than a test/build failure. | CMD-0126; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `migrations/0027_platform_v1_schema.up.sql`; `docs/architecture/platform-spec.md` | Strengthens F-1244 around the false encryption posture and rotation contract drift |
| EV-0126 | 2026-05-12 | The remaining `F-1226` gap is structurally wider than the earlier shorthand suggested. `internal/platform/apikey.go` exposes per-key `MonthlyQuota`; `internal/platform/account.go` exposes `MonthlyRequestQuotaOverride`; migrations and Postgres stores persist both; dashboard/OpenAPI surfaces accept/show monthly quotas. Repo search found no request-path use of either quota field. `APIKeyStore.TouchUsage` still exists only as a store method/test seam with no production caller. The live usage path also does not aggregate by account: `middleware.UsageTracker` writes `key:<KeyID>` when available, falls back to `id:<Identifier>`, and `handleAccountUsage` reads that same credential-scoped key back. Platform/account docs and the explorer account UI describe an account-level usage/quota view, but the current storage/read path can only prove current-credential series without an additional aggregation layer. Targeted auth/key-policy/dashboard-key/store packages pass, so this is a cross-file model gap rather than a failing unit-test path. | CMD-0127; `internal/platform/apikey.go`; `internal/platform/account.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/platform/postgresstore/account_store.go`; `internal/api/v1/middleware/usage.go`; `internal/api/v1/account.go`; `internal/auth/{subject,apikey_postgres,apikey_redis}.go`; `docs/architecture/platform-spec.md`; `web/explorer/src/app/account/AccountDashboard.tsx`; `openapi/rates-engine.v1.yaml` | Strengthens F-1226 around quota non-use and account-vs-credential usage attribution |
| EV-0127 | 2026-05-12 | The current production wiring narrows `F-1218` compared with the older shorthand. `cmd/ratesengine-api/main.go` creates both the signup account store and `RedisSignupTracker` only when Redis exists. `internal/api/v1/signup.go::parseAndValidateSignup` returns `503 account-store-unavailable` when `s.accounts == nil`, so the main binary does not proceed to mint a signup key through a Redis-less `signups == nil` path. The already-recorded Redis-backed `ReserveEmail` flow still closes the same-email concurrency race. The remaining high-severity issue is therefore the normal healthy path itself: `/v1/signup` still returns a plaintext Starter-tier key immediately after parsing an email string, without proving ownership of that address. `go test ./internal/api/v1 ./internal/auth ./cmd/ratesengine-api` passes on this state. | CMD-0128; `cmd/ratesengine-api/main.go`; `internal/api/v1/signup.go`; `internal/auth/signup_tracker.go`; `internal/auth/signup_ip_throttle_test.go`; `internal/api/v1/signup_test.go` | Narrows F-1218 to the current shipped abuse path |
| EV-0128 | 2026-05-12 | A fresh shared-workspace patch materially narrows `F-1226` without closing it. `cmd/ratesengine-api/main.go` now wires `MonthlyQuota: middleware.MonthlyQuota(usageCounter, …)` into `v1.Options`; `internal/api/v1/server.go` inserts that middleware after auth/key-policy and before rate limiting; `auth.Subject`, `auth.APIKeyRecord`, and `PostgresAPIKeyValidator` now carry `MonthlyQuota`; `usage.Counter.MonthToDate` sums current-month Redis daily counters; and new middleware tests cover unset, below-cap, at-cap, over-cap, reader-error, nil-reader, and anonymous cases. `go test ./internal/api/v1/middleware ./internal/auth ./internal/usage ./cmd/ratesengine-api ./internal/api/v1` passes. The remaining model gaps are concrete: `platform.APIKey.MonthlyQuota` still documents `0 = inherit from plan`, yet the new middleware treats `<= 0` as pass-through; `platform.Account.MonthlyRequestQuotaOverride` still has no request-path consumer; quota admission reads existing counters before the request while `UsageTracker` increments only after the wrapped handler returns, so concurrent requests near cap can all enter before any one of them is counted; `TouchUsage` still has no production caller; and account-level usage/quota aggregation remains absent. | CMD-0129; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/middleware/monthly_quota.go`; `internal/api/v1/middleware/monthly_quota_test.go`; `internal/auth/subject.go`; `internal/auth/apikey_postgres.go`; `internal/auth/apikey_redis.go`; `internal/usage/counter.go`; `internal/usage/counter_test.go`; `internal/platform/apikey.go`; `internal/platform/account.go` | Narrows but preserves F-1226 after in-flight monthly quota work |
| EV-0129 | 2026-05-12 | The file-level control ledger is synchronized again after the latest high-priority signup/quota pass. `inventory/file-coverage.tsv` now reflects the fresh reviews of `cmd/ratesengine-api/main.go`, `internal/api/v1/{account,server,signup}.go`, `internal/api/v1/middleware/usage.go`, `internal/auth/{subject,apikey_postgres,apikey_redis}.go`, `internal/platform/{account,apikey}.go`, `internal/platform/postgresstore/{account_store,apikey_store}.go`, `internal/usage/{counter,counter_test}.go`, and `web/explorer/src/app/account/AccountDashboard.tsx`. Tracked-file parity remains exact at `1882` rows; the TSV rolls up to `done=105`, `in_progress=64`, `todo=1713`; `inventory/area-counts.md` and `inventory/repo-snapshot.md` were refreshed to the current moving workspace; docs lint passed. | CMD-0130; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after the latest highest-priority review pass |
| EV-0130 | 2026-05-12 | `F-1219` is narrower on current source than the stale detailed register prose implied, but it is not closed. `cmd/ratesengine-api/main.go` now assigns `stripeCfg.Platform = &v1.StripePlatformBridge{Accounts: …, Billing: …}`. `internal/api/v1/stripe_webhook.go` routes `checkout.session.completed`, `customer.subscription.{updated,deleted}`, and `invoice.paid` through platform account/subscription side effects. However, the live key-budget mutation loop still calls only `s.stripe.Manager.ListKeysForIdentifier` + `UpdateRateLimit`, and production wires that manager to the Redis legacy key store. `internal/platform/postgresstore/apikey_store.go` has editable Postgres API-key rows, but the Stripe path never lists or updates them. `applyPlatformSideEffects` is explicitly best-effort after Redis mutation, so account/subscription write failures log warnings while the webhook still returns success. A targeted search of `stripe_webhook_test.go` still finds no bridge/subscription/invoice closure assertions. `go test ./internal/api/v1 ./cmd/ratesengine-api` passes. | CMD-0131; `cmd/ratesengine-api/main.go`; `internal/api/v1/stripe_webhook.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/api/v1/stripe_webhook_test.go` | Narrows but preserves F-1219 on current source |
| EV-0131 | 2026-05-12 | The audit control ledger is synchronized again after the monthly-quota middleware moved from in-flight workspace code into tracked scope. `inventory/file-coverage.tsv` now includes `internal/api/v1/middleware/monthly_quota.go` and `monthly_quota_test.go` as directly reviewed `in_progress` files under `F-1226`. Against `HEAD=48688b3eb92230a940040c151c6e28b8fb21dd00`, tracked non-audit scope is `1884` files and the TSV is back at `1884` rows with `done=105`, `in_progress=67`, `todo=1712`. `inventory/area-counts.md` updates `internal` to `679`, `inventory/repo-snapshot.md` records the 13-path moving workspace snapshot, and docs lint passed. | CMD-0132; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after tracked quota middleware entered scope |
| EV-0132 | 2026-05-12 | The live shared workspace now contains a substantive touch-usage remediation slice for `F-1226`. `cmd/ratesengine-api/main.go` wires `TouchUsage: touchUsageMiddlewareOrNil(...)`; `internal/api/v1/server.go` accepts and inserts the middleware into the request chain; `internal/api/v1/middleware/touch_usage.go` gates bookkeeping through a `TouchDebouncer` and invokes `APIKeyStore.TouchUsage`; `internal/auth/touch_debouncer.go` implements Redis SETNX debouncing; and the Postgres store sink already updates `last_used_at`, `last_used_ip`, and `last_used_user_agent`. `go test ./internal/api/v1/middleware ./internal/auth ./cmd/ratesengine-api ./internal/platform/postgresstore` passes against this workspace. This narrows the previous "no production caller" claim but does not close the finding: the new touch files remain untracked workspace code, the implementation is inline post-handler Redis/Postgres work rather than truly asynchronous bookkeeping despite its comments, and the quota/account aggregation gaps under `F-1226` are unaffected. | CMD-0133; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/middleware/touch_usage.go`; `internal/auth/touch_debouncer.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/api/v1/middleware/touch_usage_test.go`; `internal/auth/touch_debouncer_test.go` | Narrows but preserves F-1226 |
| EV-0133 | 2026-05-12 | The live workspace's new Redis namespace for touch-usage bookkeeping is coupled to the Ansible ACL template rather than being added in isolation. `internal/auth/touch_debouncer.go` writes `touch:apikey:<keyID>`, and the moving diff in `configs/ansible/roles/redis-sentinel/templates/users.acl.j2` adds `~touch:apikey:*` to the `ratesengine` allow-list with a matching operator comment. That keeps the remediation slice from immediately reintroducing the earlier hardened-Redis namespace denial failure mode. This evidence is intentionally narrow: both sides are workspace edits, so the check proves local cross-file coherence, not landed closure. | CMD-0134; `internal/auth/touch_debouncer.go`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2` | Cross-file deploy-contract check for the touch-usage slice |
| EV-0134 | 2026-05-12 | `F-1236` is narrower on current source than the earlier register text stated, but it is not fixed. Native XLM now has a real freshness producer path: `LCMReserveBalanceReader.MinReserveAccountLedger` returns the oldest observed reserve-account ledger, `supplyAggregatorChainReader` and `supplyChainReader` forward that interface when live data is available, and `XLMComputer` copies the resulting value into `Supply.MinComponentLedger`. Tests in `internal/supply/lcm_reader_test.go` and `internal/supply/xlm_test.go` cover the happy path plus zero-value fallback cases, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-api ./cmd/ratesengine-aggregator ./cmd/ratesengine-indexer` passes. The original integrity risk survives through the remaining permissive branches: missing LCM observations force static reserve fallback with `MinComponentLedger=0`, classic/SEP41 freshness queries swallow storage errors into zero, and XLM freshness-reader errors are likewise suppressed into the gate-bypass posture. | CMD-0135; `internal/supply/xlm.go`; `internal/supply/lcm_reader.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_sep41_reader.go`; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/supply/lcm_reader_test.go`; `internal/supply/xlm_test.go` | Narrows but preserves F-1236 |
| EV-0135 | 2026-05-12 | `F-1243` is stronger than the earlier freeze-only write-up. `internal/storage/timescale/asset_registry.go` still keeps a process-lifetime `assetRegistryDedupe` map and returns before the SQL upsert on any second asset touch in the same process, freezing `first_seen_*`, `last_seen_*`, and `observation_count` updates for continuous ingest/backfill sessions. Separately, `internal/storage/timescale/trades.go` inserts trades with `ON CONFLICT DO NOTHING` but never inspects rows affected before calling `registerClassicAssetSeen`. After a restart or replay, a duplicate trade row that was not inserted can still advance registry observation counters once per asset per process because the in-memory dedupe map starts empty again. The downstream significance is concrete: `coins.go` orders/list-pages assets by `observation_count`, `issuers.go` ranks issuers by aggregated observation counts, and API asset/issuer detail surfaces expose those stale/drifting fields. `go test ./internal/storage/timescale ./internal/api/v1` passes, which confirms the issue is a missing behavioral contract rather than a compile/test break currently caught by the suite. | CMD-0136; `internal/storage/timescale/trades.go`; `internal/storage/timescale/asset_registry.go`; `internal/storage/timescale/coins.go`; `internal/storage/timescale/issuers.go`; `internal/api/v1/assets.go`; `internal/api/v1/issuers.go` | Broadens and sharpens F-1243 |
| EV-0136 | 2026-05-12 | The control ledger is synchronized again after committed wave 39 landed the touch-usage source files at `HEAD=f3c76028053dd433b919082185ea00217d39f2d2`. `inventory/file-coverage.tsv` now includes `internal/api/v1/middleware/touch_usage.go`, `touch_usage_test.go`, `internal/auth/touch_debouncer.go`, and `touch_debouncer_test.go` as audited rows. Tracked non-audit scope and TSV rows both equal `1888`; the ledger rolls up to `done=109`, `in_progress=71`, `todo=1708`; `inventory/area-counts.md` updates `internal` to `683`; `inventory/repo-snapshot.md` reflects the settled head plus six currently dirty audit-control files; and docs lint passed. | CMD-0137; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after committed touch-usage scope growth |
| EV-0137 | 2026-05-12 | `F-1261` remains a current-head blocker, not stale historical evidence. On `HEAD=f3c76028053dd433b919082185ea00217d39f2d2`, `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` still aborts during migration bootstrap because `0030_asset_supply_history_unique_constraint.up.sql` performs `ALTER TABLE asset_supply_history ADD CONSTRAINT ... UNIQUE (...)` while the hypertable remains compression-enabled. Timescale returns `0A000 operation not supported on hypertables that have compression enabled`; the test never reaches the advisory-lock webhook/API-key quota scenarios whose closure proof depends on full migration application. The migration pair and its `0004` precedent were re-read during this pass, and the deficiency remains exactly the missing decompress/disable-DDL-restore sequence already documented in the register. | CMD-0138; `migrations/0030_asset_supply_history_unique_constraint.up.sql`; `migrations/0030_asset_supply_history_unique_constraint.down.sql`; `migrations/0004_relax_trades_ledger_for_offchain.up.sql`; `internal/storage/timescale/supply.go`; `test/integration/platform_postgres_stores_test.go` | Reconfirms F-1261 and the blocked F-1248/F-1257 closure path |
| EV-0138 | 2026-05-12 | `F-1244` remains fully live on current source. `internal/api/v1/dashboardwebhooks/handlers.go` still creates a `wsec_*` plaintext secret and stores it as `SecretHash: []byte(secret)`; `internal/platform/postgresstore/webhook_store.go` persists and later rehydrates `secret_hash`; `internal/customerwebhook/worker.go` signs payloads with `wh.SecretHash`; and `internal/platform/webhook.go` correctly admits the bytes are the literal HMAC key while simultaneously claiming a "standard column-encryption posture" that repo search did not substantiate. The contradiction persists elsewhere too: handler DTO comments still say plaintext is "never persisted," `RotateWebhookSecret` prose still says new plaintext is "not stored" even though the concrete store is unimplemented, and OpenAPI/customer prose still leans on once-only-secret language without clearly naming the recoverable signing-key storage model. `go test ./internal/platform/postgresstore ./internal/customerwebhook ./internal/api/v1/dashboardwebhooks` passes, so this remains a security-contract defect not caught by the focused unit suite. | CMD-0139; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `openapi/rates-engine.v1.yaml`; `docs/architecture/platform-spec.md` | Reconfirms F-1244 |
| EV-0139 | 2026-05-12 | `F-1251` now has the intended source-level fixes but not terminal closure evidence. `internal/storage/timescale/usd_fx_resolver.go` trims NUMERIC text via `trimNumericText`, compares staleness against the trade timestamp, and defines unambiguous freshness sentinels (`0` default, negative disabled, positive explicit); the focused resolver tests cover those cases and pass. However, the tagged integration proof `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1` still aborts in migration `0030` with Timescale `0A000 operation not supported on hypertables that have compression enabled`, the same `F-1261` blocker reconfirmed in `EV-0137`. Because the integration never reaches the repaired `USDPriceAt` assertion, `F-1251` is better classified as `needs_evidence` until the migration blocker is removed and the DB-backed proof runs green. | CMD-0140; `internal/storage/timescale/usd_fx_resolver.go`; `internal/storage/timescale/usd_fx_resolver_test.go`; `test/integration/usd_fx_resolver_test.go`; `migrations/0030_asset_supply_history_unique_constraint.up.sql` | Downgrades F-1251 to evidence-blocked |
| EV-0140 | 2026-05-12 | `F-1225` remains a current live-source drift issue. Repo source still contains `historySinceInceptionStablecoinFallback` in `internal/api/v1/history.go`, the targeted history regression tests pass, and the Timescale aggregate reader remains the backing since-inception source. On live R1, `/etc/ratesengine.toml` still enables the Circle-USDC peg path, but public `native/fiat:USD` since-inception history returns an empty `points` array while direct `native/USDC-GA5ZSEJY...` history returns nine populated daily buckets. That keeps the defect deployment-facing rather than source-missing. | CMD-0141; R1-0019; `internal/api/v1/history.go`; `internal/api/v1/history_test.go`; `internal/storage/timescale/aggregates.go` | Reconfirms F-1225 against current R1 |
| EV-0141 | 2026-05-12 | `F-1228` remains a live deployment drift, not an unreconciled historical claim. Current source still clears SSE per-request write deadlines in `internal/api/streaming/handler.go`, the related focused API/API-command test pass succeeds, and the v1 stream handlers remain wired through that common writer. A refreshed R1 loopback probe against `/v1/price/tip/stream?asset=native&quote=fiat:USD&window_seconds=60` with a 68-second client ceiling emitted `:connected`, an initial `tip_update`, and a `:keepalive` at 15.0 seconds, then terminated at elapsed `30.0` seconds with curl exit 18. That preserves the finding as a deployed runtime mismatch until R1 proves a stream survives beyond the old 30-second boundary. | CMD-0142; R1-0020; `cmd/ratesengine-api/main.go`; `internal/api/streaming/handler.go`; `internal/api/v1/observations_stream.go`; `internal/api/v1/price_stream.go`; `internal/api/v1/price_tip_stream.go` | Reconfirms F-1228 against current R1 |
| EV-0142 | 2026-05-12 | `F-1219` remains open on current head after the latest source recheck. `cmd/ratesengine-api/main.go` still wires `stripeCfg.Platform = &v1.StripePlatformBridge{Accounts: ..., Billing: ...}`, and `internal/api/v1/stripe_webhook.go` still carries checkout/subscription/invoice helpers that can update account tier and subscription records. The unresolved split is downstream of that bridge: `upgradeAllKeys` still iterates Redis-backed `auth.APIKeyRecord` values and calls `s.stripe.Manager.UpdateRateLimit`, while the Stripe path never calls the Postgres dashboard-key store even though `internal/platform/postgresstore/apikey_store.go` exposes `ListForAccount` and `Update`. The platform fan-out remains documented as best-effort/log-only after Redis mutation. A refreshed search of `stripe_webhook_test.go` finds no `StripePlatformBridge`, `subscription`, or `invoice.paid` closure assertions beyond generic platform/audit scaffolding, and `go test ./internal/api/v1 ./cmd/ratesengine-api` passes without exercising the missing cross-store contract. | CMD-0143; `cmd/ratesengine-api/main.go`; `internal/api/v1/stripe_webhook.go`; `internal/api/v1/stripe_webhook_test.go`; `internal/platform/postgresstore/apikey_store.go` | Reconfirms F-1219 |
| EV-0143 | 2026-05-12 | `F-1218` remains materially open on `HEAD=400660e6f2b6a222eadc68893265498a7400ec99` plus the current live workspace. The settled wave-44 producer side is real: `internal/api/v1/signup.go` now issues a crypto-random verifier token after key mint, reserves it through `SignupVerifier`, emits a verification email when an emailer is present, and exposes `email_verification_sent` on the wire. The abuse contract is still unchanged because plaintext key issuance happens first and remains immediately usable. The in-flight wave-45 verified-state slice is also incomplete in ways that matter: `signup_verify.go` can call `s.apiKeyEmailVerifier.MarkEmailVerified`, `server.go` defines both an `APIKeyEmailVerifier` option and a private `apiKeyEmailVerifier` field, and `internal/auth/store_mark_email_verified.go` sketches a Redis implementation, but `newServer` never copies `opts.APIKeyEmailVerifier` into `s.apiKeyEmailVerifier`, `cmd/ratesengine-api/main.go` does not provide one, that Redis marker file is still untracked workspace code, and repo search finds no implemented `RequireEmailVerified` middleware or startup wiring for such a gate. `go test ./internal/api/v1 ./internal/auth ./cmd/ratesengine-api` passes because the current suite does not exercise the absent production coupling. | CMD-0144; `internal/api/v1/signup.go`; `internal/api/v1/signup_verify.go`; `internal/api/v1/server.go`; `cmd/ratesengine-api/main.go`; `internal/auth/signup_verifier.go`; `internal/auth/store_mark_email_verified.go`; `internal/auth/{apikey_redis,subject}.go`; `openapi/rates-engine.v1.yaml` | Sharpens F-1218 around the still-missing production verification/gating contract |
| EV-0144 | 2026-05-12 | The moving workspace advanced again on `F-1218` after `EV-0143`, but still not far enough for closure. Current edits now wire `APIKeyEmailVerifier` from `cmd/ratesengine-api/main.go` into `internal/api/v1/server.go`, add optional `RequireEmailVerified` insertion ahead of rate limiting, introduce config `signup_require_email_verification`, and include focused middleware + Redis-store tests that pass under `go test ./internal/api/v1 ./internal/api/v1/middleware ./internal/auth ./cmd/ratesengine-api`. The remaining audit conclusion is still open, not fixed: `internal/api/v1/middleware/require_email_verified{,_test}.go` and `internal/auth/store_mark_email_verified{,_test}.go` are still untracked workspace files, the hardening switch defaults `false`, the signup handler still returns a usable plaintext key before ownership proof unless operators enable the gate, and `./scripts/ci/lint-docs.sh` currently fails because `docs/reference/config/README.md` has not been regenerated for the new config key. | CMD-0145; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/signup_verify.go`; `internal/api/v1/middleware/require_email_verified.go`; `internal/auth/store_mark_email_verified.go`; `internal/config/config.go`; `docs/reference/config/README.md` | Supersedes EV-0143's narrower intermediate workspace snapshot while preserving F-1218 as open |
| EV-0145 | 2026-05-12 | The generated config reference caught up after `EV-0144`: `docs/reference/config/README.md` now includes `api.signup_require_email_verification`, and `./scripts/ci/lint-docs.sh` passes again. That removes the transient doc-sync objection from the `F-1218` workspace reconciliation, but it does not remove the security finding itself. The current source still keeps the verification gate opt-in with default `false`, the signup handler still returns a usable plaintext key before ownership proof unless the operator flips that flag, and the newly added gate/store source files remain untracked workspace code rather than settled tracked scope. | CMD-0146; `docs/reference/config/README.md`; `internal/config/config.go`; `./scripts/ci/lint-docs.sh` | Current `F-1218` state after config-doc sync recovered |
| EV-0146 | 2026-05-12 | `F-1218` is now a settled policy/default-posture issue rather than an incomplete implementation issue. Commit `93594529330e84db1a81e747bb70752068d236e6` lands the email-verification gate end to end: tracked gate/store source and tests, `EmailVerifiedAt` propagation, `APIKeyEmailVerifier` wiring, config `signup_require_email_verification`, and regenerated config docs. The focused test set remains green. However, the default remains `false`, R1's `/etc/ratesengine.toml` has no explicit `signup_require_email_verification` key, and `POST /v1/signup` still returns a usable plaintext key before proof of ownership whenever the gate stays off. Under this audit's public-launch standard, that means the original abuse posture is still reachable by default and on the currently observed R1 config. | CMD-0147; R1-0021; `cmd/ratesengine-api/main.go`; `internal/api/v1/{signup,signup_verify,server}.go`; `internal/api/v1/middleware/require_email_verified.go`; `internal/auth/{apikey_redis,subject,store_mark_email_verified}.go`; `internal/config/config.go`; `docs/reference/config/README.md` | Reframes but preserves F-1218 as open after wave 45 landed |
| EV-0147 | 2026-05-12 | Wave 46 genuinely clears the old `F-1261` migration bootstrap blocker and releases two dependent evidence gates. `HEAD=ab78289e79ce913650967458f84ca59e48ce2741` rewrites `0030_asset_supply_history_unique_constraint.up.sql` to decompress existing `asset_supply_history` chunks, disable compression before the UNIQUE-constraint swap, and restore the 0005 compression settings afterward. `go test -tags=integration ./test/integration -run TestMigrationsRoundTrip -count=1` now passes. The previously blocked DB-backed FX proof `TestVWAPUSDFXResolver_QueriesPrices1m` also passes, so `F-1251` now has terminal execution evidence. The isolated webhook quota proof `TestPlatformPostgresStores/WebhookStore/Concurrent_QuotaCap_Holds` passes too, clearing the `F-1248` evidence block. | CMD-0148; `migrations/0030_asset_supply_history_unique_constraint.up.sql`; `test/integration/usd_fx_resolver_test.go`; `test/integration/platform_postgres_stores_test.go` | Fix evidence for F-1261/F-1251/F-1248 |
| EV-0148 | 2026-05-12 | Clearing the migration blocker exposed a separate Postgres API-key create defect. The full `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` no longer aborts in migration `0030`; instead it reaches `APIKeyStore/Concurrent_QuotaCap_Holds` and every concurrent create fails with `pq: null value in column "referer_allowlist" of relation "api_keys" violates not-null constraint (23502)`. Source review explains why this is production-relevant rather than a fixture artifact: `platform.APIKey.RefererAllowlist` is a nil slice by default, `dashboardkeys.HandleCreate` copies `req.RefererAllowlist` directly and clients may omit that optional field, `buildAPIKeyCreateArgs` passes `pq.StringArray(k.RefererAllowlist)`, and migration 0027 requires `referer_allowlist text[] NOT NULL DEFAULT '{}'`. The handler happy-path unit test uses a fake store and therefore misses the real SQL NULL/NOT NULL conflict. This same failure now blocks terminal proof for the API-key quota advisory-lock finding `F-1257`. | CMD-0148; CMD-0149; `internal/platform/postgresstore/apikey_store.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/apikey.go`; `migrations/0027_platform_v1_schema.up.sql`; `web/dashboard/src/lib/api.ts`; `test/integration/platform_postgres_stores_test.go` | Supports new F-1262 and keeps F-1257 evidence-blocked |
| EV-0149 | 2026-05-12 | Wave 47 materially narrows `F-1243` without closing it. Commit `bf51138be40987809d02ed98e1fb0eee5876aa3d` replaces the process-lifetime `assetRegistryDedupe` sentinel with a TTL-backed `assetRegistryDedupeTTL = 60s`, stores last-successful-upsert timestamps, and adds pure unit coverage for cache miss, within-TTL skip, past-TTL re-upsert, per-asset isolation, corrupt-cache fail-open, and the frozen TTL value. That fixes the same-process "registry freshness/counts freeze forever" half of the finding. The replay/idempotency half remains live: `internal/storage/timescale/trades.go` still performs `INSERT ... ON CONFLICT DO NOTHING` without checking rows affected before calling `registerClassicAssetSeen`, so duplicate trade replays after restart can still mutate registry counters even when no new trade row landed. `go test ./internal/storage/timescale ./internal/api/v1` passes. | CMD-0150; `internal/storage/timescale/asset_registry.go`; `internal/storage/timescale/asset_registry_test.go`; `internal/storage/timescale/trades.go` | Narrows but preserves F-1243 |
| EV-0150 | 2026-05-12 | `F-1256` is now source-reconciled on current head. `openapi/rates-engine.v1.yaml` already described the effective `rate_limit_per_min` behavior as tier-clamped with the explicit Free/Starter/Pro/Business/Enterprise ladder, and commit `70a77b0614fe3396830790134684bc0022689b6e` updates `web/dashboard/src/app/keys/page.tsx` to present the same ladder directly beside the create-key input while stating that higher typed values silently clamp on save. The dashboard still accepts values up to the global hard ceiling, but it no longer misrepresents the persisted budget semantics for lower tiers. That resolves the cross-file customer/docs mismatch recorded under `XFI-0048`. | CMD-0151; `web/dashboard/src/app/keys/page.tsx`; `openapi/rates-engine.v1.yaml`; `internal/platform/account.go`; `internal/api/v1/dashboardkeys/handlers.go` | Closes F-1256 |
| EV-0151 | 2026-05-12 | Audit controls are synchronized again after the repository advanced through wave 48. `inventory/file-coverage.tsv` now includes the previously missing `internal/storage/timescale/asset_registry_test.go` row introduced with wave 47, and its refreshed row notes reconcile the narrowed-but-open `F-1243` state plus the now-fixed `F-1256` copy contract. At `HEAD=70a77b0614fe3396830790134684bc0022689b6e`, tracked non-audit scope and TSV coverage both equal `1,897`; the ledger rolls up to `done=111`, `in_progress=87`, `todo=1699`; `inventory/area-counts.md` updates `internal` to `692`; `inventory/repo-snapshot.md` records the current 11 dirty paths without reverting the unrelated account-usage/spec edits; and `./scripts/ci/lint-docs.sh` passes. | CMD-0152; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after wave 47/48 scope growth |
| EV-0152 | 2026-05-12 | `F-1262` remains live on current head and is slightly broader than the initial create-only shorthand. Re-running `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` still fails inside `APIKeyStore/Concurrent_QuotaCap_Holds`: all 12 concurrent capped creates return `pq: null value in column "referer_allowlist" of relation "api_keys" violates not-null constraint (23502)`, yielding zero persisted active rows and blocking terminal proof for `F-1257`. Source review narrows the marshalling boundary precisely. `ip_allowlist` is handled by `cidrArray.Value()`, which emits `'{}'` on empty input, so that field does not share the SQL NULL defect. `referer_allowlist` does: both `buildAPIKeyCreateArgs` and `APIKeyStore.Update` pass `pq.StringArray(k.RefererAllowlist)` directly, preserving nil as SQL NULL against migration 0027's `referer_allowlist text[] NOT NULL DEFAULT '{}'`. The dashboard create handler and dashboard client both make omission a normal request shape, so the active user-facing outage remains default key creation while the reusable store bug also reaches future/editable update writes. | CMD-0153; `internal/platform/postgresstore/apikey_store.go`; `internal/platform/apikey.go`; `internal/api/v1/dashboardkeys/handlers.go`; `migrations/0027_platform_v1_schema.up.sql`; `web/dashboard/src/lib/api.ts`; `test/integration/platform_postgres_stores_test.go` | Reconfirms and broadens F-1262; keeps F-1257 evidence-blocked |
| EV-0153 | 2026-05-12 | The apparent `F-1259` closure is only partial in the current workspace. The account-usage handler comment now describes the live Redis-backed trailing-30-day behavior, source OpenAPI now points Redis-less degradation to `/v1/readyz` instead of `/v1/healthz`, `docs/reference/api-design.md` and `docs/architecture/explorer-data-inventory.md` both adopt the same current semantics, and the generated reference YAML mirrors the corrected source prose. However, `docs/reference/api/postman-collection.json` still exposes the stale collection item label `"Usage-summary placeholder (currently empty)."`. That leaves the published/generated artifact family internally inconsistent, so `F-1259` remains open despite the otherwise substantial workspace cleanup. | CMD-0154; `internal/api/v1/account.go`; `openapi/rates-engine.v1.yaml`; `docs/reference/api-design.md`; `docs/architecture/explorer-data-inventory.md`; `docs/reference/api/rates-engine.v1.yaml`; `docs/reference/api/postman-collection.json` | Narrows but preserves F-1259 |
| EV-0154 | 2026-05-12 | `F-1244` remains open on current source despite the summary register having drifted to `fixed`. The webhook create handler still stores `SecretHash: []byte(secret)` for a plaintext `wsec_*` signing key; `postgresstore.WebhookStore` persists and reloads that value directly from `customer_webhooks.secret_hash`; and `customerwebhook.Worker` signs outbound payloads with the recovered bytes. `platform.CustomerWebhook` truthfully admits `SecretHash` is the literal HMAC key, but the same comment still claims at-rest protection comes from a "standard column-encryption posture" that repo search did not substantiate. The contract remains inconsistent elsewhere: `dashboardwebhooks.webhookDTO` still says plaintext is shown once and never persisted, `WebhookStore.RotateWebhookSecret` says the new plaintext is "not stored", and the concrete Postgres rotation method still returns `not yet implemented`. `go test ./internal/platform/postgresstore ./internal/customerwebhook ./internal/api/v1/dashboardwebhooks` passes, which confirms this is an unresolved security-model defect rather than a unit-test failure. | CMD-0155; `internal/platform/webhook.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/customerwebhook/worker.go`; `openapi/rates-engine.v1.yaml` | Reopens and reconfirms F-1244 |
| EV-0155 | 2026-05-12 | `F-1236` remains open on current source despite the register summary having drifted to `fixed`. The producer story is now real: classic storage, SEP41 storage, and live-reader XLM can all populate `Supply.MinComponentLedger`. The remaining defect is the intentional permissive bypass family. `supply.Refresher` rejects stale component ledgers only when `MinComponentLedger > 0`; static XLM fallback in the aggregator/ops chain readers returns to config balances without a freshness signal, classic and SEP41 freshness-query errors collapse to zero, `XLMComputer` swallows freshness-reader errors into zero, and legacy XLM readers also leave zero. The focused supply/timescale/command package set passes, confirming those branches are expected current behavior rather than accidental dead code. | CMD-0156; `internal/supply/refresher.go`; `internal/supply/xlm.go`; `internal/supply/lcm_reader.go`; `internal/supply/storage_classic_reader.go`; `internal/supply/storage_sep41_reader.go`; `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go` | Reopens and reconfirms F-1236 |
| EV-0156 | 2026-05-12 | Wave 50 closes the actual `F-1262` product bug. `internal/platform/postgresstore/apikey_store.go` now routes both create-time and update-time `referer_allowlist` values through `nonNilStringArray`, which maps nil to a non-nil zero-length `pq.StringArray` so lib/pq persists `'{}'` instead of SQL NULL against migration 0027's NOT NULL array column. `go test ./internal/platform/postgresstore ./internal/api/v1/dashboardkeys` passes, and the full Postgres-store integration no longer fails on the old `referer_allowlist` constraint. | CMD-0157; `internal/platform/postgresstore/apikey_store.go`; `internal/platform/postgresstore/apikey_store_test.go`; `internal/api/v1/dashboardkeys/handlers.go`; `test/integration/platform_postgres_stores_test.go` | Fix evidence for F-1262 |
| EV-0157 | 2026-05-12 | Clearing `F-1262` exposes a new proof-harness defect rather than proving `F-1257`. The current `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` run advances into `APIKeyStore/Concurrent_QuotaCap_Holds`, but all 12 creates now fail with `pq: new row for relation \"api_keys\" violates check constraint \"api_keys_id_check\" (23514)`. The fixture code explains why: it builds IDs as `\"kid_\" + uuid.New().String()[:12]`, which includes a hyphen, while migration 0027 requires `^kid_[a-f0-9]{12,}$`. The real dashboard minting path uses `hex.EncodeToString` and therefore satisfies the schema. This is an integration-test validity failure, not a production key-create failure, but it still blocks terminal DB evidence for the advisory-lock quota finding. | CMD-0157; `test/integration/platform_postgres_stores_test.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/api/v1/dashboardkeys/handlers.go` | Opens F-1263 and keeps F-1257 evidence-blocked |
| EV-0158 | 2026-05-12 | Wave 51 materially repairs the remaining `F-1243` duplicate-replay mechanism in source. `internal/storage/timescale/trades.go::InsertTrade` now inspects `res.RowsAffected()` after `INSERT ... ON CONFLICT DO NOTHING`; when the row was a duplicate (`n == 0`), it returns before invoking the classic-asset registry hook, preventing duplicate no-op trade inserts from mutating `observation_count`. Driver `RowsAffected` errors intentionally fail open so legitimate insert paths are not silently dropped. `go test ./internal/storage/timescale ./internal/api/v1` passes. However, no new DB-backed replay integration landed with the change: repo review still finds the wave-47 TTL unit tests, but not the audit-requested duplicate-replay/process-boundary proof that asserts the repaired registry counters end to end. | CMD-0158; `internal/storage/timescale/trades.go`; `internal/storage/timescale/asset_registry_test.go`; `docs/audit-2026-05-12-codex/05-findings-register.md` | Moves F-1243 from open-source defect to needs-evidence |
| EV-0159 | 2026-05-12 | Wave 52 splits cleanly after falsification. `F-1244` is still open: `internal/platform/webhook.go`, `WebhookStore.RotateWebhookSecret`, and `dashboardwebhooks.webhookDTO` now describe recoverable signing-key persistence more honestly, but `internal/platform/postgresstore/webhook_store.go` still says rotation returns plaintext "shown once to the customer + never stored" and that callers hash the secret before persistence. The shipped create/worker path does neither; it persists recoverable `customer_webhooks.secret_hash` bytes and signs with those bytes later, while the concrete rotation method remains unimplemented. `F-1259` does close on tracked repository state. `docs/reference/api/postman-collection.json` is not tracked and is absent in the current tree; the canonical tracked `examples/postman/rates-engine.postman_collection.json`, source OpenAPI, generated YAML, API-design docs, explorer inventory, and handler comments all now describe live Redis-backed usage plus `/readyz` degradation consistently. `./scripts/ci/lint-docs.sh` passes. | CMD-0159; `internal/platform/webhook.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `internal/platform/postgresstore/webhook_store.go`; `examples/postman/rates-engine.postman_collection.json`; `openapi/rates-engine.v1.yaml`; `docs/reference/api/rates-engine.v1.yaml`; `docs/reference/api-design.md`; `docs/architecture/explorer-data-inventory.md`; `./scripts/ci/lint-docs.sh` | Preserves F-1244 and closes F-1259 after the wave-52 falsification pass |
| EV-0160 | 2026-05-12 | The moving wave-53 patch partially repairs `F-1263` but does not close it. `test/integration/platform_postgres_stores_test.go` now strips UUID hyphens before building the concurrent quota fixture's `kid_` IDs, aligning that field with migration 0027's `api_keys_id_check`. Re-running the full `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` proof advances one schema gate and then fails all 12 concurrent creates on `api_keys_key_prefix_check (23514)`. The fixture plaintext is `rek_race_<...>` and `KeyPrefix: plaintext[:12]`, while migration 0027 requires `key_prefix ~ '^rek_[a-f0-9]{8}$'`; production `generatePlaintext` emits `rek_` plus lowercase hex and therefore satisfies the schema. The advisory-lock quota assertions still never execute, so `F-1257` remains evidence-blocked and `F-1263` stays open as a broader invalid-fixture finding. | CMD-0160; `test/integration/platform_postgres_stores_test.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/api/v1/dashboardkeys/handlers.go` | Broadens F-1263 and preserves the F-1257 closure gate |
| EV-0161 | 2026-05-12 | Wave 53 removes the last inline-comment contradiction from `F-1244` but does not close the finding. `internal/platform/webhook.go`, `internal/platform/postgresstore/webhook_store.go`, and `internal/api/v1/dashboardwebhooks/handlers.go` now consistently describe a recoverable signing key persisted in `customer_webhooks.secret_hash`, and the Postgres rotation comment no longer claims hash-before-persist / never-stored semantics. The remaining mismatch is in the architecture security spec: `docs/architecture/platform-spec.md` still says customer webhook secrets use "libsodium sealed boxes" managed by an operator master key, while migration 0027, the store writer/reader, and the delivery worker continue to show direct recoverable `bytea secret_hash` storage with no application-layer encrypt/decrypt boundary in repo. | CMD-0161; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/customerwebhook/worker.go`; `docs/architecture/platform-spec.md` | Preserves F-1244 as architecture-vs-code security-model drift |
| EV-0162 | 2026-05-12 | Wave 53 completes the `F-1263` proof-harness repair and releases the final `F-1257` evidence gate. `test/integration/platform_postgres_stores_test.go` now generates schema-valid concurrent fixture IDs (`kid_<hex>`) and stored prefixes (`rek_<8hex>`) matching migration 0027 plus the dashboard's production generators. Re-running `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` passes, so the API-key concurrent quota scenario reaches and satisfies the intended advisory-lock winners/losers assertions instead of stopping at unrelated schema checks. | CMD-0162; `test/integration/platform_postgres_stores_test.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/api/v1/dashboardkeys/handlers.go` | Fix evidence for F-1263 and terminal proof for F-1257 |
| EV-0163 | 2026-05-12 | Wave 54 closes the remaining `F-1244` architecture-vs-code drift. `docs/architecture/platform-spec.md §8.1 Encryption` now separates planned sealed-box handling for TOTP seeds from customer webhook signing keys, which it describes as plain recoverable `customer_webhooks.secret_hash` bytes consumed by the delivery worker for HMAC signing. That matches `internal/platform/webhook.go`, `internal/platform/postgresstore/webhook_store.go`, `internal/api/v1/dashboardwebhooks/handlers.go`, migration 0027, and the worker path; the reviewed source/spec contract is now consistent. | CMD-0163; `docs/architecture/platform-spec.md`; `internal/platform/webhook.go`; `internal/platform/postgresstore/webhook_store.go`; `internal/api/v1/dashboardwebhooks/handlers.go`; `migrations/0027_platform_v1_schema.up.sql`; `internal/customerwebhook/worker.go` | Fix evidence for F-1244 |
| EV-0164 | 2026-05-12 | Audit controls are synchronized again after wave 54 settled at `HEAD=a783b5eda96d45fc4228b8990704149fcefc1ee8`. Tracked non-audit scope and `inventory/file-coverage.tsv` both equal `1,898`; the coverage roll-up is `done=112`, `in_progress=88`, `todo=1698`; `inventory/area-counts.md` updates `internal` to `693`; `inventory/repo-snapshot.md` records the current nine dirty Codex-audit paths; and `./scripts/ci/lint-docs.sh` passes. | CMD-0164; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after wave 54 settled |
| EV-0165 | 2026-05-12 | The two highest-priority policy/runtime findings checked immediately after wave 54 remain live. For `F-1218`, current config/docs still declare `api.signup_require_email_verification` default `false`; production wiring still exposes `RequireEmailVerified` only when operators opt in; and the signup response path continues to return a usable plaintext key before ownership proof under the default posture. For `F-1219`, `StripePlatformBridge` remains wired and account/subscription side effects exist, but `upgradeAllKeys` still enumerates only `StripeKeyManager.ListKeysForIdentifier` and calls Redis-backed `UpdateRateLimit`; existing dashboard-created Postgres API keys are never listed or updated despite the store exposing `ListForAccount`/`Update`. Focused API/auth/command tests pass, including the targeted Stripe/Signup/Email/Verify subset, without closing either cross-store/default-posture gap. | CMD-0165; `cmd/ratesengine-api/main.go`; `internal/api/v1/{signup,signup_verify,stripe_webhook}.go`; `internal/api/v1/stripe_webhook_test.go`; `internal/config/config.go`; `docs/reference/config/README.md`; `internal/platform/postgresstore/apikey_store.go` | Reconfirms F-1218 and F-1219 after wave 54 |
| EV-0166 | 2026-05-12 | Current live R1 still diverges from the source-side remedies for `F-1225` and `F-1228`. Focused source tests remain green for since-inception/stablecoin history and SSE/tip streaming. The public history endpoint returned `points: []` for `native/fiat:USD` at `as_of=2026-05-13T00:02:29.944109337Z`, while direct `native/USDC-GA5ZSEJY...` returned nine daily buckets from `2026-05-03T00:00:00Z` through `2026-05-11T00:00:00Z`. Separately, a timed public SSE probe of `/v1/price/tip/stream?asset=native&quote=fiat:USD` emitted `:connected`, multiple `tip_update` frames, and a keepalive, then ended at `ELAPSED:30.251033 EXIT:92` despite a 68-second client ceiling. | CMD-0166; R1-0022; R1-0023; `internal/api/v1/history.go`; `internal/api/streaming/handler.go`; `internal/api/v1/{price_tip_stream,price_stream,observations_stream}.go`; `cmd/ratesengine-api/main.go` | Reconfirms F-1225 and F-1228 against current live R1 |
| EV-0167 | 2026-05-12 | `F-1243` now has the DB-backed replay evidence the audit explicitly required. `test/integration/asset_registry_replay_test.go::TestAssetRegistry_DuplicateReplayDoesNotMutateCounters` uses the new `timescale.ResetAssetRegistryDedupeForTest` helper to clear the in-process dedupe cache between insert/replay attempts, modeling the fresh-process/fresh-store replay boundary that previously remained unproven. The test proves: exact duplicate replay preserves `observation_count=1` and `last_seen_ledger`, a distinct later same-asset trade advances them once, replaying that second trade stays idempotent, and the trades hypertable still holds exactly two physical rows. `go test -tags=integration ./test/integration -run 'AssetRegistry_DuplicateReplayDoesNotMutateCounters' -count=1` passes. | CMD-0167; `test/integration/asset_registry_replay_test.go`; `internal/storage/timescale/asset_registry.go`; `internal/storage/timescale/trades.go` | Fix evidence for F-1243 |
| EV-0168 | 2026-05-12 | `F-1219` is genuinely fixed on current head rather than merely narrowed. `internal/api/v1/stripe_webhook.go` now fans paid-upgrade side effects into both stores: the existing Redis signup-key path remains, while `StripePlatformBridge.APIKeys` lets `applyAccountTierAndKeyUpgrade` call `upgradePlatformAPIKeys` for dashboard-created Postgres keys. Production wiring in `cmd/ratesengine-api/main.go` provides `postgresstore.NewAPIKeyStore(pgStore)`. The focused `Stripe|Webhook|Platform` test slice passes, including `TestStripeWebhook_PlatformBridge_LiftsPostgresKeys`, which lifts two below-target active dashboard keys while skipping one revoked and one already-above-target key. The new `docs/operations/runbooks/stripe-platform-sync-errors.md` plus `ratesengine_stripe_platform_sync_errors_total{operation=...}` operation labels make the intentionally best-effort Postgres sync path explicit and observable. | CMD-0168; `internal/api/v1/stripe_webhook.go`; `internal/api/v1/stripe_webhook_test.go`; `cmd/ratesengine-api/main.go`; `internal/obs/metrics.go`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/runbooks/stripe-platform-sync-errors.md` | Fix evidence for F-1219 |
| EV-0169 | 2026-05-12 | Audit controls are synchronized again after the repo advanced to `HEAD=34511e508d18057da5bae1d0cb6b812555b5afd0`. Tracked non-audit scope and `inventory/file-coverage.tsv` both equal `1,901`; the coverage roll-up is `done=115`, `in_progress=88`, `todo=1698`; `inventory/area-counts.md` updates `docs` to `508`, `test` to `92`, and `web` to `201`; `inventory/repo-snapshot.md` records the current nine dirty Codex-audit paths; and `./scripts/ci/lint-docs.sh` passes. | CMD-0169; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `docs/audit-2026-05-12-codex/inventory/area-counts.md`; `docs/audit-2026-05-12-codex/inventory/repo-snapshot.md`; `./scripts/ci/lint-docs.sh` | Audit-control synchronization after the replay/runbook/status scope growth |
| EV-0170 | 2026-05-13 | The launch-readiness blocker remains live on current head. `go run ./scripts/ci/verify-launch-ready` still exits non-zero: L1-L3 are green, but Operations remains red because `L4.14` R2 bringup, `L4.15` R3 bringup, `L4.16` cross-region DNS, and `L4.17` cross-region Patroni replication are not closed; SLA validation remains red on `L5.6` security review and `L5.8` failover-chaos; L6 cutover rows remain red but non-blocking by script design. | CMD-0170; `scripts/ci/verify-launch-ready`; `docs/architecture/launch-readiness-backlog.md` | Reconfirms F-1206 |
| EV-0171 | 2026-05-13 | The hosted-security remainder of `F-1207` still reproduces. GitHub's vulnerability-alerts API returns the disabled-alerts 404 response for `RatesEngine/rates-engine`; the Dependabot alerts API likewise reports repository alerts disabled. The local dependency posture remains materially better than the first audit snapshot: fresh `pnpm audit --audit-level high` runs in `web/explorer`, `web/dashboard`, and `web/status` each report exactly one moderate advisory and no high/critical advisory. | CMD-0171; `.github/dependabot.yml`; `.github/workflows/ci.yml`; `web/{explorer,dashboard,status}/package.json` | Reconfirms F-1207's hosted alert posture |
| EV-0172 | 2026-05-13 | Three live R1 findings remain materially unchanged after a fresh probe. For `F-1201`, external `11726/tcp` still accepts from the audit workstation, `stellar-core` still listens on `0.0.0.0:11726`, and `/etc/nftables.conf` still explicitly accepts `{11625,11626,11725,11726}`. For `F-1205`, `sla-probe.timer` is still disabled while `archive-completeness.timer`, `verify-archive-tier-a.timer`, and `supply-snapshot.timer` are enabled. For `F-1218`, `/etc/ratesengine.toml` still has no `signup_require_email_verification` key, so the config-default `false` posture remains the only proven live state. | CMD-0172; R1-0024; R1-0025; R1-0026 | Reconfirms F-1201, F-1205, and F-1218 |
| EV-0173 | 2026-05-13 | The three highest-priority public market-data/runtime gaps remain live after a fresh external deployment probe. `native/fiat:USD` since-inception history still returns `points: []` at `as_of=2026-05-13T06:33:58.949423013Z`, while direct native/Circle-USDC since-inception history returns the same nine daily buckets from `2026-05-03T00:00:00Z` through `2026-05-11T00:00:00Z` at `as_of=2026-05-13T06:33:58.950387268Z`. That simultaneously preserves `F-1225` and `F-1230`. Separately, the public `/v1/price/tip/stream?asset=native&quote=fiat:USD` probe still emits normal frames plus a keepalive and then terminates at `ELAPSED:30.443053 EXIT:92` despite a 68-second client ceiling, preserving `F-1228`. | CMD-0173; R1-0027; R1-0028 | Reconfirms F-1225, F-1228, and F-1230 |
| EV-0174 | 2026-05-13 | The findings register passed an explicit internal-consistency falsification check after live/runtime refreshes and closure reconciliations. Before the fix, 19 detailed finding sections still carried stale `open` or `needs_evidence` labels even though their register-table rows had already reached `fixed`; that made the audit document contradict itself. The detail labels now match the table for every finding, the mismatch detector returns no rows, and `./scripts/ci/lint-docs.sh` passes afterward. | CMD-0174; `docs/audit-2026-05-12-codex/05-findings-register.md`; `./scripts/ci/lint-docs.sh` | Audit-record self-consistency proof |
| EV-0175 | 2026-05-13 | The R1 degradation picture has narrowed since the earlier broad source-stopped snapshot, but it is still materially live. Prometheus now shows firing anomaly-freeze, anomaly-freeze-sustained, supply-refresh-never-initialized, supply-snapshot-never-initialized, ECB external-poller-stale, latency-burn-slow, cache-miss, deadmansswitch, and host-memory-high alerts, plus pending Redstone source-stopped. `/v1/status` reports `overall=degraded`, `active_sources=12`, `total_sources=17`, and eight active incidents. Host capacity remains stressed: `Mem` shows 181,943 MiB used of 192,822 MiB total, swap shows 20,451 MiB used of 20,474 MiB, and `/var/lib/minio` remains 4.9T of 6.4T used (78%). | CMD-0175; R1-0029; R1-0030 | Current live-state refinement for F-1208 and F-1209 |
| EV-0176 | 2026-05-13 | Hosted governance remains materially open on current GitHub state. The branch-protection endpoint for `main` still returns the private-plan/upgrade 403 rather than a real enforceable ruleset; the five production/deployment environments (`docs-production`, `explorer-production`, `github-pages`, `r1`, `status-production`) still report empty `protection_rules`, `can_admins_bypass=true`, and no deployment branch policy; repository Actions policy remains `enabled=true`, `allowed_actions=all`, and `sha_pinning_required=false`; workflow defaults remain read-only but do not offset the unbounded allowed-actions posture. | CMD-0176; GitHub hosted repository settings | Reconfirms F-1214, F-1215, and F-1216 |
| EV-0177 | 2026-05-13 | The wave-97 closure-summary counts remain mechanically aligned with the underlying ledgers after the latest live-state refreshes and status-label reconciliation. The findings register still rolls up to `50 fixed / 13 open`; the cross-file ledger to `45 fixed / 10 open`; and the remediation plan to `47 fixed / 14 open`. Those totals match the published summary at the top of the findings register exactly. | CMD-0177; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md` | Close-out count consistency proof |
| EV-0178 | 2026-05-13 | `F-1211` was closed too early. A wave-98 breadth pass over active non-audit operator/planning/comms surfaces found stale status-page instructions that still disagree with the shipped Cloudflare Pages `web/status` implementation: `CLAUDE.md` calls the area a `cstate scaffold`; `docs/architecture/launch-readiness-backlog.md` still prescribes Upptime on GitHub Pages with `.upptimerc.yml` and `GH_PAT`; `docs/launch-task-list.md` still treats `cstate` as acceptable launch evidence and, more severely, says there is no `status.ratesengine.net`, no `deploy/` artefacts, no status worker, and nowhere to publish updates; and `deploy/comms/README.md` plus `deploy/comms/incident-update.md` still instruct operators to work in `RatesEngine/ratesengine-status` Upptime-created issues. The status-page setup doc and status-page README correctly describe those predecessors as retired, so the audit state reopens because the surviving prescriptive docs can still misdirect operators during an incident. | CMD-0178; `CLAUDE.md`; `docs/architecture/launch-readiness-backlog.md`; `docs/launch-task-list.md`; `deploy/comms/README.md`; `deploy/comms/incident-update.md`; `docs/operations/status-page-setup.md`; `deploy/status-page/README.md` | Reopens F-1211 and XFI-0007 |
| EV-0179 | 2026-05-13 | The audit ledgers are consistent again after reopening `F-1211`. Mechanical recounts now produce `49 fixed / 14 open` findings, `44 fixed / 11 open` XFI rows, and `46 fixed / 15 open` remediation rows. The file-coverage ledger reflects the newly touched root/planning docs at `done=115`, `in_progress=91`, `todo=1695`, and `./scripts/ci/lint-docs.sh` remains green after the reopen edits. | CMD-0179; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Post-reopen reconciliation proof |
| EV-0180 | 2026-05-13 | The root/control-doc pass did not uncover a separate new defect beyond reopened `F-1211`. `README.md`'s status-page launch-blocker language remains aligned with still-yellow `L4.11`; `AGENTS.md`, `CODE_OF_CONDUCT.md`, and `LICENSE` do not introduce additional technical/control claims needing remediation; `CHANGELOG.md`'s older cstate/Upptime references are historical release-log entries rather than current operator instructions; `docs/architecture/status-page-hosting-comparison.md` is explicitly marked `status: superseded` and points at the shipped `web/status` path; and `docs/review-2026-05-10.md` labels itself "working notes (not ratified)". The tool-version rows in `VERSIONS.md` still line up with `Makefile` / CI pins for gofumpt, goimports, golangci-lint, govulncheck, and gitleaks; Go build-path equivalence remains the already-closed `F-1240` topic rather than a new finding from this pass. | CMD-0180; `README.md`; `AGENTS.md`; `CHANGELOG.md`; `VERSIONS.md`; `CODE_OF_CONDUCT.md`; `LICENSE`; `docs/review-2026-05-10.md`; `docs/architecture/status-page-hosting-comparison.md`; `go.mod`; `Makefile`; `.github/workflows/ci.yml` | Root-doc breadth pass evidence |
| EV-0181 | 2026-05-13 | `configs/prometheus/README.md` and `configs/loki/README.md` are stale against current R1 network reality. Both still say the host has "no firewall today" and that the Prometheus/Loki web APIs are publicly reachable, pending future Caddy fronting. Fresh live inspection shows Prometheus and Loki do still listen on-host (`*:9090`, `*:3100`), but `/etc/nftables.conf` now has `policy drop` and only explicitly accepts `{11625,11626,11725,11726}`; external probes to `136.243.90.96:9090` and `:3100` both time out. | CMD-0181; `configs/prometheus/README.md`; `configs/loki/README.md`; live R1 `ss -ltnp`; live R1 `/etc/nftables.conf` | Supports F-1264 |
| EV-0182 | 2026-05-13 | The Alertmanager migration docs/comments retain an obsolete cross-file claim. `configs/alertmanager/README.md` says the Ansible template "currently hardcodes `critical/warning/info` matchers" and should be adapted to `page/ticket/informational`; the header in `configs/alertmanager/alertmanager.r1.yml` repeats that the multi-host template differs. The actual Ansible template at `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2` already routes `severity = "page"`, `severity = "ticket"`, and `severity = "informational"` and explicitly says it mirrors the standalone config. | CMD-0182; `configs/alertmanager/README.md`; `configs/alertmanager/alertmanager.r1.yml`; `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2` | Supports F-1265 |
| EV-0183 | 2026-05-13 | The audit state is synchronized again after the monitoring-doc tranche. The findings register now totals `49 fixed / 16 open`; the cross-file ledger totals `44 fixed / 13 open`; the remediation plan totals `46 fixed / 17 open`; and the file-coverage ledger advances to `done=125`, `in_progress=95`, `todo=1681`. `./scripts/ci/lint-docs.sh` remains green after adding `F-1264` and `F-1265`. | CMD-0183; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Post-monitoring reconciliation proof |
| EV-0184 | 2026-05-13 | The Ansible role-doc breadth pass found one new deployment-doc defect and broadened an existing monitoring-doc defect. `configs/ansible/README.md` still says "Today the only role is `archival-node`", despite landed role metadata/playbooks for `haproxy`, `loki`, `patroni`, `prometheus`, and `redis-sentinel`; the same README says archival-node observability includes `promtail (Loki shipper) on a configurable target`, while `roles/archival-node/tasks/10-observability.yml` still ends with `TODO(#0): wire promtail -> loki_push_url. Skeleton only this round.` Separately, `configs/ansible/roles/prometheus/README.md` still describes PagerDuty/chat routing in `critical` / `warning` / `info` terms even though the rendered Alertmanager template already uses `page` / `ticket` / `informational`. | CMD-0184; `configs/ansible/README.md`; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/ansible/playbooks/*`; `configs/ansible/roles/{haproxy,loki,patroni,prometheus,redis-sentinel}/meta/main.yml` | Supports F-1265 and F-1266 |
| EV-0185 | 2026-05-13 | The audit control ledgers are synchronized after the Ansible doc pass. Findings now total `49 fixed / 17 open`; XFI rows total `44 fixed / 14 open`; remediation rows total `46 fixed / 18 open`; and file coverage advances to `done=126`, `in_progress=98`, `todo=1677`. `./scripts/ci/lint-docs.sh` remains green after broadening `F-1265` and opening `F-1266`. | CMD-0185; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Post-Ansible reconciliation proof |
| EV-0186 | 2026-05-13 | Healthchecks setup documentation is stale against the current installer/env/timer contract. `configs/healthchecks/README.md` says "create four Checks" even though it immediately lists five env vars and describes the SLA probe timer; `configs/healthchecks/install.sh` comments say "the four Healthchecks.io URLs (3 heartbeats + 1 smoke)" while the generated env file contains `HEALTHCHECKS_URL_SLA_PROBE=` and the script enables `ratesengine-sla-probe.timer`; and `docs/operations/pre-launch-hardening.md` still documents only the older four URLs. | CMD-0186; `configs/healthchecks/README.md`; `configs/healthchecks/install.sh`; `configs/healthchecks/ratesengine-sla-probe.timer`; `docs/operations/pre-launch-hardening.md` | Supports F-1267 |
| EV-0187 | 2026-05-13 | The audit control ledgers are synchronized after the Healthchecks-doc pass. Findings now total `49 fixed / 18 open`; XFI rows total `44 fixed / 15 open`; remediation rows total `46 fixed / 19 open`; and file coverage advances to `done=126`, `in_progress=101`, `todo=1674`. `./scripts/ci/lint-docs.sh` remains green after opening `F-1267`. | CMD-0187; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Post-Healthchecks reconciliation proof |
| EV-0188 | 2026-05-13 | The R1 overlay rule docs disagree with the config that Prometheus actually loads. `configs/prometheus/rules.r1/README.md` copies files to `/etc/prometheus/rules.d/` and claims `prometheus.r1.yml` already loads `/etc/prometheus/rules.d/*.yml`; `configs/prometheus/prometheus.r1.yml` instead documents and configures `rule_files: /etc/prometheus/rules.r1/*.yml`. | CMD-0188; `configs/prometheus/rules.r1/README.md`; `configs/prometheus/prometheus.r1.yml` | Supports F-1268 |
| EV-0189 | 2026-05-13 | The audit control ledgers are synchronized after the R1 rules-overlay doc pass. Findings now total `49 fixed / 19 open`; XFI rows total `44 fixed / 16 open`; remediation rows total `46 fixed / 20 open`; and file coverage advances to `done=126`, `in_progress=103`, `todo=1672`. `./scripts/ci/lint-docs.sh` remains green after opening `F-1268`. | CMD-0189; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Post-rule-overlay reconciliation proof |
| EV-0190 | 2026-05-13 | The WASM audit-input README is stale against the curated YAML it describes. `configs/audit/README.md` says the file contains one block per ingested Soroban source plus an `_unattributed` block for operational contracts whose `ContractInstance` entries were TTL-evicted. `configs/audit/wasm-walk-contracts.yaml` instead has 540 contracts across exactly eight named top-level source blocks (`aquarius`, `band`, `blend`, `comet`, `phoenix`, `redstone`, `reflector`, `soroswap`) and no `_unattributed` key. Its footer says three Reflector TESTNET addresses once misread as "TTL-evicted" were intentionally removed on 2026-05-01 and should not appear in future mainnet walks. | CMD-0190; `configs/audit/README.md`; `configs/audit/wasm-walk-contracts.yaml` | Supports F-1269 |
| EV-0191 | 2026-05-13 | The parallel workspace remediation edits close four of the six newly surfaced docs drifts, but not the R1 rule-overlay path bug. `configs/prometheus/README.md` and `configs/loki/README.md` now distinguish on-host listeners from firewall-blocked ingress and point operators to SSH tunnels; Alertmanager standalone docs plus the Prometheus role README now describe the same `page` / `ticket` / `informational` ladder as the Ansible template; `configs/ansible/README.md` now lists the sibling HA/monitoring roles and says archival-node Promtail wiring remains TODO-backed; and Healthchecks README/install/pre-launch hardening now agree on five checks including `HEALTHCHECKS_URL_SLA_PROBE`. The falsification pass still finds `/etc/prometheus/rules.d/` in `configs/prometheus/rules.r1/README.md`, while `prometheus.r1.yml` continues to load `/etc/prometheus/rules.r1/*.yml`, so `F-1268` must remain open. | CMD-0191; `configs/prometheus/README.md`; `configs/loki/README.md`; `configs/alertmanager/README.md`; `configs/alertmanager/alertmanager.r1.yml`; `configs/ansible/README.md`; `configs/ansible/roles/prometheus/README.md`; `configs/healthchecks/README.md`; `configs/healthchecks/install.sh`; `docs/operations/pre-launch-hardening.md`; `configs/prometheus/rules.r1/README.md`; `configs/prometheus/prometheus.r1.yml` | Closes F-1264/F-1265/F-1266/F-1267; keeps F-1268 open |
| EV-0192 | 2026-05-13 | The wave-104 audit control plane is synchronized again after the moving-workspace remediation recheck and the newly tracked `internal/obstest/histogram.go` inventory addition. Mechanical recounts produce `54 fixed / 15 open` findings, `49 fixed / 12 open` XFI rows, and `51 fixed / 16 open` remediation rows. File coverage rolls up to `done=137`, `in_progress=95`, `todo=1670`; tracked-file parity is exact at `1902 / 1902`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0192; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-104 audit-control synchronization proof |
| EV-0193 | 2026-05-13 | The active Caddy/operator docs disagree with the trust-boundary architecture the repo actually chose. ADR-0025 says Cloudflare CIDR validation happens inside Caddy's `trusted_proxies` block, and the API keeps `trusted_proxy_cidrs = ["127.0.0.1/32"]` because Caddy is still its only immediate peer. `internal/config/config.go` and generated config docs define `trusted_proxy_cidrs` as the immediate-peer allow-list. But `configs/caddy/README.md` says Cloudflare-fronted deployment would "trust additional X-Forwarded-* CIDRs ... in the API config", and `docs/operations/pre-launch-hardening.md` tells operators to add Cloudflare ranges directly to API `trusted_proxy_cidrs`. | CMD-0193; `configs/caddy/README.md`; `docs/operations/pre-launch-hardening.md`; `docs/adr/0025-caddy-cloudflare-trusted-proxy.md`; `internal/config/config.go`; `docs/reference/config/README.md` | Supports F-1270 |
| EV-0194 | 2026-05-13 | The current workspace closes the two doc drifts previously tracked as `F-1268` and `F-1269`. `configs/prometheus/rules.r1/README.md` now copies rules to `/etc/prometheus/rules.r1/` and says that directory is what `prometheus.r1.yml` loads. `configs/audit/README.md` now describes one entry per Soroban source, explains that the earlier `_unattributed` concept was intentionally removed during the 2026-05-01 cleanup, and therefore matches the curated eight-source YAML. | CMD-0194; `configs/prometheus/rules.r1/README.md`; `configs/prometheus/prometheus.r1.yml`; `configs/audit/README.md`; `configs/audit/wasm-walk-contracts.yaml` | Closes F-1268 and F-1269 |
| EV-0196 | 2026-05-13 | The Redis Sentinel HA path is internally inconsistent at listener auth. `configs/ansible/roles/redis-sentinel/README.md`, the Sentinel design note, and `docs/operations/runbooks/redis-master-down.md` all show `redis-cli -p 26379 -a "$REDIS_PASSWORD" ...`; `internal/storage/redisclient/redisclient.go` sets `SentinelPassword: cfg.RedisPassword` whenever Sentinel mode is enabled; and go-redis v9's `sentinel.go` documents that `SentinelPassword` activates Sentinel-side AUTH. The rendered `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2` never emits Sentinel listener auth (`requirepass` or ACL equivalent). It only emits `sentinel auth-pass`, which is Sentinel's credential for authenticating to the monitored Redis primary. | CMD-0196; `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`; `configs/ansible/roles/redis-sentinel/README.md`; `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `docs/operations/runbooks/redis-master-down.md`; `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md`; `internal/storage/redisclient/redisclient.go`; `/Users/ash/go/pkg/mod/github.com/redis/go-redis/v9@v9.18.0/sentinel.go` | Supports F-1271 |
| EV-0197 | 2026-05-13 | Redis ACL lockdown still misses one generated Redis client: `redis_exporter`. `configs/ansible/roles/redis-sentinel/templates/users.acl.j2` disables `default` and defines the named `ratesengine` user; reference config guidance says hardened clients must use that username. Yet `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml` still generates `redis_exporter.service` with only `Environment=REDIS_PASSWORD={{ redis_password }}` and `-redis.addr=redis://127.0.0.1:{{ redis_port }}`. There is no rendered username in the URI, no username environment surface, and no lockdown-aware branch. `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` and the Redis role README still treat `redis_exporter` as the live Redis metrics path, so an ACL-lockdown rollout can blind that observability leg. | CMD-0197; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2`; `configs/ansible/roles/redis-sentinel/README.md`; `docs/reference/config/README.md` | Supports F-1272 |
| EV-0198 | 2026-05-13 | Two freshly raised findings are now closed on the current workspace after source edits landed in parallel. For `F-1270`, `configs/caddy/README.md` says the API's `trusted_proxy_cidrs = ["127.0.0.1/32"]` config stays unchanged under Cloudflare because Caddy remains the only proxy the API trusts, and `docs/operations/pre-launch-hardening.md` says no API-side CIDR change is needed while Cloudflare CIDRs live in Caddy's trusted-proxy block. For `F-1271`, `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2` now renders `requirepass {{ redis_password }}` for Sentinel listener auth while preserving `sentinel auth-pass` for Sentinel-to-primary authentication, matching the docs/runbooks and `SentinelPassword` client contract already audited. | CMD-0198; `configs/caddy/README.md`; `docs/operations/pre-launch-hardening.md`; `docs/adr/0025-caddy-cloudflare-trusted-proxy.md`; `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`; `internal/storage/redisclient/redisclient.go` | Closes F-1270 and F-1271 |
| EV-0199 | 2026-05-13 | The earlier `F-1266` doc closure was not final. `configs/ansible/README.md` now says multi-host bring-ups are orchestrated by `monitoring.yml`, `postgres-cluster.yml`, `redis-cluster.yml`, `haproxy.yml`, and `loki.yml`; `configs/ansible/roles/redis-sentinel/README.md` tells operators to run `playbooks/redis-cluster.yml`. A tracked-file inventory check via `rg --files configs/ansible/playbooks` finds only `archival-node.yml`, `deploy-binary.yml`, and `monitoring.yml`. The role inventory prose is better than before, but the execution instructions now advertise orchestration paths that do not exist in the repository. | CMD-0199; `configs/ansible/README.md`; `configs/ansible/roles/redis-sentinel/README.md`; `configs/ansible/playbooks/archival-node.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/playbooks/monitoring.yml` | Reopens F-1266 |
| EV-0200 | 2026-05-13 | The first `redis_exporter` fix did not close `F-1272`; it moved the breakage onto the default non-lockdown branch. The current workspace adds `user redis_exporter on >{{ redis_password }}` to `users.acl.j2` and always renders `-redis.user=redis_exporter` inside `tasks/07-monitoring.yml`. But `defaults/main.yml` still sets `redis_acl_lockdown: false`, `redis.conf.j2` includes `aclfile /etc/redis/users.acl` only when lockdown is true, and `03-redis-configure.yml` renders that ACL file only inside the same conditional. In the default supported role mode, Redis never receives a `redis_exporter` ACL user, while the exporter still tries `AUTH redis_exporter <password>`. The scrape target therefore remains branch-inconsistent even though the original lockdown-on defect is no longer the exact failure shape. | CMD-0200; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/redis-sentinel/templates/redis.conf.j2`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/tasks/03-redis-configure.yml`; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml` | Preserves F-1272 as open |
| EV-0201 | 2026-05-13 | The shipped Redis Sentinel docs remain materially behind the implementation. `docs/architecture/redis-sentinel-ansible-role-design-note.md` is marked `status: shipped`, but it still says the Sentinel-aware client is a future `internal/cachekeys` change, its role layout omits the landed ACL file, and its `sentinel.conf` excerpt omits the listener `requirepass` now present in `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`. The actual connection factory is `internal/storage/redisclient/redisclient.go`, which already builds `FailoverClient` with `SentinelPassword`. Separately, `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md` still tells participants to run `redis-cli -p 26379 SENTINEL get-master-addr-by-name` without the password now required by the rendered Sentinel listener. | CMD-0201; `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md`; `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2`; `internal/storage/redisclient/redisclient.go` | Supports F-1273 |
| EV-0202 | 2026-05-13 | The wave-107 audit control plane is synchronized after the Redis/Sentinel deepening pass. Mechanical recounts produce `59 fixed / 14 open` findings, `52 fixed / 13 open` XFI rows, and `54 fixed / 17 open` remediation rows. File coverage rolls up to `done=143`, `in_progress=99`, `todo=1660`; tracked-file parity remains exact at `1902 / 1902`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0202; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-107 audit-control synchronization proof |
| EV-0203 | 2026-05-13 | The Ansible docs repair narrowed but did not close `F-1266`. `configs/ansible/README.md` now truthfully says only `archival-node.yml`, `deploy-binary.yml`, and `monitoring.yml` are tracked, and `configs/ansible/roles/redis-sentinel/README.md` now comments out its previously executable `playbooks/redis-cluster.yml` examples while admitting that playbook has not landed. However, the sibling role READMEs still advertise absent playbooks as real operator commands: `roles/haproxy/README.md` uses `playbooks/haproxy.yml`, `roles/loki/README.md` uses `playbooks/loki.yml`, `roles/patroni/README.md` uses `playbooks/postgres-cluster.yml`, and `roles/prometheus/README.md` uses `playbooks/prometheus.yml`. None of those files exist under `configs/ansible/playbooks/`. | CMD-0203; `configs/ansible/README.md`; `configs/ansible/roles/redis-sentinel/README.md`; `configs/ansible/roles/haproxy/README.md`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/patroni/README.md`; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/playbooks/archival-node.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/playbooks/monitoring.yml` | Narrows but preserves F-1266 |
| EV-0204 | 2026-05-13 | One shipped monitoring design note still contradicts the severity vocabulary now standardized everywhere else. `configs/alertmanager/README.md`, `configs/alertmanager/alertmanager.r1.yml`, `configs/ansible/roles/prometheus/README.md`, and `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2` all use the `page` / `ticket` / `informational` ladder. `docs/architecture/prometheus-ansible-role-design-note.md`, despite `status: shipped`, still shows example Alertmanager routes with `severity: critical`, `severity: warning`, and `severity: info`. | CMD-0204; `docs/architecture/prometheus-ansible-role-design-note.md`; `configs/alertmanager/README.md`; `configs/alertmanager/alertmanager.r1.yml`; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2` | Reopens F-1265 |
| EV-0205 | 2026-05-13 | The wave-108 audit control plane is synchronized after narrowing the Ansible playbook-doc finding and reopening the residual monitoring design-note drift. Mechanical recounts produce `57 fixed / 16 open` findings, `51 fixed / 14 open` XFI rows, and `53 fixed / 18 open` remediation rows. File coverage rolls up to `done=143`, `in_progress=103`, `todo=1656`; tracked-file parity remains exact at `1902 / 1902`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0205; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-108 audit-control synchronization proof |
| EV-0206 | 2026-05-13 | The current workspace closes the residual `F-1265` design-note drift. `docs/architecture/prometheus-ansible-role-design-note.md` now shows Alertmanager example routes with `severity: page`, `severity: ticket`, and `severity: informational`, and the note explicitly records that an earlier `critical / warning / info` version was retired after R1 standup. The runnable Alertmanager template and operator docs were already on that vocabulary, so the design note is now aligned with them. | CMD-0206; `docs/architecture/prometheus-ansible-role-design-note.md`; `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2`; `configs/ansible/roles/prometheus/README.md`; `configs/alertmanager/README.md`; `configs/alertmanager/alertmanager.r1.yml` | Closes F-1265 |
| EV-0207 | 2026-05-13 | The HAProxy role's claimed companion runbook is absent. `configs/ansible/roles/haproxy/README.md` links to `docs/operations/runbooks/api-pod-down.md` and says the role changes that runbook's mitigation flow; `docs/architecture/haproxy-ansible-role-design-note.md` repeats the same runbook name while noting it "may not yet exist." A tracked-file inventory scan of `docs/operations/runbooks/` finds no `api-pod-down.md`. | CMD-0207; `configs/ansible/roles/haproxy/README.md`; `docs/architecture/haproxy-ansible-role-design-note.md`; `docs/operations/runbooks/` | Supports F-1274 |
| EV-0208 | 2026-05-13 | The Redis Sentinel doc remediation narrowed but did not close `F-1273`. `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md` now uses the authenticated form `redis-cli -p 26379 -a "$REDIS_PASSWORD" SENTINEL get-master-addr-by-name ...`, matching the listener `requirepass` rendered in source. But `docs/architecture/redis-sentinel-ansible-role-design-note.md` still retains future-state architecture claims that the Sentinel-aware client is an `internal/cachekeys` change and later repeats `internal/cachekeys/` in its implementation work list, even though `internal/storage/redisclient/redisclient.go` already owns the live `FailoverClient` path. | CMD-0208; `docs/operations/drills/scenarios/sev2-redis-sentinel-failover.md`; `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `internal/storage/redisclient/redisclient.go`; `configs/ansible/roles/redis-sentinel/templates/sentinel.conf.j2` | Narrows but preserves F-1273 |
| EV-0209 | 2026-05-13 | The wave-109 audit control plane is synchronized after closing the residual Prometheus design-note drift, narrowing the Sentinel docs finding, and adding the HAProxy missing-runbook finding. Mechanical recounts produce `58 fixed / 16 open` findings, `52 fixed / 14 open` XFI rows, and `54 fixed / 18 open` remediation rows. File coverage rolls up to `done=145`, `in_progress=102`, `todo=1655`; tracked-file parity remains exact at `1902 / 1902`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0209; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-109 audit-control synchronization proof |
| EV-0210 | 2026-05-13 | The remaining Ansible role-doc drift behind `F-1266` is now closed. `configs/ansible/roles/haproxy/README.md`, `roles/loki/README.md`, and `roles/patroni/README.md` all retain their missing-playbook examples only as commented backlog placeholders that explicitly say the orchestration files have not landed. `configs/ansible/roles/prometheus/README.md` points at tracked `playbooks/monitoring.yml` instead of the old absent `playbooks/prometheus.yml` path. | CMD-0210; `configs/ansible/roles/haproxy/README.md`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/patroni/README.md`; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/playbooks/monitoring.yml` | Closes F-1266 |
| EV-0211 | 2026-05-13 | The Redis exporter auth branch mismatch behind `F-1272` is now source-closed. `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml` wraps `-redis.user=redis_exporter` in `{% if redis_acl_lockdown | bool %}`. That matches the ACL-file branch in `templates/redis.conf.j2` and `tasks/03-redis-configure.yml`: lockdown-off keeps legacy password-only exporter auth, lockdown-on activates the ACL file that defines `user redis_exporter ...` in `templates/users.acl.j2`. | CMD-0211; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/templates/redis.conf.j2`; `configs/ansible/roles/redis-sentinel/tasks/03-redis-configure.yml`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2` | Closes F-1272 |
| EV-0212 | 2026-05-13 | The open Redis Sentinel docs drift is broader than the earlier design-note-only narrowing captured in `EV-0208`. `docs/architecture/redis-sentinel-ansible-role-design-note.md` still presents the already-landed FailoverClient ownership as future `internal/cachekeys` work, and `configs/ansible/roles/redis-sentinel/README.md` independently says "we plumb this through `internal/cachekeys` after this role lands." The implementation is already live in `internal/storage/redisclient/redisclient.go`. | CMD-0212; `docs/architecture/redis-sentinel-ansible-role-design-note.md`; `configs/ansible/roles/redis-sentinel/README.md`; `internal/storage/redisclient/redisclient.go` | Widens but preserves F-1273 |
| EV-0213 | 2026-05-13 | The HAProxy missing-runbook drift behind `F-1274` is now source-closed. `configs/ansible/roles/haproxy/README.md` no longer presents `api-pod-down.md` as a missing companion document; it redirects operators to tracked `docs/operations/runbooks/api-down.md` and records that the older path was wrong. `docs/architecture/haproxy-ansible-role-design-note.md` makes the same correction and treats future single-pod-eject guidance as a section to add inside `api-down.md`, not a phantom standalone file. | CMD-0213; `configs/ansible/roles/haproxy/README.md`; `docs/architecture/haproxy-ansible-role-design-note.md`; `docs/operations/runbooks/api-down.md` | Closes F-1274 |
| EV-0214 | 2026-05-13 | The shipped HA path turns Redis unavailability into a routing failure even though the maintained outage contract says Redis should degrade serving rather than stop it. `configs/ansible/roles/haproxy/defaults/main.yml` sets `haproxy_health_check_path: /v1/readyz`, and `templates/haproxy.cfg.j2` only keeps backends UP when that path returns 200. `cmd/ratesengine-api/main.go` appends `redisChecker` whenever Redis is configured; `internal/api/v1/server.go` returns 503 from `/v1/readyz` if any checker fails. `docs/architecture/ha-plan.md` explicitly says Redis master loss makes `readyz` false during Sentinel failover. In conflict, `docs/operations/runbooks/redis-master-down.md` says customers still get Timescale fallback with `stale=true` and "so not an outage," while `docs/operations/runbooks/api-down.md` says Redis red should not take a host out of Ready. | CMD-0214; `configs/ansible/roles/haproxy/defaults/main.yml`; `configs/ansible/roles/haproxy/templates/haproxy.cfg.j2`; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `docs/architecture/ha-plan.md`; `docs/operations/runbooks/redis-master-down.md`; `docs/operations/runbooks/api-down.md` | Supports F-1275 |
| EV-0215 | 2026-05-13 | API incident-query docs remain on a selector family that no live config uses. `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` uses `job_name: "ratesengine_api"` for multi-host HA, `configs/prometheus/prometheus.r1.yml` uses `job_name: ratesengine-api` for R1, and the two API rule files match those shapes. But `docs/operations/alerts-catalog.md`, `docs/operations/runbooks/api-down.md`, `docs/operations/runbooks/api-5xx.md`, `docs/operations/runbooks/api-latency.md`, `docs/operations/runbooks/sla-probe-p95-breach.md`, and the source comment in `internal/obs/metrics.go` still publish `job="api"` examples. | CMD-0215; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2`; `configs/prometheus/prometheus.r1.yml`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/alerts-catalog.md`; `docs/operations/runbooks/api-down.md`; `docs/operations/runbooks/api-5xx.md`; `docs/operations/runbooks/api-latency.md`; `docs/operations/runbooks/sla-probe-p95-breach.md`; `internal/obs/metrics.go` | Supports F-1276 |
| EV-0216 | 2026-05-13 | `docs/operations/runbooks/api-down.md` sends readers to `internal/api/v1/healthz.go` for `/v1/readyz` semantics, but that file does not exist anywhere in the tracked repository. The readiness handler and its 503-on-failing-check behavior are implemented in `internal/api/v1/server.go`. | CMD-0216; `docs/operations/runbooks/api-down.md`; `internal/api/v1/server.go` | Supports F-1277 |
| EV-0217 | 2026-05-13 | The reviewed HA-role nftables composition is structurally unsound. `configs/ansible/roles/{haproxy,redis-sentinel,prometheus}/tasks/06-firewall.yml`, `roles/loki/tasks/server-05-firewall.yml`, and `roles/patroni/tasks/10-firewall.yml` all render separate input base chains with `priority 0; policy accept;` and accept rules only. The repo's existing hardening baseline in `configs/ansible/roles/archival-node/templates/nftables.conf.j2` is a separate default-drop input base chain at filter priority 0. Primary nftables documentation says equal-priority base-chain ordering is undefined and that `accept` in one base chain is not final if a later base chain drops the packet. So these drop-ins neither enforce internal-only policy by themselves nor deterministically open their documented ports beside the repo default-drop baseline. | CMD-0217; `configs/ansible/roles/haproxy/tasks/06-firewall.yml`; `configs/ansible/roles/redis-sentinel/tasks/06-firewall.yml`; `configs/ansible/roles/prometheus/tasks/06-firewall.yml`; `configs/ansible/roles/loki/tasks/server-05-firewall.yml`; `configs/ansible/roles/patroni/tasks/10-firewall.yml`; `configs/ansible/roles/archival-node/templates/nftables.conf.j2` | Supports F-1278 |
| EV-0218 | 2026-05-13 | Patroni's firewall task ordering is wrong for a clean host. `configs/ansible/roles/patroni/tasks/10-firewall.yml` first copies `/etc/nftables.d/40-patroni.conf`, then edits `/etc/nftables.conf`, and only afterward ensures `/etc/nftables.d/` exists. The sibling HA roles create `/etc/nftables.d/` before their copy tasks. | CMD-0218; `configs/ansible/roles/patroni/tasks/10-firewall.yml`; sibling HA firewall task ordering | Supports F-1279 |
| EV-0219 | 2026-05-13 | The wave-110 audit control plane is synchronized after the HAProxy/readiness, API incident-doc, and cross-role nftables passes. Mechanical recounts produce `59 fixed / 20 open` findings, `55 fixed / 16 open` XFI rows, and `57 fixed / 20 open` remediation rows. File coverage rolls up to `done=152`, `in_progress=111`, `todo=1663`; tracked-file parity is exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0219; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-110 audit-control synchronization proof |
| EV-0220 | 2026-05-13 | Patroni's etcd installer is not runnable from the documented defaults. `configs/ansible/roles/patroni/tasks/02-etcd-install.yml` downloads etcd v3.5.21 with `checksum: "sha256:{{ etcd_release_sha256 | default('REPLACE_WITH_RELEASE_SHA') }}"`; `defaults/main.yml` does not define `etcd_release_sha256`; and `configs/ansible/roles/patroni/README.md` does not list that variable as a prerequisite. | CMD-0220; `configs/ansible/roles/patroni/tasks/02-etcd-install.yml`; `configs/ansible/roles/patroni/defaults/main.yml`; `configs/ansible/roles/patroni/README.md` | Supports F-1280 |
| EV-0221 | 2026-05-13 | Patroni's monitoring timer can fail on a clean host because its script has an undeclared dependency. `tasks/11-monitoring.yml` writes `patroni-textfile-scraper` with two `jq -r` invocations, but `tasks/05-patroni-install.yml` installs only `patroni`, `python3-etcd3`, `python3-psycopg2`, and `python3-prettytable`; the role README does not list `jq` as a prerequisite. | CMD-0221; `configs/ansible/roles/patroni/tasks/11-monitoring.yml`; `configs/ansible/roles/patroni/tasks/05-patroni-install.yml`; `configs/ansible/roles/patroni/README.md` | Supports F-1281 |
| EV-0222 | 2026-05-13 | Patroni's documented pgBackRest restore target is dead configuration. `defaults/main.yml`, the role README, and `docs/architecture/patroni-ansible-role-design-note.md` advertise `patroni_pgbackrest_restore_target` as `latest` or a point-in-time value. But `tasks/08-patroni-bootstrap.yml` never references that variable; it always runs `pgbackrest --stanza={{ patroni_pgbackrest_stanza }} --type=immediate --target-action=promote --delta restore`. | CMD-0222; `configs/ansible/roles/patroni/defaults/main.yml`; `configs/ansible/roles/patroni/tasks/08-patroni-bootstrap.yml`; `configs/ansible/roles/patroni/README.md`; `docs/architecture/patroni-ansible-role-design-note.md` | Supports F-1282 |
| EV-0223 | 2026-05-13 | The wave-111 audit control plane is synchronized after the Patroni runnable-defaults/DR tranche. Mechanical recounts produce `59 fixed / 23 open` findings, `55 fixed / 19 open` XFI rows, and `57 fixed / 23 open` remediation rows. File coverage rolls up to `done=151`, `in_progress=118`, `todo=1657`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0223; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-111 audit-control synchronization proof |
| EV-0224 | 2026-05-13 | The moving workspace remediated three Patroni findings and narrowed two others. `tasks/10-firewall.yml` now creates `/etc/nftables.d/` before writing the Patroni drop-in, closing `F-1279`. `tasks/05-patroni-install.yml` installs `jq`, closing `F-1281`. `tasks/08-patroni-bootstrap.yml` validates `patroni_pgbackrest_restore_target` and maps latest/immediate/time targets to pgBackRest flags, closing `F-1282`. `F-1280` narrows because `02-etcd-install.yml` now asserts a non-placeholder 64-character `etcd_release_sha256` and defaults comment the requirement, but the role README still omits that required checksum variable. `F-1278` remains open: HA role drop-ins now use `priority -100`, but their comments incorrectly claim early accepts stop the later default-drop chain; nftables can still drop a packet accepted by an earlier base chain. | CMD-0224; `configs/ansible/roles/{haproxy,redis-sentinel,prometheus}/tasks/06-firewall.yml`; `configs/ansible/roles/loki/tasks/server-05-firewall.yml`; `configs/ansible/roles/patroni/tasks/10-firewall.yml`; `configs/ansible/roles/patroni/tasks/02-etcd-install.yml`; `configs/ansible/roles/patroni/defaults/main.yml`; `configs/ansible/roles/patroni/README.md`; `configs/ansible/roles/patroni/tasks/05-patroni-install.yml`; `configs/ansible/roles/patroni/tasks/08-patroni-bootstrap.yml` | Closes F-1279, F-1281, F-1282; narrows F-1280; preserves F-1278 |
| EV-0225 | 2026-05-13 | The Timescale primary-down runbook's etcd diagnosis path does not match the shipped Patroni role. The runbook uses `etcdctl --endpoints=https://etcd-1.internal:2379 get /ratesengine/leader` and says at least `3 of 5` etcd members must be healthy. The role renders `ETCD_LISTEN_CLIENT_URLS` and `ETCD_ADVERTISE_CLIENT_URLS` as `http://{{ ansible_host }}:2379`, its health task runs `etcdctl --endpoints=http://{{ ansible_host }}:2379 endpoint health`, Patroni config uses `namespace: /service/` with per-cluster `scope`, and preflight asserts exactly three `postgres_cluster` hosts. | CMD-0225; `docs/operations/runbooks/timescale-primary-down.md`; `configs/ansible/roles/patroni/templates/etcd.conf.j2`; `configs/ansible/roles/patroni/templates/patroni.yml.j2`; `configs/ansible/roles/patroni/tasks/04-etcd-systemd.yml`; `configs/ansible/roles/patroni/tasks/01-preflight.yml`; `docs/architecture/patroni-ansible-role-design-note.md` | Supports F-1283 |
| EV-0226 | 2026-05-13 | The wave-112 audit control plane is synchronized after the Patroni remediation recheck and DCS-runbook finding. Mechanical recounts produce `62 fixed / 21 open` findings, `58 fixed / 17 open` XFI rows, and `60 fixed / 21 open` remediation rows. File coverage rolls up to `done=153`, `in_progress=121`, `todo=1652`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0226; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-112 audit-control synchronization proof |
| EV-0227 | 2026-05-13 | The original HAProxy/Redis readiness outage path behind `F-1275` is source-closed. `cmd/ratesengine-api/main.go::redisChecker.Critical()` returns `false`; `internal/api/v1/server.go::handleReadyz` returns HTTP 200 with body `status="degraded"` when only non-critical checks fail; and `internal/api/v1/server_test.go::TestReadyz_NonCriticalFailureReturns200Degraded` pins that a Redis-only failure keeps the backend in HAProxy's pool. The remaining mismatch is documentation-only: `configs/ansible/roles/haproxy/defaults/main.yml`, `templates/haproxy.cfg.j2`, `README.md`, and `docs/architecture/ha-plan.md` still describe `/v1/readyz` as requiring Redis reachability or going false during Redis failover. | CMD-0227; `cmd/ratesengine-api/main.go`; `internal/api/v1/server.go`; `internal/api/v1/server_test.go`; `configs/ansible/roles/haproxy/defaults/main.yml`; `configs/ansible/roles/haproxy/templates/haproxy.cfg.j2`; `configs/ansible/roles/haproxy/README.md`; `docs/architecture/ha-plan.md` | Closes F-1275 and supports F-1284 |
| EV-0228 | 2026-05-13 | The wave-113 audit control plane is synchronized after closing the runtime `F-1275` path and splitting residual HAProxy documentation drift into `F-1284`. Mechanical recounts produce `63 fixed / 21 open` findings, `59 fixed / 17 open` XFI rows, and `61 fixed / 21 open` remediation rows. File coverage rolls up to `done=152`, `in_progress=123`, `todo=1651`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0228; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-113 audit-control synchronization proof |
| EV-0229 | 2026-05-13 | The API runbook/source-breadcrumb pass narrowed one finding and closed another. Maintained API operator docs now use real selector families: `alerts-catalog.md` and `api-down.md` use `job=~"ratesengine[_-]api"`, while the rule files continue to use `ratesengine_api` for multi-host and `ratesengine-api` for R1. The only remaining stale `job="api"` hit in the audited surface is `internal/obs/metrics.go`. Separately, `api-down.md` now cites `internal/api/v1/server.go::handleReadyz` and records that the earlier `internal/api/v1/healthz.go` breadcrumb was corrected. | CMD-0229; `docs/operations/alerts-catalog.md`; `docs/operations/runbooks/api-down.md`; `docs/operations/runbooks/api-5xx.md`; `docs/operations/runbooks/api-latency.md`; `docs/operations/runbooks/sla-probe-p95-breach.md`; `internal/obs/metrics.go`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml` | Narrows F-1276 and closes F-1277 |
| EV-0230 | 2026-05-13 | The wave-114 audit control plane is synchronized after closing `F-1277` and narrowing `F-1276`. Mechanical recounts produce `64 fixed / 20 open` findings, `60 fixed / 16 open` XFI rows, and `62 fixed / 20 open` remediation rows. File coverage rolls up to `done=157`, `in_progress=118`, `todo=1651`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0230; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-114 audit-control synchronization proof |
| EV-0231 | 2026-05-13 | The monitoring HA roles do not consistently authenticate upstream release archives before installing host binaries. `configs/ansible/roles/loki/tasks/server-02-install.yml` downloads `loki-linux-amd64.zip` from the Grafana Loki GitHub release URL with no checksum. `configs/ansible/roles/loki/tasks/agent-01-install.yml` does the same for `promtail-linux-amd64.zip`. `configs/ansible/roles/prometheus/tasks/02-install.yml` includes checksum fields for Prometheus and Alertmanager, but both are optional via `prometheus_sha256 | default(omit)` and `alertmanager_sha256 | default(omit)`. The Loki and Prometheus defaults pin versions only, and neither README requires checksum inventory variables. | CMD-0231; `configs/ansible/roles/loki/tasks/server-02-install.yml`; `configs/ansible/roles/loki/tasks/agent-01-install.yml`; `configs/ansible/roles/prometheus/tasks/02-install.yml`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/README.md` | Supports F-1285 |
| EV-0232 | 2026-05-13 | Loki's systemd service does not implement the S3 credential indirection described by its defaults and docs. `templates/loki.service.j2` sets `Environment=AWS_ACCESS_KEY_ID=${RATESENGINE_S3_ACCESS_KEY}` and `Environment=AWS_SECRET_ACCESS_KEY=${RATESENGINE_S3_SECRET_KEY}` before `EnvironmentFile=-/etc/default/loki`; defaults define those source variable names as `loki_s3_access_key_env` and `loki_s3_secret_key_env`; the README says the credentials are referenced via the same env-var pair as Galexie; and `loki-config.yaml.j2` says Loki receives AWS env vars set from that pair. systemd `Environment=` assignments are not shell expansions against `EnvironmentFile`, so the unit can pass literal `${RATESENGINE_S3_ACCESS_KEY}` / `${RATESENGINE_S3_SECRET_KEY}` strings rather than the credential values Loki needs for MinIO chunk storage. | CMD-0232; `configs/ansible/roles/loki/templates/loki.service.j2`; `configs/ansible/roles/loki/templates/loki-config.yaml.j2`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md` | Supports F-1286 |
| EV-0233 | 2026-05-13 | The wave-115 audit control plane is synchronized after adding the monitoring supply-chain and Loki credential findings. Mechanical recounts produce `64 fixed / 22 open` findings, `60 fixed / 18 open` XFI rows, and `62 fixed / 22 open` remediation rows. File coverage rolls up to `done=155`, `in_progress=127`, `todo=1644`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0233; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-115 audit-control synchronization proof |
| EV-0234 | 2026-05-13 | The Prometheus role's HTTP bind defaults do not match the generated targets Prometheus uses for its own monitoring and alert delivery. `defaults/main.yml` sets `prometheus_listen: "127.0.0.1:9090"` and `alertmanager_listen: "127.0.0.1:9093"`. The systemd templates pass those directly to Prometheus `--web.listen-address` and Alertmanager `--web.listen-address`. But `templates/prometheus.yml.j2` renders `alerting.alertmanagers` as `{{ hostvars[h].ansible_host }}:9093`, renders `prometheus_pair` self-scrape targets as `{{ hostvars[h].ansible_host }}:9090`, and renders `alertmanager_pair` self-scrape targets as `{{ hostvars[h].ansible_host }}:9093`. The README reinforces the loopback-only UI posture, so the default documented role topology points clients at addresses the services do not listen on. | CMD-0234; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/templates/prometheus.service.j2`; `configs/ansible/roles/prometheus/templates/alertmanager.service.j2`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2`; `configs/ansible/roles/prometheus/README.md` | Supports F-1287 |
| EV-0235 | 2026-05-13 | The wave-116 audit control plane is synchronized after adding the Prometheus/Alertmanager bind-target finding. Mechanical recounts produce `64 fixed / 23 open` findings, `60 fixed / 19 open` XFI rows, and `62 fixed / 23 open` remediation rows. File coverage rolls up to `done=155`, `in_progress=129`, `todo=1642`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0235; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-116 audit-control synchronization proof |
| EV-0236 | 2026-05-13 | Prometheus's TSDB capacity preflight is warning-only despite operator docs presenting it as a hard prerequisite. `configs/ansible/roles/prometheus/README.md` says each Prometheus host needs at least 20 GB free on `/var` and that preflight asserts that capacity for the 30-day retention plan. `configs/ansible/roles/prometheus/defaults/main.yml` keeps `prometheus_retention_days: 30`. But `tasks/01-preflight.yml` marks the `assert disk space at /var` task with `ignore_errors: yes`, so insufficient free space or any expression/mount-fact mismatch does not block role application. | CMD-0236; `configs/ansible/roles/prometheus/tasks/01-preflight.yml`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/README.md` | Supports F-1288 |
| EV-0237 | 2026-05-13 | The wave-117 audit control plane is synchronized after adding the Prometheus disk-preflight finding. Mechanical recounts produce `64 fixed / 24 open` findings, `60 fixed / 20 open` XFI rows, and `62 fixed / 24 open` remediation rows. File coverage rolls up to `done=155`, `in_progress=130`, `todo=1641`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0237; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-117 audit-control synchronization proof |
| EV-0238 | 2026-05-13 | `F-1286` is source-closed in the moving workspace. `configs/ansible/roles/loki/templates/loki.service.j2` now uses only `EnvironmentFile=-/etc/default/loki` and explicitly documents that operators must put `AWS_ACCESS_KEY_ID=` and `AWS_SECRET_ACCESS_KEY=` in that file; the earlier literal `Environment=AWS_ACCESS_KEY_ID=${RATESENGINE_S3_ACCESS_KEY}` and secret-key equivalent are gone. `configs/ansible/roles/loki/README.md` now repeats the direct `AWS_*` contract and explains why the prior Galexie-style indirection was invalid under systemd. Defaults retain the old env-var-name knobs only for compatibility and state the service template no longer reads them. | CMD-0238; `configs/ansible/roles/loki/templates/loki.service.j2`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/templates/loki-config.yaml.j2` | Closes F-1286 |
| EV-0239 | 2026-05-13 | Loki's documented storage prerequisites are not enforced by the shipped server preflight. `configs/ansible/roles/loki/defaults/main.yml` says the operator creates `loki-chunks` and "the role asserts it exists at preflight." `configs/ansible/roles/loki/README.md` requires at least 50 GB free on `/var`, MinIO reachability, and a pre-created bucket/IAM user. `docs/architecture/loki-ansible-role-design-note.md` says the role creates the bucket if absent and asserts at least 50 GB free. In contrast, `tasks/server-01-preflight.yml` only checks Ubuntu version, `log_aggregator`/`log_shippers` groups, time sync, and creates the Loki user; there is no MinIO, bucket, credential, or local-capacity validation. | CMD-0239; `configs/ansible/roles/loki/tasks/server-01-preflight.yml`; `configs/ansible/roles/loki/defaults/main.yml`; `configs/ansible/roles/loki/README.md`; `docs/architecture/loki-ansible-role-design-note.md` | Supports F-1289 |
| EV-0240 | 2026-05-13 | The wave-118 audit control plane is synchronized after closing the Loki credential finding and adding the Loki storage-preflight finding. Mechanical recounts produce `65 fixed / 24 open` findings, `61 fixed / 20 open` XFI rows, and `63 fixed / 24 open` remediation rows. File coverage rolls up to `done=155`, `in_progress=132`, `todo=1639`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0240; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-118 audit-control synchronization proof |
| EV-0241 | 2026-05-13 | Parallel monitoring-role fixes changed three findings. For `F-1285`, all four install paths now assert and use release checksums: Loki uses `loki_release_sha256`, Promtail uses `promtail_release_sha256`, Prometheus uses `prometheus_sha256`, and Alertmanager uses `alertmanager_sha256`. The finding remains open only because the role READMEs/defaults do not document those required variables. `F-1287` is source-closed: defaults bind Prometheus and Alertmanager to `0.0.0.0:9090/9093`, and `tasks/06-firewall.yml` opens 9090/9093/9094 to internal CIDRs so generated private-IP targets are reachable. `F-1288` is source-closed: `tasks/01-preflight.yml` now runs `df --output=avail -B1 /var` and blocks below the documented 20 GB threshold without `ignore_errors`. | CMD-0241; `configs/ansible/roles/loki/tasks/server-02-install.yml`; `configs/ansible/roles/loki/tasks/agent-01-install.yml`; `configs/ansible/roles/prometheus/tasks/02-install.yml`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/tasks/06-firewall.yml`; `configs/ansible/roles/prometheus/tasks/01-preflight.yml`; `configs/ansible/roles/loki/README.md`; `configs/ansible/roles/prometheus/README.md` | Narrows F-1285 and closes F-1287/F-1288 |
| EV-0242 | 2026-05-13 | The Prometheus role README did not fully follow the bind-address remediation. `configs/ansible/roles/prometheus/defaults/main.yml` now binds `prometheus_listen` and `alertmanager_listen` to `0.0.0.0:9090/9093`, and `tasks/06-firewall.yml` opens TCP 9090, TCP 9093, and TCP/UDP 9094 to `prometheus_internal_cidrs`. The README still describes the Prometheus and Alertmanager UIs as "loopback-only" and says `06-firewall.yml` opens only 9094 for Alertmanager gossip. | CMD-0242; `configs/ansible/roles/prometheus/README.md`; `configs/ansible/roles/prometheus/defaults/main.yml`; `configs/ansible/roles/prometheus/tasks/06-firewall.yml` | Supports F-1290 |
| EV-0243 | 2026-05-13 | The wave-119 audit control plane is synchronized after the Prometheus moving-fix reconciliation. Mechanical recounts produce `67 fixed / 23 open` findings, `63 fixed / 19 open` XFI rows, and `65 fixed / 23 open` remediation rows. File coverage rolls up to `done=161`, `in_progress=126`, `todo=1639`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0243; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-119 audit-control synchronization proof |
| EV-0244 | 2026-05-13 | Archival-node privileged binary installs are unauthenticated. `configs/ansible/roles/archival-node/defaults/main.yml` pins `minio_version: "RELEASE.2024-11-07T00-52-20Z"` and `node_exporter_version: "1.8.2"`, but `tasks/09-minio.yml` downloads `/usr/local/bin/minio` without `checksum:` and downloads `/usr/local/bin/mc` from the mutable `https://dl.min.io/client/mc/release/linux-amd64/mc` URL without version or checksum. `tasks/10-observability.yml` downloads the node_exporter tarball without `checksum:` before extracting the executable into `/usr/local/bin/`. The two MinIO tasks retain `TODO(#0)` comments to pin SHA256/version. | CMD-0244; `configs/ansible/roles/archival-node/tasks/09-minio.yml`; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/ansible/roles/archival-node/defaults/main.yml` | Supports F-1291 |
| EV-0245 | 2026-05-13 | The active `core-lag` incident runbook conflicts with the repo's no-Horizon operational invariant. `docs/adr/0001-horizon-deprecated.md` says Horizon is not in the architecture and states "Runbooks never reference Horizon commands" under operational impact; `README.md` and `CLAUDE.md` repeat that Horizon is not in the architecture. `docs/operations/runbooks/core-lag.md` Step 1 still says to cross-check via SDF's Horizon / stellar.expert. | CMD-0244; `docs/operations/runbooks/core-lag.md`; `docs/adr/0001-horizon-deprecated.md`; `README.md`; `CLAUDE.md` | Supports F-1292 |
| EV-0246 | 2026-05-13 | The wave-120 audit control plane is synchronized after adding the archival-node supply-chain and no-Horizon runbook findings. Mechanical recounts produce `67 fixed / 25 open` findings, `63 fixed / 21 open` XFI rows, and `65 fixed / 25 open` remediation rows. File coverage rolls up to `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0245; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-120 audit-control synchronization proof |
| EV-0247 | 2026-05-13 | Moving fixes changed both fresh findings. `configs/ansible/roles/archival-node/tasks/09-minio.yml` now asserts `minio_release_sha256`, uses it as `get_url.checksum`, asserts `mc_version` plus `mc_release_sha256`, and downloads `mc` from a versioned archive path with checksum enforcement. `configs/ansible/roles/archival-node/tasks/10-observability.yml` now asserts and uses `node_exporter_release_sha256`. `F-1291` remains open only because `defaults/main.yml` and active archival-node operator docs still do not list the new required variables. `docs/operations/runbooks/core-lag.md` no longer instructs operators to use Horizon; it points at stellar.expert/explorer or a public stellar-rpc endpoint and notes the ADR-0001 correction. | CMD-0246; `configs/ansible/roles/archival-node/tasks/09-minio.yml`; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/ansible/roles/archival-node/defaults/main.yml`; `docs/operations/runbooks/core-lag.md` | Narrows F-1291 and closes F-1292 |
| EV-0248 | 2026-05-13 | The wave-121 audit control plane is synchronized after the moving archival-node/no-Horizon recheck. Mechanical recounts produce `68 fixed / 24 open` findings, `64 fixed / 20 open` XFI rows, and `66 fixed / 24 open` remediation rows. File coverage rolls up to `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0247; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-121 audit-control synchronization proof |
| EV-0249 | 2026-05-13 | Redis Sentinel's exporter install path lacks artifact authentication. `configs/ansible/roles/redis-sentinel/defaults/main.yml` pins `redis_exporter_version: "v1.66.0"`, and `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` scrapes `redis_exporter` on cache hosts. But `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml` downloads the GitHub release tarball with no `checksum:`, extracts it, and copies `/usr/local/bin/redis_exporter`. The role README documents exporter behavior but no digest requirement. | CMD-0248; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/redis-sentinel/README.md`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | Supports F-1293 |
| EV-0250 | 2026-05-13 | CI trusts unauthenticated external tool downloads. `.github/workflows/ci.yml` pins `PROM_VERSION=2.55.1` and `GITLEAKS_VERSION=8.21.2`, but `monitoring-rules` pipes the Prometheus release archive directly into `tar` before installing `promtool`, and `vuln` pipes the gitleaks archive directly into `tar -xz -C /usr/local/bin`. `gh api repos/RatesEngine/rates-engine/actions/permissions/workflow` reports `default_workflow_permissions=read`, so the runner token cannot write by default; the remaining risk is CI verdict integrity. | CMD-0248; `.github/workflows/ci.yml`; GitHub Actions workflow-permission API; `scripts/ci/lint-actions-pinning.sh` | Supports F-1294 |
| EV-0251 | 2026-05-13 | The wave-122 audit control plane is synchronized after adding Redis exporter and CI installer supply-chain findings. Mechanical recounts produce `68 fixed / 26 open` findings, `64 fixed / 22 open` XFI rows, and `66 fixed / 26 open` remediation rows. File coverage rolls up to `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0249; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-122 audit-control synchronization proof |
| EV-0252 | 2026-05-13 | The CI tool-download finding narrowed. `.github/workflows/ci.yml` now declares `PROM_TARBALL_SHA256` and `GITLEAKS_TARBALL_SHA256`; the promtool install step fails if `PROM_TARBALL_SHA256` is empty, downloads the Prometheus tarball to `/tmp/prom.tar.gz`, verifies it with `sha256sum -c`, then extracts. The gitleaks step still runs `curl ... | tar -xz -C /usr/local/bin gitleaks` and does not use `GITLEAKS_TARBALL_SHA256`. Repository workflow token permissions remain read-only. | CMD-0250; `.github/workflows/ci.yml`; GitHub Actions workflow-permission API | Narrows F-1294 |
| EV-0253 | 2026-05-13 | Redis exporter leaks the Redis password through its systemd unit. `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml` writes `/etc/systemd/system/redis_exporter.service` with `mode: "0644"` and inline `Environment=REDIS_PASSWORD={{ redis_password }}`. The same role uses that password for Redis/Sentinel auth and the optional `redis_exporter` ACL user, while Prometheus relies on the exporter scrape for Redis HA monitoring. | CMD-0250; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/prometheus/templates/prometheus.yml.j2` | Supports F-1295 |
| EV-0254 | 2026-05-13 | The wave-123 audit control plane is synchronized after narrowing the CI installer finding and adding the Redis exporter secret-boundary finding. Mechanical recounts produce `68 fixed / 27 open` findings, `64 fixed / 23 open` XFI rows, and `66 fixed / 27 open` remediation rows. File coverage rolls up to `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0251; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-123 audit-control synchronization proof |
| EV-0255 | 2026-05-13 | Additional moving fixes changed the two latest supply-chain findings. `.github/workflows/ci.yml` now verifies both external tool archives: Prometheus with `PROM_TARBALL_SHA256` and gitleaks with `GITLEAKS_TARBALL_SHA256`, each downloaded to `/tmp`, checked with `sha256sum -c`, and extracted only after digest verification. Redis Sentinel's exporter install now asserts `redis_exporter_release_sha256` and passes it to `get_url.checksum`, so the privileged binary install path is authenticated in code. The remaining Redis gap is documentation/inventory contract drift: `configs/ansible/roles/redis-sentinel/defaults/main.yml` and the role README still do not list `redis_exporter_release_sha256` or its checksum source. | CMD-0252; `.github/workflows/ci.yml`; `configs/ansible/roles/redis-sentinel/tasks/07-monitoring.yml`; `configs/ansible/roles/redis-sentinel/defaults/main.yml`; `configs/ansible/roles/redis-sentinel/README.md` | Closes F-1294 and narrows F-1293 |
| EV-0256 | 2026-05-13 | The wave-124 audit control plane is synchronized after closing `F-1294` and narrowing `F-1293`. Mechanical recounts produce `69 fixed / 26 open` findings, `65 fixed / 22 open` XFI rows, and `67 fixed / 26 open` remediation rows. File coverage remains `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0253; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-124 audit-control synchronization proof |
| EV-0257 | 2026-05-13 | Workflow permission review found an unnecessary OIDC token surface in release/deploy jobs. `.github/workflows/api-docs.yml` grants `pages: write` plus `id-token: write` and uses `actions/deploy-pages`, which is the expected GitHub Pages OIDC pattern. `.github/workflows/release.yml` grants `id-token: write` only for "future: keyless signing via cosign", and `.github/workflows/deploy.yml` grants it only for "future: keyless attestation"; repository search found no `ACTIONS_ID_TOKEN_*`, cosign/sigstore, cloud OIDC auth, or attestation step in those workflows. | CMD-0254; `.github/workflows/release.yml`; `.github/workflows/deploy.yml`; `.github/workflows/api-docs.yml`; workflow OIDC consumer search | Supports F-1296 |
| EV-0258 | 2026-05-13 | The wave-125 audit control plane is synchronized after adding `F-1296`. Mechanical recounts produce `69 fixed / 27 open` findings, `65 fixed / 23 open` XFI rows, and `67 fixed / 27 open` remediation rows. File coverage remains `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0255; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-125 audit-control synchronization proof |
| EV-0259 | 2026-05-13 | Deploy SSH host-key pinning fails open. `docs/operations/deploy-workflow.md` lists `<REGION>_SSH_KNOWN_HOSTS` as a required secret and says pinning prevents MITM. `.github/workflows/deploy.yml` decodes `R1_SSH_KNOWN_HOSTS` when set, but when it is empty the workflow emits `::warning::R1_SSH_KNOWN_HOSTS unset — falling back to ssh-keyscan (less safe)` and writes a live `ssh-keyscan -T 5 -t ed25519,rsa "$HOST"` result into `~/.ssh/known_hosts`. The Ansible deploy then runs with `ANSIBLE_HOST_KEY_CHECKING=True`, but the trusted key was collected during the same deploy rather than pinned out-of-band. | CMD-0256; `.github/workflows/deploy.yml`; `docs/operations/deploy-workflow.md`; `docs/operations/r1-deployment-state.md`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml` | Supports F-1297 |
| EV-0260 | 2026-05-13 | The wave-126 audit control plane is synchronized after adding `F-1297` and correcting summary-table status drift back to the detailed finding statuses. Mechanical recounts produce `69 fixed / 28 open` findings, `65 fixed / 24 open` XFI rows, and `67 fixed / 28 open` remediation rows. File coverage advances to `done=166`, `in_progress=127`, `todo=1633`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0257; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-126 audit-control synchronization proof |
| EV-0261 | 2026-05-13 | `F-1221` closure was too narrow. `.github/workflows/release.yml` currently builds six linux/amd64 binaries only and explicitly says arm64 was dropped on 2026-05-08; it also states container images are not built/pushed. Active docs still conflict: `CLAUDE.md` says release tags cross-compile every binary for `linux/{amd64,arm64}` and build/push `ghcr.io/RatesEngine/<binary>:<tag>` plus `:latest`; `docs/operations/release-process.md`'s top pipeline summary still says `linux/amd64 + linux/arm64`; and `docs/operations/r1-deployment-state.md` still describes the first pipeline smoke release as "all 12 binaries + SHA256SUMS." | CMD-0258; `.github/workflows/release.yml`; `CLAUDE.md`; `docs/operations/release-process.md`; `docs/operations/r1-deployment-state.md` | Reopens F-1221 and XFI-0014 |
| EV-0262 | 2026-05-13 | The wave-127 audit control plane is synchronized after reopening `F-1221`. Mechanical recounts produce `68 fixed / 29 open` findings, `64 fixed / 25 open` XFI rows, and `66 fixed / 29 open` remediation rows. File coverage advances to `done=166`, `in_progress=128`, `todo=1632`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0259; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-127 audit-control synchronization proof |
| EV-0263 | 2026-05-13 | Manual input shell-injection surface found in release/deploy workflows. `.github/workflows/deploy.yml` embeds free-form dispatch values directly in shell source before validation: `echo "${{ inputs.version }}"`, `echo '${{ inputs.binaries }}'`, repeated `gh release download "${{ inputs.version }}"`, `gh api ...${{ inputs.version }}...`, and `ansible-playbook -e "binaries_csv=${{ inputs.binaries }}" -e "health_grace_seconds=${{ inputs.health_grace_seconds }}"`. `.github/workflows/release.yml` sets `tag="${{ inputs.tag }}"` before validating SemVer. Static-site deploy workflows use choice inputs for branch selection and pass free-form API base URLs through `env:`, which is the safer pattern. | CMD-0260; `.github/workflows/deploy.yml`; `.github/workflows/release.yml`; `.github/workflows/explorer-deploy.yml`; `.github/workflows/status-page.yml`; `.github/workflows/docs-deploy.yml` | Supports F-1298 |
| EV-0264 | 2026-05-13 | The wave-128 audit control plane is synchronized after adding `F-1298`. Mechanical recounts produce `68 fixed / 30 open` findings, `64 fixed / 26 open` XFI rows, and `66 fixed / 30 open` remediation rows. File coverage remains `done=166`, `in_progress=128`, `todo=1632`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0261; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-128 audit-control synchronization proof |
| EV-0265 | 2026-05-13 | Production deploy tooling is resolved from mutable ranges during the deploy. `.github/workflows/deploy.yml` runs `python3 -m pip install 'ansible-core==2.18.*'` and `ansible-galaxy collection install -r configs/ansible/requirements.yml` immediately before invoking the deploy playbook. `configs/ansible/requirements.yml` uses lower-bound ranges (`ansible.posix >=1.5.0`, `community.general >=8.0.0`, `community.postgresql >=3.4.0`) and repeats `community.general`; the Ansible README similarly tells operators to install `ansible-core>=2.16`. | CMD-0262; `.github/workflows/deploy.yml`; `configs/ansible/requirements.yml`; `configs/ansible/README.md`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml` | Supports F-1299 |
| EV-0266 | 2026-05-13 | The wave-129 audit control plane is synchronized after adding `F-1299`. Mechanical recounts produce `68 fixed / 31 open` findings, `64 fixed / 27 open` XFI rows, and `66 fixed / 31 open` remediation rows. File coverage advances to `done=167`, `in_progress=128`, `todo=1631`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0263; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-129 audit-control synchronization proof |
| EV-0267 | 2026-05-13 | Healthchecks SLA evidence path is broken by the installed sandbox. `configs/healthchecks/sla-probe.sh` defaults `SLA_PROBE_TEXTFILE_OUTPUT` to `/var/lib/node_exporter/textfile_collector/sla_probe.prom` and passes it to `ratesengine-sla-probe -textfile-output`. `cmd/ratesengine-sla-probe/main.go` exits `2` on textfile write failure. `configs/healthchecks/ratesengine-sla-probe.service` uses `DynamicUser=true` and `ProtectSystem=strict` with no `ReadWritePaths`, unlike `deploy/systemd/sla-probe.service`, which explicitly grants `/var/lib/node_exporter/textfile_collector`. | CMD-0264; `configs/healthchecks/sla-probe.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `cmd/ratesengine-sla-probe/main.go`; `deploy/systemd/sla-probe.service`; `deploy/monitoring/rules/sla-probe.yml`; `docs/operations/sla-probe.md`; `docs/operations/runbooks/sla-probe-stale.md` | Supports F-1300 |
| EV-0268 | 2026-05-13 | The wave-130 audit control plane is synchronized after adding `F-1300`. Mechanical recounts produce `68 fixed / 32 open` findings, `64 fixed / 28 open` XFI rows, and `66 fixed / 32 open` remediation rows. File coverage advances to `done=170`, `in_progress=131`, `todo=1625`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0265; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-130 audit-control synchronization proof |
| EV-0269 | 2026-05-13 | The aggregator P1 runbook uses the wrong metrics endpoint for R1/default single-host deployments. `cmd/ratesengine-aggregator/main.go` documents and implements an automatic shift from `127.0.0.1:9464` to `127.0.0.1:9465` when the aggregator would collide with the indexer default listener. `configs/prometheus/prometheus.r1.yml` scrapes `ratesengine-indexer` at `localhost:9464` and `ratesengine-aggregator` at `localhost:9465`; `configs/healthchecks/heartbeat.sh` maps the same ports. `docs/operations/runbooks/aggregator-silent.md` still tells responders to curl `localhost:9464/metrics` for `ratesengine_aggregator_*` counters, and the Prometheus role README's scrape table still lists the aggregator as `9464` without the single-host caveat. | CMD-0266; `docs/operations/runbooks/aggregator-silent.md`; `cmd/ratesengine-aggregator/main.go`; `configs/prometheus/prometheus.r1.yml`; `configs/healthchecks/heartbeat.sh`; `configs/ansible/roles/prometheus/README.md` | Supports F-1301 |
| EV-0270 | 2026-05-13 | The wave-131 audit control plane is synchronized after adding `F-1301`. Mechanical recounts produce `68 fixed / 33 open` findings, `64 fixed / 29 open` XFI rows, and `66 fixed / 33 open` remediation rows. File coverage advances to `done=172`, `in_progress=131`, `todo=1623`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0267; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-131 audit-control synchronization proof |
| EV-0271 | 2026-05-13 | The Healthchecks smoke wrapper can silently disable the API-surface smoke. `configs/healthchecks/install.sh` copies `scripts/dev/r1-smoke.sh` to `/opt/ratesengine/healthchecks/r1-smoke.sh`, and `ratesengine-smoke.service` runs `/opt/ratesengine/healthchecks/smoke.sh` under `DynamicUser=true`. The wrapper's missing-script branch prints `smoke: ... not found or not executable` and exits `0` before it runs the smoke or calls `${HEALTHCHECKS_URL_SMOKE}/fail`. This contradicts `configs/healthchecks/README.md`, which says a failed smoke run pings `${URL}/fail`. | CMD-0268; `configs/healthchecks/smoke.sh`; `configs/healthchecks/ratesengine-smoke.service`; `configs/healthchecks/install.sh`; `configs/healthchecks/README.md`; `scripts/dev/r1-smoke.sh` | Supports F-1302 |
| EV-0272 | 2026-05-13 | The wave-132 audit control plane is synchronized after adding `F-1302`. Mechanical recounts produce `68 fixed / 34 open` findings, `64 fixed / 30 open` XFI rows, and `66 fixed / 34 open` remediation rows. File coverage advances to `done=175`, `in_progress=131`, `todo=1620`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0269; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-132 audit-control synchronization proof |
| EV-0273 | 2026-05-13 | The current workspace closes the Healthchecks SLA textfile sandbox finding. `configs/healthchecks/ratesengine-sla-probe.service` now keeps `DynamicUser=true` and `ProtectSystem=strict`, but adds `ReadWritePaths=/var/lib/node_exporter/textfile_collector` and `SupplementaryGroups=ratesengine`. `configs/ansible/roles/archival-node/tasks/10-observability.yml` provisions that directory as `ratesengine:ratesengine` mode `0775`, and the dedicated `deploy/systemd/sla-probe.service` already used the same writable path model. | CMD-0270; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/sla-probe.sh`; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `deploy/systemd/sla-probe.service` | Closes F-1300 |
| EV-0274 | 2026-05-13 | The SLA Healthchecks wrapper has the same fail-open missing-executable shape as the smoke wrapper. `configs/healthchecks/sla-probe.sh` defaults `PROBE_BIN` to `/usr/local/bin/ratesengine-sla-probe`; if that path is absent or non-executable, it prints `sla-probe: ... not found or not executable` and exits `0` before running the probe or calling `${HEALTHCHECKS_URL_SLA_PROBE}/fail`. The Healthchecks installer installs wrapper/unit files but does not install the binary, so this branch covers a real deploy boundary between release artifacts and monitoring timers. | CMD-0271; `configs/healthchecks/sla-probe.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/install.sh`; `cmd/ratesengine-sla-probe/main.go`; `docs/operations/sla-probe.md` | Supports F-1303 |
| EV-0275 | 2026-05-13 | The wave-133 audit control plane is synchronized after closing `F-1300`, adding `F-1303`, and correcting another summary-table status drift back to the detailed finding statuses. Mechanical recounts produce `70 fixed / 33 open` findings, `65 fixed / 30 open` XFI rows, and `67 fixed / 34 open` remediation rows. File coverage advances to `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0272; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-133 audit-control synchronization proof |
| EV-0276 | 2026-05-13 | One active Healthchecks setup path still omits the SLA timer restart. `docs/operations/pre-launch-hardening.md` lists five URLs including `HEALTHCHECKS_URL_SLA_PROBE`, but the apply block restarts only `ratesengine-heartbeat@*.timer` and `ratesengine-smoke.timer`. `configs/healthchecks/README.md` and `configs/healthchecks/install.sh` both include `ratesengine-sla-probe.timer` in the restart instruction, and `configs/healthchecks/ratesengine-sla-probe.service` reads the URL from `/etc/default/ratesengine-healthchecks`, so a restart is required to load the edited value. | CMD-0273; `docs/operations/pre-launch-hardening.md`; `configs/healthchecks/README.md`; `configs/healthchecks/install.sh`; `configs/healthchecks/ratesengine-sla-probe.service`; `configs/healthchecks/ratesengine-sla-probe.timer` | Supports F-1304 |
| EV-0277 | 2026-05-13 | The wave-134 audit control plane is synchronized after adding `F-1304`. Mechanical recounts produce `70 fixed / 34 open` findings, `65 fixed / 31 open` XFI rows, and `67 fixed / 35 open` remediation rows. File coverage remains `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0274; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-134 audit-control synchronization proof |
| EV-0278 | 2026-05-13 | A live R1 Healthchecks refresh shows the SLA proof path is still absent from production. `systemctl list-timers --all 'ratesengine-*'` lists only heartbeat indexer/api/aggregator and smoke timers. `ratesengine-sla-probe.timer` reports `not-found`/inactive, `/opt/ratesengine/healthchecks` contains `heartbeat.sh`, `smoke.sh`, and `r1-smoke.sh` but no `sla-probe.sh`, and the redacted `/etc/default/ratesengine-healthchecks` file contains indexer/aggregator/api/smoke URLs with no `HEALTHCHECKS_URL_SLA_PROBE` key. Recent journals show heartbeat and smoke successful oneshots only. | CMD-0275; R1-0031; R1 live systemd/env-file/journal snapshot | Reconfirms F-1205 and supports F-1304 |
| EV-0279 | 2026-05-13 | The wave-135 moving-workspace audit control plane is synchronized after closure updates landed for the status-page, signup-default, release-docs, HA/Ansible docs, workflow-security, deploy-tooling, and Healthchecks wrapper/apply-step findings, while the live R1 SLA timer rollout remains open under `F-1205`. Mechanical recounts produce `93 fixed / 11 open` findings, `87 fixed / 9 open` XFI rows, and `89 fixed / 13 open` remediation rows. File coverage remains `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0276; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-135 audit-control synchronization proof |
| EV-0280 | 2026-05-13 | Direct R1 verification shows the SLA timer rollout has landed but the service is currently failing its own SLA proof. `ratesengine-sla-probe.timer` is enabled and active with a recent run; `/opt/ratesengine/healthchecks/sla-probe.sh` and `/usr/local/bin/ratesengine-sla-probe` are executable; `HEALTHCHECKS_URL_SLA_PROBE` exists in the redacted env file; and `/var/lib/node_exporter/textfile_collector/sla_probe.prom` is present. The textfile reports `ratesengine_sla_probe_unit_failed 1` and `ratesengine_sla_probe_freshness_sec{endpoint="price"} 186.574`, above the documented 30s freshness target. | CMD-0277; R1-0032; R1 live systemd/env-file/textfile/journal snapshot | Closes F-1205 rollout evidence and supports F-1305 |
| EV-0281 | 2026-05-13 | The wave-136 audit control plane is synchronized after splitting the live SLA timer verification into fixed rollout finding `F-1205` and new open SLA-verdict finding `F-1305`. Mechanical recounts produce `94 fixed / 11 open` findings, `88 fixed / 9 open` XFI rows, and `90 fixed / 13 open` remediation rows. File coverage is `done=185`, `in_progress=131`, `todo=1610`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0278; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-136 audit-control synchronization proof |
| EV-0282 | 2026-05-13 | The documented price-stale alert path is currently dead. R1 loopback `/v1/price?asset=native&quote=fiat:USD` returned `flags.stale=true` with `observed_at=2026-05-13T12:00:00Z`, and `/v1/status` was degraded without a `ratesengine_api_price_stale` incident. API `/metrics` exposed request metrics but no `ratesengine_price_staleness_seconds` series; Prometheus returned empty vectors for both `ratesengine_price_staleness_seconds` and `ratesengine_price_staleness_seconds > 120` while `up{job="ratesengine-api"}=1`. Source confirms `internal/api/v1/price.go` intentionally skips `obs.PriceStalenessSeconds`, and repository search finds no real producer outside metric registration/test warmup. | CMD-0279; R1-0033; `internal/api/v1/price.go`; `internal/obs/metrics.go`; `deploy/monitoring/rules/api.yml`; `configs/prometheus/rules.r1/api.yml`; `docs/operations/runbooks/price-stale.md` | Supports F-1306 |
| EV-0283 | 2026-05-13 | The wave-137 audit control plane is synchronized after adding the dead `ratesengine_api_price_stale` alert finding. Mechanical recounts produce `94 fixed / 12 open` findings, `88 fixed / 10 open` XFI rows, and `90 fixed / 14 open` remediation rows. File coverage is `done=186`, `in_progress=132`, `todo=1608`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0280; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-137 audit-control synchronization proof |
| EV-0284 | 2026-05-13 | Hosted GitHub controls remain materially weaker than the repository policy. The branch API reports `main.protected=false`, and direct branch-protection reads fail with the current private-plan feature message. Repository vulnerability alerts return disabled, Dependabot alerts return disabled/forbidden, and Actions permissions report `allowed_actions=all` while selected-actions reads conflict because all actions/workflows are allowed. Workflow token defaults are read-only, which is good, but all five production environments (`r1`, docs, explorer, status, GitHub Pages) still have empty `protection_rules` and `can_admins_bypass=true`. | CMD-0281; GitHub API branch/protection/security/actions/environment reads | Reconfirms F-1207, F-1214, F-1215, F-1216 |
| EV-0285 | 2026-05-13 | The wave-138 audit control plane is synchronized after refreshing hosted GitHub posture evidence. Mechanical recounts remain `94 fixed / 12 open` findings, `88 fixed / 10 open` XFI rows, and `90 fixed / 14 open` remediation rows. File coverage remains `done=186`, `in_progress=132`, `todo=1608`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0282; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-138 audit-control synchronization proof |
| EV-0286 | 2026-05-13 | The R1 SLA probe textfile is not entering Prometheus. Prometheus queries for `ratesengine_sla_probe_unit_failed` and `ratesengine_sla_probe_freshness_sec` return empty vectors; the live node_exporter process is `/usr/local/bin/node_exporter --web.listen-address=0.0.0.0:9100 --collector.systemd --collector.processes` with no textfile collector directory flag; direct node_exporter scrape shows `node_textfile_scrape_error 0` but no `ratesengine_sla_probe_*` samples. Source/ops docs expect `--collector.textfile.directory=/var/lib/node_exporter/textfile_collector`, so live R1 drift makes all SLA-probe Prometheus alerts inert. | CMD-0283; R1-0034; `configs/ansible/roles/archival-node/tasks/10-observability.yml`; `configs/prometheus/rules.r1/sla-probe.yml`; `deploy/monitoring/rules/sla-probe.yml`; `docs/operations/sla-probe.md`; `docs/operations/runbooks/sla-probe-stale.md` | Supports F-1307 |
| EV-0287 | 2026-05-13 | Follow-up verification splits the two moving monitoring fixes. `F-1307` is live-closed: node_exporter now runs with `--collector.textfile --collector.textfile.directory=/var/lib/node_exporter/textfile_collector`, direct node_exporter scrape exposes `ratesengine_sla_probe_unit_failed=1` and `ratesengine_sla_probe_freshness_sec{endpoint="price"}=206.709`, and Prometheus returns those samples. `F-1306` remains open for live R1: source now includes an aggregator-side bounded `PriceStalenessSeconds` producer, but Prometheus and the live aggregator metrics endpoint still have no `ratesengine_price_staleness_seconds` series. | CMD-0284; R1-0035; `internal/aggregate/orchestrator/orchestrator.go`; R1 node_exporter/Prometheus/aggregator metrics | Closes F-1307 and narrows F-1306 |
| EV-0288 | 2026-05-13 | The wave-139 audit control plane is synchronized after closing the SLA textfile scrape-chain finding and narrowing the price-staleness alert finding to a live deployment/proof gap. Mechanical recounts produce `95 fixed / 12 open` findings, `89 fixed / 10 open` XFI rows, and `91 fixed / 14 open` remediation rows. File coverage is `done=186`, `in_progress=133`, `todo=1607`; tracked-file parity remains exact at `1926 / 1926`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0285; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-139 audit-control synchronization proof |
| EV-0289 | 2026-05-13 | Refreshed R1 SSE probes close the former live streaming cutoff. A loopback 68-second `curl -skN --max-time 68` against `/v1/price/tip/stream?asset=native&quote=fiat:USD` ended only at the client timeout (`EXIT:28`, `HTTP:200`) while receiving `tip_update` frames and keepalives. The public `https://api.ratesengine.net` probe behaved the same. Neither path reproduced the earlier `EXIT:92` server reset around `30.443s`. | CMD-0286; R1-0036; R1 loopback/public SSE probes | Closes F-1228 |
| EV-0290 | 2026-05-13 | Live R1 now has a real producer for the price-staleness alert. Aggregator `:9465/metrics` exposes `ratesengine_price_staleness_seconds{asset="crypto:BTC"}`, `crypto:ETH`, `crypto:XLM`, and `native`, all currently `0`; Prometheus returns the same four series under `job="ratesengine-aggregator"`. The `ratesengine_price_staleness_seconds > 120` query is empty because the values are not above threshold, not because the metric is missing. | CMD-0287; R1-0037; R1 aggregator/Prometheus metrics | Closes F-1306 |
| EV-0291 | 2026-05-13 | The wave-140 audit control plane is synchronized after live closure of the SSE timeout and price-staleness metric findings. Mechanical recounts produce `98 fixed / 9 open` findings, `91 fixed / 8 open` XFI rows, and `93 fixed / 12 open` remediation rows. File coverage remains `done=186`, `in_progress=133`, `todo=1607`; the findings-register table/detail status mismatch detector returns no rows; and `./scripts/ci/lint-docs.sh` passes. | CMD-0288; `docs/audit-2026-05-12-codex/05-findings-register.md`; `docs/audit-2026-05-12-codex/evidence/cross-file-interactions.md`; `docs/audit-2026-05-12-codex/07-remediation-plan.md`; `docs/audit-2026-05-12-codex/inventory/file-coverage.tsv`; `./scripts/ci/lint-docs.sh` | Wave-140 audit-control synchronization proof |
