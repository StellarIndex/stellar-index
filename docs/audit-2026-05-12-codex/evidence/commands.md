# Command Evidence

Summarize commands here. For long outputs, record the important lines
and attach generated artifacts where possible.

| ID | Date | Command | Result | Evidence Produced | Notes |
| --- | --- | --- | --- | --- | --- |
| CMD-0001 | 2026-05-11 | `git rev-parse HEAD` | `80c57e38eeee729ec2d879d54286419206cee864` | EV-0001 | Planning snapshot |
| CMD-0002 | 2026-05-11 | `git status --short` | no output before audit files were created | EV-0001 | Clean initial worktree |
| CMD-0003 | 2026-05-11 | `git ls-files \| wc -l` | `1,747` | EV-0002 | Tracked file count before audit directory |
| CMD-0004 | 2026-05-11 | `tracked=$(git ls-files ':!:docs/audit-2026-05-12-codex/**' \| wc -l \| tr -d ' '); rows=$(tail -n +2 docs/audit-2026-05-12-codex/inventory/file-coverage.tsv \| wc -l \| tr -d ' '); printf 'tracked=%s\nrows=%s\n' "$tracked" "$rows"` | `tracked=1747`; `rows=1747` | EV-0003 | Second-pass inventory reconciliation |
| CMD-0005 | 2026-05-11 | `awk -F '\t' 'NR>1 {count[$5]++} END {for (s in count) print s, count[s]}' docs/audit-2026-05-12-codex/inventory/file-coverage.tsv \| sort` | `todo 1747` | EV-0004 | Third-pass false-completion check |
| CMD-0006 | 2026-05-11 | `find docs/audit-2026-05-12-codex -type f \| sort` | 19 files under the Codex audit directory | EV-0004 | Deliverable presence check |
| CMD-0007 | 2026-05-12 | `go list ./...` | package list succeeded; sandbox emitted module stat-cache permission warning | EV-0005 | Topology/package inventory |
| CMD-0008 | 2026-05-12 | `GOCACHE=/private/tmp/ratesengine-gocache go test ./...` | sandbox run failed because httptest/miniredis could not bind localhost ports; escalated run passed | EV-0006 | Test reality check |
| CMD-0009 | 2026-05-12 | `./scripts/ci/lint-docs.sh` | passed | EV-0007 | Docs lint does not catch all generated drift |
| CMD-0010 | 2026-05-12 | `go run ./scripts/ci/lint-openapi-urls openapi/rates-engine.v1.yaml` | passed | EV-0008 | ADR-0018 URL discipline |
| CMD-0011 | 2026-05-12 | `go run ./scripts/ci/verify-launch-ready` | failed; L4.14-L4.17, L5.6, L5.8, and L6 finalisation blockers remain | EV-0009 | Launch-readiness gate |
| CMD-0012 | 2026-05-12 | `./scripts/dev/docs-api.sh` | regenerated `docs/reference/api/rates-engine.v1.yaml` and produced a git diff | EV-0010 | Generated API reference drift |
| CMD-0013 | 2026-05-12 | `cd web/explorer && pnpm generate:api` | regenerated `web/explorer/src/api/types.ts` and produced a git diff | EV-0011 | Generated TS API type drift |
| CMD-0014 | 2026-05-12 | `rg -n "/v1/(coins\|currencies)\|/coins\|/currencies" ...` | found active frontend/status/examples references to removed API paths | EV-0012 | Endpoint removal cross-file check |
| CMD-0015 | 2026-05-12 | `make verify` | passed all local canonical gates; explorer lint warning remains non-fatal | EV-0013 | Full local quality gate |
| CMD-0016 | 2026-05-12 | `pnpm audit --audit-level moderate` in `web/explorer`, `web/dashboard`, `web/status` | each app failed with 27 vulnerabilities: 4 low, 13 moderate, 8 high, 2 critical | EV-0014 | Web dependency vulnerability audit |
| CMD-0017 | 2026-05-12 | `gitleaks detect --redact --no-banner` | passed; 2,067 commits, about 24.97 MB scanned, no leaks found | EV-0015 | Secret scan |
| CMD-0018 | 2026-05-12 | `rg -n "panic\(\|t\.Skip\|Skip\(\|TODO\|FIXME\|nolint\|int64\|Horizon\|horizon" cmd internal configs deploy scripts web .github openapi migrations` | identified panic/skip/TODO/int64/Horizon review queue | EV-0016 | Static hostile-pattern sweep |
| CMD-0019 | 2026-05-12 | `rg -n "CREATE TABLE\|CREATE INDEX\|CREATE MATERIALIZED\|ALTER TABLE\|DROP TABLE\|DROP INDEX" migrations` | mapped 28 migration families and destructive/down-migration surfaces | EV-0017 | Migration inventory |
| CMD-0020 | 2026-05-12 | `git ls-files ...` topology commands | top-level tracked counts: 651 `internal`, 421 `docs`, 199 `web`, 163 `configs`, 57 `migrations`, etc. | EV-0018 | Repo topology |
| CMD-0021 | 2026-05-12 | `nc -vz -w2 136.243.90.96 ...` | public TCP connections succeeded to R1 ports 9000, 9001, 9090, 3100, 9080, 9100, 6061, 11726 | R1-0008; EV-0019 | External exposure proof |
| CMD-0022 | 2026-05-12 | `bash scripts/dev/audit-public-api.sh` against `https://api.ratesengine.net` | passed 37/37 checks, including `/v1/coins` and `/v1/currencies` checks | EV-0020 | Live API audit confirms deployed legacy routes |
| CMD-0023 | 2026-05-12 | `API_BASE_URL=https://api.ratesengine.net bash scripts/dev/r1-smoke.sh` | passed all checks, including `/v1/coins`, `/v1/currencies`, and coins negative-behavior pins | EV-0020 | Smoke script still encodes removed route expectations |
| CMD-0024 | 2026-05-12 | `find deploy/status-page -maxdepth 3 -type d`; status-page docs/runbook/comms reads | only `deploy/status-page` exists; runbooks/comms still reference removed `deploy/status-page/cstate/**` and Upptime GitHub issue flow while deployed status page is `web/status` on Cloudflare Pages | EV-0021; XFI-0007 | Status-page operational drift |
| CMD-0025 | 2026-05-12 | `git ls-files configs/ansible/inventory/r1.secrets.yml configs/ansible/inventory/*.secrets.yml`; `git check-ignore -v configs/ansible/inventory/r1.secrets.yml` | vault file is present locally but not tracked; ignored by `.gitignore:64` `/configs/ansible/inventory/r*.yml` | EV-0022 | No tracked-vault-secret finding from this pass |
| CMD-0026 | 2026-05-12 | dashboard key-management/rate-limit code reads | dashboard create-key UI submits `rate_limit_per_min`, backend accepts up to `100000` for owner/admin/member sessions, Postgres validator returns that value as `Subject.RateLimitPerMin`, and rate-limit middleware uses it as the per-key override | EV-0023; XFI-0008 | Supports F-1212 |
| CMD-0027 | 2026-05-12 | amount/VWAP/stablecoin proxy source reads across `internal/scval`, `internal/canonical`, `internal/aggregate`, `internal/storage/timescale`, and Stellar DEX decoders | i128/u128/u256 decode preserves `big.Int`, but fiat-USD stablecoin proxy reuses classic USD pegs while `windowUSDVolume` still divides all quote amounts by `1e8`; storage USD-volume code separately knows classic/SAC USD pegs are `1e7` | EV-0024; XFI-0009 | Supports F-1213 |
| CMD-0028 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'grep -nE "enable_stablecoin_fiat_proxy|min_usd_volume|usd_pegged_classic_assets" /etc/ratesengine.toml || true'` and surrounding non-secret config read | R1 has `enable_stablecoin_fiat_proxy = true`, `min_usd_volume = 0`, and Circle USDC in `usd_pegged_classic_assets`; the threshold is disabled on R1 despite the source default being 10000 | R1-0011; EV-0024 | Runtime context for F-1213 |
| CMD-0029 | 2026-05-12 | GitHub hosted control reads: `gh repo view`, `gh api repos/RatesEngine/rates-engine/branches/main`, `gh api repos/RatesEngine/rates-engine/rulesets`, `gh api repos/RatesEngine/rates-engine/environments`, `gh api repos/RatesEngine/rates-engine/actions/permissions`, `gh api repos/RatesEngine/rates-engine/actions/permissions/workflow` | repo is private; `main.protected=false`; repository rulesets and branch protection are unavailable on the current private repo tier; five environments have empty `protection_rules`; Actions allows all actions and does not require SHA pinning; workflow token default is read-only | EV-0025; XFI-0010 | Supports F-1214/F-1215/F-1216 |
| CMD-0030 | 2026-05-12 | workflow and governance source reads for CODEOWNERS, `CONTRIBUTING.md`, deploy workflow, release workflow, and `uses:` actions | local docs claim green CI, CODEOWNER review, and signed commits are enforced by branch protection; deploy workflow comments make r1 reviewers optional; workflows use tag-pinned third-party actions while hosted settings allow all actions and no SHA pinning | EV-0025; EV-0026; XFI-0010 | Supports F-1214/F-1215/F-1216 |
| CMD-0031 | 2026-05-12 | auth/cache/signup source reads across `internal/api/v1/{auth_sep10,signup}.go`, `internal/auth/sep10/validator.go`, `internal/auth/signup_tracker.go`, cache-control middleware/tests, OpenAPI, and signup UI/docs | cache-control table defaults unknown auth/signup/dashboard/webhook routes to `private, no-store`; self-service signup mints 1000/min keys on email string only, duplicate tracker is optional, and tracker lookup/create/mark is non-atomic; SEP-10 replay wording from this row is superseded by CMD-0057 | EV-0027; EV-0028; EV-0053; XFI-0011; XFI-0012 | Supports F-1217/F-1218; negative cache-control evidence |
| CMD-0032 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'curl ... /v1/auth/sep10/challenge?...'` | R1 returns 503 `SEP-10 not configured` for challenge route | R1-0012 | Runtime context only; source-level replay finding remains latent until SEP-10 is enabled |
| CMD-0033 | 2026-05-12 | Stripe/billing source reads across `internal/api/v1/stripe_webhook.go`, tests, `cmd/ratesengine-api/main.go`, `internal/platform/billing.go`, and migration 0027 | webhook verifies HMAC/timestamp and still updates Redis key state rather than platform subscriptions/dashboard keys; durable dedupe/audit wording from this row is superseded by CMD-0057 | EV-0030; EV-0053; XFI-0013 | Supports F-1219 |
| CMD-0034 | 2026-05-12 | release/deploy/migration source reads across `.github/workflows/{release,deploy}.yml`, Ansible deploy tasks, archival-node role, `docs/operations/{deploy-workflow,release-process}.md`, and `docs/operations/runbooks/fx-history-missing.md` | deploy workflow downloads and swaps binaries only; migration sync/apply exists in the initial archival-node role and manual runbooks, not the tagged-release deploy path; release docs still claim GHCR container pushes and `/opt/ratesengine/release-<tag>` rollback directories that the current workflow does not produce | EV-0031; EV-0032; XFI-0014 | Supports F-1220/F-1221/F-1222 |
| CMD-0035 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'sudo -u postgres psql ... schema_migrations; ls ... deployed-versions ...'` | R1 database is at schema version 28 dirty=false; binary sidecars show API/indexer/aggregator at `v0.5.0-rc.47`, ops at `v0.5.0-rc.29`, and sla-probe at `v0.5.0-rc.17`; `/usr/local/share/ratesengine/migrations` produced no listing in the probe | R1-0013; EV-0031 | Runtime context for release/schema coupling |
| CMD-0036 | 2026-05-12 | R1 `/etc/caddy/Caddyfile` and `/etc/ratesengine.toml` read; repo `configs/caddy/Caddyfile.api`, `configs/caddy/README.md`, and middleware remote-IP reads | R1 API config trusts `127.0.0.1/32`, but live Caddy overwrites `X-Forwarded-For` with `{remote_host}` and lacks the repo's `trusted_proxies`/`client_ip_headers` block and `/metrics` 404 guard | EV-0033; R1-0014; XFI-0015 | Supports F-1223 |
| CMD-0037 | 2026-05-12 | `curl -skI https://api.ratesengine.net/metrics`; `curl -sk https://api.ratesengine.net/metrics`; CORS header probes | public `/metrics` returned HTTP 200 with Go/runtime/API Prometheus metrics including route counters; disallowed Origin got no `Access-Control-Allow-Origin`; allowed dashboard Origin got explicit allow header | EV-0033; R1-0014 | Supports F-1223; negative CORS evidence |
| CMD-0038 | 2026-05-12 | dashboard auth IP source reads across `internal/api/v1/dashboardauth/{handlers,middleware}.go`, platform user/token stores, migration 0027, Caddy/API proxy config | dashboard auth login/callback/session touch paths call a local `clientIP(r)` that reads `r.RemoteAddr`, not the resolved middleware remote IP; behind Caddy this records the loopback peer rather than the real client IP | EV-0034; XFI-0016 | Supports F-1224 |
| CMD-0039 | 2026-05-12 | history/chart/stablecoin fallback source reads across `internal/api/v1/{history,chart,vwap,ohlc,price}.go`, Timescale aggregate readers, OpenAPI, and R1 curl probes for XLM/USD history/chart/direct USDC history | `/v1/history/since-inception` reads the literal CAGG pair only and returned empty for `native/fiat:USD`; `/v1/chart` has a stablecoin fallback and returned XLM/USD data from USDC with `flags.triangulated=true`; direct `native/USDC-GA5...` since-inception history returned populated points | EV-0035; R1-0015; XFI-0017 | Supports F-1225 |
| CMD-0040 | 2026-05-12 | platform API-key policy source reads across `internal/platform/apikey.go`, `internal/platform/postgresstore/apikey_store.go`, `internal/api/v1/dashboardkeys/handlers.go`, `internal/auth/apikey_postgres.go`, `internal/api/v1/middleware/auth.go`, OpenAPI, dashboard UI/types, and migration 0027 | platform keys store/display `monthly_quota`, `permissions`, `ip_allowlist`, `referer_allowlist`, and last-used fields, but runtime auth returns a Subject with only identity/tier/rate-limit and has no request-aware enforcement for allowlists, referer, permissions, monthly quotas, or `TouchUsage` | EV-0036; XFI-0018 | Supports F-1226 |
| CMD-0041 | 2026-05-12 | Docker/self-hosting migration source reads across `docker/ratesengine-migrate.Dockerfile`, `cmd/ratesengine-migrate/main.go`, `Makefile`, `docker/README.md`, `deploy/docker-compose/README.md`, and Ansible migration tasks | the migration binary defaults `-migrations migrations`; the migrate image copies only `/usr/local/bin/ratesengine-migrate` and no SQL files; `smoke-docker` only executes `--help`; bare-metal Ansible succeeds by separately syncing `/usr/local/share/ratesengine/migrations` | EV-0037; XFI-0019 | Supports F-1227 |
| CMD-0042 | 2026-05-12 | SSE timeout audit across `cmd/ratesengine-api/main.go`, stream handlers/tests/docs, `go doc net/http.Server.WriteTimeout`, and R1 loopback curl of `/v1/price/tip/stream` | API server sets `WriteTimeout: 30s`; stream handlers/tests/docs expect long-lived SSE with 15s heartbeats and 60s+ demo runs; R1 stream emitted through 25s and ended at elapsed 30s | EV-0038; R1-0016; XFI-0020 | Supports F-1228 |
| CMD-0043 | 2026-05-12 | CDN verification script audit across `scripts/dev/verify-cdn.sh`, price/tip handlers, OpenAPI, and R1 loopback probes | `verify-cdn.sh` uses `base=native` for `/v1/price` and `/v1/price/tip/stream` while those surfaces require `asset`; the script expects SSE `Cache-Control: no-store` while the stream writer sets `no-cache`; R1 returns 400 missing-asset for the script's SSE URL | EV-0039; XFI-0021 | Supports F-1229 |
| CMD-0044 | 2026-05-12 | since-inception/backfill audit across RFP docs, OpenAPI, history handler, backfill command, and R1 direct XLM/USDC history | R1 `/history/since-inception?asset=native&quote=USDC-GA5...&granularity=1d` returned 9 buckets from 2026-05-03 through 2026-05-11; Freighter asks for minimum 1 year and ideally since inception; project matrix targets ledger 2 -> today | EV-0040; R1-0017; XFI-0022 | Supports F-1230 |
| CMD-0045 | 2026-05-12 | hosted CI trigger audit across `.github/workflows/ci.yml`, `.github/workflows/api-audit.yml`, GitHub recent runs, and branch-protection API evidence | canonical `ci` triggers only on pull_request and explicitly disables push-to-main; `api-audit` runs on selected main path pushes only; hosted `main` is unprotected, so direct pushes can bypass the full lint/test/build/security suite | EV-0041; XFI-0023 | Supports F-1231 |
| CMD-0046 | 2026-05-12 | stablecoin USD price parity audit across `/v1/price`, `/v1/price/batch`, `/v1/assets/{id}`, `price.go`, `assets_f2.go`, and R1 live probes | R1 asset detail for Circle USDC returned `price_usd=0.9999838427`, but `/v1/price?asset=USDC-GA5...&quote=fiat:USD` returned 404 and GET/POST batch returned empty arrays; `tryStablecoinFiatProxy` skips when the requested asset equals the peg | EV-0042; R1-0018; XFI-0024 | Supports F-1232 |
| CMD-0047 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after audit ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0043 | Audit artifact syntax/sync sanity check |
| CMD-0048 | 2026-05-12 | SDEX historical decoder audit across `internal/sources/sdex`, dispatcher op routing, backfill path, and protocol/discovery docs | `decodeClaimAtom` rejects `ClaimAtomTypeV0`, but `Decoder.Decode` suppresses per-claim errors and continues, so the dispatcher never increments `DecodeErrors`; this silently drops pre-P17/pre-P18 SDEX historical fills despite docs/backfill claims that SDEX trades go back to genesis | EV-0044; XFI-0025 | Supports F-1233 |
| CMD-0049 | 2026-05-12 | Oracle partial-coverage audit across Reflector, Redstone, Band decoders, dispatcher error metrics, decoder-stats docs, and oracle coverage runbooks | mixed oracle batches skip unknown symbols/feed IDs while returning success; dispatcher decode-error metrics only increment on returned errors, so partial coverage drift is invisible unless the entire batch has no known entries | EV-0045; XFI-0026 | Supports F-1234 |
| CMD-0050 | 2026-05-12 | External CEX streamer parse-error audit across Binance, Kraken, Bitstamp, Coinbase stream loops, external runner metrics, and external connector docs | all four streamers skip malformed/unknown frames without incrementing `SourceDecodeErrorsTotal` or another parse-error counter; docs claim external connectors contribute to the same decode-error budget | EV-0046; XFI-0027 | Supports F-1235 |
| CMD-0051 | 2026-05-12 | Supply refresher freshness audit across aggregator/ops ledger resolution, storage supply readers, LCM readers, and Timescale at-or-before queries | supply snapshots use max ingestion cursor ledger as the snapshot ledger, while all supply component readers select latest rows at or before that ledger without enforcing component freshness or observer cursor alignment | EV-0047; XFI-0028 | Supports F-1236 |
| CMD-0052 | 2026-05-12 | CoinMarketCap identity audit across verified-currency catalogue, CMC poller, indexer aggregator-pair wiring, and CMC tests | catalogue stores CMC numeric IDs, but indexer passes only ticker-based pairs and CMC poller queries `symbol=` then takes the first returned coin for duplicate tickers | EV-0048; XFI-0029 | Supports F-1237 |
| CMD-0053 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after findings F-1233 through F-1237 | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0049 | Audit artifact syntax/sync sanity check |
| CMD-0054 | 2026-05-12 | Governance/workflow root audit across `.github/**`, `CODEOWNERS`, `CONTRIBUTING.md`, `SECURITY.md`, `.gitignore`, `.gitleaks.toml`, `.golangci.yml`, `.spectral.yaml`, and `commitlint.config.js` | reviewed workflow triggers, deploy/release permissions, generated-doc publish paths, PR/release templates, secret ignore rules, lint/security configs, and contributor/security policy claims | EV-0050; XFI-0010 | Closed reviewed governance/workflow inventory rows; no new finding beyond existing governance/release/dependency findings |
| CMD-0055 | 2026-05-12 | GitHub hosted dependency-security probes: `gh api repos/RatesEngine/rates-engine/vulnerability-alerts` and `gh api repos/RatesEngine/rates-engine/dependabot/alerts --paginate` | vulnerability alerts API returned disabled; Dependabot alerts API returned disabled/forbidden with required scope note | EV-0051; XFI-0005 | Extends F-1207 with hosted alert-control evidence |
| CMD-0056 | 2026-05-12 | `go test ./cmd/...` | passed for aggregator, API, indexer, migrate, ops, and SLA probe command packages | EV-0052 | Command-surface regression baseline |
| CMD-0057 | 2026-05-12 | Current SEP-10 and Stripe command/API wiring audit across `cmd/ratesengine-api/main.go`, `internal/auth/sep10/**`, `internal/api/v1/stripe_webhook.go`, and platform billing stores | SEP-10 now supports Redis replay guarding but still permits guardless operation when Redis is absent; Stripe webhook now has Postgres event dedupe/audit seams but still mutates only Redis key records and does not mirror subscriptions/platform keys | EV-0053; XFI-0011; XFI-0013 | Corrects stale F-1217/F-1219 wording to current source reality |
| CMD-0058 | 2026-05-12 | API Redis optionality/streaming subscriber audit across `cmd/ratesengine-api/main.go`, `internal/api/streaming/redispub`, config docs, and stream docs | API constructs `streaming.NewHub(0)` unconditionally, then calls `redispub.NewSubscriber(rdb, ...)` whenever `hub != nil`; `NewSubscriber` rejects nil Redis, despite API comments/docs saying Redis is optional and empty streaming pairs merely yield heartbeats | EV-0054; XFI-0030 | Supports F-1238 |
| CMD-0059 | 2026-05-12 | Full command-surface source/test audit across `cmd/ratesengine-{api,indexer,aggregator,migrate,ops,sla-probe}` plus hostile-pattern scan for progress counters, exits, panics, `int64`, and skipped tests | command packages pass tests; API/indexer/aggregator/migrate/SLA findings are covered by existing rows; ops WASM history/extraction tools expose a new modulo-by-zero crash when `-progress-every 0` is supplied | EV-0055; XFI-0031 | Supports F-1239 and closes `cmd/**` inventory rows |
| CMD-0060 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after command-surface ledger and inventory updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0056 | Audit artifact syntax/sync sanity check |
| CMD-0061 | 2026-05-12 | Docker packaging audit across all six `docker/ratesengine-*.Dockerfile` files, `docker/README.md`, `go.mod`, CI/release Go setup, `Makefile` docker targets, and Docker Compose docs | all Dockerfiles build with `golang:1.26-alpine`, while `go.mod` pins `go 1.25.10`, CI/release use `go-version-file: go.mod`, and Docker README still claims `golang:1.25-alpine` plus binary equivalence to release builds; migrate image missing migrations remains covered by F-1227 | EV-0057; XFI-0032 | Supports F-1240 and closes `docker/**` inventory rows |
| CMD-0062 | 2026-05-12 | Migration/schema audit across `migrations/*.sql`, `migrations/README.md`, and store/runtime references for newly added 0021-0029 schema families | the tree has 29 dense paired migration families through `0029_drop_unused_blend_jsonb_gin_indexes`, but `migrations/README.md` still stops its "Current migrations" table at `0015` despite instructing maintainers to update it when a migration lands | EV-0058; XFI-0033 | Supports F-1241 |
| CMD-0063 | 2026-05-12 | `go test -tags=integration ./test/integration -run TestMigrationsRoundTrip -count=1` | passed; the TimescaleDB integration test applied every current migration and rolled them all back cleanly | EV-0059 | Positive execution evidence for W12 migration SQL integrity |
| CMD-0064 | 2026-05-12 | Per-source contribution storage audit across `cmd/ratesengine-aggregator/contribution_sink.go`, `internal/aggregate/{vwap,orchestrator}/`, `internal/storage/timescale/price_source_contributions.go`, and migration 0026 | aggregator production wiring always writes contribution rows, the schema/commentary reserves `volume_usd` for the per-source USD tooltip, the storage API accepts it, but the sink never computes or forwards any `VolumeUSD` value, so every persisted row receives SQL `NULL` | EV-0060; XFI-0034 | Supports F-1242 |
| CMD-0065 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after W12 migration/contribution ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0061 | Audit artifact syntax/sync sanity check |
| CMD-0066 | 2026-05-12 | Classic asset registry writer audit across `internal/storage/timescale/{trades,asset_registry}.go`, registry read APIs, and integration tests | `InsertTrade` calls `registerClassicAssetSeen` after every stored trade, but `assetRegistryDedupe` turns every same-process repeat asset into an early return before the SQL `LEAST`/`GREATEST`/`observation_count + 1` upsert runs; existing integration coverage seeds `classic_assets` directly and does not exercise the writer contract | EV-0062; XFI-0035 | Supports F-1243 |
| CMD-0067 | 2026-05-12 | Audit-scope drift check: `git rev-parse HEAD`; `git ls-files ':!:docs/audit-2026-05-12-codex/**' | wc -l`; `tail -n +2 docs/audit-2026-05-12-codex/inventory/file-coverage.tsv | wc -l`; migration 0029 history/inventory comparison | current `HEAD` is `6e873cac...`, newer than the original anchor `80c57e...`; current tracked tree excluding this Codex audit directory has 1,869 rows while the generated inventory still has 1,747; tracked `migrations/0029_*` files exist in Git but are absent from the Codex inventory | EV-0063 | Audit-control caveat for literal whole-repo closure |
| CMD-0068 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after classic-registry finding F-1243 and audit-scope caveat updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0064 | Audit artifact syntax/sync sanity check |
| CMD-0069 | 2026-05-12 | Current-source legacy-route reconciliation across explorer/status/examples/scripts plus live `bash scripts/dev/audit-public-api.sh` against `https://api.ratesengine.net` | live explorer/status callers and `r1-smoke.sh` have been migrated off `/v1/coins` and `/v1/currencies`; the public audit script still probes those removed paths and now fails 5/37 checks because R1 returns 404 for all five legacy routes | EV-0065; EV-0066; XFI-0002 | Supersedes stale portions of F-1202/F-1204 evidence |
| CMD-0070 | 2026-05-12 | `./scripts/dev/docs-api.sh` on current `HEAD` | regenerated API reference without leaving a diff in `docs/reference/api/rates-engine.v1.yaml` | EV-0067 | Current generated reference is clean |
| CMD-0071 | 2026-05-12 | `cd web/explorer && pnpm generate:api`; follow-up `git diff --stat -- docs/reference/api/rates-engine.v1.yaml web/explorer/src/api/types.ts` | explorer type generation leaves a 362-line diff in `web/explorer/src/api/types.ts`, including new dashboard webhook paths and updated legacy-route wording; docs reference remains clean | EV-0067 | Keeps F-1203 open on current `HEAD` |
| CMD-0072 | 2026-05-12 | Dashboard customer-webhook security audit across `internal/api/v1/dashboardwebhooks`, `internal/platform/{webhook,postgresstore/webhook_store}.go`, `internal/customerwebhook/worker.go`, migration 0027, OpenAPI docs, and tests | create flow stores the generated signing secret bytes directly in `secret_hash` while comments/API prose promise hash/never-persist plaintext semantics; URL validation accepts any `https://` string and the worker posts with a default redirect-following client, with no private-IP/DNS-rebinding/redirect protections in tests or code | EV-0068; EV-0069; XFI-0036; XFI-0037 | Supports F-1244/F-1245 |
| CMD-0073 | 2026-05-12 | `go test ./internal/api/v1/dashboardwebhooks ./internal/customerwebhook ./internal/platform/postgresstore` | dashboard webhook handlers and customer webhook worker tests passed; Postgres webhook store package has no direct unit tests | EV-0070 | Positive regression baseline for current webhook slice |
| CMD-0074 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after F-1244/F-1245 and latest reconciliation updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0071 | Audit artifact syntax/sync sanity check |
| CMD-0075 | 2026-05-12 | Webhook docs-truth reconciliation across `docs/reference/api-design.md`, generated API reference, customer webhook runbook, and shipped webhook handlers/worker | the design reference still states "Webhook callbacks? Not in v1. Customers who want push use SSE," while current OpenAPI/reference docs ship `/v1/dashboard/webhooks*`, the API mounts handlers, the worker runs in `ratesengine-api`, and the runbook documents operational recovery | EV-0072; XFI-0038 | Supports F-1246 |
| CMD-0076 | 2026-05-12 | Customer-webhook queue concurrency audit across worker startup, `ListPendingDeliveries`, completion updates, and multi-region docs | every API process with dashboard webhooks enabled starts a delivery worker; the queue reader is a plain due-row `SELECT ... LIMIT` with no claim/lease/`FOR UPDATE SKIP LOCKED`; delivery completion updates do not guard against another worker already acting on the same row; the worker comment explicitly limits correctness assumptions to "operator runs one worker" despite multi-region rollout docs | EV-0073; XFI-0039 | Supports F-1247 |
| CMD-0077 | 2026-05-12 | Dashboard webhook quota-concurrency audit across `HandleCreate`, `checkQuota`, `WebhookStore.CreateWebhook`, migration 0027 constraints, and handler tests | the advertised 10-webhook/account cap is enforced by `ListWebhooksForAccount` -> `len(hooks)` -> later insert, with no transactional lock, unique/exclusion/check constraint, or store-level compare-and-insert guard; tests cover only sequential quota fill, so concurrent creates can pass the pre-check together and exceed the cap | EV-0074; XFI-0040 | Supports F-1248 |
| CMD-0078 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after webhook quota finding F-1248 and XFI-0040 ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0075 | Audit artifact syntax/sync sanity check |
| CMD-0079 | 2026-05-12 | Customer-webhook producer audit: repo-wide `EnqueueDelivery`/event-type searches plus shipped docs/runbook/API worker reconciliation | production code defines webhook event enums, validates subscriptions, starts the delivery worker, documents queue fan-out, and has a runbook for failed deliveries, but no non-test production caller ever invokes `EnqueueDelivery` or otherwise fans incidents/anomalies/divergence into pending delivery rows | EV-0076; XFI-0041 | Supports F-1249 |
| CMD-0080 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after F-1249, XFI-0041, and remediation R-1247 updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0077 | Audit artifact syntax/sync sanity check |
| CMD-0081 | 2026-05-12 | Inventory refresh/merge against current `HEAD`: reran `inventory/generate.sh`, preserved prior row statuses from the pre-refresh TSV, then reconciled tracked-file and row counts | current tracked tree excluding this audit directory is `1,869`; refreshed `file-coverage.tsv` now also has `1,869` rows while preserving prior review states (`done=64`, `in_progress=35`, `todo=1770`) | EV-0078 | Resolves the execution-time inventory-count drift documented by EV-0063 while leaving per-file closure open |
| CMD-0082 | 2026-05-12 | Durable freeze-event dedupe audit across `freeze.Writer`, `timescale.FreezeEventSink.RecordFreeze`, the `freeze_events` insert SQL, and current integration tests | open-row dedupe is asserted only serially; production SQL uses unlocked `WHERE NOT EXISTS` plus a PK on `(asset_id, quote_id, frozen_at)`, so concurrent same-pair callers with different timestamps can both insert open rows despite comments claiming same-transaction protection | EV-0079; XFI-0042 | Supports F-1250 |
| CMD-0083 | 2026-05-12 | FX-derived USD-volume audit across indexer wiring, `VWAPUSDFXResolver`, trade enrichment, and targeted integration test `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1` | targeted integration test failed: seeded EURC/USDC VWAP at trade time was rejected with `ok=false`; source review shows freshness is checked as `clock().Sub(observedAt)` instead of relative to the requested trade timestamp, and `Freshness: 0` docs disagree with constructor behavior | EV-0080; XFI-0043 | Supports F-1251 |
| CMD-0084 | 2026-05-12 | Post-pass audit-control sanity: `./scripts/ci/lint-docs.sh`; TSV status roll-up after marking webhook/freeze/FX files reviewed | docs lint passed; refreshed inventory now reports `done=76`, `in_progress=35`, `todo=1758` | EV-0081 | Confirms ledger syntax and coverage-progress bookkeeping after F-1250/F-1251 |
| CMD-0085 | 2026-05-12 | Multi-region cutover command reconciliation across `docs/operations/multi-region-cutover.md`, `scripts/dev/verify-cross-region.sh`, and `Makefile` | runbook pre-flight names `make verify-cross-region`, but repository search finds no Makefile target by that name; only direct script/binary invocations exist | EV-0082; XFI-0044 | Supports F-1252 |
| CMD-0086 | 2026-05-12 | Redis ACL deployment coupling audit across Sentinel ACL/server templates, archival-node application TOML template, Redis config docs, and `redisclient.Build` | lockdown renders `user default off` plus `user ratesengine`, while application config/templates never set `storage.redis_username`; the client builder preserves empty username by default, so the rendered binaries continue authenticating on the disabled default-user path | EV-0083; XFI-0045 | Supports F-1253 |
| CMD-0087 | 2026-05-12 | Redis ACL keyspace reconciliation across `users.acl.j2`, active Redis key builders, ADR-0007 cache-schema notes, and API runtime wiring | the lockdown ACL names `~ratelimit:*` and `~subscriber:*`, but active code writes `rl:*` and `sub:*`; it also omits active `signup:email:*`, `sep10:seen:*`, `usage:*`, `assets:list:*`, and `markets:list:*` families even though current API/auth/catalogue code uses them | EV-0084; XFI-0046 | Supports F-1254 |
| CMD-0088 | 2026-05-12 | Post-pass audit-control sanity after Redis ACL finding F-1254: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=88`, `in_progress=38`, `todo=1743` | EV-0085 | Confirms ledger syntax and coverage bookkeeping after the ACL keyspace pass |
| CMD-0089 | 2026-05-12 | Dashboard first-login provisioning race audit across `HandleLogin`, magic-link token consumption, `signupNewUser`, account/user stores, and migration 0027 uniqueness constraints | multiple valid magic links can be minted for one new email; each token is individually single-use, but concurrent callbacks both branch on `GetUserByEmail` not-found, each creates an account, and the loser later hits the `users_email_idx` unique constraint after `signupNewUser` has already persisted a suffixed fallback account | EV-0086; XFI-0047 | Supports F-1255 |
| CMD-0090 | 2026-05-12 | `go test ./internal/api/v1/dashboardauth ./internal/platform/postgresstore` after the first-login provisioning race review | dashboard-auth tests passed; `internal/platform/postgresstore` has no direct unit tests; current coverage does not exercise concurrent same-email first-login callbacks across real stores | EV-0087 | Positive baseline context for F-1255 |
| CMD-0091 | 2026-05-12 | Post-pass audit-control sanity after first-login provisioning finding F-1255: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=95`, `in_progress=39`, `todo=1735` | EV-0088 | Confirms ledger syntax and coverage bookkeeping after the auth provisioning pass |
| CMD-0092 | 2026-05-12 | Current-head dashboard key budget reconciliation across `dashboardkeys.HandleCreate`, `platform.Tier.MaxRateLimitPerMin`, dashboard key tests, UI form copy, and OpenAPI `CreateKeyRequest` docs | persisted dashboard-minted key rate limits are now clamped to tier ceilings and `go test ./internal/api/v1/dashboardkeys` passes that regression path, closing the security claim in F-1212; the dashboard still says free-tier default is 1000, exposes `max={100000}`, and OpenAPI documents `Default 1000` with `maximum: 100000` without explaining tier-specific clamping | EV-0089; EV-0090; XFI-0048 | Supersedes F-1212's current-source status and supports F-1256 |
| CMD-0094 | 2026-05-12 | Dashboard API-key quota concurrency audit across `HandleCreate`, `checkQuota`, `APIKeyStore.Create`, migration 0027 constraints, and current handler tests | the 25-active-key/account cap is enforced by `ListForAccount` -> active-count -> later insert, with no transaction, compare-and-insert guard, or schema invariant limiting active rows per account; tests cover only serial over-quota rejection | EV-0092; XFI-0049 | Supports F-1257 |
| CMD-0095 | 2026-05-12 | Post-pass audit-control sanity after dashboard-key reconciliation F-1212/F-1256/F-1257: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=97`, `in_progress=38`, `todo=1734` | EV-0093 | Confirms ledger syntax and coverage bookkeeping after the dashboard-key pass |
| CMD-0096 | 2026-05-12 | Redis-optional usage-tracker wiring audit across `cmd/ratesengine-api/main.go`, `middleware.UsageTracker`, and `usage.Counter` | API startup comments say usage tracking degrades to a no-op when Redis is nil, but `usage.New(rdb)` is created unconditionally, the middleware checks only `counter == nil`, and `Counter.Increment`/`Read` immediately dereference `c.rdb`; Redis-less authenticated traffic can therefore panic in the usage middleware instead of degrading cleanly | EV-0094; XFI-0050 | Supports F-1258 |
| CMD-0097 | 2026-05-12 | `/v1/account/usage` contract reconciliation across current runtime wiring, handler comments, OpenAPI/reference docs, architecture docs, and `go test ./internal/api/v1 ./internal/usage` | current API wiring supplies `UsageTracker` + `UsageReader` and the handler returns Redis-backed daily rows when available; tests pass for current packages, but OpenAPI, generated reference YAML, Postman reference, API-design doc, architecture inventory, and an in-file handler comment still describe the endpoint as an always-empty placeholder | EV-0095; XFI-0051 | Supports F-1259 |
| CMD-0098 | 2026-05-12 | Current-workspace remediation reconciliation for SEP-10 replay guard optionality, Redis-less stream startup, customer-webhook SSRF, and webhook design-doc truth; then `go test ./internal/api/v1/dashboardwebhooks ./internal/customerwebhook ./cmd/ratesengine-api` | current workspace now fails startup for `auth_mode=sep10` without Redis, gates the Redis pub/sub stream subscriber on `rdb != nil`, validates webhook destinations against internal/private targets with send-time guarded dialing + redirect refusal, and updates `docs/reference/api-design.md` to say webhook callbacks shipped; targeted packages pass | EV-0096 | Marks F-1217, F-1238, F-1245, and F-1246 fixed in current workspace |
| CMD-0099 | 2026-05-12 | Post-pass control checkpoint after usage/runtime reconciliation and live scope drift: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; `git rev-parse HEAD`; `git status --short` review | docs lint passed; scope reconciles at `tracked=1870`, `rows=1870`; current `HEAD` is `9f68ef9a...`; the audit worktree currently has 15 dirty paths, including concurrent non-audit edits outside this directory | EV-0097 | Confirms audit bookkeeping after `internal/customerwebhook/ssrf.go` entered tracked scope |
| CMD-0100 | 2026-05-12 | Current-head Wave 5 remediation reconciliation across CEX streamer decode metrics, webhook delivery claiming, Redis ACL username/keyspace rollout, and the attempted webhook-account quota fix; then `go test ./internal/api/v1/dashboardwebhooks ./internal/platform/postgresstore ./internal/sources/external/binance ./internal/sources/external/bitstamp ./internal/sources/external/coinbase ./internal/sources/external/kraken` | settled `HEAD=7c9e79ae...` now increments `SourceDecodeErrorsTotal` in all four CEX streamers, atomically claims webhook delivery rows with `FOR UPDATE SKIP LOCKED` plus a 5-minute lease, renders `redis_username` under ACL lockdown, and aligns the ACL key families with live namespaces; targeted packages pass. The new webhook-cap `WITH current_count ... INSERT` still performs an unlocked count under PostgreSQL MVCC, so concurrent creates can still overrun the ten-row ceiling and `F-1248` remains open. | EV-0098 | Marks F-1235, F-1247, F-1253, and F-1254 fixed while preserving F-1248 |
| CMD-0101 | 2026-05-12 | Current-head Wave 7 reconciliation across push-to-main CI, web advisory gates, and signup duplicate-reservation flow; then `go test ./internal/api/v1 ./internal/auth` | settled `HEAD=27343a46...` now runs canonical `ci` on pushes to `main`, closing F-1231; all three web CI jobs now execute `pnpm audit --audit-level high`, narrowing but not closing F-1207 because the vulnerable Next pins and hosted alert posture remain; signup now reserves `signup:email:*` with `SETNX` before minting when Redis tracking is wired, closing only the same-email race slice of F-1218 while unverified plaintext key minting and tracker-nil duplicate behavior remain. Targeted API/auth tests pass. | EV-0099 | Fixes F-1231; partial current-head reconciliation for F-1207/F-1218 |
| CMD-0102 | 2026-05-12 | Current-workspace policy-enforcement reconciliation for F-1226 across `cmd/ratesengine-api/main.go`, `internal/api/v1/server.go`, `internal/api/v1/middleware/keypolicy.go`, `internal/auth/{subject,apikey_postgres,apikey_redis}.go`, and `go test ./internal/api/v1/middleware ./internal/auth` | the in-progress workspace adds `KeyPolicy` production wiring plus IP/referer/permission fields on Postgres-served subjects, and the targeted middleware/auth packages pass. It still does not close F-1226: `cacheStore` writes the legacy `APIKeyRecord` shape, `cacheLookup` rebuilds subjects without policy fields, so a cache hit can silently bypass policy that a cache miss enforced; monthly quota remains unenforced and `TouchUsage` still has no production caller. `internal/api/v1/middleware/keypolicy.go` is currently untracked in the shared workspace. | EV-0100 | Preserves F-1226 as open despite partial remediation in flight |
| CMD-0103 | 2026-05-12 | Post-pass control checkpoint after committed Wave 8 policy files entered tracked scope: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up; `git rev-parse HEAD`; `git status --short` review | docs lint passed; current `HEAD` is `8a61c17e...`; tracked scope now reconciles at `tracked=1872`, `rows=1872` after adding `internal/api/v1/middleware/keypolicy.go` and `keypolicy_test.go` to the TSV; current roll-up is `done=99`, `in_progress=50`, `todo=1723`; only the two inventory-maintenance files were dirty during the snapshot. | EV-0101 | Restores audit inventory parity after Wave 8 tracked-scope growth |
| CMD-0104 | 2026-05-12 | Settled Wave 9 reconciliation across CMC ID disambiguation, FX trade-time freshness, and concurrent first-login callback handling; `go test ./internal/sources/external/coinmarketcap ./internal/storage/timescale ./internal/api/v1/dashboardauth`; repo search for committed CMC ID-mode fixtures / ops wiring; `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1`; tracked-vs-inventory count check at `HEAD=82a6052c...` | package-level CMC/timescale/dashboard-auth tests pass and tracked scope remains reconciled at `tracked=1872`, `rows=1872`. The Wave 9 fixes are still partial: ops verification still omits `CMCIDs` and no committed ID-mode CMC response fixture exists; the FX integration now finds the historical rate but fails on `USDPriceAt = "1.08500000000000000000", want "1.085"`; the first-login handler now reloads the winning user but still leaves the speculative loser account row orphaned. | EV-0102 | Narrows but preserves F-1237/F-1251/F-1255 |
| CMD-0105 | 2026-05-12 | In-flight Wave 10 workspace reconciliation across deploy migrations, contribution-history USD volume, dashboard-key quota enforcement, Redis-less usage handling, usage docs, and the SDEX V0 decoder; `git diff` review of the changed files; `go test ./cmd/ratesengine-aggregator ./cmd/ratesengine-api ./internal/api/v1/dashboardkeys ./internal/aggregate/orchestrator ./internal/usage ./internal/platform/postgresstore`; `go test ./internal/sources/sdex`; `./scripts/ci/lint-docs.sh`; repo searches across generated usage docs and deploy workflow/playbook wiring | targeted package tests and docs lint pass. The workspace still leaves `F-1220`, `F-1242`, `F-1257`, `F-1258`, and `F-1259` open: the deploy workflow stages migrations but relies on `ansible.posix.synchronize` while installing only `ansible-core`; contribution USD rows split a pre-filter total by post-filter weights; dashboard-key quota uses an unlocked count CTE; Redis-less `/v1/account/usage` can still dereference `usageReaderAdapter{c:nil}`; generated/reference usage docs and handler comments remain stale, and the new OpenAPI source misstates `/v1/healthz` checks. The SDEX V0 decode branch itself now decodes and its package tests pass, so `F-1233` should be reconsidered when that workspace settles. | EV-0103 | Partial-remediation evidence and unresolved-risk preservation for the Wave 10 workspace |
| CMD-0106 | 2026-05-12 | Follow-on shared-workspace reconciliation for CI action-pinning lint and the contribution-volume rewrite; `bash scripts/ci/lint-actions-pinning.sh`; `PR_DIFF=1 bash scripts/ci/lint-actions-pinning.sh`; code review of `.github/workflows/ci.yml`, `scripts/ci/lint-actions-pinning.sh`, `cmd/ratesengine-aggregator/contribution_sink.go`, `internal/aggregate/orchestrator/orchestrator.go`, and `internal/canonical/trade.go`; `go test ./cmd/ratesengine-aggregator ./internal/aggregate/orchestrator` | the new lint exits zero but warns on 12 current mutable third-party action tags in both normal and PR-diff modes, so `F-1216` narrows rather than closes. The contribution rewrite now carries per-trade USD values keyed by stable `Trade.ID()` and sums them after filtering, which addresses the specific pre-filter/post-filter mismatch recorded in `EV-0103`; targeted aggregator/orchestrator tests pass, but the change is still in-flight at this checkpoint. | EV-0104 | Narrows F-1216 and flags F-1242 for re-evaluation after the latest workspace rewrite |
| CMD-0107 | 2026-05-12 | Settled Wave 11 / current shared-workspace reconciliation across contribution-volume closure, SDEX V0 closure, the new customer-webhook fan-out producer slice, and a fresh aggregator min-volume adversarial pass; `git diff` review of `cmd/ratesengine-aggregator/main.go`, `internal/customerwebhook/fanout.go`, `internal/platform/{webhook,postgresstore/webhook_store}.go`, and `internal/storage/timescale/freeze_events.go`; repo searches for declared webhook-event producers; `go test ./cmd/ratesengine-aggregator ./internal/customerwebhook ./internal/storage/timescale ./internal/platform/postgresstore`; `go test ./internal/sources/sdex ./cmd/ratesengine-aggregator ./internal/aggregate/orchestrator` | committed `HEAD=e787c11b...` contains the contribution rewrite, and current code now attributes `price_source_contributions.volume_usd` from post-filter per-trade USD survivors, closing the specific `F-1242` mismatch; the SDEX V0 decode branch is committed and targeted tests pass, closing the original V0-drop defect in `F-1233`. The concurrent webhook workspace now adds a real `anomaly.freeze` producer/fan-out path, but no producer exists for `incident.sev1`, `incident.resolved`, or `divergence.firing`, so `F-1249` narrows rather than closes. Separately, the aggregator still evaluates `dropForMinUSDVolume` against `usdVolume` computed before class/outlier filtering, even though the config/comment contract says the threshold is post-filter; that can publish a surviving thin window whose pre-filter volume alone exceeded the manipulation floor. | EV-0105 | Closes F-1233/F-1242, narrows F-1249, and supports new F-1260 |
| CMD-0108 | 2026-05-12 | Wave 13 / current-worktree reconciliation for customer-webhook producer coverage and the in-flight `F-1236` supply-ledger remediation attempt; code review of `internal/divergence/worker.go`, `internal/divergence/worker_test.go`, `cmd/ratesengine-aggregator/main.go`, `cmd/ratesengine-ops/supply.go`, and repo searches for remaining incident-event producers; `go test ./internal/divergence`; `go test ./internal/divergence ./cmd/ratesengine-aggregator ./internal/customerwebhook ./internal/storage/timescale ./internal/platform/postgresstore`; `go test ./cmd/ratesengine-ops`; `./scripts/ci/lint-docs.sh` | settled `HEAD=60c8a2e4...` now wires `divergence.firing` through an edge-triggered warning hook and fan-out payload builder, with `./internal/divergence` tests passing. `F-1249` therefore narrows again: only `incident.sev1` and `incident.resolved` remain producer-less, and integration/producer observability is still absent. The current uncommitted `F-1236` remediation attempt does not close the supply freshness finding: both `cmd/ratesengine-aggregator` and `cmd/ratesengine-ops` fail to build because the patch filters cursors via `c.Name`, but `timescale.Cursor` exposes `Source`/`Sub`, not `Name`. Docs lint remains green. | EV-0106 | Narrows F-1249 to incident producers and preserves F-1236 as open after a non-building workspace attempt |
| CMD-0109 | 2026-05-12 | Current-workspace Stripe remediation audit across `internal/api/v1/stripe_webhook.go`, production API wiring in `cmd/ratesengine-api/main.go`, webhook tests, and platform account/billing stores; repo search for `StripePlatformBridge` production use; `go test ./internal/api/v1` | the new Stripe helper introduces a platform-side bridge that can upsert subscriptions and mutate account tier, but no production wiring sets `StripeWebhookConfig.Platform`, no existing webhook test exercises the new bridge, and the helper still does not update Postgres dashboard API-key limits. The package test suite passes, but `F-1219` remains open because the paid-upgrade side effect is still not end-to-end on the runtime path. | EV-0107 | Preserves F-1219 after partial uncommitted remediation |
| CMD-0110 | 2026-05-12 | Settled Wave 15 / current-worktree reconciliation across Stripe subscription-event handling and the supply freshness-gate follow-up; code review of `internal/api/v1/stripe_webhook.go`, `cmd/ratesengine-api/main.go`, `internal/supply/{refresher,supply,classic,sep41,storage_classic_reader,storage_sep41_reader}.go`; repo searches for production `StripePlatformBridge` wiring, committed `customer.subscription.*` webhook tests, and `MinComponentLedger` producers; `go test ./internal/api/v1 ./internal/supply ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | settled `HEAD=c82c1602...` adds `customer.subscription.updated`/`deleted` webhook handling plus the supply stale-component rejector, and the targeted API/supply/command packages pass. `F-1219` remains open because production API wiring still never sets `StripeWebhookConfig.Platform`, the webhook suite has no bridge/subscription-event assertions, and Postgres dashboard-key limits remain outside the side effect. `F-1236` remains open because production readers still do not populate `MinComponentLedger`; the current dirty `classic.go`/`sep41.go` follow-up only threads a freshness field that real storage-backed readers continue to leave zero. | EV-0108 | Preserves F-1219/F-1236 after Wave 15 narrows but does not close them |
| CMD-0111 | 2026-05-12 | Settled Wave 16 reconciliation plus scope refresh; reviewed `HEAD=65197ec0...`, `internal/supply/{classic,sep41,storage_classic_reader,storage_sep41_reader}.go`, the new `docs/operations/github-actions-sha-pinning.md`, tracked-vs-inventory parity, and `./scripts/ci/lint-docs.sh` | Wave 16 commits the classic/SEP41 `MinComponentLedger` pass-through and adds one new operations document, increasing non-audit tracked scope to `1,875`. `F-1236` remains open because storage-backed readers still never populate that freshness field, so real snapshots continue down the zero-value bypass. Audit inventory was refreshed manually to re-establish `tracked=1875`, `rows=1875`, with roll-up `done=105`, `in_progress=51`, `todo=1719`; docs lint passes. | EV-0109 | Preserves F-1236, records Wave 16 scope drift, and restores inventory parity |
| CMD-0112 | 2026-05-12 | Current-workspace classic-supply freshness audit across `internal/storage/timescale/classic_supply_observations.go`, `internal/supply/storage_classic_reader.go`, `internal/supply/storage_classic_reader_test.go`, repo searches for remaining SEP41/XLM freshness producers, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | the workspace adds `MinClassicComponentLedger`, wires it through `ClassicSupplyStore`/`StorageClassicSupplyReader`, and the targeted tests pass. `F-1236` remains open because the producer work is classic-only, SEP41/XLM still have no corresponding freshness source, and classic query failures intentionally degrade to `MinComponentLedger=0`, preserving the permissive bypass for that snapshot. | EV-0110 | Narrows but preserves F-1236 during the classic-only producer attempt |
| CMD-0113 | 2026-05-12 | Settled Wave 17 / current-workspace SEP41 freshness audit across `HEAD=6819e7dc...`, `cmd/ratesengine-aggregator/main.go`, `internal/storage/timescale/sep41_supply_events.go`, `internal/supply/storage_sep41_reader.go`, repo searches for XLM freshness producers, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | Wave 17 commits the classic-storage `MinClassicComponentLedger` producer. The current SEP41 follow-up adds `MinSEP41ComponentLedger` storage/reader plumbing plus aggregator adaptation, but `./internal/supply` now fails to build because `fakeSEP41Store` lacks the new interface method. Timescale, aggregator, and ops package tests pass; XLM still has no analogous freshness producer. | EV-0111 | Preserves F-1236 after Wave 17 and records the non-building SEP41 follow-up |
| CMD-0114 | 2026-05-12 | Settled Wave 18 / current Stripe webhook workspace reconciliation across `HEAD=fb0b3073...`, supply package verification, `internal/api/v1/stripe_webhook.go`, production API wiring in `cmd/ratesengine-api/main.go`, repo search for production `StripePlatformBridge` use, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | Wave 18 commits SEP41 storage freshness production and the targeted supply/timescale/command tests pass. The current Stripe workspace adds `invoice.paid` period refresh, but `F-1219` remains open because production wiring still never sets `StripeWebhookConfig.Platform`, committed bridge/event coverage is still not closure-grade, and Postgres dashboard-key limits remain outside the side effect. | EV-0112 | Records Wave 18 supply progress and preserves F-1219 after the new invoice path |
| CMD-0115 | 2026-05-12 | Late live-R1 reconciliation for firewall, service listeners, timers, and Caddy drift: `systemctl list-timers --all`; `systemctl list-unit-files`; `systemctl is-enabled sla-probe.timer archive-completeness.timer verify-archive-tier-a.timer supply-snapshot.timer`; `systemctl is-active nftables`; `ufw status`; `nft list ruleset`; `ss -ltnp`; `/etc/nftables.conf`; `/etc/caddy/Caddyfile`; public `curl -skI https://api.ratesengine.net/metrics`; public `nc` probes for `9000`, `9001`, `9090`, `3100`, `9080`, `9100`, `6061`, `38563`, `11625`, `11626`, `11725`, and `11726` | `archive-completeness`, `verify-archive-tier-a`, and `supply-snapshot` timers are enabled; `sla-probe.timer` remains disabled. nftables is active, UFW inactive, and the reviewed internal-service ports now time out externally. Public `/metrics` now returns `404` under the hardened live Caddyfile. Residual firewall drift remains: live nftables explicitly accepts `{11625,11626,11725,11726}`, `stellar-core` listens on `0.0.0.0:11726`, and the public `11726/tcp` probe succeeds. | EV-0113 | Narrows F-1201/F-1205 and marks F-1223 closure-grade on live R1 |
| CMD-0116 | 2026-05-12 | Late web/GitHub dependency-security reconciliation: package-version scans across the three web `package.json` files; `.github/dependabot.yml` review; `pnpm audit --audit-level high` in explorer/dashboard/status; `gh api repos/RatesEngine/rates-engine/vulnerability-alerts`; `gh api 'repos/RatesEngine/rates-engine/dependabot/alerts?state=open'` | All three apps now pin `next@15.5.18`; Dependabot has npm ecosystem entries for each web app; every high-severity `pnpm audit` run returns only one moderate advisory. Hosted GitHub repository controls remain disabled: vulnerability alerts return disabled/404 and Dependabot alerts return disabled/403. | EV-0114 | Narrows F-1207 to hosted dependency-alert posture |
| CMD-0117 | 2026-05-12 | Highest-priority current-source/runtime pass over residual auth/usage enforcement: reviewed `internal/auth/apikey_postgres.go`, `internal/api/v1/middleware/keypolicy.go`, `cmd/ratesengine-api/main.go` usage wiring, and `internal/api/v1/account.go`; ran `go test ./internal/api/v1 ./internal/auth ./internal/platform/...`; ran `go test ./cmd/ratesengine-aggregator ./internal/aggregate/...` | Targeted packages remain green. `F-1226` stays open because the Postgres validator's Redis cache row still sheds policy fields on cache hit, monthly quota enforcement remains absent, and `TouchUsage` still lacks production wiring. `F-1258` stays open because Redis-less startup now avoids the middleware panic path, but `UsageReader: usageReaderAdapter{c:nil}` still leaves `/v1/account/usage` able to dereference a nil inner counter. | EV-0115 | Preserves F-1226/F-1258 after direct current-head review |
