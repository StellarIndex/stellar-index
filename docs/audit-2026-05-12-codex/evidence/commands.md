# Command Evidence

Summarize commands here. For long outputs, record the important lines
and attach generated artifacts where possible.

| ID | Date | Command | Result | Evidence Produced | Notes |
| --- | --- | --- | --- | --- | --- |
| CMD-0001 | 2026-05-11 | `git rev-parse HEAD` | `80c57e38eeee729ec2d879d54286419206cee864` | EV-0001 | Planning snapshot |
| CMD-0002 | 2026-05-11 | `git status --short` | no output before audit files were created | EV-0001 | Clean initial worktree |
| CMD-0003 | 2026-05-11 | `git ls-files \| wc -l` | `1,747` | EV-0002 | Tracked file count before audit directory |
| CMD-0004 | 2026-05-11 | `tracked=$(git ls-files ':!:docs/audit-2026-05-12-codex/**' \| wc -l \| tr -d ' '); rows=$(tail -n +2 docs/audit-2026-05-12-codex/inventory/file-coverage.tsv \| wc -l \| tr -d ' '); printf 'tracked=%s\nrows=%s\n' "$tracked" "$rows"` | `tracked=1747`; `rows=1747` | EV-0003 | Second-pass inventory reconciliation |
| CMD-0005 | 2026-05-11 | `awk -F '\t' 'NR>1 {count[$5]++} END {for (s in count) print s, count[s]}' docs/audit-2026-05-12-codex/inventory/file-coverage.tsv \| sort` | `todo 1747` | EV-0004 | Third-pass false-completion check |
| CMD-0006 | 2026-05-11 | `find docs/audit-2026-05-12-codex -type f \| sort` | 19 files under the Codex audit directory | EV-0004 | Deliverable presence check |
| CMD-0007 | 2026-05-12 | `go list ./...` | package list succeeded; sandbox emitted module stat-cache permission warning | EV-0005 | Topology/package inventory |
| CMD-0008 | 2026-05-12 | `GOCACHE=/private/tmp/ratesengine-gocache go test ./...` | sandbox run failed because httptest/miniredis could not bind localhost ports; escalated run passed | EV-0006 | Test reality check |
| CMD-0009 | 2026-05-12 | `./scripts/ci/lint-docs.sh` | passed | EV-0007 | Docs lint does not catch all generated drift |
| CMD-0010 | 2026-05-12 | `go run ./scripts/ci/lint-openapi-urls openapi/rates-engine.v1.yaml` | passed | EV-0008 | ADR-0018 URL discipline |
| CMD-0011 | 2026-05-12 | `go run ./scripts/ci/verify-launch-ready` | failed; L4.14-L4.17, L5.6, L5.8, and L6 finalisation blockers remain | EV-0009 | Launch-readiness gate |
| CMD-0012 | 2026-05-12 | `./scripts/dev/docs-api.sh` | regenerated `docs/reference/api/rates-engine.v1.yaml` and produced a git diff | EV-0010 | Generated API reference drift |
| CMD-0013 | 2026-05-12 | `cd web/explorer && pnpm generate:api` | regenerated `web/explorer/src/api/types.ts` and produced a git diff | EV-0011 | Generated TS API type drift |
| CMD-0014 | 2026-05-12 | `rg -n "/v1/(coins\|currencies)\|/coins\|/currencies" ...` | found active frontend/status/examples references to removed API paths | EV-0012 | Endpoint removal cross-file check |
| CMD-0015 | 2026-05-12 | `make verify` | passed all local canonical gates; explorer lint warning remains non-fatal | EV-0013 | Full local quality gate |
| CMD-0016 | 2026-05-12 | `pnpm audit --audit-level moderate` in `web/explorer`, `web/dashboard`, `web/status` | each app failed with 27 vulnerabilities: 4 low, 13 moderate, 8 high, 2 critical | EV-0014 | Web dependency vulnerability audit |
| CMD-0017 | 2026-05-12 | `gitleaks detect --redact --no-banner` | passed; 2,067 commits, about 24.97 MB scanned, no leaks found | EV-0015 | Secret scan |
| CMD-0018 | 2026-05-12 | `rg -n "panic\(\|t\.Skip\|Skip\(\|TODO\|FIXME\|nolint\|int64\|Horizon\|horizon" cmd internal configs deploy scripts web .github openapi migrations` | identified panic/skip/TODO/int64/Horizon review queue | EV-0016 | Static hostile-pattern sweep |
| CMD-0019 | 2026-05-12 | `rg -n "CREATE TABLE\|CREATE INDEX\|CREATE MATERIALIZED\|ALTER TABLE\|DROP TABLE\|DROP INDEX" migrations` | mapped 28 migration families and destructive/down-migration surfaces | EV-0017 | Migration inventory |
| CMD-0020 | 2026-05-12 | `git ls-files ...` topology commands | top-level tracked counts: 651 `internal`, 421 `docs`, 199 `web`, 163 `configs`, 57 `migrations`, etc. | EV-0018 | Repo topology |
| CMD-0021 | 2026-05-12 | `nc -vz -w2 136.243.90.96 ...` | public TCP connections succeeded to R1 ports 9000, 9001, 9090, 3100, 9080, 9100, 6061, 11726 | R1-0008; EV-0019 | External exposure proof |
| CMD-0022 | 2026-05-12 | `bash scripts/dev/audit-public-api.sh` against `https://api.ratesengine.net` | passed 37/37 checks, including `/v1/coins` and `/v1/currencies` checks | EV-0020 | Live API audit confirms deployed legacy routes |
| CMD-0023 | 2026-05-12 | `API_BASE_URL=https://api.ratesengine.net bash scripts/dev/r1-smoke.sh` | passed all checks, including `/v1/coins`, `/v1/currencies`, and coins negative-behavior pins | EV-0020 | Smoke script still encodes removed route expectations |
| CMD-0024 | 2026-05-12 | `find deploy/status-page -maxdepth 3 -type d`; status-page docs/runbook/comms reads | only `deploy/status-page` exists; runbooks/comms still reference removed `deploy/status-page/cstate/**` and Upptime GitHub issue flow while deployed status page is `web/status` on Cloudflare Pages | EV-0021; XFI-0007 | Status-page operational drift |
| CMD-0025 | 2026-05-12 | `git ls-files configs/ansible/inventory/r1.secrets.yml configs/ansible/inventory/*.secrets.yml`; `git check-ignore -v configs/ansible/inventory/r1.secrets.yml` | vault file is present locally but not tracked; ignored by `.gitignore:64` `/configs/ansible/inventory/r*.yml` | EV-0022 | No tracked-vault-secret finding from this pass |
| CMD-0026 | 2026-05-12 | dashboard key-management/rate-limit code reads | dashboard create-key UI submits `rate_limit_per_min`, backend accepts up to `100000` for owner/admin/member sessions, Postgres validator returns that value as `Subject.RateLimitPerMin`, and rate-limit middleware uses it as the per-key override | EV-0023; XFI-0008 | Supports F-1212 |
| CMD-0027 | 2026-05-12 | amount/VWAP/stablecoin proxy source reads across `internal/scval`, `internal/canonical`, `internal/aggregate`, `internal/storage/timescale`, and Stellar DEX decoders | i128/u128/u256 decode preserves `big.Int`, but fiat-USD stablecoin proxy reuses classic USD pegs while `windowUSDVolume` still divides all quote amounts by `1e8`; storage USD-volume code separately knows classic/SAC USD pegs are `1e7` | EV-0024; XFI-0009 | Supports F-1213 |
| CMD-0028 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'grep -nE "enable_stablecoin_fiat_proxy|min_usd_volume|usd_pegged_classic_assets" /etc/ratesengine.toml || true'` and surrounding non-secret config read | R1 has `enable_stablecoin_fiat_proxy = true`, `min_usd_volume = 0`, and Circle USDC in `usd_pegged_classic_assets`; the threshold is disabled on R1 despite the source default being 10000 | R1-0011; EV-0024 | Runtime context for F-1213 |
| CMD-0029 | 2026-05-12 | GitHub hosted control reads: `gh repo view`, `gh api repos/RatesEngine/rates-engine/branches/main`, `gh api repos/RatesEngine/rates-engine/rulesets`, `gh api repos/RatesEngine/rates-engine/environments`, `gh api repos/RatesEngine/rates-engine/actions/permissions`, `gh api repos/RatesEngine/rates-engine/actions/permissions/workflow` | repo is private; `main.protected=false`; repository rulesets and branch protection are unavailable on the current private repo tier; five environments have empty `protection_rules`; Actions allows all actions and does not require SHA pinning; workflow token default is read-only | EV-0025; XFI-0010 | Supports F-1214/F-1215/F-1216 |
| CMD-0030 | 2026-05-12 | workflow and governance source reads for CODEOWNERS, `CONTRIBUTING.md`, deploy workflow, release workflow, and `uses:` actions | local docs claim green CI, CODEOWNER review, and signed commits are enforced by branch protection; deploy workflow comments make r1 reviewers optional; workflows use tag-pinned third-party actions while hosted settings allow all actions and no SHA pinning | EV-0025; EV-0026; XFI-0010 | Supports F-1214/F-1215/F-1216 |
| CMD-0031 | 2026-05-12 | auth/cache/signup source reads across `internal/api/v1/{auth_sep10,signup}.go`, `internal/auth/sep10/validator.go`, `internal/auth/signup_tracker.go`, cache-control middleware/tests, OpenAPI, and signup UI/docs | cache-control table defaults unknown auth/signup/dashboard/webhook routes to `private, no-store`; self-service signup mints 1000/min keys on email string only, duplicate tracker is optional, and tracker lookup/create/mark is non-atomic; SEP-10 replay wording from this row is superseded by CMD-0057 | EV-0027; EV-0028; EV-0053; XFI-0011; XFI-0012 | Supports F-1217/F-1218; negative cache-control evidence |
| CMD-0032 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'curl ... /v1/auth/sep10/challenge?...'` | R1 returns 503 `SEP-10 not configured` for challenge route | R1-0012 | Runtime context only; source-level replay finding remains latent until SEP-10 is enabled |
| CMD-0033 | 2026-05-12 | Stripe/billing source reads across `internal/api/v1/stripe_webhook.go`, tests, `cmd/ratesengine-api/main.go`, `internal/platform/billing.go`, and migration 0027 | webhook verifies HMAC/timestamp and still updates Redis key state rather than platform subscriptions/dashboard keys; durable dedupe/audit wording from this row is superseded by CMD-0057 | EV-0030; EV-0053; XFI-0013 | Supports F-1219 |
| CMD-0034 | 2026-05-12 | release/deploy/migration source reads across `.github/workflows/{release,deploy}.yml`, Ansible deploy tasks, archival-node role, `docs/operations/{deploy-workflow,release-process}.md`, and `docs/operations/runbooks/fx-history-missing.md` | deploy workflow downloads and swaps binaries only; migration sync/apply exists in the initial archival-node role and manual runbooks, not the tagged-release deploy path; release docs still claim GHCR container pushes and `/opt/ratesengine/release-<tag>` rollback directories that the current workflow does not produce | EV-0031; EV-0032; XFI-0014 | Supports F-1220/F-1221/F-1222 |
| CMD-0035 | 2026-05-12 | `ssh -o ConnectTimeout=8 root@136.243.90.96 'sudo -u postgres psql ... schema_migrations; ls ... deployed-versions ...'` | R1 database is at schema version 28 dirty=false; binary sidecars show API/indexer/aggregator at `v0.5.0-rc.47`, ops at `v0.5.0-rc.29`, and sla-probe at `v0.5.0-rc.17`; `/usr/local/share/ratesengine/migrations` produced no listing in the probe | R1-0013; EV-0031 | Runtime context for release/schema coupling |
| CMD-0036 | 2026-05-12 | R1 `/etc/caddy/Caddyfile` and `/etc/ratesengine.toml` read; repo `configs/caddy/Caddyfile.api`, `configs/caddy/README.md`, and middleware remote-IP reads | R1 API config trusts `127.0.0.1/32`, but live Caddy overwrites `X-Forwarded-For` with `{remote_host}` and lacks the repo's `trusted_proxies`/`client_ip_headers` block and `/metrics` 404 guard | EV-0033; R1-0014; XFI-0015 | Supports F-1223 |
| CMD-0037 | 2026-05-12 | `curl -skI https://api.ratesengine.net/metrics`; `curl -sk https://api.ratesengine.net/metrics`; CORS header probes | public `/metrics` returned HTTP 200 with Go/runtime/API Prometheus metrics including route counters; disallowed Origin got no `Access-Control-Allow-Origin`; allowed dashboard Origin got explicit allow header | EV-0033; R1-0014 | Supports F-1223; negative CORS evidence |
| CMD-0038 | 2026-05-12 | dashboard auth IP source reads across `internal/api/v1/dashboardauth/{handlers,middleware}.go`, platform user/token stores, migration 0027, Caddy/API proxy config | dashboard auth login/callback/session touch paths call a local `clientIP(r)` that reads `r.RemoteAddr`, not the resolved middleware remote IP; behind Caddy this records the loopback peer rather than the real client IP | EV-0034; XFI-0016 | Supports F-1224 |
| CMD-0039 | 2026-05-12 | history/chart/stablecoin fallback source reads across `internal/api/v1/{history,chart,vwap,ohlc,price}.go`, Timescale aggregate readers, OpenAPI, and R1 curl probes for XLM/USD history/chart/direct USDC history | `/v1/history/since-inception` reads the literal CAGG pair only and returned empty for `native/fiat:USD`; `/v1/chart` has a stablecoin fallback and returned XLM/USD data from USDC with `flags.triangulated=true`; direct `native/USDC-GA5...` since-inception history returned populated points | EV-0035; R1-0015; XFI-0017 | Supports F-1225 |
| CMD-0040 | 2026-05-12 | platform API-key policy source reads across `internal/platform/apikey.go`, `internal/platform/postgresstore/apikey_store.go`, `internal/api/v1/dashboardkeys/handlers.go`, `internal/auth/apikey_postgres.go`, `internal/api/v1/middleware/auth.go`, OpenAPI, dashboard UI/types, and migration 0027 | platform keys store/display `monthly_quota`, `permissions`, `ip_allowlist`, `referer_allowlist`, and last-used fields, but runtime auth returns a Subject with only identity/tier/rate-limit and has no request-aware enforcement for allowlists, referer, permissions, monthly quotas, or `TouchUsage` | EV-0036; XFI-0018 | Supports F-1226 |
| CMD-0041 | 2026-05-12 | Docker/self-hosting migration source reads across `docker/ratesengine-migrate.Dockerfile`, `cmd/ratesengine-migrate/main.go`, `Makefile`, `docker/README.md`, `deploy/docker-compose/README.md`, and Ansible migration tasks | the migration binary defaults `-migrations migrations`; the migrate image copies only `/usr/local/bin/ratesengine-migrate` and no SQL files; `smoke-docker` only executes `--help`; bare-metal Ansible succeeds by separately syncing `/usr/local/share/ratesengine/migrations` | EV-0037; XFI-0019 | Supports F-1227 |
| CMD-0042 | 2026-05-12 | SSE timeout audit across `cmd/ratesengine-api/main.go`, stream handlers/tests/docs, `go doc net/http.Server.WriteTimeout`, and R1 loopback curl of `/v1/price/tip/stream` | API server sets `WriteTimeout: 30s`; stream handlers/tests/docs expect long-lived SSE with 15s heartbeats and 60s+ demo runs; R1 stream emitted through 25s and ended at elapsed 30s | EV-0038; R1-0016; XFI-0020 | Supports F-1228 |
| CMD-0043 | 2026-05-12 | CDN verification script audit across `scripts/dev/verify-cdn.sh`, price/tip handlers, OpenAPI, and R1 loopback probes | `verify-cdn.sh` uses `base=native` for `/v1/price` and `/v1/price/tip/stream` while those surfaces require `asset`; the script expects SSE `Cache-Control: no-store` while the stream writer sets `no-cache`; R1 returns 400 missing-asset for the script's SSE URL | EV-0039; XFI-0021 | Supports F-1229 |
| CMD-0044 | 2026-05-12 | since-inception/backfill audit across RFP docs, OpenAPI, history handler, backfill command, and R1 direct XLM/USDC history | R1 `/history/since-inception?asset=native&quote=USDC-GA5...&granularity=1d` returned 9 buckets from 2026-05-03 through 2026-05-11; Freighter asks for minimum 1 year and ideally since inception; project matrix targets ledger 2 -> today | EV-0040; R1-0017; XFI-0022 | Supports F-1230 |
| CMD-0045 | 2026-05-12 | hosted CI trigger audit across `.github/workflows/ci.yml`, `.github/workflows/api-audit.yml`, GitHub recent runs, and branch-protection API evidence | canonical `ci` triggers only on pull_request and explicitly disables push-to-main; `api-audit` runs on selected main path pushes only; hosted `main` is unprotected, so direct pushes can bypass the full lint/test/build/security suite | EV-0041; XFI-0023 | Supports F-1231 |
| CMD-0046 | 2026-05-12 | stablecoin USD price parity audit across `/v1/price`, `/v1/price/batch`, `/v1/assets/{id}`, `price.go`, `assets_f2.go`, and R1 live probes | R1 asset detail for Circle USDC returned `price_usd=0.9999838427`, but `/v1/price?asset=USDC-GA5...&quote=fiat:USD` returned 404 and GET/POST batch returned empty arrays; `tryStablecoinFiatProxy` skips when the requested asset equals the peg | EV-0042; R1-0018; XFI-0024 | Supports F-1232 |
| CMD-0047 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after audit ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0043 | Audit artifact syntax/sync sanity check |
| CMD-0048 | 2026-05-12 | SDEX historical decoder audit across `internal/sources/sdex`, dispatcher op routing, backfill path, and protocol/discovery docs | `decodeClaimAtom` rejects `ClaimAtomTypeV0`, but `Decoder.Decode` suppresses per-claim errors and continues, so the dispatcher never increments `DecodeErrors`; this silently drops pre-P17/pre-P18 SDEX historical fills despite docs/backfill claims that SDEX trades go back to genesis | EV-0044; XFI-0025 | Supports F-1233 |
| CMD-0049 | 2026-05-12 | Oracle partial-coverage audit across Reflector, Redstone, Band decoders, dispatcher error metrics, decoder-stats docs, and oracle coverage runbooks | mixed oracle batches skip unknown symbols/feed IDs while returning success; dispatcher decode-error metrics only increment on returned errors, so partial coverage drift is invisible unless the entire batch has no known entries | EV-0045; XFI-0026 | Supports F-1234 |
| CMD-0050 | 2026-05-12 | External CEX streamer parse-error audit across Binance, Kraken, Bitstamp, Coinbase stream loops, external runner metrics, and external connector docs | all four streamers skip malformed/unknown frames without incrementing `SourceDecodeErrorsTotal` or another parse-error counter; docs claim external connectors contribute to the same decode-error budget | EV-0046; XFI-0027 | Supports F-1235 |
| CMD-0051 | 2026-05-12 | Supply refresher freshness audit across aggregator/ops ledger resolution, storage supply readers, LCM readers, and Timescale at-or-before queries | supply snapshots use max ingestion cursor ledger as the snapshot ledger, while all supply component readers select latest rows at or before that ledger without enforcing component freshness or observer cursor alignment | EV-0047; XFI-0028 | Supports F-1236 |
| CMD-0052 | 2026-05-12 | CoinMarketCap identity audit across verified-currency catalogue, CMC poller, indexer aggregator-pair wiring, and CMC tests | catalogue stores CMC numeric IDs, but indexer passes only ticker-based pairs and CMC poller queries `symbol=` then takes the first returned coin for duplicate tickers | EV-0048; XFI-0029 | Supports F-1237 |
| CMD-0053 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after findings F-1233 through F-1237 | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0049 | Audit artifact syntax/sync sanity check |
| CMD-0054 | 2026-05-12 | Governance/workflow root audit across `.github/**`, `CODEOWNERS`, `CONTRIBUTING.md`, `SECURITY.md`, `.gitignore`, `.gitleaks.toml`, `.golangci.yml`, `.spectral.yaml`, and `commitlint.config.js` | reviewed workflow triggers, deploy/release permissions, generated-doc publish paths, PR/release templates, secret ignore rules, lint/security configs, and contributor/security policy claims | EV-0050; XFI-0010 | Closed reviewed governance/workflow inventory rows; no new finding beyond existing governance/release/dependency findings |
| CMD-0055 | 2026-05-12 | GitHub hosted dependency-security probes: `gh api repos/RatesEngine/rates-engine/vulnerability-alerts` and `gh api repos/RatesEngine/rates-engine/dependabot/alerts --paginate` | vulnerability alerts API returned disabled; Dependabot alerts API returned disabled/forbidden with required scope note | EV-0051; XFI-0005 | Extends F-1207 with hosted alert-control evidence |
| CMD-0056 | 2026-05-12 | `go test ./cmd/...` | passed for aggregator, API, indexer, migrate, ops, and SLA probe command packages | EV-0052 | Command-surface regression baseline |
| CMD-0057 | 2026-05-12 | Current SEP-10 and Stripe command/API wiring audit across `cmd/ratesengine-api/main.go`, `internal/auth/sep10/**`, `internal/api/v1/stripe_webhook.go`, and platform billing stores | SEP-10 now supports Redis replay guarding but still permits guardless operation when Redis is absent; Stripe webhook now has Postgres event dedupe/audit seams but still mutates only Redis key records and does not mirror subscriptions/platform keys | EV-0053; XFI-0011; XFI-0013 | Corrects stale F-1217/F-1219 wording to current source reality |
| CMD-0058 | 2026-05-12 | API Redis optionality/streaming subscriber audit across `cmd/ratesengine-api/main.go`, `internal/api/streaming/redispub`, config docs, and stream docs | API constructs `streaming.NewHub(0)` unconditionally, then calls `redispub.NewSubscriber(rdb, ...)` whenever `hub != nil`; `NewSubscriber` rejects nil Redis, despite API comments/docs saying Redis is optional and empty streaming pairs merely yield heartbeats | EV-0054; XFI-0030 | Supports F-1238 |
| CMD-0059 | 2026-05-12 | Full command-surface source/test audit across `cmd/ratesengine-{api,indexer,aggregator,migrate,ops,sla-probe}` plus hostile-pattern scan for progress counters, exits, panics, `int64`, and skipped tests | command packages pass tests; API/indexer/aggregator/migrate/SLA findings are covered by existing rows; ops WASM history/extraction tools expose a new modulo-by-zero crash when `-progress-every 0` is supplied | EV-0055; XFI-0031 | Supports F-1239 and closes `cmd/**` inventory rows |
| CMD-0060 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after command-surface ledger and inventory updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0056 | Audit artifact syntax/sync sanity check |
| CMD-0061 | 2026-05-12 | Docker packaging audit across all six `docker/ratesengine-*.Dockerfile` files, `docker/README.md`, `go.mod`, CI/release Go setup, `Makefile` docker targets, and Docker Compose docs | all Dockerfiles build with `golang:1.26-alpine`, while `go.mod` pins `go 1.25.10`, CI/release use `go-version-file: go.mod`, and Docker README still claims `golang:1.25-alpine` plus binary equivalence to release builds; migrate image missing migrations remains covered by F-1227 | EV-0057; XFI-0032 | Supports F-1240 and closes `docker/**` inventory rows |
| CMD-0062 | 2026-05-12 | Migration/schema audit across `migrations/*.sql`, `migrations/README.md`, and store/runtime references for newly added 0021-0029 schema families | the tree has 29 dense paired migration families through `0029_drop_unused_blend_jsonb_gin_indexes`, but `migrations/README.md` still stops its "Current migrations" table at `0015` despite instructing maintainers to update it when a migration lands | EV-0058; XFI-0033 | Supports F-1241 |
| CMD-0063 | 2026-05-12 | `go test -tags=integration ./test/integration -run TestMigrationsRoundTrip -count=1` | passed; the TimescaleDB integration test applied every current migration and rolled them all back cleanly | EV-0059 | Positive execution evidence for W12 migration SQL integrity |
| CMD-0064 | 2026-05-12 | Per-source contribution storage audit across `cmd/ratesengine-aggregator/contribution_sink.go`, `internal/aggregate/{vwap,orchestrator}/`, `internal/storage/timescale/price_source_contributions.go`, and migration 0026 | aggregator production wiring always writes contribution rows, the schema/commentary reserves `volume_usd` for the per-source USD tooltip, the storage API accepts it, but the sink never computes or forwards any `VolumeUSD` value, so every persisted row receives SQL `NULL` | EV-0060; XFI-0034 | Supports F-1242 |
| CMD-0065 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after W12 migration/contribution ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0061 | Audit artifact syntax/sync sanity check |
| CMD-0066 | 2026-05-12 | Classic asset registry writer audit across `internal/storage/timescale/{trades,asset_registry}.go`, registry read APIs, and integration tests | `InsertTrade` calls `registerClassicAssetSeen` after every stored trade, but `assetRegistryDedupe` turns every same-process repeat asset into an early return before the SQL `LEAST`/`GREATEST`/`observation_count + 1` upsert runs; existing integration coverage seeds `classic_assets` directly and does not exercise the writer contract | EV-0062; XFI-0035 | Supports F-1243 |
| CMD-0067 | 2026-05-12 | Audit-scope drift check: `git rev-parse HEAD`; `git ls-files ':!:docs/audit-2026-05-12-codex/**' | wc -l`; `tail -n +2 docs/audit-2026-05-12-codex/inventory/file-coverage.tsv | wc -l`; migration 0029 history/inventory comparison | current `HEAD` is `6e873cac...`, newer than the original anchor `80c57e...`; current tracked tree excluding this Codex audit directory has 1,869 rows while the generated inventory still has 1,747; tracked `migrations/0029_*` files exist in Git but are absent from the Codex inventory | EV-0063 | Audit-control caveat for literal whole-repo closure |
| CMD-0068 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after classic-registry finding F-1243 and audit-scope caveat updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0064 | Audit artifact syntax/sync sanity check |
| CMD-0069 | 2026-05-12 | Current-source legacy-route reconciliation across explorer/status/examples/scripts plus live `bash scripts/dev/audit-public-api.sh` against `https://api.ratesengine.net` | live explorer/status callers and `r1-smoke.sh` have been migrated off `/v1/coins` and `/v1/currencies`; the public audit script still probes those removed paths and now fails 5/37 checks because R1 returns 404 for all five legacy routes | EV-0065; EV-0066; XFI-0002 | Supersedes stale portions of F-1202/F-1204 evidence |
| CMD-0070 | 2026-05-12 | `./scripts/dev/docs-api.sh` on current `HEAD` | regenerated API reference without leaving a diff in `docs/reference/api/rates-engine.v1.yaml` | EV-0067 | Current generated reference is clean |
| CMD-0071 | 2026-05-12 | `cd web/explorer && pnpm generate:api`; follow-up `git diff --stat -- docs/reference/api/rates-engine.v1.yaml web/explorer/src/api/types.ts` | explorer type generation leaves a 362-line diff in `web/explorer/src/api/types.ts`, including new dashboard webhook paths and updated legacy-route wording; docs reference remains clean | EV-0067 | Keeps F-1203 open on current `HEAD` |
| CMD-0072 | 2026-05-12 | Dashboard customer-webhook security audit across `internal/api/v1/dashboardwebhooks`, `internal/platform/{webhook,postgresstore/webhook_store}.go`, `internal/customerwebhook/worker.go`, migration 0027, OpenAPI docs, and tests | create flow stores the generated signing secret bytes directly in `secret_hash` while comments/API prose promise hash/never-persist plaintext semantics; URL validation accepts any `https://` string and the worker posts with a default redirect-following client, with no private-IP/DNS-rebinding/redirect protections in tests or code | EV-0068; EV-0069; XFI-0036; XFI-0037 | Supports F-1244/F-1245 |
| CMD-0073 | 2026-05-12 | `go test ./internal/api/v1/dashboardwebhooks ./internal/customerwebhook ./internal/platform/postgresstore` | dashboard webhook handlers and customer webhook worker tests passed; Postgres webhook store package has no direct unit tests | EV-0070 | Positive regression baseline for current webhook slice |
| CMD-0074 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after F-1244/F-1245 and latest reconciliation updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0071 | Audit artifact syntax/sync sanity check |
| CMD-0075 | 2026-05-12 | Webhook docs-truth reconciliation across `docs/reference/api-design.md`, generated API reference, customer webhook runbook, and shipped webhook handlers/worker | the design reference still states "Webhook callbacks? Not in v1. Customers who want push use SSE," while current OpenAPI/reference docs ship `/v1/dashboard/webhooks*`, the API mounts handlers, the worker runs in `ratesengine-api`, and the runbook documents operational recovery | EV-0072; XFI-0038 | Supports F-1246 |
| CMD-0076 | 2026-05-12 | Customer-webhook queue concurrency audit across worker startup, `ListPendingDeliveries`, completion updates, and multi-region docs | every API process with dashboard webhooks enabled starts a delivery worker; the queue reader is a plain due-row `SELECT ... LIMIT` with no claim/lease/`FOR UPDATE SKIP LOCKED`; delivery completion updates do not guard against another worker already acting on the same row; the worker comment explicitly limits correctness assumptions to "operator runs one worker" despite multi-region rollout docs | EV-0073; XFI-0039 | Supports F-1247 |
| CMD-0077 | 2026-05-12 | Dashboard webhook quota-concurrency audit across `HandleCreate`, `checkQuota`, `WebhookStore.CreateWebhook`, migration 0027 constraints, and handler tests | the advertised 10-webhook/account cap is enforced by `ListWebhooksForAccount` -> `len(hooks)` -> later insert, with no transactional lock, unique/exclusion/check constraint, or store-level compare-and-insert guard; tests cover only sequential quota fill, so concurrent creates can pass the pre-check together and exceed the cap | EV-0074; XFI-0040 | Supports F-1248 |
| CMD-0078 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after webhook quota finding F-1248 and XFI-0040 ledger updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0075 | Audit artifact syntax/sync sanity check |
| CMD-0079 | 2026-05-12 | Customer-webhook producer audit: repo-wide `EnqueueDelivery`/event-type searches plus shipped docs/runbook/API worker reconciliation | production code defines webhook event enums, validates subscriptions, starts the delivery worker, documents queue fan-out, and has a runbook for failed deliveries, but no non-test production caller ever invokes `EnqueueDelivery` or otherwise fans incidents/anomalies/divergence into pending delivery rows | EV-0076; XFI-0041 | Supports F-1249 |
| CMD-0080 | 2026-05-12 | `./scripts/ci/lint-docs.sh` after F-1249, XFI-0041, and remediation R-1247 updates | passed all docs/config/API/metrics/ADR/runbook/Prometheus checks | EV-0077 | Audit artifact syntax/sync sanity check |
| CMD-0081 | 2026-05-12 | Inventory refresh/merge against current `HEAD`: reran `inventory/generate.sh`, preserved prior row statuses from the pre-refresh TSV, then reconciled tracked-file and row counts | current tracked tree excluding this audit directory is `1,869`; refreshed `file-coverage.tsv` now also has `1,869` rows while preserving prior review states (`done=64`, `in_progress=35`, `todo=1770`) | EV-0078 | Resolves the execution-time inventory-count drift documented by EV-0063 while leaving per-file closure open |
| CMD-0082 | 2026-05-12 | Durable freeze-event dedupe audit across `freeze.Writer`, `timescale.FreezeEventSink.RecordFreeze`, the `freeze_events` insert SQL, and current integration tests | open-row dedupe is asserted only serially; production SQL uses unlocked `WHERE NOT EXISTS` plus a PK on `(asset_id, quote_id, frozen_at)`, so concurrent same-pair callers with different timestamps can both insert open rows despite comments claiming same-transaction protection | EV-0079; XFI-0042 | Supports F-1250 |
| CMD-0083 | 2026-05-12 | FX-derived USD-volume audit across indexer wiring, `VWAPUSDFXResolver`, trade enrichment, and targeted integration test `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1` | targeted integration test failed: seeded EURC/USDC VWAP at trade time was rejected with `ok=false`; source review shows freshness is checked as `clock().Sub(observedAt)` instead of relative to the requested trade timestamp, and `Freshness: 0` docs disagree with constructor behavior | EV-0080; XFI-0043 | Supports F-1251 |
| CMD-0084 | 2026-05-12 | Post-pass audit-control sanity: `./scripts/ci/lint-docs.sh`; TSV status roll-up after marking webhook/freeze/FX files reviewed | docs lint passed; refreshed inventory now reports `done=76`, `in_progress=35`, `todo=1758` | EV-0081 | Confirms ledger syntax and coverage-progress bookkeeping after F-1250/F-1251 |
| CMD-0085 | 2026-05-12 | Multi-region cutover command reconciliation across `docs/operations/multi-region-cutover.md`, `scripts/dev/verify-cross-region.sh`, and `Makefile` | runbook pre-flight names `make verify-cross-region`, but repository search finds no Makefile target by that name; only direct script/binary invocations exist | EV-0082; XFI-0044 | Supports F-1252 |
| CMD-0086 | 2026-05-12 | Redis ACL deployment coupling audit across Sentinel ACL/server templates, archival-node application TOML template, Redis config docs, and `redisclient.Build` | lockdown renders `user default off` plus `user ratesengine`, while application config/templates never set `storage.redis_username`; the client builder preserves empty username by default, so the rendered binaries continue authenticating on the disabled default-user path | EV-0083; XFI-0045 | Supports F-1253 |
| CMD-0087 | 2026-05-12 | Redis ACL keyspace reconciliation across `users.acl.j2`, active Redis key builders, ADR-0007 cache-schema notes, and API runtime wiring | the lockdown ACL names `~ratelimit:*` and `~subscriber:*`, but active code writes `rl:*` and `sub:*`; it also omits active `signup:email:*`, `sep10:seen:*`, `usage:*`, `assets:list:*`, and `markets:list:*` families even though current API/auth/catalogue code uses them | EV-0084; XFI-0046 | Supports F-1254 |
| CMD-0088 | 2026-05-12 | Post-pass audit-control sanity after Redis ACL finding F-1254: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=88`, `in_progress=38`, `todo=1743` | EV-0085 | Confirms ledger syntax and coverage bookkeeping after the ACL keyspace pass |
| CMD-0089 | 2026-05-12 | Dashboard first-login provisioning race audit across `HandleLogin`, magic-link token consumption, `signupNewUser`, account/user stores, and migration 0027 uniqueness constraints | multiple valid magic links can be minted for one new email; each token is individually single-use, but concurrent callbacks both branch on `GetUserByEmail` not-found, each creates an account, and the loser later hits the `users_email_idx` unique constraint after `signupNewUser` has already persisted a suffixed fallback account | EV-0086; XFI-0047 | Supports F-1255 |
| CMD-0090 | 2026-05-12 | `go test ./internal/api/v1/dashboardauth ./internal/platform/postgresstore` after the first-login provisioning race review | dashboard-auth tests passed; `internal/platform/postgresstore` has no direct unit tests; current coverage does not exercise concurrent same-email first-login callbacks across real stores | EV-0087 | Positive baseline context for F-1255 |
| CMD-0091 | 2026-05-12 | Post-pass audit-control sanity after first-login provisioning finding F-1255: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=95`, `in_progress=39`, `todo=1735` | EV-0088 | Confirms ledger syntax and coverage bookkeeping after the auth provisioning pass |
| CMD-0092 | 2026-05-12 | Current-head dashboard key budget reconciliation across `dashboardkeys.HandleCreate`, `platform.Tier.MaxRateLimitPerMin`, dashboard key tests, UI form copy, and OpenAPI `CreateKeyRequest` docs | persisted dashboard-minted key rate limits are now clamped to tier ceilings and `go test ./internal/api/v1/dashboardkeys` passes that regression path, closing the security claim in F-1212; the dashboard still says free-tier default is 1000, exposes `max={100000}`, and OpenAPI documents `Default 1000` with `maximum: 100000` without explaining tier-specific clamping | EV-0089; EV-0090; XFI-0048 | Supersedes F-1212's current-source status and supports F-1256 |
| CMD-0094 | 2026-05-12 | Dashboard API-key quota concurrency audit across `HandleCreate`, `checkQuota`, `APIKeyStore.Create`, migration 0027 constraints, and current handler tests | the 25-active-key/account cap is enforced by `ListForAccount` -> active-count -> later insert, with no transaction, compare-and-insert guard, or schema invariant limiting active rows per account; tests cover only serial over-quota rejection | EV-0092; XFI-0049 | Supports F-1257 |
| CMD-0095 | 2026-05-12 | Post-pass audit-control sanity after dashboard-key reconciliation F-1212/F-1256/F-1257: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up | docs lint passed; tracked scope remains `tracked=1869`, `rows=1869`; refreshed inventory now reports `done=97`, `in_progress=38`, `todo=1734` | EV-0093 | Confirms ledger syntax and coverage bookkeeping after the dashboard-key pass |
| CMD-0096 | 2026-05-12 | Redis-optional usage-tracker wiring audit across `cmd/ratesengine-api/main.go`, `middleware.UsageTracker`, and `usage.Counter` | API startup comments say usage tracking degrades to a no-op when Redis is nil, but `usage.New(rdb)` is created unconditionally, the middleware checks only `counter == nil`, and `Counter.Increment`/`Read` immediately dereference `c.rdb`; Redis-less authenticated traffic can therefore panic in the usage middleware instead of degrading cleanly | EV-0094; XFI-0050 | Supports F-1258 |
| CMD-0097 | 2026-05-12 | `/v1/account/usage` contract reconciliation across current runtime wiring, handler comments, OpenAPI/reference docs, architecture docs, and `go test ./internal/api/v1 ./internal/usage` | current API wiring supplies `UsageTracker` + `UsageReader` and the handler returns Redis-backed daily rows when available; tests pass for current packages, but OpenAPI, generated reference YAML, Postman reference, API-design doc, architecture inventory, and an in-file handler comment still describe the endpoint as an always-empty placeholder | EV-0095; XFI-0051 | Supports F-1259 |
| CMD-0098 | 2026-05-12 | Current-workspace remediation reconciliation for SEP-10 replay guard optionality, Redis-less stream startup, customer-webhook SSRF, and webhook design-doc truth; then `go test ./internal/api/v1/dashboardwebhooks ./internal/customerwebhook ./cmd/ratesengine-api` | current workspace now fails startup for `auth_mode=sep10` without Redis, gates the Redis pub/sub stream subscriber on `rdb != nil`, validates webhook destinations against internal/private targets with send-time guarded dialing + redirect refusal, and updates `docs/reference/api-design.md` to say webhook callbacks shipped; targeted packages pass | EV-0096 | Marks F-1217, F-1238, F-1245, and F-1246 fixed in current workspace |
| CMD-0099 | 2026-05-12 | Post-pass control checkpoint after usage/runtime reconciliation and live scope drift: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; `git rev-parse HEAD`; `git status --short` review | docs lint passed; scope reconciles at `tracked=1870`, `rows=1870`; current `HEAD` is `9f68ef9a...`; the audit worktree currently has 15 dirty paths, including concurrent non-audit edits outside this directory | EV-0097 | Confirms audit bookkeeping after `internal/customerwebhook/ssrf.go` entered tracked scope |
| CMD-0100 | 2026-05-12 | Current-head Wave 5 remediation reconciliation across CEX streamer decode metrics, webhook delivery claiming, Redis ACL username/keyspace rollout, and the attempted webhook-account quota fix; then `go test ./internal/api/v1/dashboardwebhooks ./internal/platform/postgresstore ./internal/sources/external/binance ./internal/sources/external/bitstamp ./internal/sources/external/coinbase ./internal/sources/external/kraken` | settled `HEAD=7c9e79ae...` now increments `SourceDecodeErrorsTotal` in all four CEX streamers, atomically claims webhook delivery rows with `FOR UPDATE SKIP LOCKED` plus a 5-minute lease, renders `redis_username` under ACL lockdown, and aligns the ACL key families with live namespaces; targeted packages pass. The new webhook-cap `WITH current_count ... INSERT` still performs an unlocked count under PostgreSQL MVCC, so concurrent creates can still overrun the ten-row ceiling and `F-1248` remains open. | EV-0098 | Marks F-1235, F-1247, F-1253, and F-1254 fixed while preserving F-1248 |
| CMD-0101 | 2026-05-12 | Current-head Wave 7 reconciliation across push-to-main CI, web advisory gates, and signup duplicate-reservation flow; then `go test ./internal/api/v1 ./internal/auth` | settled `HEAD=27343a46...` now runs canonical `ci` on pushes to `main`, closing F-1231; all three web CI jobs now execute `pnpm audit --audit-level high`, narrowing but not closing F-1207 because the vulnerable Next pins and hosted alert posture remain; signup now reserves `signup:email:*` with `SETNX` before minting when Redis tracking is wired, closing only the same-email race slice of F-1218 while unverified plaintext key minting and tracker-nil duplicate behavior remain. Targeted API/auth tests pass. | EV-0099 | Fixes F-1231; partial current-head reconciliation for F-1207/F-1218 |
| CMD-0102 | 2026-05-12 | Current-workspace policy-enforcement reconciliation for F-1226 across `cmd/ratesengine-api/main.go`, `internal/api/v1/server.go`, `internal/api/v1/middleware/keypolicy.go`, `internal/auth/{subject,apikey_postgres,apikey_redis}.go`, and `go test ./internal/api/v1/middleware ./internal/auth` | the in-progress workspace adds `KeyPolicy` production wiring plus IP/referer/permission fields on Postgres-served subjects, and the targeted middleware/auth packages pass. It still does not close F-1226: `cacheStore` writes the legacy `APIKeyRecord` shape, `cacheLookup` rebuilds subjects without policy fields, so a cache hit can silently bypass policy that a cache miss enforced; monthly quota remains unenforced and `TouchUsage` still has no production caller. `internal/api/v1/middleware/keypolicy.go` is currently untracked in the shared workspace. | EV-0100 | Preserves F-1226 as open despite partial remediation in flight |
| CMD-0103 | 2026-05-12 | Post-pass control checkpoint after committed Wave 8 policy files entered tracked scope: `./scripts/ci/lint-docs.sh`; tracked-vs-inventory count check; TSV status roll-up; `git rev-parse HEAD`; `git status --short` review | docs lint passed; current `HEAD` is `8a61c17e...`; tracked scope now reconciles at `tracked=1872`, `rows=1872` after adding `internal/api/v1/middleware/keypolicy.go` and `keypolicy_test.go` to the TSV; current roll-up is `done=99`, `in_progress=50`, `todo=1723`; only the two inventory-maintenance files were dirty during the snapshot. | EV-0101 | Restores audit inventory parity after Wave 8 tracked-scope growth |
| CMD-0104 | 2026-05-12 | Settled Wave 9 reconciliation across CMC ID disambiguation, FX trade-time freshness, and concurrent first-login callback handling; `go test ./internal/sources/external/coinmarketcap ./internal/storage/timescale ./internal/api/v1/dashboardauth`; repo search for committed CMC ID-mode fixtures / ops wiring; `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1`; tracked-vs-inventory count check at `HEAD=82a6052c...` | package-level CMC/timescale/dashboard-auth tests pass and tracked scope remains reconciled at `tracked=1872`, `rows=1872`. The Wave 9 fixes are still partial: ops verification still omits `CMCIDs` and no committed ID-mode CMC response fixture exists; the FX integration now finds the historical rate but fails on `USDPriceAt = "1.08500000000000000000", want "1.085"`; the first-login handler now reloads the winning user but still leaves the speculative loser account row orphaned. | EV-0102 | Narrows but preserves F-1237/F-1251/F-1255 |
| CMD-0105 | 2026-05-12 | In-flight Wave 10 workspace reconciliation across deploy migrations, contribution-history USD volume, dashboard-key quota enforcement, Redis-less usage handling, usage docs, and the SDEX V0 decoder; `git diff` review of the changed files; `go test ./cmd/ratesengine-aggregator ./cmd/ratesengine-api ./internal/api/v1/dashboardkeys ./internal/aggregate/orchestrator ./internal/usage ./internal/platform/postgresstore`; `go test ./internal/sources/sdex`; `./scripts/ci/lint-docs.sh`; repo searches across generated usage docs and deploy workflow/playbook wiring | targeted package tests and docs lint pass. The workspace still leaves `F-1220`, `F-1242`, `F-1257`, `F-1258`, and `F-1259` open: the deploy workflow stages migrations but relies on `ansible.posix.synchronize` while installing only `ansible-core`; contribution USD rows split a pre-filter total by post-filter weights; dashboard-key quota uses an unlocked count CTE; Redis-less `/v1/account/usage` can still dereference `usageReaderAdapter{c:nil}`; generated/reference usage docs and handler comments remain stale, and the new OpenAPI source misstates `/v1/healthz` checks. The SDEX V0 decode branch itself now decodes and its package tests pass, so `F-1233` should be reconsidered when that workspace settles. | EV-0103 | Partial-remediation evidence and unresolved-risk preservation for the Wave 10 workspace |
| CMD-0106 | 2026-05-12 | Follow-on shared-workspace reconciliation for CI action-pinning lint and the contribution-volume rewrite; `bash scripts/ci/lint-actions-pinning.sh`; `PR_DIFF=1 bash scripts/ci/lint-actions-pinning.sh`; code review of `.github/workflows/ci.yml`, `scripts/ci/lint-actions-pinning.sh`, `cmd/ratesengine-aggregator/contribution_sink.go`, `internal/aggregate/orchestrator/orchestrator.go`, and `internal/canonical/trade.go`; `go test ./cmd/ratesengine-aggregator ./internal/aggregate/orchestrator` | the new lint exits zero but warns on 12 current mutable third-party action tags in both normal and PR-diff modes, so `F-1216` narrows rather than closes. The contribution rewrite now carries per-trade USD values keyed by stable `Trade.ID()` and sums them after filtering, which addresses the specific pre-filter/post-filter mismatch recorded in `EV-0103`; targeted aggregator/orchestrator tests pass, but the change is still in-flight at this checkpoint. | EV-0104 | Narrows F-1216 and flags F-1242 for re-evaluation after the latest workspace rewrite |
| CMD-0107 | 2026-05-12 | Settled Wave 11 / current shared-workspace reconciliation across contribution-volume closure, SDEX V0 closure, the new customer-webhook fan-out producer slice, and a fresh aggregator min-volume adversarial pass; `git diff` review of `cmd/ratesengine-aggregator/main.go`, `internal/customerwebhook/fanout.go`, `internal/platform/{webhook,postgresstore/webhook_store}.go`, and `internal/storage/timescale/freeze_events.go`; repo searches for declared webhook-event producers; `go test ./cmd/ratesengine-aggregator ./internal/customerwebhook ./internal/storage/timescale ./internal/platform/postgresstore`; `go test ./internal/sources/sdex ./cmd/ratesengine-aggregator ./internal/aggregate/orchestrator` | committed `HEAD=e787c11b...` contains the contribution rewrite, and current code now attributes `price_source_contributions.volume_usd` from post-filter per-trade USD survivors, closing the specific `F-1242` mismatch; the SDEX V0 decode branch is committed and targeted tests pass, closing the original V0-drop defect in `F-1233`. The concurrent webhook workspace now adds a real `anomaly.freeze` producer/fan-out path, but no producer exists for `incident.sev1`, `incident.resolved`, or `divergence.firing`, so `F-1249` narrows rather than closes. Separately, the aggregator still evaluates `dropForMinUSDVolume` against `usdVolume` computed before class/outlier filtering, even though the config/comment contract says the threshold is post-filter; that can publish a surviving thin window whose pre-filter volume alone exceeded the manipulation floor. | EV-0105 | Closes F-1233/F-1242, narrows F-1249, and supports new F-1260 |
| CMD-0108 | 2026-05-12 | Wave 13 / current-worktree reconciliation for customer-webhook producer coverage and the in-flight `F-1236` supply-ledger remediation attempt; code review of `internal/divergence/worker.go`, `internal/divergence/worker_test.go`, `cmd/ratesengine-aggregator/main.go`, `cmd/ratesengine-ops/supply.go`, and repo searches for remaining incident-event producers; `go test ./internal/divergence`; `go test ./internal/divergence ./cmd/ratesengine-aggregator ./internal/customerwebhook ./internal/storage/timescale ./internal/platform/postgresstore`; `go test ./cmd/ratesengine-ops`; `./scripts/ci/lint-docs.sh` | settled `HEAD=60c8a2e4...` now wires `divergence.firing` through an edge-triggered warning hook and fan-out payload builder, with `./internal/divergence` tests passing. `F-1249` therefore narrows again: only `incident.sev1` and `incident.resolved` remain producer-less, and integration/producer observability is still absent. The current uncommitted `F-1236` remediation attempt does not close the supply freshness finding: both `cmd/ratesengine-aggregator` and `cmd/ratesengine-ops` fail to build because the patch filters cursors via `c.Name`, but `timescale.Cursor` exposes `Source`/`Sub`, not `Name`. Docs lint remains green. | EV-0106 | Narrows F-1249 to incident producers and preserves F-1236 as open after a non-building workspace attempt |
| CMD-0109 | 2026-05-12 | Current-workspace Stripe remediation audit across `internal/api/v1/stripe_webhook.go`, production API wiring in `cmd/ratesengine-api/main.go`, webhook tests, and platform account/billing stores; repo search for `StripePlatformBridge` production use; `go test ./internal/api/v1` | the new Stripe helper introduces a platform-side bridge that can upsert subscriptions and mutate account tier, but no production wiring sets `StripeWebhookConfig.Platform`, no existing webhook test exercises the new bridge, and the helper still does not update Postgres dashboard API-key limits. The package test suite passes, but `F-1219` remains open because the paid-upgrade side effect is still not end-to-end on the runtime path. | EV-0107 | Preserves F-1219 after partial uncommitted remediation |
| CMD-0110 | 2026-05-12 | Settled Wave 15 / current-worktree reconciliation across Stripe subscription-event handling and the supply freshness-gate follow-up; code review of `internal/api/v1/stripe_webhook.go`, `cmd/ratesengine-api/main.go`, `internal/supply/{refresher,supply,classic,sep41,storage_classic_reader,storage_sep41_reader}.go`; repo searches for production `StripePlatformBridge` wiring, committed `customer.subscription.*` webhook tests, and `MinComponentLedger` producers; `go test ./internal/api/v1 ./internal/supply ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | settled `HEAD=c82c1602...` adds `customer.subscription.updated`/`deleted` webhook handling plus the supply stale-component rejector, and the targeted API/supply/command packages pass. `F-1219` remains open because production API wiring still never sets `StripeWebhookConfig.Platform`, the webhook suite has no bridge/subscription-event assertions, and Postgres dashboard-key limits remain outside the side effect. `F-1236` remains open because production readers still do not populate `MinComponentLedger`; the current dirty `classic.go`/`sep41.go` follow-up only threads a freshness field that real storage-backed readers continue to leave zero. | EV-0108 | Preserves F-1219/F-1236 after Wave 15 narrows but does not close them |
| CMD-0111 | 2026-05-12 | Settled Wave 16 reconciliation plus scope refresh; reviewed `HEAD=65197ec0...`, `internal/supply/{classic,sep41,storage_classic_reader,storage_sep41_reader}.go`, the new `docs/operations/github-actions-sha-pinning.md`, tracked-vs-inventory parity, and `./scripts/ci/lint-docs.sh` | Wave 16 commits the classic/SEP41 `MinComponentLedger` pass-through and adds one new operations document, increasing non-audit tracked scope to `1,875`. `F-1236` remains open because storage-backed readers still never populate that freshness field, so real snapshots continue down the zero-value bypass. Audit inventory was refreshed manually to re-establish `tracked=1875`, `rows=1875`, with roll-up `done=105`, `in_progress=51`, `todo=1719`; docs lint passes. | EV-0109 | Preserves F-1236, records Wave 16 scope drift, and restores inventory parity |
| CMD-0112 | 2026-05-12 | Current-workspace classic-supply freshness audit across `internal/storage/timescale/classic_supply_observations.go`, `internal/supply/storage_classic_reader.go`, `internal/supply/storage_classic_reader_test.go`, repo searches for remaining SEP41/XLM freshness producers, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | the workspace adds `MinClassicComponentLedger`, wires it through `ClassicSupplyStore`/`StorageClassicSupplyReader`, and the targeted tests pass. `F-1236` remains open because the producer work is classic-only, SEP41/XLM still have no corresponding freshness source, and classic query failures intentionally degrade to `MinComponentLedger=0`, preserving the permissive bypass for that snapshot. | EV-0110 | Narrows but preserves F-1236 during the classic-only producer attempt |
| CMD-0113 | 2026-05-12 | Settled Wave 17 / current-workspace SEP41 freshness audit across `HEAD=6819e7dc...`, `cmd/ratesengine-aggregator/main.go`, `internal/storage/timescale/sep41_supply_events.go`, `internal/supply/storage_sep41_reader.go`, repo searches for XLM freshness producers, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | Wave 17 commits the classic-storage `MinClassicComponentLedger` producer. The current SEP41 follow-up adds `MinSEP41ComponentLedger` storage/reader plumbing plus aggregator adaptation, but `./internal/supply` now fails to build because `fakeSEP41Store` lacks the new interface method. Timescale, aggregator, and ops package tests pass; XLM still has no analogous freshness producer. | EV-0111 | Preserves F-1236 after Wave 17 and records the non-building SEP41 follow-up |
| CMD-0114 | 2026-05-12 | Settled Wave 18 / current Stripe webhook workspace reconciliation across `HEAD=fb0b3073...`, supply package verification, `internal/api/v1/stripe_webhook.go`, production API wiring in `cmd/ratesengine-api/main.go`, repo search for production `StripePlatformBridge` use, and `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | Wave 18 commits SEP41 storage freshness production and the targeted supply/timescale/command tests pass. The current Stripe workspace adds `invoice.paid` period refresh, but `F-1219` remains open because production wiring still never sets `StripeWebhookConfig.Platform`, committed bridge/event coverage is still not closure-grade, and Postgres dashboard-key limits remain outside the side effect. | EV-0112 | Records Wave 18 supply progress and preserves F-1219 after the new invoice path |
| CMD-0115 | 2026-05-12 | Late live-R1 reconciliation for firewall, service listeners, timers, and Caddy drift: `systemctl list-timers --all`; `systemctl list-unit-files`; `systemctl is-enabled sla-probe.timer archive-completeness.timer verify-archive-tier-a.timer supply-snapshot.timer`; `systemctl is-active nftables`; `ufw status`; `nft list ruleset`; `ss -ltnp`; `/etc/nftables.conf`; `/etc/caddy/Caddyfile`; public `curl -skI https://api.ratesengine.net/metrics`; public `nc` probes for `9000`, `9001`, `9090`, `3100`, `9080`, `9100`, `6061`, `38563`, `11625`, `11626`, `11725`, and `11726` | `archive-completeness`, `verify-archive-tier-a`, and `supply-snapshot` timers are enabled; `sla-probe.timer` remains disabled. nftables is active, UFW inactive, and the reviewed internal-service ports now time out externally. Public `/metrics` now returns `404` under the hardened live Caddyfile. Residual firewall drift remains: live nftables explicitly accepts `{11625,11626,11725,11726}`, `stellar-core` listens on `0.0.0.0:11726`, and the public `11726/tcp` probe succeeds. | EV-0113 | Narrows F-1201/F-1205 and marks F-1223 closure-grade on live R1 |
| CMD-0116 | 2026-05-12 | Late web/GitHub dependency-security reconciliation: package-version scans across the three web `package.json` files; `.github/dependabot.yml` review; `pnpm audit --audit-level high` in explorer/dashboard/status; `gh api repos/RatesEngine/rates-engine/vulnerability-alerts`; `gh api 'repos/RatesEngine/rates-engine/dependabot/alerts?state=open'` | All three apps now pin `next@15.5.18`; Dependabot has npm ecosystem entries for each web app; every high-severity `pnpm audit` run returns only one moderate advisory. Hosted GitHub repository controls remain disabled: vulnerability alerts return disabled/404 and Dependabot alerts return disabled/403. | EV-0114 | Narrows F-1207 to hosted dependency-alert posture |
| CMD-0117 | 2026-05-12 | Highest-priority current-source/runtime pass over residual auth/usage enforcement: reviewed `internal/auth/apikey_postgres.go`, `internal/api/v1/middleware/keypolicy.go`, `cmd/ratesengine-api/main.go` usage wiring, and `internal/api/v1/account.go`; ran `go test ./internal/api/v1 ./internal/auth ./internal/platform/...`; ran `go test ./cmd/ratesengine-aggregator ./internal/aggregate/...` | Targeted packages remain green. `F-1226` stays open because the Postgres validator's Redis cache row still sheds policy fields on cache hit, monthly quota enforcement remains absent, and `TouchUsage` still lacks production wiring. `F-1258` stays open because Redis-less startup now avoids the middleware panic path, but `UsageReader: usageReaderAdapter{c:nil}` still leaves `/v1/account/usage` able to dereference a nil inner counter. | EV-0115 | Preserves F-1226/F-1258 after direct current-head review |
| CMD-0118 | 2026-05-12 | Market-data parity reconciliation across the stablecoin USD-volume gate and since-inception history fallback: reviewed `internal/aggregate/orchestrator/orchestrator.go`, `internal/aggregate/orchestrator/orchestrator_test.go`, `internal/api/v1/history.go`, and `internal/api/v1/history_test.go`; ran `go test ./internal/aggregate/orchestrator ./internal/api/v1`; inspected R1 `/etc/ratesengine.toml`; checked live `native/fiat:USD` versus direct Circle-USDC since-inception history; read deployed API marker/version | The arithmetic defect in `F-1213` is fixed in current source and pinned by the classic-USDC `$10k` regression; R1 still carries `min_usd_volume=0` as an operator posture. `F-1225` stays open because source and tests now implement the fallback, yet public R1 still returns zero `native/fiat:USD` points while direct Circle-USDC history returns populated daily rows under a config that enables the peg list; deployed API reports `v0.5.0-rc.49`. | EV-0116 | Fixes F-1213 in source and narrows F-1225 to live rollout/runtime drift |
| CMD-0119 | 2026-05-12 | Current-head severe data-integrity/security pass across classic-asset registry freshness and dashboard webhook secret semantics: reviewed `internal/storage/timescale/asset_registry.go`, `internal/platform/webhook.go`, `internal/platform/postgresstore/webhook_store.go`, `internal/customerwebhook/worker.go`, `internal/api/v1/dashboardwebhooks/handlers.go`, `migrations/0027_platform_v1_schema.up.sql`, and webhook OpenAPI prose; ran `go test ./internal/storage/timescale ./internal/platform/postgresstore ./internal/customerwebhook` | `F-1243` remains open: `assetRegistryDedupe` still returns before the UPSERT that updates first/last/count semantics, and no closure-grade integration test covers repeat same-process observations. `F-1244` also remains open: `platform.CustomerWebhook` now truthfully comments that `SecretHash` carries the live HMAC key, but the DB column still stores those raw key bytes, the handler still says plaintext is never persisted, the store interface says rotate returns plaintext “not stored,” and OpenAPI/customer prose still implies once-only handling without making the persisted signing-key model explicit. | EV-0117 | Preserves F-1243/F-1244 after current-head review |
| CMD-0120 | 2026-05-12 | In-flight `F-1226` cache-parity reconciliation: reviewed the shared-workspace diff in `internal/auth/apikey_postgres.go`, `internal/auth/apikey_postgres_test.go`, `internal/auth/apikey_redis.go`, and `internal/auth/subject.go`; ran `go test ./internal/auth ./internal/api/v1/middleware` | The new patch extends `APIKeyRecord` with policy fields, round-trips IP CIDRs plus referer/permission entries through Redis, adds JSON tags for permission entries, and `TestPostgresValidator_CacheRoundTripsPolicy` passes. That closes the previously-recorded cache-hit policy shedding slice of `F-1226`, but the finding remains open because monthly quota enforcement and production `TouchUsage` / `last_used_*` updates are still absent. | EV-0118 | Narrows F-1226 after shared-workspace remediation |
| CMD-0121 | 2026-05-12 | SSE runtime reconciliation for `F-1228`: reviewed `cmd/ratesengine-api/main.go`, `internal/api/streaming/handler.go`, and middleware wrapper comments preserving `ResponseController`; ran `go test ./internal/api/v1 ./cmd/ratesengine-api`; executed an HTTP/1.1 R1-origin probe against `/v1/price/tip/stream?asset=native&quote=fiat:USD&window_seconds=60` with a 65s client ceiling | Current source now clears per-request write deadlines for SSE handlers via `http.ResponseController.SetWriteDeadline(time.Time{})`, and targeted packages pass. Live R1 still does not demonstrate closure-grade durability: the stream emits events through the first roughly 30 seconds, then exits before the next expected 5-second frame despite the 65-second client ceiling. | EV-0119 | Preserves F-1228 as source/live runtime drift after the source-side fix |
| CMD-0122 | 2026-05-12 | Highest-priority quota/migration reconciliation: reviewed the current advisory-lock diffs in `internal/platform/postgresstore/{webhook_store,apikey_store}.go` plus `test/integration/platform_postgres_stores_test.go`; ran `go test ./internal/api/v1/dashboardwebhooks ./internal/api/v1/dashboardkeys ./internal/platform/postgresstore`; reran `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1`; inspected `migrations/{0004,0005,0030}_*.sql` and `migrations/README.md`; queried live R1 `schema_migrations`, Timescale hypertable compression state, and `asset_supply_history` indexes | Package-level handler/store compilation remains green and the new concurrent quota tests are present in source, but the integration proof cannot run because migration `0030_asset_supply_history_unique_constraint` aborts with Timescale `operation not supported on hypertables that have compression enabled (0A000)`. R1 is still at schema version `28`, `asset_supply_history` reports compression enabled, and only the pre-0030 unique index exists. | EV-0120; EV-0121 | Supports new F-1261 and downgrades F-1248/F-1257 from apparent closure to evidence-blocked |
| CMD-0123 | 2026-05-12 | Inventory-control reconciliation after the migration pass: compared tracked files outside this Codex audit directory against `inventory/file-coverage.tsv`, added missing rows for the newly tracked incident-emitter/signup-locker files plus migration `0030`, refreshed directly reviewed row notes for the advisory-lock/migration surfaces, reran status roll-up, and reran `./scripts/ci/lint-docs.sh` | Coverage parity is restored at `tracked=1882`, `rows=1882`; the TSV now rolls up to `done=105`, `in_progress=54`, `todo=1723`; docs lint passes. | EV-0122 | Restores literal inventory parity after current repo drift and the new F-1261 slice |
| CMD-0124 | 2026-05-12 | Highest-priority supply-freshness continuation for `F-1236`: reviewed `internal/supply/{refresher,xlm,lcm_reader,storage_classic_reader,storage_sep41_reader}.go`, `cmd/ratesengine-aggregator/main.go`, and `cmd/ratesengine-ops/supply.go`; searched for `MinComponentLedger` producers across supply code; ran `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-aggregator ./cmd/ratesengine-ops` | The targeted packages pass. Classic and SEP-41 storage-backed readers now produce freshness ledgers, but native XLM still emits no `MinComponentLedger`. More specifically, the XLM chain reader falls back from live LCM observations to static `reserve_balances_stroops` config on `ErrNoObservation`, stamps the snapshot at the freshest cursor ledger, and leaves `MinComponentLedger=0`, so the refresher's stale-component rejector is guaranteed to bypass exactly when reserve-balance provenance is weakest. | EV-0123 | Sharpens F-1236 around the XLM live-to-static fallback path |
| CMD-0125 | 2026-05-12 | Post-supply-pass audit-control reconciliation: updated directly reviewed inventory rows for the XLM/classic/SEP41 freshness path, recomputed TSV status roll-up, reconfirmed tracked-file parity, and reran `./scripts/ci/lint-docs.sh` | Coverage parity remains `tracked=1882`, `rows=1882`; status roll-up moves to `done=105`, `in_progress=60`, `todo=1717`; docs lint passes. | EV-0124 | Keeps the coverage ledger synchronized after the F-1236 deepening pass |
| CMD-0126 | 2026-05-12 | Highest-priority webhook-secret continuation for `F-1244`: reviewed `internal/api/v1/dashboardwebhooks/handlers.go`, `internal/platform/webhook.go`, `internal/platform/postgresstore/webhook_store.go`, `internal/customerwebhook/worker.go`, migration `0027`, OpenAPI webhook prose, and architecture encryption docs; searched for any real column-encryption/KMS layer; ran `go test ./internal/api/v1/dashboardwebhooks ./internal/customerwebhook ./internal/platform/postgresstore` | Tests pass, but the contradiction is stronger than the earlier shorthand: `platform.CustomerWebhook` now claims a “standard column-encryption posture,” while repo search finds no such layer, migration `0027` stores a plain `bytea secret_hash`, architecture docs describe only disk/volume at-rest encryption, and the worker signs directly with the recovered bytes. `RotateWebhookSecret` also remains unimplemented while its interface comment still says the new plaintext is “not stored.” | EV-0125 | Sharpens F-1244 around the false at-rest protection claim and unimplemented rotation semantics |
| CMD-0127 | 2026-05-12 | Highest-priority API-key/quota continuation for `F-1226`: reviewed `internal/platform/{apikey,account}.go`, `internal/platform/postgresstore/{apikey_store,account_store}.go`, `internal/auth/{subject,apikey_postgres,apikey_redis}.go`, `internal/api/v1/middleware/{keypolicy,usage}.go`, `internal/api/v1/account.go`, OpenAPI/dashboard/account architecture prose, and usage tests; ran `go test ./internal/auth ./internal/api/v1/middleware ./internal/api/v1/dashboardkeys ./internal/platform/postgresstore` | Targeted packages pass, but the unresolved half of `F-1226` is broader than a missing hook: repo search finds no runtime consumer of per-key `monthly_quota` or account-level `MonthlyRequestQuotaOverride`, no production caller of `APIKeyStore.TouchUsage`, and the live usage reader keys Redis by the current credential (`key:<KeyID>` first, `id:<Identifier>` second) rather than aggregating all keys under an account. That means the present request path cannot yet back the account-level quota/dashboard model described in platform docs without more attribution work. | EV-0126 | Sharpens F-1226 around quota non-use plus account-usage attribution mismatch |
| CMD-0128 | 2026-05-12 | Highest-priority signup continuation for `F-1218`: reviewed `internal/api/v1/signup.go`, `cmd/ratesengine-api/main.go`, Redis-backed signup tracker/throttle wiring, and the current signup tests; ran `go test ./internal/api/v1 ./internal/auth ./cmd/ratesengine-api` | Targeted packages pass. This pass narrows the finding rather than broadening it: the Redis-backed `ReserveEmail` path still closes the same-email race, and the current main binary does not mint signup keys when Redis is absent because `Accounts` is nil too and `parseAndValidateSignup` returns 503 before creation. The still-open severe abuse path is the intended healthy path itself: a syntactically valid email string yields a usable plaintext Starter key without email ownership proof. | EV-0127 | Narrows F-1218 to the verified current production path |
| CMD-0129 | 2026-05-12 | Re-audited `F-1226` after the shared workspace grew a monthly-quota remediation slice: reviewed the diff in `cmd/ratesengine-api/main.go`, `internal/api/v1/server.go`, `internal/auth/{subject,apikey_postgres,apikey_redis}.go`, `internal/usage/counter.go`, and untracked `internal/api/v1/middleware/monthly_quota{,_test}.go`; ran `go test ./internal/api/v1/middleware ./internal/auth ./internal/usage ./cmd/ratesengine-api ./internal/api/v1`; searched for all remaining `MonthlyQuota` / `MonthlyRequestQuotaOverride` consumers | The new patch materially narrows `F-1226`: positive per-key quota values now propagate into `auth.Subject`, round-trip through the Redis auth cache, and are checked by a new month-to-date middleware backed by `usage.Counter.MonthToDate`; targeted tests pass. It still does not close the finding. `MonthlyQuota == 0` is documented in the platform model as "inherit from plan" but the middleware treats it as a bypass; `Account.MonthlyRequestQuotaOverride` still has no request-path consumer; quota admission is read-before-request while usage increments only after the handler returns, so concurrent near-cap requests can overshoot; and `TouchUsage` plus account-level aggregation remain unresolved. | EV-0128 | Narrows but preserves F-1226 after the in-flight quota patch |
| CMD-0130 | 2026-05-12 | Post-auth/quota/signup audit-control reconciliation: refreshed directly reviewed inventory rows for the latest `F-1226` / `F-1218` surfaces, recomputed TSV status roll-up, reconfirmed tracked-file parity, refreshed `inventory/area-counts.md` plus `inventory/repo-snapshot.md`, and reran `./scripts/ci/lint-docs.sh` | Coverage parity remains `tracked=1882`, `rows=1882`; status roll-up moves to `done=105`, `in_progress=64`, `todo=1713`; top-level area counts now reflect `cmd=38`, `internal=677`, `migrations=61`; the repo snapshot records current `HEAD=8ff641689dd59fc2bd1a024ca7b5fa9bbffc750b` with 18 dirty paths in this moving workspace; docs lint passes. | EV-0129 | Keeps the control ledger synchronized after the latest highest-priority review pass |
| CMD-0131 | 2026-05-12 | High-priority Stripe/platform recheck for `F-1219`: reviewed `cmd/ratesengine-api/main.go`, `internal/api/v1/stripe_webhook.go`, `internal/platform/postgresstore/apikey_store.go`, and the current webhook test file; searched for production `StripePlatformBridge` wiring plus any Postgres dashboard-key update path; ran `go test ./internal/api/v1 ./cmd/ratesengine-api` | Current source does wire `stripeCfg.Platform` and the webhook now carries checkout/subscription/invoice account-state handlers, so the stale "Platform never wired" wording is no longer true. The finding remains open for a narrower reason: the actual key-budget mutation loop still targets only Redis-backed legacy keys via `StripeKeyManager.UpdateRateLimit`, there is no corresponding Postgres dashboard-key upgrade path, platform side effects are logged best-effort after Redis mutation, and the webhook test file still lacks closure-grade bridge/subscription/invoice assertions. | EV-0130 | Narrows but preserves F-1219 against current source |
| CMD-0132 | 2026-05-12 | Post-commit audit-control resync after monthly-quota files entered tracked scope: refreshed `inventory/file-coverage.tsv`, `inventory/area-counts.md`, and `inventory/repo-snapshot.md`; recomputed tracked-file parity and TSV status counts against `HEAD=48688b3eb92230a940040c151c6e28b8fb21dd00`; reran `./scripts/ci/lint-docs.sh` | Coverage parity returns to `tracked=1884`, `rows=1884`; status roll-up becomes `done=105`, `in_progress=67`, `todo=1712`; top-level counts now reflect `internal=679`; the moving workspace snapshot records 13 dirty paths after the resync, including the current in-flight touch-usage files plus the touched API server wiring; docs lint passes. | EV-0131 | Restores the control ledger after tracked scope advanced again |
| CMD-0133 | 2026-05-12 | Highest-priority `F-1226` touch-usage continuation: reviewed the new workspace-only `internal/api/v1/middleware/touch_usage.go`, `internal/auth/touch_debouncer.go`, the `main.go` / `server.go` wiring diff, the Postgres `APIKeyStore.TouchUsage` sink, and the accompanying touch-usage/debouncer tests; ran `go test ./internal/api/v1/middleware ./internal/auth ./cmd/ratesengine-api ./internal/platform/postgresstore` | The remediation slice materially narrows the old "TouchUsage has no production caller" statement: current workspace wiring reaches API startup and targeted tests pass. It does not close `F-1226`: the touch files are still untracked shared-workspace code, the middleware executes Redis SETNX plus Postgres touch bookkeeping inline after `next.ServeHTTP` despite comments calling it asynchronous, and the finding's quota atomicity/account-aggregation gaps remain unchanged. | EV-0132 | Narrows but preserves F-1226 against the live workspace remediation |
| CMD-0134 | 2026-05-12 | Cross-file ACL check for the `F-1226` touch-usage slice: reviewed `internal/auth/touch_debouncer.go` and the moving diff in `configs/ansible/roles/redis-sentinel/templates/users.acl.j2`; searched for `touch:apikey:*` allow-list coverage | The current workspace adds `~touch:apikey:*` beside the new Redis debouncer namespace, so the touch slice does not recreate the prior Redis ACL drift pattern in this local state. Because both the debouncer code and the ACL change are still moving workspace edits, this is dependency coherence evidence, not closure evidence. | EV-0133 | Preserves the touch middleware's deploy-time Redis contract in the audit record |
| CMD-0135 | 2026-05-12 | High-priority `F-1236` supply freshness recheck: reviewed current `internal/supply/{xlm,lcm_reader,storage_classic_reader,storage_sep41_reader}.go`, native-XLM chain readers in `cmd/ratesengine-{aggregator,ops}`, and the relevant freshness tests; ran `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-api ./cmd/ratesengine-aggregator ./cmd/ratesengine-indexer` | The prior audit wording was stale: current source now has an XLM freshness producer through `LCMReserveBalanceReader.MinReserveAccountLedger` and `XLMComputer` threads it into `Supply.MinComponentLedger`. `F-1236` remains open for a narrower but still material reason: missing LCM observations trigger static reserve fallback with `MinComponentLedger=0`, and classic/SEP41/XLM freshness-query failures deliberately collapse to the same gate-permissive zero. | EV-0134 | Narrows but preserves F-1236 against current source |
| CMD-0136 | 2026-05-12 | High-priority `F-1243` asset-registry recheck: reviewed `internal/storage/timescale/{trades,asset_registry,coins,issuers}.go`, the finding/XFI text, and direct ranking consumers; ran `go test ./internal/storage/timescale ./internal/api/v1` | The original stale-count defect still reproduces in source: process-lifetime registry dedupe returns before the upsert on later same-process asset touches. The review also found a second current bug in the same interaction: `InsertTrade` uses `ON CONFLICT DO NOTHING` but invokes the registry hook without checking whether the trade row was inserted, so duplicate replay after process restart can still inflate registry counters once per asset per process. | EV-0135 | Broadens F-1243 from freeze-only to freeze-plus-idempotency drift |
| CMD-0137 | 2026-05-12 | Post-wave-39 audit-control resync after `HEAD=f3c76028053dd433b919082185ea00217d39f2d2` landed the four touch-usage files: added their inventory rows, recomputed tracked-file parity/status counts, refreshed `inventory/area-counts.md` plus `inventory/repo-snapshot.md`, and reran `./scripts/ci/lint-docs.sh` | Coverage parity is exact at `tracked=1888`, `rows=1888`; status roll-up is `done=109`, `in_progress=71`, `todo=1708`; `internal` area count is now `683`; the refreshed moving-workspace snapshot records six dirty audit-control files only; docs lint passes. | EV-0136 | Restores inventory/snapshot parity after committed wave 39 scope growth |
| CMD-0138 | 2026-05-12 | High-priority `F-1261` recheck on current head: reviewed migration `0030` up/down SQL, the compressed-hypertable precedent in `0004`, the `asset_supply_history` writer dependency, and reran `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` | The same release blocker still reproduces on `HEAD=f3c76028...`: migration `0030_asset_supply_history_unique_constraint.up.sql` fails with Timescale `0A000 operation not supported on hypertables that have compression enabled`, and the targeted platform-store integration never reaches its advisory-lock quota assertions. | EV-0137 | Reconfirms the live migration blocker and blocked closure proof path |
| CMD-0139 | 2026-05-12 | Current-head `F-1244` webhook-secret contract recheck: reviewed `internal/platform/webhook.go`, `internal/platform/postgresstore/webhook_store.go`, `internal/customerwebhook/worker.go`, `internal/api/v1/dashboardwebhooks/handlers.go`, and related OpenAPI/platform-spec prose; ran `go test ./internal/platform/postgresstore ./internal/customerwebhook ./internal/api/v1/dashboardwebhooks` | The high-severity finding remains open unchanged. Live HMAC key bytes are still stored/read directly through `secret_hash`, the worker signs from the recovered bytes, source still claims a row-level "column-encryption posture" not evidenced elsewhere in repo, handler/store/OpenAPI prose still implies once-only/non-persisted semantics, and rotation remains unimplemented. Targeted packages pass. | EV-0138 | Reconfirms F-1244 against current source |
| CMD-0140 | 2026-05-12 | Current-head `F-1251` FX resolver reconciliation: reviewed `internal/storage/timescale/usd_fx_resolver.go`, its focused tests, and `test/integration/usd_fx_resolver_test.go`; ran `go test ./internal/storage/timescale ./test/integration -run 'TestVWAPUSDFXResolver|Test.*USDFXResolver' -count=1` plus `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1` | Source-level closure work is present and focused package tests pass, but the integration proof still cannot run to completion: the tagged integration test aborts during migration `0030` with Timescale `0A000` before the FX resolver assertion executes. `F-1251` should therefore be `needs_evidence`, not fixed, until `F-1261` is cleared and the integration proof goes green. | EV-0139 | Downgrades F-1251 from apparent fixed to evidence-blocked |
| CMD-0141 | 2026-05-12 | Highest-priority `F-1225` live/source reconciliation refresh: reviewed `internal/api/v1/history.go`, `history_test.go`, and `internal/storage/timescale/aggregates.go`; ran `go test ./internal/api/v1 -run 'History|SinceInception|Stablecoin' -count=1`; checked live R1 peg config and public since-inception history for `native/fiat:USD` versus direct `native/USDC-GA5ZSEJY...` | Source regression coverage passes and the fallback path remains in repo, but current live R1 still serves `points: []` for `native/fiat:USD` while direct Circle-USDC daily history returns nine populated points under a config that enables the peg list. `F-1225` remains open as deployed runtime drift. | EV-0140; R1-0019 | Reconfirms F-1225 on current live deployment |
| CMD-0142 | 2026-05-12 | Highest-priority `F-1228` SSE live/source reconciliation refresh: reviewed `cmd/ratesengine-api/main.go`, `internal/api/streaming/handler.go`, and the three v1 stream handlers; ran `go test ./internal/api/v1 ./cmd/ratesengine-api -run 'Stream|SSE|Tip' -count=1`; executed an R1 loopback `/v1/price/tip/stream` probe with a 68-second client ceiling | Current source still carries the `http.ResponseController(...).SetWriteDeadline(time.Time{})` reset path and the focused stream-related packages pass, but live R1 remains on old observable behavior: the tip stream closes at elapsed `30.0` seconds after the initial frame plus one heartbeat instead of surviving beyond the former server write-timeout boundary. | EV-0141; R1-0020 | Reconfirms F-1228 as deployed source/runtime drift |
| CMD-0143 | 2026-05-12 | Highest-priority `F-1219` paid-upgrade recheck on current head: reviewed `cmd/ratesengine-api/main.go`, `internal/api/v1/stripe_webhook.go`, `internal/platform/postgresstore/apikey_store.go`, and current webhook tests; searched for production `StripePlatformBridge` wiring, Postgres key update usage, and bridge/subscription/invoice assertions; ran `go test ./internal/api/v1 ./cmd/ratesengine-api` | The source-level narrowing remains real: production wiring still enables the platform bridge, and the webhook can maintain account/subscription rows. The defect also remains real: the budget-lift loop still calls only Redis `UpdateRateLimit`, the Stripe path never lists or updates Postgres dashboard API keys despite the store exposing those primitives, platform writes stay best-effort after Redis mutation, and the webhook test file still lacks bridge/subscription/invoice closure assertions. | EV-0142 | Reconfirms F-1219 on current head |
| CMD-0144 | 2026-05-12 | Highest-priority `F-1218` signup-verification recheck against `HEAD=400660e6...` plus the live workspace: reviewed `internal/api/v1/{signup,signup_verify,server}.go`, `cmd/ratesengine-api/main.go`, `internal/auth/{signup_verifier,store_mark_email_verified}.go`, current OpenAPI prose, and the auth subject/key comments; searched for `APIKeyEmailVerifier` wiring and `RequireEmailVerified` middleware implementation; ran `go test ./internal/api/v1 ./internal/auth ./cmd/ratesengine-api` | Wave 44 is real: signup now issues verifier tokens and can email a link. `F-1218` still does not close. Signup still returns a usable plaintext key before ownership proof, the workspace's wave-45 marker seam is not assigned in `newServer` or passed from `main`, the Redis marker implementation is still untracked, and repo search finds no actual request-path `RequireEmailVerified` middleware implementation. Focused API/auth/command tests pass without exercising the missing production gate. | EV-0143 | Sharpens F-1218 against the current moving workspace |
| CMD-0145 | 2026-05-12 | Immediate follow-up after the moving workspace advanced again on `F-1218`: reviewed the new `RequireEmailVerified` middleware/tests, the Redis `MarkEmailVerified` store/tests, the `cmd/ratesengine-api/main.go` / `internal/api/v1/server.go` wiring diff, and `internal/config/config.go`; reran `go test ./internal/api/v1 ./internal/api/v1/middleware ./internal/auth ./cmd/ratesengine-api`; reran `./scripts/ci/lint-docs.sh` | The live workspace now contains a materially stronger wave-45 remediation slice: verified-state marker wiring, optional request-path gate insertion, config flag plumbing, and focused tests all pass. `F-1218` remains open because those new middleware/store files are still untracked, the gate defaults off (`signup_require_email_verification=false`), current source still returns a usable plaintext key before ownership proof unless operators opt in, and docs lint fails because the generated config reference has not yet absorbed the new key. | EV-0144 | Supersedes the narrower intermediate state recorded by CMD-0144 without closing F-1218 |
| CMD-0146 | 2026-05-12 | Final `F-1218` workspace refresh after the generated config reference caught up: rechecked `docs/reference/config/README.md` for `api.signup_require_email_verification`, reran `./scripts/ci/lint-docs.sh`, and reconciled the finding text against the now-green docs gate | The docs-reference drift seen in CMD-0145 cleared; config docs now include the new key and docs lint passes. That narrows the remaining issue but does not close it: the wave-45 gate/store source files are still untracked workspace code, the hardening flag still defaults false, and `/v1/signup` still returns a usable key before ownership proof unless operators opt in. | EV-0145 | Supersedes the doc-lint portion of CMD-0145 while preserving F-1218 as open |
| CMD-0147 | 2026-05-12 | Settled `F-1218` recheck after `HEAD=93594529...` committed wave 45: reran `go test ./internal/api/v1 ./internal/api/v1/middleware ./internal/auth ./cmd/ratesengine-api`; reviewed the landed gate/marker/config/docs state; checked R1 `/etc/ratesengine.toml` for `signup_require_email_verification` | The code remediation is now landed and the focused test set remains green, but the finding stays open as a policy/default posture defect: the new hardening flag still defaults `false`, R1 has no explicit override line, and the signup handler still returns a usable plaintext key before ownership proof whenever operators do not opt in. | EV-0146; R1-0021 | Reclassifies F-1218 from "implementation incomplete" to "opt-in-off/public posture remains permissive" |
| CMD-0148 | 2026-05-12 | Post-wave-46 proof sweep after `HEAD=ab78289e...` landed the compressed-hypertable migration rewrite: ran `go test -tags=integration ./test/integration -run TestMigrationsRoundTrip -count=1`, `go test -tags=integration ./test/integration -run TestVWAPUSDFXResolver_QueriesPrices1m -count=1`, `go test -tags=integration ./test/integration -run 'TestPlatformPostgresStores/WebhookStore/Concurrent_QuotaCap_Holds' -count=1`, and the full `TestPlatformPostgresStores` integration | Migration round-trip now passes, the DB-backed FX resolver proof now passes, and the webhook concurrent-cap proof now passes, clearing the old `F-1261` evidence block for those paths. The full platform-store integration reaches the API-key quota subtest but fails there for a different reason: every concurrent create errors on `api_keys.referer_allowlist` being inserted as SQL NULL into a NOT NULL column. | EV-0147; EV-0148 | Fixes F-1261/F-1251/F-1248 evidence state and exposes new F-1262 while leaving F-1257 evidence-blocked |
| CMD-0149 | 2026-05-12 | New API-key Postgres create-path audit after the platform-store integration unblocked: reviewed `internal/platform/postgresstore/apikey_store.go`, `internal/api/v1/dashboardkeys/handlers.go`, `internal/platform/apikey.go`, migration `0027`, the dashboard API client shape, and the failing concurrent integration trace | `buildAPIKeyCreateArgs` passes `pq.StringArray(k.RefererAllowlist)` straight through. The normal zero value for omitted `referer_allowlist` is nil, and the integration now proves that path reaches SQL as NULL, violating `api_keys.referer_allowlist text[] NOT NULL DEFAULT '{}'`. The dashboard handler leaves `req.RefererAllowlist` nil when clients omit it; the dashboard client type marks it optional; the existing happy-path handler test uses a fake store, so this production SQL failure is not caught there. | EV-0148; XFI-0054 | Supports new F-1262 |
| CMD-0150 | 2026-05-12 | Current-head `F-1243` reconciliation after `HEAD=bf51138b...` landed the TTL-backed classic-asset registry dedupe: reviewed `internal/storage/timescale/{asset_registry,trades}.go`, the new `asset_registry_test.go`, and downstream registry/ranking evidence; ran `go test ./internal/storage/timescale ./internal/api/v1` | Wave 47 does close the freeze-half of `F-1243`: same-process registry touches are now only coalesced for 60 seconds, then the upsert runs again and tests pin that TTL behavior. The finding still remains open because the second defect recorded in `EV-0135` survives unchanged: `InsertTrade` still ignores whether `INSERT ... ON CONFLICT DO NOTHING` inserted a row before invoking registry mutation, so duplicate replay can still drift observation counters across restart/replay boundaries. | EV-0149 | Narrows but preserves F-1243 after wave 47 |
| CMD-0151 | 2026-05-12 | Current-head `F-1256` reconciliation after `HEAD=70a77b06...` landed the dashboard copy fix: reviewed `web/dashboard/src/app/keys/page.tsx`, the create-key `rate_limit_per_min` OpenAPI prose, the tier-cap source in `internal/platform/account.go`, and the dashboard create handler contract | The source surfaces now agree. OpenAPI already documented the tier clamp ladder and wave 48 replaces the stale dashboard free-tier `1000` hint with explicit tier caps plus the silent-clamp-on-save behavior. The backend still clamps through the same account-tier source of truth and the response object remains the persisted effective value. | EV-0150; XFI-0048 | Reconciles and closes F-1256 on current head |
| CMD-0152 | 2026-05-12 | Post-wave-48 audit-control resync: restored the missing `internal/storage/timescale/asset_registry_test.go` inventory row, recomputed tracked-file parity and TSV status counts against `HEAD=70a77b06...`, refreshed `inventory/area-counts.md` plus `inventory/repo-snapshot.md`, and reran `./scripts/ci/lint-docs.sh` | Coverage parity is exact at `tracked=1897`, `rows=1897`; status roll-up is `done=111`, `in_progress=87`, `todo=1699`; the `internal` area count is now `692`; the snapshot records the current 11-path dirty workspace, including non-audit account-usage/spec edits that were preserved; docs lint passes. | EV-0151 | Restores literal audit-control parity after wave 47/48 tracked-scope growth |
| CMD-0153 | 2026-05-12 | Current-head `F-1262` continuation after wave 48: reran `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1`; re-read `internal/platform/postgresstore/apikey_store.go`, `internal/platform/apikey.go`, `internal/api/v1/dashboardkeys/handlers.go`, migration `0027`, and the dashboard API client allowlist shape | The full platform-store integration still fails in `APIKeyStore/Concurrent_QuotaCap_Holds`: all 12 concurrent creates error on `referer_allowlist` SQL NULL against the NOT NULL array column, so the quota-proof finding `F-1257` remains evidence-blocked. Source review also confirms `ip_allowlist` is not the same bug because its custom `cidrArray.Value()` emits `'{}'` when empty, while both API-key Create and Update pass `pq.StringArray(k.RefererAllowlist)` directly and preserve nil as SQL NULL. | EV-0152; XFI-0049; XFI-0054 | Broadens and reconfirms F-1262 on current head |
| CMD-0154 | 2026-05-12 | Current-workspace `F-1259` falsification pass after usage-doc edits appeared mid-audit: reviewed `internal/api/v1/account.go`, `openapi/rates-engine.v1.yaml`, `docs/reference/api-design.md`, `docs/architecture/explorer-data-inventory.md`, `docs/reference/api/rates-engine.v1.yaml`, and `docs/reference/api/postman-collection.json`; searched for residual placeholder/healthz wording | The remediation slice is real but incomplete. Source OpenAPI, handler prose, API-design, explorer inventory, and generated reference YAML now describe live Redis-backed usage plus `/readyz` degradation correctly. The Postman collection still advertises `"Usage-summary placeholder (currently empty)."`, so `F-1259` remains open as residual generated-artifact drift rather than fully fixed. | EV-0153; XFI-0051 | Prevents premature closure of F-1259 |
| CMD-0155 | 2026-05-12 | Highest-priority `F-1244` current-source recheck after the register had drifted to a premature fixed status: reviewed `internal/platform/webhook.go`, `internal/api/v1/dashboardwebhooks/handlers.go`, `internal/platform/postgresstore/webhook_store.go`, `internal/customerwebhook/worker.go`, current webhook OpenAPI prose, and reran `go test ./internal/platform/postgresstore ./internal/customerwebhook ./internal/api/v1/dashboardwebhooks` | The focused packages pass, but the security-contract finding remains open. Current source still persists recoverable HMAC bytes in `secret_hash`, signs with those recovered bytes, claims an unevidenced "standard column-encryption posture", leaves handler DTO prose saying plaintext is never persisted, and keeps an unimplemented rotation method whose interface/store comments say new plaintext is not stored. | EV-0154; XFI-0036 | Reopens F-1244 in the summary register to match current source |
| CMD-0156 | 2026-05-12 | Highest-priority `F-1236` supply-freshness summary recheck: reviewed `internal/supply/{refresher,xlm,lcm_reader,storage_classic_reader,storage_sep41_reader}.go`, `cmd/ratesengine-{aggregator,ops}`, and current summary/remediation/XFI text; reran `go test ./internal/supply ./internal/storage/timescale ./cmd/ratesengine-api ./cmd/ratesengine-aggregator ./cmd/ratesengine-indexer` | The targeted packages pass, and the source now genuinely has classic, SEP41, and live-reader XLM freshness producers. The finding still remains open because the static-XLM fallback path, classic/SEP41 freshness-query errors, XLM freshness-reader errors, and legacy readers all leave `MinComponentLedger=0`, which the refresher explicitly treats as gate-permissive "no freshness signal." | EV-0155; XFI-0028 | Reopens F-1236 in the summary register to match current source |
| CMD-0157 | 2026-05-12 | Post-wave-50 API-key proof reconciliation on `HEAD=b137d489...`: reviewed `internal/platform/postgresstore/apikey_store.go`, the new `nonNilStringArray` helper/tests, the concurrent quota integration fixture, migration `0027`, and dashboard key ID generation; ran `go test ./internal/platform/postgresstore ./internal/api/v1/dashboardkeys` plus `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` | Focused store/dashboard-key packages pass and the former `referer_allowlist` SQL NULL failure no longer reproduces, closing `F-1262`. The full integration still fails before quota assertions for a new reason: all 12 concurrent creates violate `api_keys_id_check` because the test uses `\"kid_\" + uuid.New().String()[:12]` (contains a hyphen), while production `generateKeyID` emits hex-only IDs. That opens `F-1263` and keeps `F-1257` in `needs_evidence`. | EV-0156; EV-0157; XFI-0049; XFI-0054; XFI-0055 | Closes F-1262, opens F-1263, and preserves the F-1257 proof gate |
| CMD-0158 | 2026-05-12 | Wave-51 `F-1243` duplicate-replay reconciliation on `HEAD=002715fd...`: reviewed `internal/storage/timescale/trades.go`, the existing TTL-dedupe tests, the updated finding register prose, and searched for a new DB-backed replay integration; reran `go test ./internal/storage/timescale ./internal/api/v1` | The source remediation is real: `InsertTrade` now returns early before registry mutation when `RowsAffected()==0` on the `ON CONFLICT DO NOTHING` insert, and focused packages pass. I did not find the closure-grade replay integration this audit had required, so `F-1243` should move to `needs_evidence`, not fully `fixed`, until duplicate replay/process-boundary proof lands. | EV-0158; XFI-0035 | Narrows F-1243 to an execution-evidence gate after the wave-51 source fix |
| CMD-0159 | 2026-05-12 | Wave-52 falsification pass for `F-1244` and `F-1259`: re-read webhook-domain/API/store comments, searched tracked usage-doc/Postman surfaces, verified `docs/reference/api/postman-collection.json` is not tracked and absent, confirmed the canonical tracked `examples/postman/rates-engine.postman_collection.json` carries the corrected live-usage prose, and reran `./scripts/ci/lint-docs.sh` | `F-1244` remains open because `internal/platform/postgresstore/webhook_store.go` still documents a non-stored/hash-before-persist rotation model that the shipped recoverable `secret_hash` contract does not implement, despite the higher-level comments improving. `F-1259` closes: all tracked repo artifacts now describe the Redis-backed usage path consistently, and the stale deleted local Postman byproduct is not part of the repository evidence surface. Docs lint passes. | EV-0159; XFI-0036; XFI-0051 | Preserves F-1244, closes F-1259, and reconciles the moving wave-52 register drift |
| CMD-0160 | 2026-05-12 | Wave-53 `F-1263` proof-harness rerun after the moving fixture patch stripped UUID hyphens from the concurrent API-key IDs: reviewed migration 0027 ID/prefix checks, the patched integration fixture, production key ID/plaintext generators, and reran `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` | The malformed `kid_` ID layer is cleared, but the full integration still fails before quota assertions: all 12 concurrent creates now violate `api_keys_key_prefix_check` because the fixture uses `KeyPrefix: plaintext[:12]` from `rek_race_...`, while production-shaped prefixes are `rek_<8hex>`. `F-1263` remains open and `F-1257` stays `needs_evidence`. | EV-0160; XFI-0049; XFI-0055 | Broadens F-1263 from malformed-ID-only to still-invalid quota-fixture identity fields |
| CMD-0161 | 2026-05-12 | Wave-53 `F-1244` second-pass falsification after the Postgres store comment was corrected: re-read `internal/platform/{webhook,postgresstore/webhook_store}.go`, dashboard webhook DTO/API prose, migration 0027, and `docs/architecture/platform-spec.md`; searched for the remaining claimed webhook-secret protection model | The inline implementation comments now agree that webhook signing keys are recoverable from `secret_hash`, but the architecture spec still promises customer webhook secrets are protected with libsodium sealed boxes under an operator master key. Current migration/store/worker code still shows direct recoverable `bytea` storage with no encrypt/decrypt layer, so `F-1244` remains open on architecture-vs-code security-model drift. | EV-0161; XFI-0036 | Preserves F-1244 after the wave-53 source-comment fix |
| CMD-0162 | 2026-05-12 | Wave-53 `F-1263` closure rerun after the fixture also switched the stored prefix to a production-shaped `rek_<8hex>` value: reviewed the patched concurrent quota fixture and reran `go test -tags=integration ./test/integration -run TestPlatformPostgresStores -count=1` | The full integration now passes. The concurrent API-key quota scenario reaches its intended advisory-lock assertions instead of schema checks, so `F-1263` is fixed and the execution-evidence gate on `F-1257` is cleared. | EV-0162; XFI-0049; XFI-0055 | Closes F-1263 and F-1257's remaining proof gate |
| CMD-0163 | 2026-05-12 | Wave-54 `F-1244` closure check after `docs/architecture/platform-spec.md §8.1 Encryption` was rewritten: compared the revised architecture prose against migration 0027, webhook store comments, delivery worker behavior, and the dashboard/API secret visibility text | The architecture spec now distinguishes planned TOTP sealed-box storage from customer webhook signing keys, which it correctly describes as recoverable plain `bytea` read back for HMAC signing. The reviewed source/spec surfaces now agree, so `F-1244` closes. | EV-0163; XFI-0036 | Closes F-1244's remaining architecture-vs-code security-model drift |
| CMD-0164 | 2026-05-12 | Post-wave-54 control resync on settled `HEAD=a783b5ed...`: recomputed tracked non-audit file count vs TSV row count, recomputed TSV status roll-up, refreshed `inventory/area-counts.md` and `inventory/repo-snapshot.md`, and reran `./scripts/ci/lint-docs.sh` | Coverage parity is exact at `tracked=1898`, `rows=1898`; the roll-up is `done=112`, `in_progress=88`, `todo=1698`; `internal` area count is `693`; the repo snapshot records the current nine-path Codex-audit-only dirty workspace; docs lint passes. | EV-0164 | Restores literal control-surface parity after wave 54 settled |
| CMD-0165 | 2026-05-12 | Highest-priority post-wave-54 recheck for `F-1218` and `F-1219`: reviewed signup verification defaults/wiring plus Stripe bridge/update paths across `cmd/ratesengine-api/main.go`, `internal/api/v1`, `internal/auth`, `internal/config`, and the current OpenAPI/config docs; reran `go test ./internal/api/v1 ./internal/auth ./cmd/ratesengine-api` and `go test ./internal/api/v1 ./cmd/ratesengine-api -run 'Stripe|Signup|Email|Verify' -count=1` | Both findings remain open on `HEAD=a783b5ed...`. Signup verification still defaults off (`signup_require_email_verification=false`) and therefore leaves the original healthy-path plaintext-key abuse posture reachable when operators do not opt in. Stripe upgrade processing still updates Redis-backed legacy keys only; the Postgres dashboard-key store remains outside `upgradeAllKeys`, and focused tests pass without closure-proving that cross-store contract. | EV-0165; XFI-0012; XFI-0013 | Reconfirms F-1218 and F-1219 on the settled wave-54 head |
| CMD-0166 | 2026-05-12 | Highest-priority live R1 refresh for `F-1225` and `F-1228`: reran focused source tests `go test ./internal/api/v1 -run 'History|SinceInception|Stablecoin' -count=1` and `go test ./internal/api/v1 ./cmd/ratesengine-api -run 'Stream|SSE|Tip' -count=1`; probed public since-inception history for `native/fiat:USD` vs direct Circle-USDC; then ran a timed public SSE probe with `curl -skN --max-time 68 ... -w time_total/exitcode` | Source tests remain green, but both live findings still reproduce. Public `native/fiat:USD` since-inception history returns `points: []` while direct native/Circle-USDC returns nine daily points. The public tip SSE stream emits frames and a keepalive, then terminates at `ELAPSED:30.251033 EXIT:92` instead of surviving the 68-second client ceiling. | EV-0166; R1-0022; R1-0023; XFI-0017; XFI-0020 | Reconfirms F-1225 and F-1228 on current live deployment |
| CMD-0167 | 2026-05-12 | Closure proof for `F-1243`: reviewed `test/integration/asset_registry_replay_test.go`, `timescale.ResetAssetRegistryDedupeForTest`, and reran `go test -tags=integration ./test/integration -run 'AssetRegistry_DuplicateReplayDoesNotMutateCounters' -count=1` | The tagged integration test passes. It clears the dedupe cache between insert/replay steps to model a fresh-store replay boundary, proves exact replay leaves registry counters unchanged, proves a genuinely new same-asset trade advances them once, and proves replaying that newer trade remains idempotent. | EV-0167; XFI-0035 | Closes the remaining F-1243 evidence gate |
| CMD-0168 | 2026-05-12 | Current-head `F-1219` closure check across `internal/api/v1/stripe_webhook.go`, `cmd/ratesengine-api/main.go`, `docs/operations/runbooks/stripe-platform-sync-errors.md`, operation-specific sync-error metric references, and `go test ./internal/api/v1 ./cmd/ratesengine-api -run 'Stripe|Webhook|Platform' -count=1` | The targeted platform-webhook suite passes. Current source now wires `postgresstore.NewAPIKeyStore(pgStore)` into `StripePlatformBridge`, upgrades dashboard-created Postgres keys idempotently through `upgradePlatformAPIKeys`, and documents/metrics-instruments best-effort platform-sync failure modes instead of leaving them implicit. | EV-0168; XFI-0013 | Reconciles F-1219 from narrowed to fixed on current source |
| CMD-0169 | 2026-05-12 | Post-wave-55/64 control resync on settled `HEAD=34511e508d18057da5bae1d0cb6b812555b5afd0`: recomputed tracked non-audit file count vs TSV row count, recomputed TSV status roll-up, refreshed `inventory/area-counts.md` and `inventory/repo-snapshot.md`, and reran `./scripts/ci/lint-docs.sh` | Coverage parity is exact at `tracked=1901`, `rows=1901`; the roll-up is `done=115`, `in_progress=88`, `todo=1698`; area counts now show `docs=508`, `test=92`, `web=201`; the repo snapshot records the current nine-path Codex-audit-only dirty workspace; docs lint passes. | EV-0169 | Restores literal control-surface parity after the replay/runbook/status scope growth |
| CMD-0170 | 2026-05-13 | Refreshed launch gate execution: `go run ./scripts/ci/verify-launch-ready` | Still exits non-zero. Blocking rows remain `L4.14`, `L4.15`, `L4.16`, `L4.17`, `L5.6`, and `L5.8`; L6 cutover remains red but non-blocking by script design. | EV-0170; XFI-0004 | Reconfirms F-1206 on current head |
| CMD-0171 | 2026-05-13 | Refreshed hosted/dependency posture check: `gh api repos/RatesEngine/rates-engine/vulnerability-alerts -i`; `gh api repos/RatesEngine/rates-engine/dependabot/alerts -i`; `pnpm audit --audit-level high` in `web/explorer`, `web/dashboard`, and `web/status` | Vulnerability alerts API still returns the disabled-alerts 404 shape; Dependabot alerts API still says repository alerts are disabled; all three web app audits now return one moderate advisory and no high/critical advisories. | EV-0171; XFI-0005 | Reconfirms F-1207's hosted-security remainder |
| CMD-0172 | 2026-05-13 | Refreshed R1 live-state pass: external `nc -vz -w2 136.243.90.96 11726`; SSH inspection of timer enablement, `/etc/ratesengine.toml` for `signup_require_email_verification`, `ss -ltnp` for `11726`, and `/etc/nftables.conf` permit lines | Public `11726/tcp` still accepts; R1 reports `stellar-core` listening on `0.0.0.0:11726`; nftables still explicitly permits `{11625,11626,11725,11726}`; `sla-probe.timer=disabled` while archive/verify/supply timers are enabled; and `signup_require_email_verification=<unset>`. | EV-0172; R1-0024; R1-0025; R1-0026; XFI-0001; XFI-0003; XFI-0012 | Reconfirms F-1201, F-1205, and F-1218 against current R1 |
| CMD-0173 | 2026-05-13 | Refreshed public R1 history/stream pass: `curl -sk` since-inception history for `native/fiat:USD` and direct native/Circle-USDC; timed `curl -skN --max-time 68` on `/v1/price/tip/stream?asset=native&quote=fiat:USD` | Public `native/fiat:USD` still returns `points: []` at `as_of=2026-05-13T06:33:58.949423013Z`; direct native/Circle-USDC still returns nine daily points from `2026-05-03` through `2026-05-11`; the SSE stream emits frames plus a keepalive and then ends at `ELAPSED:30.443053 EXIT:92`. | EV-0173; R1-0027; R1-0028; XFI-0017; XFI-0020; XFI-0022 | Reconfirms F-1225, F-1228, and F-1230 against current public deployment |
| CMD-0174 | 2026-05-13 | Audit-record falsification pass: compared every register-table finding status against the corresponding detailed finding section status with an `awk` mismatch detector, then reran `./scripts/ci/lint-docs.sh` | The mismatch detector returns no rows after reconciling 19 stale detail-status labels to their already-settled summary-table values; docs lint passes afterward. | EV-0174 | Confirms the findings register no longer contradicts itself on terminal status labels |
| CMD-0175 | 2026-05-13 | Refreshed R1 source-health/capacity pass: SSH Prometheus `/api/v1/alerts` snapshot with alert/state/source fields, SSH host `free -m`, `swapon --show`, `df -h / /var/lib/minio`, and loopback `/v1/status` fetch | Current R1 status is still degraded: `active_sources=12/17`, eight active incidents, firing ECB external-poller stale plus other anomaly/supply/cache/latency/memory alerts, pending Redstone source-stopped, memory at 94.19%, swap almost fully consumed, and MinIO 78% full. | EV-0175; R1-0029; R1-0030; XFI-0006 | Reconciles F-1208/F-1209 to current live reality rather than the older broader alert set |
| CMD-0176 | 2026-05-13 | Refreshed hosted governance pass: `gh api repos/RatesEngine/rates-engine/branches/main/protection -i`; `gh api repos/RatesEngine/rates-engine/environments`; `gh api repos/RatesEngine/rates-engine/actions/permissions`; `gh api repos/RatesEngine/rates-engine/actions/permissions/workflow`; `gh api repos/RatesEngine/rates-engine/actions/permissions/selected-actions` | Branch protection endpoint still returns the private-plan/upgrade 403; all five environments still have `protection_rules: []`, `can_admins_bypass: true`, and no deployment branch policy; Actions remain enabled with `allowed_actions=all`, `sha_pinning_required=false`, and selected-actions API reports that all actions/workflows are allowed. | EV-0176; XFI-0010 | Reconfirms F-1214, F-1215, and F-1216 on current hosted repo state |
| CMD-0177 | 2026-05-13 | Closure-summary cardinality check: recomputed register-table, XFI-table, and remediation-table terminal-status counts with `awk` | Counts match the wave-97 closure summary exactly: findings register `fixed=50/open=13`, XFI `fixed=45/open=10`, remediation plan `fixed=47/open=14`. | EV-0177 | Proves the headline close-out counts still match the underlying ledgers |
| CMD-0178 | 2026-05-13 | Wave-98 status-page closure falsification: `rg -n "Upptime|cstate|ratesengine-status|\\.upptimerc\\.yml|GH_PAT" CLAUDE.md docs/architecture/launch-readiness-backlog.md docs/launch-task-list.md deploy/comms/README.md deploy/comms/incident-update.md docs/operations/status-page-setup.md deploy/status-page/README.md` | The primary setup doc and status-page README describe the retired model historically, but five active non-audit surfaces still use it prescriptively: repo orientation, launch-readiness backlog, launch checklist, and both comms templates. | EV-0178; XFI-0007 | Reopens F-1211 after the breadth pass falsified the wave-57 closure claim |
| CMD-0179 | 2026-05-13 | Post-reopen control reconciliation: recomputed findings/XFI/remediation status totals with `awk`, recomputed `inventory/file-coverage.tsv` status roll-up, and reran `./scripts/ci/lint-docs.sh` | Post-reopen totals are findings `fixed=49/open=14`, XFI `fixed=44/open=11`, remediation `fixed=46/open=15`; file coverage is `done=115`, `in_progress=91`, `todo=1695`; docs lint passes. | EV-0179 | Restores count/coverage/doc-lint consistency after reopening F-1211 |
| CMD-0180 | 2026-05-13 | Root/control-doc breadth pass: read `README.md`, `AGENTS.md`, `CHANGELOG.md`, `VERSIONS.md`, `CODE_OF_CONDUCT.md`, `LICENSE`, `docs/review-2026-05-10.md`, and `docs/architecture/status-page-hosting-comparison.md`; cross-checked status-page references plus toolchain/version claims against `go.mod`, `Makefile`, and `.github/workflows/ci.yml` | No new finding beyond reopened `F-1211`. `README.md`'s "status page" launch blocker is consistent with still-yellow L4.11; the hosting-comparison doc is explicitly `superseded`; the 2026-05-10 review marks itself "working notes (not ratified)"; tool-version rows in `VERSIONS.md` still match Makefile/CI pins for gofumpt/goimports/golangci/govulncheck/gitleaks, while the already-audited Go toolchain equivalence remains tracked under closed `F-1240`. | EV-0180 | Advances the root-doc inventory without manufacturing findings from explicitly historical docs |
| CMD-0181 | 2026-05-13 | Monitoring-doc/live-firewall reconciliation: read `configs/prometheus/README.md` and `configs/loki/README.md`; inspected R1 listeners/firewall via SSH (`ss -ltnp`, `/etc/nftables.conf`); ran external `nc -vz -w2 136.243.90.96 9090` and `nc -vz -w2 136.243.90.96 3100` | Prometheus and Loki still listen on-host (`*:9090`, `*:3100`), but current nftables is default-drop and only the captive-core port set is explicitly accepted; external `9090` and `3100` probes both time out. The docs still say the host has no firewall and those ports are public. | EV-0181; XFI-0056 | Supports new finding F-1264 |
| CMD-0182 | 2026-05-13 | Alertmanager severity-doc reconciliation: read `configs/alertmanager/README.md`, `configs/alertmanager/alertmanager.r1.yml`, and `configs/ansible/roles/prometheus/templates/alertmanager.yml.j2` | The README migration note and standalone YAML header still claim the Ansible template uses `critical/warning/info`, but the actual template already routes `page`, `ticket`, and `informational` and says it mirrors the standalone R1 config. | EV-0182; XFI-0057 | Supports new finding F-1265 |
| CMD-0183 | 2026-05-13 | Post-monitoring breadth reconciliation: recomputed findings/XFI/remediation counts, recomputed file-coverage status totals, and reran `./scripts/ci/lint-docs.sh` | Findings now reconcile at `fixed=49/open=16`, XFI at `fixed=44/open=13`, remediation at `fixed=46/open=17`; file coverage is `done=125`, `in_progress=95`, `todo=1681`; docs lint passes. | EV-0183 | Restores headline/state parity after adding F-1264 and F-1265 |
| CMD-0184 | 2026-05-13 | Ansible role-doc breadth pass: read `configs/ansible/README.md`, the role READMEs for `haproxy`, `loki`, `patroni`, `prometheus`, and `redis-sentinel`, inspected `roles/archival-node/tasks/10-observability.yml`, and compared the README role-inventory claims against landed playbooks/meta files | New `F-1266`: the top-level Ansible README still says only `archival-node` exists even though multiple sibling roles/playbooks have landed, and it says archival-node includes Promtail/Loki shipping even though the observability task still marks that wiring TODO. The pass also broadens `F-1265`: `configs/ansible/roles/prometheus/README.md` still uses retired `critical/warning/info` severity prose while the template already uses `page/ticket/informational`. | EV-0184; XFI-0057; XFI-0058 | Supports F-1265 expansion and new F-1266 |
| CMD-0185 | 2026-05-13 | Post-Ansible breadth reconciliation: recomputed findings/XFI/remediation counts, recomputed file-coverage status totals, and reran `./scripts/ci/lint-docs.sh` | Findings reconcile at `fixed=49/open=17`, XFI at `fixed=44/open=14`, remediation at `fixed=46/open=18`; file coverage is `done=126`, `in_progress=98`, `todo=1677`; docs lint passes. | EV-0185 | Restores headline/state parity after expanding F-1265 and adding F-1266 |
| CMD-0186 | 2026-05-13 | Healthchecks setup breadth pass: read `configs/healthchecks/README.md`, `configs/healthchecks/install.sh`, the SLA timer references, and `docs/operations/pre-launch-hardening.md`; grepped the Healthchecks URL/count language | New `F-1267`: the current installer/env/timer surface requires five external Healthchecks URLs including `HEALTHCHECKS_URL_SLA_PROBE`, but the README still says "create four Checks", the installer comment still says "four Healthchecks.io URLs (3 heartbeats + 1 smoke)", and the hardening doc lists only the older four URLs. | EV-0186; XFI-0059 | Supports new finding F-1267 |
| CMD-0187 | 2026-05-13 | Post-Healthchecks breadth reconciliation: recomputed findings/XFI/remediation counts, recomputed file-coverage status totals, and reran `./scripts/ci/lint-docs.sh` | Findings reconcile at `fixed=49/open=18`, XFI at `fixed=44/open=15`, remediation at `fixed=46/open=19`; file coverage is `done=126`, `in_progress=101`, `todo=1674`; docs lint passes. | EV-0187 | Restores headline/state parity after adding F-1267 |
| CMD-0188 | 2026-05-13 | R1 rules-overlay path reconciliation: read `configs/prometheus/rules.r1/README.md` and `configs/prometheus/prometheus.r1.yml`, then compared the documented copy target with the active `rule_files` include path | New `F-1268`: the README instructs copies into `/etc/prometheus/rules.d/` and says that path is loaded, while `prometheus.r1.yml` actually points at `/etc/prometheus/rules.r1/*.yml`. | EV-0188; XFI-0060 | Supports new finding F-1268 |
| CMD-0189 | 2026-05-13 | Post-R1-rules breadth reconciliation: recomputed findings/XFI/remediation counts, recomputed file-coverage status totals, and reran `./scripts/ci/lint-docs.sh` | Findings reconcile at `fixed=49/open=19`, XFI at `fixed=44/open=16`, remediation at `fixed=46/open=20`; file coverage is `done=126`, `in_progress=103`, `todo=1672`; docs lint passes. | EV-0189 | Restores headline/state parity after adding F-1268 |
| CMD-0190 | 2026-05-13 | WASM audit-input schema reconciliation: read `configs/audit/README.md` and `configs/audit/wasm-walk-contracts.yaml`; searched for `_unattributed`, `TTL-evicted`, 2026-05-01 correction text, and current top-level source blocks | New `F-1269`: the README still promises an `_unattributed` TTL-evicted bucket, while the YAML now documents 540 contracts across exactly eight named source blocks and explicitly says the former Reflector-testnet leftovers were removed on 2026-05-01 because they were misclassified. | EV-0190; XFI-0061 | Supports new finding F-1269 |
| CMD-0191 | 2026-05-13 | Current-workspace remediation falsification pass across the newly edited monitoring/deployment docs: reread the Prometheus/Loki exposure docs, Alertmanager standalone + role prose, Ansible top-level README, Healthchecks README/install script, pre-launch hardening guide, and the still-unedited R1 rules overlay README; grepped for residual stale phrases and cross-checked against the previously recorded live/config facts | `F-1264`, `F-1265`, `F-1266`, and `F-1267` now genuinely close in the workspace. `F-1268` does not: `configs/prometheus/rules.r1/README.md` still copies into `/etc/prometheus/rules.d/` and claims that directory is loaded, while `prometheus.r1.yml` still points at `/etc/prometheus/rules.r1/*.yml`. | EV-0191; XFI-0056; XFI-0057; XFI-0058; XFI-0059; XFI-0060 | Closes four doc findings and prevents premature closure of F-1268 |
| CMD-0192 | 2026-05-13 | Wave-104 control reconciliation: recomputed finding/XFI/remediation terminal-status counts, recomputed `inventory/file-coverage.tsv` status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=54/open=15`, XFI `fixed=49/open=12`, remediation `fixed=51/open=16`; file coverage is `done=137`, `in_progress=95`, `todo=1670`; tracked-file parity is exact at `tracked=1902`, `rows=1902`; docs lint passes; and the mismatch detector returns no rows. | EV-0192 | Restores full audit-control consistency after the wave-104 moving-workspace remediation recheck |
| CMD-0193 | 2026-05-13 | Caddy trust-boundary reconciliation: compared `configs/caddy/README.md` and `docs/operations/pre-launch-hardening.md` against ADR-0025, generated config docs, and the API config source for `trusted_proxy_cidrs` semantics | New `F-1270`: the active operator docs incorrectly tell readers to add Cloudflare edge CIDRs to API `trusted_proxy_cidrs`, while ADR-0025 and the config source say Cloudflare trust stays at Caddy and the API continues trusting only its immediate proxy peer. | EV-0193; XFI-0062 | Supports new finding F-1270 |
| CMD-0194 | 2026-05-13 | Current-workspace remediation recheck for `F-1268` and `F-1269`: reread `configs/prometheus/rules.r1/README.md`, `configs/prometheus/prometheus.r1.yml`, and `configs/audit/README.md`; checked whether the previously stale paths/schema prose still survive | Both findings now genuinely close. The R1 rules README copies into `/etc/prometheus/rules.r1/` and says that path matches the loaded config, while the WASM audit README says `_unattributed` was intentionally removed during the 2026-05-01 cleanup and accurately describes the eight-source live schema. | EV-0194; XFI-0060; XFI-0061 | Closes F-1268 and F-1269 on the current workspace |
| CMD-0196 | 2026-05-13 | Redis Sentinel listener-auth audit: compared `sentinel.conf.j2`, redis-role docs/design note, Redis runbooks/drill docs, `redisclient.Build`, and go-redis v9 Sentinel auth handling from the local module cache | New `F-1271`: the committed HA Redis path passes a Sentinel password everywhere, but the rendered Sentinel config never enables listener-side auth. It only sets `sentinel auth-pass`, which authenticates Sentinel to Redis masters, not clients to Sentinel. | EV-0196; XFI-0063 | Supports new finding F-1271 |
| CMD-0197 | 2026-05-13 | Redis exporter ACL-lockdown cross-file audit: compared `users.acl.j2`, `tasks/07-monitoring.yml`, Prometheus Redis scrape wiring, Redis role docs, and generated Redis config guidance | New `F-1272`: hardened Redis disables `default` and moves application clients onto named-user auth, but the generated `redis_exporter.service` still authenticates without any username, risking loss of Redis metrics/alerts under lockdown. | EV-0197; XFI-0064 | Supports new finding F-1272 |
| CMD-0198 | 2026-05-13 | Current-workspace remediation recheck for `F-1270` and `F-1271`: reread the Caddy/operator proxy-trust docs plus `sentinel.conf.j2`, then compared both surfaces against the earlier finding evidence and the audited contract sources | Both findings now close in source. Caddy/operator docs explicitly keep Cloudflare CIDRs in Caddy while API trust stays on the immediate Caddy peer, and Sentinel rendering now emits listener `requirepass {{ redis_password }}` in addition to `sentinel auth-pass`. | EV-0198; XFI-0062; XFI-0063 | Closes F-1270 and F-1271 on the current workspace |
| CMD-0199 | 2026-05-13 | Ansible playbook-inventory falsification pass: compared `configs/ansible/README.md` and `roles/redis-sentinel/README.md` against `rg --files configs/ansible/playbooks` | Reopens `F-1266`: the docs now advertise `postgres-cluster.yml`, `redis-cluster.yml`, `haproxy.yml`, and `loki.yml`, and the Redis role commands use `playbooks/redis-cluster.yml`, but the tracked playbook directory only contains `archival-node.yml`, `deploy-binary.yml`, and `monitoring.yml`. | EV-0199; XFI-0058 | Reopens F-1266 after the earlier doc remediation introduced a new execution-path drift |
| CMD-0200 | 2026-05-13 | Redis exporter remediation falsification pass: compared `redis_acl_lockdown` defaults, ACL-file emission in `redis.conf.j2`/`03-redis-configure.yml`, the new `redis_exporter` ACL user, and the exporter service render in `07-monitoring.yml` | `F-1272` remains open on the current workspace, but the failure mode shifted. The exporter now always passes `-redis.user=redis_exporter`, while that ACL user is only defined when lockdown is enabled. The default `redis_acl_lockdown: false` path therefore points exporter auth at a nonexistent Redis user. | EV-0200; XFI-0064 | Preserves F-1272 as open after the first fix attempt |
| CMD-0201 | 2026-05-13 | Sentinel docs-truth follow-up: compared the shipped design note and tabletop drill against `internal/storage/redisclient`, the current Sentinel template, and the authenticated runbook commands | New `F-1273`: the note still presents Sentinel discovery as future `internal/cachekeys` work and omits landed auth/ACL shape, while the tabletop drill keeps an unauthenticated Sentinel CLI command that conflicts with the source-rendered listener `requirepass`. | EV-0201; XFI-0065 | Supports new finding F-1273 |
| CMD-0202 | 2026-05-13 | Wave-107 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=59/open=14`, XFI `fixed=52/open=13`, remediation `fixed=54/open=17`; file coverage is `done=143`, `in_progress=99`, `todo=1660`; tracked-file parity is exact at `tracked=1902`, `rows=1902`; docs lint passes; and the mismatch detector returns no rows. | EV-0202 | Restores full audit-control consistency after reopening F-1266, preserving F-1272, and adding F-1273 |
| CMD-0203 | 2026-05-13 | Post-repair Ansible playbook-doc narrowing pass: reread the top-level Ansible README, Redis role README, and sibling role READMEs for HAProxy/Loki/Patroni/Prometheus after the parallel docs edits | `F-1266` remains open but narrows. The top-level README and Redis role README now admit the missing cluster playbooks are not landed, while `haproxy`, `loki`, `patroni`, and `prometheus` role READMEs still present absent role-specific playbooks as runnable commands. | EV-0203; XFI-0058 | Narrows but preserves F-1266 |
| CMD-0204 | 2026-05-13 | Monitoring design-note falsification pass: compared `docs/architecture/prometheus-ansible-role-design-note.md` against the corrected Alertmanager standalone config, Prometheus role README, severity ladder runbook, and Ansible alertmanager template | Reopens `F-1265`: the runnable docs/configs now agree on `page/ticket/informational`, but the shipped Prometheus role design note still documents `critical/warning/info` severity routes. | EV-0204; XFI-0057 | Reopens F-1265 on the residual shipped design-note drift |
| CMD-0205 | 2026-05-13 | Wave-108 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=57/open=16`, XFI `fixed=51/open=14`, remediation `fixed=53/open=18`; file coverage is `done=143`, `in_progress=103`, `todo=1656`; tracked-file parity is exact at `tracked=1902`, `rows=1902`; docs lint passes; and the mismatch detector returns no rows. | EV-0205 | Restores full audit-control consistency after narrowing F-1266 and reopening F-1265 |
| CMD-0206 | 2026-05-13 | Current-workspace remediation recheck for `F-1265`: reread the Prometheus role design note after the parallel docs edit and compared it against the Alertmanager template plus corrected operator docs | `F-1265` now closes again. The design note's example routes use `page`, `ticket`, and `informational`, and the surrounding text explicitly records the retirement of `critical/warning/info`. | EV-0206; XFI-0057 | Closes F-1265 on the current workspace |
| CMD-0207 | 2026-05-13 | HAProxy companion-runbook falsification pass: compared the HAProxy role README and shipped design note against the tracked `docs/operations/runbooks/` inventory | New `F-1274`: both HAProxy docs point at `docs/operations/runbooks/api-pod-down.md`, but that runbook does not exist in the tracked repository. | EV-0207; XFI-0066 | Supports new finding F-1274 |
| CMD-0208 | 2026-05-13 | Current-workspace narrowing pass for `F-1273`: reread the Redis Sentinel design note and tabletop drill after the parallel docs edits | `F-1273` remains open but narrows. The drill's Sentinel command now includes `-a "$REDIS_PASSWORD"`, while the design note still retains future-state `internal/cachekeys` FailoverClient language despite the live `internal/storage/redisclient` implementation. | EV-0208; XFI-0065 | Narrows but preserves F-1273 |
| CMD-0209 | 2026-05-13 | Wave-109 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=58/open=16`, XFI `fixed=52/open=14`, remediation `fixed=54/open=18`; file coverage is `done=145`, `in_progress=102`, `todo=1655`; tracked-file parity is exact at `tracked=1902`, `rows=1902`; docs lint passes; and the mismatch detector returns no rows. | EV-0209 | Restores full audit-control consistency after closing F-1265, narrowing F-1273, and adding F-1274 |
| CMD-0210 | 2026-05-13 | Current-workspace closure pass for `F-1266`: reread the HAProxy, Loki, Patroni, and Prometheus role READMEs after the moving-doc edits and compared them back to the tracked Ansible playbook inventory | `F-1266` now closes. HAProxy/Loki/Patroni explicitly mark the missing orchestration playbooks as backlog-only commented examples, while Prometheus points at the real `playbooks/monitoring.yml` entrypoint. | EV-0210; XFI-0058 | Closes F-1266 |
| CMD-0211 | 2026-05-13 | Redis exporter auth remediation recheck: compared the ACL-file branch in `redis.conf.j2` and `03-redis-configure.yml` with the current exporter systemd render in `07-monitoring.yml` | `F-1272` now closes. The exporter username flag is gated by `redis_acl_lockdown | bool`, so lockdown-off keeps the legacy password-only path and lockdown-on uses the ACL user that is actually rendered. | EV-0211; XFI-0064 | Closes F-1272 |
| CMD-0212 | 2026-05-13 | Redis Sentinel docs breadth pass: reread the role README after the design-note/drill narrowing and compared both docs against `internal/storage/redisclient` | `F-1273` remains open and widens. The architecture note is still stale, and the shipped role README also says the live Sentinel client plumbing will happen later in `internal/cachekeys`. | EV-0212; XFI-0065 | Widens but preserves F-1273 |
| CMD-0213 | 2026-05-13 | HAProxy companion-runbook remediation recheck: reread the role README and architecture note after the doc edits and compared both against the tracked runbook inventory | `F-1274` now closes. Both docs redirect to tracked `docs/operations/runbooks/api-down.md` guidance and record that the earlier `api-pod-down.md` reference was wrong. | EV-0213; XFI-0066 | Closes F-1274 |
| CMD-0214 | 2026-05-13 | HAProxy/readiness/Redis serving-contract cross-check: compared the HAProxy health-check path, API `redisChecker` readiness wiring, `/v1/readyz` failure semantics, the HA failure matrix, and Redis/API outage runbooks | New `F-1275`: shared Redis loss drives `/v1/readyz` to 503, HAProxy routes only on `/v1/readyz == 200`, and the HA plan says Redis failover makes readyz false, yet maintained Redis outage docs promise degraded-but-serving behavior rather than a public routing outage. | EV-0214; XFI-0067 | Supports new finding F-1275 |
| CMD-0215 | 2026-05-13 | API incident-query selector audit: compared operator docs and metric-source comments against the current multi-host Prometheus template, R1 Prometheus config, and both API alert-rule families | New `F-1276`: real jobs are `ratesengine_api` (multi-host) and `ratesengine-api` (R1), but alerts catalog, API runbooks, SLA probe runbook, and `internal/obs/metrics.go` still publish copy-paste `job="api"` examples. | EV-0215; XFI-0068 | Supports new finding F-1276 |
| CMD-0216 | 2026-05-13 | API-down runbook source-breadcrumb falsification: compared the cited `internal/api/v1/healthz.go` path against tracked files and the live readiness implementation | New `F-1277`: `api-down.md` points at a non-existent `internal/api/v1/healthz.go`; the readiness handler actually lives in `internal/api/v1/server.go`. | EV-0216; XFI-0069 | Supports new finding F-1277 |
| CMD-0217 | 2026-05-13 | Cross-role nftables composition audit: compared HAProxy, Redis Sentinel, Patroni, Prometheus, and Loki firewall drop-ins against the archival-node default-drop template, then checked nftables base-chain ordering/accept semantics from primary documentation | New `F-1278`: the reviewed HA roles all emit separate `priority 0; policy accept` input base chains with accept rules only. That enforces no deny posture by itself and does not reliably compose with the repo's same-hook default-drop base chain, because equal-priority chain order is undefined and accept is not final against later base chains. | EV-0217; XFI-0070 | Supports new finding F-1278 |
| CMD-0218 | 2026-05-13 | Patroni clean-host firewall task-order pass: compared `configs/ansible/roles/patroni/tasks/10-firewall.yml` against the sibling HA role firewall tasks | New `F-1279`: Patroni is the outlier that writes `/etc/nftables.d/40-patroni.conf` before creating `/etc/nftables.d/`, so a clean-host first run can fail at the copy task. | EV-0218; XFI-0071 | Supports new finding F-1279 |
| CMD-0219 | 2026-05-13 | Wave-110 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=59/open=20`, XFI `fixed=55/open=16`, remediation `fixed=57/open=20`; file coverage is `done=152`, `in_progress=111`, `todo=1663`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0219 | Restores full audit-control consistency after closing F-1266/F-1272/F-1274, widening F-1273, adding F-1275-F-1279, and adding codex audit artifacts to inventory |
| CMD-0220 | 2026-05-13 | Patroni etcd-install runnable-defaults pass: compared the etcd `get_url` checksum expression against role defaults and README prerequisites | New `F-1280`: the role falls back to `REPLACE_WITH_RELEASE_SHA` for the etcd checksum, but neither defaults nor README define `etcd_release_sha256`, so the documented clean run is not runnable. | EV-0220; XFI-0072 | Supports new finding F-1280 |
| CMD-0221 | 2026-05-13 | Patroni monitoring dependency pass: compared the installed textfile scraper body against Patroni package install tasks and README prerequisites | New `F-1281`: the scraper requires `jq`, but the Patroni role never installs or documents it, so Patroni textfile metrics can fail on clean hosts. | EV-0221; XFI-0073 | Supports new finding F-1281 |
| CMD-0222 | 2026-05-13 | Patroni DR restore-target pass: compared `patroni_pgbackrest_restore_target` defaults/docs/design-note prose against the actual pgBackRest restore command | New `F-1282`: the advertised latest/time restore target variable is unused; the role always runs an immediate pgBackRest restore. | EV-0222; XFI-0074 | Supports new finding F-1282 |
| CMD-0223 | 2026-05-13 | Wave-111 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=59/open=23`, XFI `fixed=55/open=19`, remediation `fixed=57/open=23`; file coverage is `done=151`, `in_progress=118`, `todo=1657`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0223 | Restores full audit-control consistency after adding F-1280-F-1282 and correcting status-table drift for the currently open findings |
| CMD-0224 | 2026-05-13 | Moving-workspace remediation recheck for HA/Patroni findings: reread all HA firewall drop-ins, Patroni etcd install/defaults/README, Patroni install/monitoring tasks, and Patroni pgBackRest bootstrap command | `F-1279`, `F-1281`, and `F-1282` now close in source. `F-1280` narrows because code preflight-validates `etcd_release_sha256`, but the README still omits the required checksum variable. `F-1278` remains open because the new `priority -100` comments misunderstand nftables: early base-chain accepts can still be dropped by the later default-drop base chain. | EV-0224; XFI-0070; XFI-0071; XFI-0072; XFI-0073; XFI-0074 | Closes three findings, narrows F-1280, and preserves F-1278 |
| CMD-0225 | 2026-05-13 | Timescale primary-down runbook vs Patroni role DCS pass: compared runbook etcd commands against `etcd.conf.j2`, `patroni.yml.j2`, the etcd health task, preflight cluster-size assertion, and shipped Patroni design note | New `F-1283`: the runbook uses HTTPS etcd endpoints, a top-level `/ratesengine/leader` key, and a five-node quorum threshold, while the shipped role renders HTTP etcd URLs, Patroni namespace `/service/` plus per-cluster scope, and an exactly three-node DCS. | EV-0225; XFI-0075 | Supports new finding F-1283 |
| CMD-0226 | 2026-05-13 | Wave-112 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=62/open=21`, XFI `fixed=58/open=17`, remediation `fixed=60/open=21`; file coverage is `done=153`, `in_progress=121`, `todo=1652`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0226 | Restores full audit-control consistency after the Patroni remediation recheck and F-1283 addition |
| CMD-0227 | 2026-05-13 | HAProxy/API readiness remediation recheck: compared `redisChecker.Critical`, `/v1/readyz` critical-vs-non-critical response handling, server regression tests, HAProxy defaults/template/README, and HA plan readiness prose | `F-1275` now closes in runtime source: Redis-only failure returns 200 degraded and is test-covered. New `F-1284` captures the narrower residual docs drift: HAProxy role comments/README and HA plan still say `/v1/readyz` requires Redis reachability. | EV-0227; XFI-0067; XFI-0076 | Closes F-1275 and supports new F-1284 |
| CMD-0228 | 2026-05-13 | Wave-113 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=63/open=21`, XFI `fixed=59/open=17`, remediation `fixed=61/open=21`; file coverage is `done=152`, `in_progress=123`, `todo=1651`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0228 | Restores full audit-control consistency after closing F-1275 and adding F-1284 |
| CMD-0229 | 2026-05-13 | API incident-doc selector/source-breadcrumb remediation recheck: searched maintained API runbooks, alert catalog, API rule files, Prometheus scrape configs, and `internal/obs/metrics.go` for stale `job="api"` and `internal/api/v1/healthz.go` references | `F-1277` now closes because `api-down.md` cites `internal/api/v1/server.go::handleReadyz`. `F-1276` narrows: operator docs now use the real `ratesengine[_-]api` selector family, but `internal/obs/metrics.go` still has a stale `job="api"` source comment. | EV-0229; XFI-0068; XFI-0069 | Closes F-1277 and narrows F-1276 |
| CMD-0230 | 2026-05-13 | Wave-114 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector | Counts now reconcile at findings `fixed=64/open=20`, XFI `fixed=60/open=16`, remediation `fixed=62/open=20`; file coverage is `done=157`, `in_progress=118`, `todo=1651`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0230 | Restores full audit-control consistency after closing F-1277 and narrowing F-1276 |
| CMD-0231 | 2026-05-13 | Monitoring-role upstream binary supply-chain pass: compared Loki server, Promtail agent, Prometheus, and Alertmanager `get_url` tasks against role defaults and READMEs for required checksum variables | New `F-1285`: Loki and Promtail download GitHub release zip files without checksums, while Prometheus and Alertmanager checksum fields are optional and undocumented, leaving the documented clean install path unauthenticated. | EV-0231; XFI-0077 | Supports new finding F-1285 |
| CMD-0232 | 2026-05-13 | Loki S3 credential handoff pass: compared the rendered systemd unit, Loki config comments, role defaults, README storage-backend guidance, and systemd environment-assignment semantics | New `F-1286`: the Loki unit renders `AWS_ACCESS_KEY_ID=${RATESENGINE_S3_ACCESS_KEY}` and `AWS_SECRET_ACCESS_KEY=${RATESENGINE_S3_SECRET_KEY}` even though systemd does not shell-expand `Environment=` values from `EnvironmentFile`, so Loki can receive literal credential strings. | EV-0232; XFI-0078 | Supports new finding F-1286 |
| CMD-0233 | 2026-05-13 | Wave-115 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1285` and `F-1286` | Counts now reconcile at findings `fixed=64/open=22`, XFI `fixed=60/open=18`, remediation `fixed=62/open=22`; file coverage is `done=155`, `in_progress=127`, `todo=1644`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0233 | Restores full audit-control consistency after adding the monitoring supply-chain and Loki credential findings |
| CMD-0234 | 2026-05-13 | Prometheus/Alertmanager bind-target cross-file audit: compared role listen defaults, systemd service flags, generated `prometheus.yml` self-scrape and `alertmanagers` target addresses, and the role README access/security posture | New `F-1287`: the services bind HTTP ports to loopback by default, but the generated Prometheus config targets each prom host's private IP for Prometheus and Alertmanager, so default self-scrape and alert delivery can fail. | EV-0234; XFI-0079 | Supports new finding F-1287 |
| CMD-0235 | 2026-05-13 | Wave-116 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1287` | Counts now reconcile at findings `fixed=64/open=23`, XFI `fixed=60/open=19`, remediation `fixed=62/open=23`; file coverage is `done=155`, `in_progress=129`, `todo=1642`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0235 | Restores full audit-control consistency after adding the Prometheus/Alertmanager bind-target finding |
| CMD-0236 | 2026-05-13 | Prometheus TSDB disk preflight audit: compared the documented 20 GB prerequisite and retention defaults against `tasks/01-preflight.yml` disk-space assertion behavior | New `F-1288`: the role documents a blocking TSDB disk-space assertion, but the corresponding assert task has `ignore_errors: yes`, allowing under-capacity or unparsable hosts to continue. | EV-0236; XFI-0080 | Supports new finding F-1288 |
| CMD-0237 | 2026-05-13 | Wave-117 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1288` | Counts now reconcile at findings `fixed=64/open=24`, XFI `fixed=60/open=20`, remediation `fixed=62/open=24`; file coverage is `done=155`, `in_progress=130`, `todo=1641`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0237 | Restores full audit-control consistency after adding the Prometheus disk-preflight finding |
| CMD-0238 | 2026-05-13 | Moving-workspace recheck for `F-1286`: reread Loki service template, README storage-backend section, defaults, and Loki config comments after the credential-handoff fix landed | `F-1286` now closes in source. The unit no longer renders literal `${RATESENGINE_S3_*}` assignments and the role README/defaults tell operators to put direct `AWS_ACCESS_KEY_ID=` and `AWS_SECRET_ACCESS_KEY=` values in `/etc/default/loki`. | EV-0238; XFI-0078 | Closes F-1286 |
| CMD-0239 | 2026-05-13 | Loki storage-preflight audit: compared `server-01-preflight.yml` against role defaults, README prerequisites, and the shipped Loki design note claims for MinIO bucket/access and local `/var` capacity checks | New `F-1289`: defaults/docs/design note promise or require storage checks, but the server preflight does not verify the `loki-chunks` bucket, MinIO access, or local disk capacity. | EV-0239; XFI-0081 | Supports new finding F-1289 |
| CMD-0240 | 2026-05-13 | Wave-118 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1286` and adding `F-1289` | Counts now reconcile at findings `fixed=65/open=24`, XFI `fixed=61/open=20`, remediation `fixed=63/open=24`; file coverage is `done=155`, `in_progress=132`, `todo=1639`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0240 | Restores full audit-control consistency after the Loki credential closure and storage-preflight finding |
| CMD-0241 | 2026-05-13 | Moving-workspace recheck for `F-1285`, `F-1287`, and `F-1288`: reread monitoring install tasks, Prometheus defaults, firewall task, service templates, README snippets, and Prometheus preflight after parallel fixes landed | `F-1287` and `F-1288` close in source. Prometheus/Alertmanager bind/target topology now matches, and disk-space preflight is blocking. `F-1285` narrows: all four install tasks use required checksums, but READMEs/defaults still do not document the required digest variables. | EV-0241; XFI-0077; XFI-0079; XFI-0080 | Closes F-1287/F-1288 and narrows F-1285 |
| CMD-0242 | 2026-05-13 | Prometheus README network-boundary drift audit after the bind-address remediation: compared README access/firewall prose against defaults and `tasks/06-firewall.yml` | New `F-1290`: source now binds 9090/9093 on all interfaces and opens 9090/9093/9094 to internal CIDRs, but the README still says loopback-only UI and 9094-only firewall. | EV-0242; XFI-0082 | Supports new finding F-1290 |
| CMD-0243 | 2026-05-13 | Wave-119 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1287`/`F-1288`, narrowing `F-1285`, and adding `F-1290` | Counts now reconcile at findings `fixed=67/open=23`, XFI `fixed=63/open=19`, remediation `fixed=65/open=23`; file coverage is `done=161`, `in_progress=126`, `todo=1639`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0243 | Restores full audit-control consistency after the Prometheus moving-fix reconciliation |
| CMD-0244 | 2026-05-13 | Archival-node supply-chain and no-Horizon invariant pass: compared MinIO, `mc`, node_exporter, apt-key `get_url` tasks, defaults pins, and active core-lag runbook wording against ADR-0001/repo invariants | New `F-1291`: archival-node installs MinIO, `mc`, and node_exporter into privileged paths without enforced checksums; `mc` also uses a mutable unversioned URL. New `F-1292`: `core-lag.md` still tells operators to cross-check via SDF Horizon even though ADR-0001 says runbooks never reference Horizon commands. | EV-0244; EV-0245; XFI-0083; XFI-0084 | Supports new findings F-1291 and F-1292 |
| CMD-0245 | 2026-05-13 | Wave-120 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1291` and `F-1292` | Counts now reconcile at findings `fixed=67/open=25`, XFI `fixed=63/open=21`, remediation `fixed=65/open=25`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0246 | Restores full audit-control consistency after the archival-node/no-Horizon pass |
| CMD-0246 | 2026-05-13 | Moving-workspace recheck for `F-1291` and `F-1292`: reread archival-node MinIO/`mc`/node_exporter tasks, archival defaults, and the core-lag runbook after concurrent fixes landed | `F-1292` closes: `core-lag.md` now uses stellar.expert or public stellar-rpc for network cross-checks. `F-1291` narrows: install tasks now require and use checksums, but defaults/operator docs still do not document the required digest variables. | EV-0247; XFI-0083; XFI-0084 | Closes F-1292 and narrows F-1291 |
| CMD-0247 | 2026-05-13 | Wave-121 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1292` and narrowing `F-1291` | Counts now reconcile at findings `fixed=68/open=24`, XFI `fixed=64/open=20`, remediation `fixed=66/open=24`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0248 | Restores full audit-control consistency after the moving archival-node/no-Horizon fix recheck |
| CMD-0248 | 2026-05-13 | Redis exporter and CI curl-to-tar supply-chain pass: compared Redis Sentinel exporter install/defaults/docs/Prometheus scrape wiring, CI promtool/gitleaks installers, action-pinning lint, and repository workflow token permissions | New `F-1293`: Redis Sentinel installs `redis_exporter` from a pinned GitHub tag without checksum before Prometheus relies on it for Redis HA metrics. New `F-1294`: CI pipes Prometheus and gitleaks archives directly into `tar` without digest verification; workflow token default is read-only, narrowing the blast radius. | EV-0249; EV-0250; XFI-0085; XFI-0086 | Supports new findings F-1293 and F-1294 |
| CMD-0249 | 2026-05-13 | Wave-122 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1293` and `F-1294` | Counts now reconcile at findings `fixed=68/open=26`, XFI `fixed=64/open=22`, remediation `fixed=66/open=26`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0251 | Restores full audit-control consistency after Redis exporter and CI installer findings |
| CMD-0250 | 2026-05-13 | CI moving-fix and Redis secret-boundary pass: reread CI tool installer changes, GitHub workflow permissions, Redis exporter systemd render, Redis ACL templates, and Redis/Prometheus scrape coupling | `F-1294` narrows because promtool now verifies `PROM_TARBALL_SHA256`, but gitleaks still pipes to `tar` without using `GITLEAKS_TARBALL_SHA256`. New `F-1295`: Redis exporter systemd unit writes `REDIS_PASSWORD` inline into a 0644 unit file. | EV-0252; EV-0253; XFI-0086; XFI-0087 | Narrows F-1294 and supports new finding F-1295 |
| CMD-0251 | 2026-05-13 | Wave-123 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after narrowing `F-1294` and adding `F-1295` | Counts now reconcile at findings `fixed=68/open=27`, XFI `fixed=64/open=23`, remediation `fixed=66/open=27`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0254 | Restores full audit-control consistency after the CI moving-fix and Redis secret-boundary pass |
| CMD-0252 | 2026-05-13 | Moving-workspace recheck for `F-1293` and `F-1294`: reread Redis Sentinel exporter install/defaults/README and CI tool installer lines after additional fixes landed | `F-1294` now closes because gitleaks downloads to `/tmp/gitleaks.tar.gz`, verifies `GITLEAKS_TARBALL_SHA256` with `sha256sum -c`, and extracts only afterward. `F-1293` narrows: Redis Sentinel now verifies `redis_exporter_release_sha256`, but defaults/README still omit the required digest variable and checksum source. | EV-0255; XFI-0085; XFI-0086 | Closes F-1294 and narrows F-1293 |
| CMD-0253 | 2026-05-13 | Wave-124 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1294` and narrowing `F-1293` | Counts now reconcile at findings `fixed=69/open=26`, XFI `fixed=65/open=22`, remediation `fixed=67/open=26`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0256 | Restores full audit-control consistency after the latest moving supply-chain reconciliation |
| CMD-0254 | 2026-05-13 | Workflow permission and OIDC trust-boundary pass: compared all workflow `permissions:` blocks and `id-token: write` grants against actual OIDC consumers (`ACTIONS_ID_TOKEN_*`, cosign/sigstore, cloud-auth actions, Pages deploy) plus release/deploy docs | New `F-1296`: `release.yml` and `deploy.yml` grant `id-token: write` only for future keyless signing/attestation, while no current step uses OIDC. `api-docs.yml` keeps a legitimate Pages OIDC grant. | EV-0257; XFI-0088 | Supports new finding F-1296 |
| CMD-0255 | 2026-05-13 | Wave-125 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1296` | Counts now reconcile at findings `fixed=69/open=27`, XFI `fixed=65/open=23`, remediation `fixed=67/open=27`; file coverage is `done=164`, `in_progress=126`, `todo=1636`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0258 | Restores full audit-control consistency after the workflow OIDC permission finding |
| CMD-0256 | 2026-05-13 | Deploy SSH trust-boundary pass: compared deploy workflow known_hosts handling, operator deploy docs, R1 deployment-state notes, and Ansible deploy playbooks/tasks | New `F-1297`: the docs make `<REGION>_SSH_KNOWN_HOSTS` the host-key pinning/MITM control, but `deploy.yml` merely warns when `R1_SSH_KNOWN_HOSTS` is unset and trusts a live `ssh-keyscan` result for the same root SSH deploy. | EV-0259; XFI-0089 | Supports new finding F-1297 |
| CMD-0257 | 2026-05-13 | Wave-126 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1297` and correcting table-status drift | Counts now reconcile at findings `fixed=69/open=28`, XFI `fixed=65/open=24`, remediation `fixed=67/open=28`; file coverage is `done=166`, `in_progress=127`, `todo=1633`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0260 | Restores full audit-control consistency after the deploy SSH trust-boundary finding |
| CMD-0258 | 2026-05-13 | Release-artifact doc closure falsification: compared `release.yml`'s current matrix and binary-only comments against `CLAUDE.md`, `release-process.md`, and `r1-deployment-state.md` release artifact claims | Reopens `F-1221`: `release-process.md` step 4 is fixed, but active repo orientation still says releases build `linux/{amd64,arm64}` and push GHCR images, the release-process pipeline summary still says `linux/amd64 + linux/arm64`, and R1 state still says the smoke release had all 12 binaries. | EV-0261; XFI-0014 | Reopens and broadens F-1221 |
| CMD-0259 | 2026-05-13 | Wave-127 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after reopening `F-1221` | Counts now reconcile at findings `fixed=68/open=29`, XFI `fixed=64/open=25`, remediation `fixed=66/open=29`; file coverage is `done=166`, `in_progress=128`, `todo=1632`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0262 | Restores full audit-control consistency after the release-artifact closure falsification |
| CMD-0260 | 2026-05-13 | Manual workflow input injection pass: grepped every `${{ inputs.* }}` and `${{ github.event.inputs.* }}` occurrence in workflows, then reviewed deploy/release run-step contexts against safer env/choice-input patterns in the static-site deploy workflows | New `F-1298`: deploy and release workflows interpolate free-form `workflow_dispatch` inputs directly into shell scripts before validation; explorer/status/docs deploy workflows mainly use choice inputs or env-passed free-form values and do not show the same shell-source injection shape. | EV-0263; XFI-0090 | Supports new finding F-1298 |
| CMD-0261 | 2026-05-13 | Wave-128 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1298` | Counts now reconcile at findings `fixed=68/open=30`, XFI `fixed=64/open=26`, remediation `fixed=66/open=30`; file coverage is `done=166`, `in_progress=128`, `todo=1632`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0264 | Restores full audit-control consistency after the workflow input-injection finding |
| CMD-0262 | 2026-05-13 | Deploy Ansible dependency reproducibility pass: inspected `deploy.yml` Python/Galaxy install steps, `configs/ansible/requirements.yml`, Ansible README install guidance, and deploy playbook module dependencies | New `F-1299`: production deploy resolves mutable Ansible tooling at run time (`ansible-core==2.18.*` plus lower-bound Galaxy collection ranges), so deploy behavior can drift without a repo diff. | EV-0265; XFI-0091 | Supports new finding F-1299 |
| CMD-0263 | 2026-05-13 | Wave-129 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1299` | Counts now reconcile at findings `fixed=68/open=31`, XFI `fixed=64/open=27`, remediation `fixed=66/open=31`; file coverage is `done=167`, `in_progress=128`, `todo=1631`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0266 | Restores full audit-control consistency after the mutable deploy-tooling finding |
| CMD-0264 | 2026-05-13 | Healthchecks SLA-probe sandbox pass: compared `configs/healthchecks/sla-probe.sh`, `ratesengine-sla-probe.service`, the probe binary's textfile error behavior, dedicated `deploy/systemd/sla-probe.service`, SLA alert rules, and SLA runbooks | New `F-1300`: the Healthchecks SLA wrapper defaults to writing `/var/lib/node_exporter/textfile_collector/sla_probe.prom`, but its DynamicUser/ProtectSystem systemd unit lacks `ReadWritePaths`, so the probe exits on textfile-write failure under the default install. | EV-0267; XFI-0092 | Supports new finding F-1300 |
| CMD-0265 | 2026-05-13 | Wave-130 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1300` | Counts now reconcile at findings `fixed=68/open=32`, XFI `fixed=64/open=28`, remediation `fixed=66/open=32`; file coverage is `done=170`, `in_progress=131`, `todo=1625`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0268 | Restores full audit-control consistency after the Healthchecks SLA sandbox finding |
| CMD-0266 | 2026-05-13 | Aggregator metrics-port runbook pass: compared `aggregator-silent.md` against the aggregator listener collision-shift logic, R1 Prometheus targets, Healthchecks heartbeat port mapping, and Prometheus role README scrape-port table | New `F-1301`: R1/default single-host deployments expose aggregator metrics on `9465`, while the P1 aggregator-silent runbook still tells operators to query `localhost:9464`, the indexer endpoint in that topology. | EV-0269; XFI-0093 | Supports new finding F-1301 |
| CMD-0267 | 2026-05-13 | Wave-131 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1301` | Counts now reconcile at findings `fixed=68/open=33`, XFI `fixed=64/open=29`, remediation `fixed=66/open=33`; file coverage is `done=172`, `in_progress=131`, `todo=1623`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0270 | Restores full audit-control consistency after the aggregator metrics-port runbook finding |
| CMD-0268 | 2026-05-13 | Healthchecks smoke failure-mode pass: compared `smoke.sh`, `ratesengine-smoke.service`, `install.sh`, the Healthchecks README contract, and `scripts/dev/r1-smoke.sh` install target | New `F-1302`: if the copied smoke script is missing or non-executable, the wrapper exits `0` before sending a Healthchecks failure, silently disabling the 5-minute API-surface smoke evidence path. | EV-0271; XFI-0094 | Supports new finding F-1302 |
| CMD-0269 | 2026-05-13 | Wave-132 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1302` | Counts now reconcile at findings `fixed=68/open=34`, XFI `fixed=64/open=30`, remediation `fixed=66/open=34`; file coverage is `done=175`, `in_progress=131`, `todo=1620`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0272 | Restores full audit-control consistency after the Healthchecks smoke wrapper finding |
| CMD-0270 | 2026-05-13 | Moving-workspace recheck for `F-1300`: reread the Healthchecks SLA service, wrapper, archival-node textfile collector provisioning, and dedicated SLA systemd unit after the sandbox fix landed | `F-1300` now closes: the Healthchecks SLA service grants `ReadWritePaths=/var/lib/node_exporter/textfile_collector` and joins `SupplementaryGroups=ratesengine`, matching the directory ownership/mode provisioned by the archival-node role. | EV-0273; XFI-0092 | Closes F-1300 |
| CMD-0271 | 2026-05-13 | Healthchecks SLA missing-binary failure-mode pass: compared `sla-probe.sh`, `ratesengine-sla-probe.service`, the Healthchecks installer boundary, and the SLA probe binary path | New `F-1303`: if `/usr/local/bin/ratesengine-sla-probe` is missing or non-executable, the wrapper exits `0` before sending a Healthchecks failure, silently disabling the external SLA proof path. | EV-0274; XFI-0095 | Supports new finding F-1303 |
| CMD-0272 | 2026-05-13 | Wave-133 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1300`, adding `F-1303`, and correcting summary-table drift | Counts now reconcile at findings `fixed=70/open=33`, XFI `fixed=65/open=30`, remediation `fixed=67/open=34`; file coverage is `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0275 | Restores full audit-control consistency after the SLA Healthchecks moving-fix/missing-binary pass |
| CMD-0273 | 2026-05-13 | Healthchecks hardening-guide apply-step pass: compared `docs/operations/pre-launch-hardening.md` with the Healthchecks README/install restart commands and SLA probe service EnvironmentFile wiring | New `F-1304`: the hardening guide lists the SLA-probe URL but omits `ratesengine-sla-probe.timer` from the restart command, so operators can edit the URL without reloading it into the running timer. | EV-0276; XFI-0096 | Supports new finding F-1304 |
| CMD-0274 | 2026-05-13 | Wave-134 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1304` | Counts now reconcile at findings `fixed=70/open=34`, XFI `fixed=65/open=31`, remediation `fixed=67/open=35`; file coverage remains `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0277 | Restores full audit-control consistency after the Healthchecks hardening-guide restart finding |
| CMD-0275 | 2026-05-13 | Live R1 Healthchecks evidence refresh: SSH-inspected Rates Engine timers, enabled/active state, `/opt/ratesengine/healthchecks`, a redacted `/etc/default/ratesengine-healthchecks`, and recent Healthchecks unit journals | R1 has heartbeat and smoke timers active, but `ratesengine-sla-probe.timer` is not installed/found; the Healthchecks directory lacks `sla-probe.sh`; and the redacted env file has no `HEALTHCHECKS_URL_SLA_PROBE` key. | EV-0278; R1-0031 | Reconfirms F-1205 live and supports F-1304's operator-path risk |
| CMD-0276 | 2026-05-13 | Wave-135 moving-workspace reconciliation: synchronized the register/detail/XFI/remediation state after the shared workspace closed multiple docs, workflow, Ansible, and Healthchecks findings; added the missing detailed `F-1205` section; refreshed counts, tracked-file parity, docs lint, and table/detail mismatch checks | Counts now reconcile at findings `fixed=93/open=11`, XFI `fixed=87/open=9`, remediation `fixed=89/open=13`; file coverage remains `done=178`, `in_progress=131`, `todo=1617`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0279 | Restores full audit-control consistency after moving-workspace closure updates and the live Healthchecks evidence refresh |
| CMD-0277 | 2026-05-13 | Live R1 SLA timer verification: SSH-inspected `ratesengine-sla-probe.timer` enabled/active state, timer schedule, wrapper/binary presence, redacted `HEALTHCHECKS_URL_SLA_PROBE`, SLA textfile output, and recent service journal | The SLA timer rollout is now live: timer enabled/active, wrapper and binary executable, URL key present redacted, and `sla_probe.prom` written after a successful service run. The textfile reports `ratesengine_sla_probe_unit_failed 1` and `price` freshness `186.574s`, so the live SLA proof is failing even though the timer exists. | EV-0280; R1-0032; XFI-0003; XFI-0097 | Closes F-1205 rollout and supports new live SLA finding F-1305 |
| CMD-0278 | 2026-05-13 | Wave-136 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1205` and adding `F-1305` | Counts now reconcile at findings `fixed=94/open=11`, XFI `fixed=88/open=9`, remediation `fixed=90/open=13`; file coverage is `done=185`, `in_progress=131`, `todo=1610`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0281 | Restores full audit-control consistency after the live SLA timer verification/failing-verdict split |
| CMD-0279 | 2026-05-13 | Price-staleness alert producer pass: inspected `cmd/ratesengine-sla-probe`, `internal/api/v1/price.go`, `internal/obs/metrics.go`, API alert rules, `price-stale.md`, R1 `/v1/price`, R1 `/v1/status`, API `/metrics`, and Prometheus instant queries for `ratesengine_price_staleness_seconds` | New `F-1306`: R1 serves a stale `native/fiat:USD` price with `flags.stale=true`, but `ratesengine_price_staleness_seconds` has no live series; Prometheus alert query is empty; and source confirms the handler intentionally does not emit the metric while no alternate producer exists. | EV-0282; R1-0033; XFI-0098 | Supports new finding F-1306 |
| CMD-0280 | 2026-05-13 | Wave-137 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after adding `F-1306` | Counts now reconcile at findings `fixed=94/open=12`, XFI `fixed=88/open=10`, remediation `fixed=90/open=14`; file coverage is `done=186`, `in_progress=132`, `todo=1608`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0283 | Restores full audit-control consistency after the dead price-stale alert finding |
| CMD-0281 | 2026-05-13 | Hosted GitHub security posture refresh: queried branch `main`, branch protection, repository vulnerability alerts, Dependabot alerts, Actions permissions, selected-actions permissions, workflow token permissions, and production environment protection rules | Existing hosted-control findings remain open: `main.protected=false`; branch protection API fails with the current private-plan feature message; repository vulnerability alerts and Dependabot alerts are disabled; Actions allows all actions/workflows; workflow token default is read-only; and `r1`, docs, explorer, status, and GitHub Pages environments have empty protection rules with admin bypass enabled. | EV-0284; XFI-0005; XFI-0010 | Reconfirms F-1207, F-1214, F-1215, and F-1216 |
| CMD-0282 | 2026-05-13 | Wave-138 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after the hosted GitHub posture refresh | Counts remain findings `fixed=94/open=12`, XFI `fixed=88/open=10`, remediation `fixed=90/open=14`; file coverage remains `done=186`, `in_progress=132`, `todo=1608`; docs lint passes; and the mismatch detector returns no rows. | EV-0285 | Restores full audit-control consistency after the hosted-control evidence refresh |
| CMD-0283 | 2026-05-13 | SLA textfile scrape-chain pass: queried R1 Prometheus for `ratesengine_sla_probe_unit_failed` and `ratesengine_sla_probe_freshness_sec`, inspected live node_exporter flags, scraped node_exporter metrics directly, and compared against the archival-node role/source docs that promise textfile collector wiring | New `F-1307`: R1 has `sla_probe.prom`, but node_exporter is running without `--collector.textfile.directory`, node_exporter exposes no `ratesengine_sla_probe_*` samples, and Prometheus has empty vectors for the SLA probe series. | EV-0286; R1-0034; XFI-0099 | Supports new finding F-1307 |
| CMD-0284 | 2026-05-13 | Moving-workspace verification for `F-1306`/`F-1307`: inspected the new source-side `PriceStalenessSeconds` producer, checked live R1 node_exporter flags and SLA series through node_exporter/Prometheus, and checked live Prometheus plus aggregator metrics for `ratesengine_price_staleness_seconds` | `F-1307` closes: live node_exporter now has the textfile collector flag and Prometheus returns SLA probe verdict/freshness samples. `F-1306` remains open on live R1: source now has a bounded aggregator producer, but R1 Prometheus and live aggregator metrics still have no `ratesengine_price_staleness_seconds` series. | EV-0287; R1-0035; XFI-0098; XFI-0099 | Closes F-1307 and narrows F-1306 to deployment/live-proof gap |
| CMD-0285 | 2026-05-13 | Wave-139 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, rechecked tracked-file parity against `git ls-files`, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1307` and narrowing `F-1306` | Counts now reconcile at findings `fixed=95/open=12`, XFI `fixed=89/open=10`, remediation `fixed=91/open=14`; file coverage is `done=186`, `in_progress=133`, `todo=1607`; tracked-file parity is exact at `tracked=1926`, `rows=1926`; docs lint passes; and the mismatch detector returns no rows. | EV-0288 | Restores full audit-control consistency after the SLA textfile scrape-chain closure |
| CMD-0286 | 2026-05-13 | Refreshed R1 SSE lifetime verification: ran 68-second loopback and public `curl -skN` probes against `/v1/price/tip/stream?asset=native&quote=fiat:USD` from R1 | Both streams stayed open until the client-side 68-second timeout (`EXIT:28`, `HTTP:200`) and emitted `tip_update` frames plus keepalives. The earlier 30.443s server reset no longer reproduces. | EV-0289; R1-0036; XFI-0020 | Closes F-1228 live SSE cutoff |
| CMD-0287 | 2026-05-13 | Live price-staleness metric verification: queried R1 aggregator `:9465/metrics` and Prometheus for `ratesengine_price_staleness_seconds`, then queried the alert threshold expression `ratesengine_price_staleness_seconds > 120` | `F-1306` closes live: aggregator and Prometheus now expose bounded `ratesengine_price_staleness_seconds` series for `crypto:BTC`, `crypto:ETH`, `crypto:XLM`, and `native`; the threshold query is empty because values are currently `0`, not because the series is absent. | EV-0290; R1-0037; XFI-0098 | Closes F-1306 |
| CMD-0288 | 2026-05-13 | Wave-140 control reconciliation: recomputed findings/XFI/remediation terminal-status counts, recomputed file-coverage status totals, reran `./scripts/ci/lint-docs.sh`, and reran the findings-table vs detailed-section status mismatch detector after closing `F-1228` and `F-1306` | Counts now reconcile at findings `fixed=98/open=9`, XFI `fixed=91/open=8`, remediation `fixed=93/open=12`; file coverage remains `done=186`, `in_progress=133`, `todo=1607`; docs lint passes; and the mismatch detector returns no rows. | EV-0291 | Restores full audit-control consistency after the live SSE and price-staleness metric closures |
