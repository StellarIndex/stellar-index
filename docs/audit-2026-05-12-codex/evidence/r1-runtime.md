# R1 Runtime Evidence

R1 runtime checks are allowed by the user. The entries below are
read-only observations captured during audit execution on 2026-05-12.

## Rules

- read-only commands first
- no private keys, tokens, or secret values in evidence
- redact sensitive output
- source review remains mandatory

## Register

| ID | Date | Host | Command | Purpose | Result Summary | Redactions | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1-0001 | 2026-05-12 | `ratesengine-archival-r1` | `hostname; date -u; systemctl status ratesengine-api ratesengine-aggregator ratesengine-indexer` | Confirm core service state | API, aggregator, and indexer were active/running. API uptime was about 10h29m. Recent API logs included `/v1/coins` 200, `/v1/assets/native` 200, and `/v1/markets` 200 with multi-second latencies. Indexer logs showed repeated Coingecko 429 throttling and Kraken reconnects. | No secrets captured | EV-0012; F-1202; F-1208 |
| R1-0002 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl list-timers --no-pager` | Timer inventory | Only `ratesengine-smoke.timer` and `ratesengine-heartbeat@.timer` from the Rates Engine timer family appeared active. SLA, archive-verify, archive-completeness, and supply-snapshot timers were absent from the active timer list. | none | F-1205 |
| R1-0003 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl list-unit-files --no-pager --plain \| grep -E "ratesengine\|sla\|verify\|archive\|supply"` | Installed unit inventory | Installed Rates Engine units were limited to API, aggregator, indexer, heartbeat, and smoke service/timer units. `sla-probe.timer`, `verify-archive-tier-a.timer`, `supply-snapshot.timer`, and `archive-completeness.timer` were not installed. | none | F-1205 |
| R1-0004 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl status ratesengine-sla-probe.timer sla-probe.timer verify-archive-tier-a.timer supply-snapshot.timer archive-completeness.timer` | Confirm missing operational timers | All queried SLA/archive/supply timers returned `Unit ... could not be found.` | none | F-1205 |
| R1-0005 | 2026-05-12 | `ratesengine-archival-r1` | `ss -tlnp` | Listening socket inventory | Public/wildcard listeners included `*:443`, `*:80`, `*:3100` Loki, `*:6061` Galexie admin, `*:9000` MinIO, `*:9001` MinIO console, `*:9080` Promtail, `*:9090` Prometheus, `*:9100` node_exporter, `*:9096` Loki grpc, plus `0.0.0.0:11726`. Loopback listeners included API/Postgres/Redis and metrics ports. | PIDs/service names retained; no secret values | F-1201 |
| R1-0006 | 2026-05-12 | `ratesengine-archival-r1` | `nft list ruleset; systemctl status nftables ufw; ufw status verbose` | Firewall posture | `nft list ruleset` returned an empty ruleset; `nftables.service` was disabled and inactive; `ufw.service` was enabled but `ufw status verbose` reported `Status: inactive`. | none | F-1201 |
| R1-0007 | 2026-05-12 | `ratesengine-archival-r1` | `df -h; free -h` | Host capacity snapshot | `/var/lib/minio` was 6.4T total, 4.9T used, 78% full. Memory was 188Gi total, 179Gi used, 8.4Gi available. Swap was 4.0Gi total, 4.0Gi used. | none | F-1209 |
| R1-0008 | 2026-05-12 | external workstation to `136.243.90.96` | `nc -vz -w2 ...` | External reachability proof | TCP connects succeeded externally to ports 9000, 9001, 9090, 3100, 9080, 9100, 6061, and 11726. | none | EV-0019; F-1201 |
| R1-0009 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://localhost:3000/v1/healthz`; `curl http://localhost:3000/v1/readyz` | API health/ready truth | Health returned `ok`; readiness returned `ok` with postgres/redis true, despite R1 having missing timers, public infra ports, source-stopped alerts, p99 latency alert, and memory alert. | none | F-1210 |
| R1-0010 | 2026-05-12 | `ratesengine-archival-r1` | `curl -sS http://localhost:9090/api/v1/alerts` | Alert truth | Firing alerts included `ratesengine_ingestion_source_stopped` for `ecb`, `soroswap`, `band`, `phoenix`; pending alerts included `comet`, `blend`, `redstone`; `ratesengine_api_latency_p99_high` fired with API p99 about 3.325s; `ratesengine_host_memory_high` fired at about 95.41%. Deadmansswitch was firing as designed. | Alert payload only; no secrets | F-1208; F-1209; F-1210 |
| R1-0011 | 2026-05-12 | `ratesengine-archival-r1` | `grep -nE "enable_stablecoin_fiat_proxy|min_usd_volume|usd_pegged_classic_assets" /etc/ratesengine.toml`; `sed -n '44,62p' /etc/ratesengine.toml` | Non-secret aggregator/trades config check | R1 has `[aggregate] enable_stablecoin_fiat_proxy = true`, `min_usd_volume = 0`, and `[trades] usd_pegged_classic_assets` containing Circle USDC. The config comment says the default is 10000 but R1 disables it because on-chain micro-volume XLM/USDC trades are current data. | no secret values present in the selected lines | F-1213 |
| R1-0012 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://127.0.0.1:3000/v1/auth/sep10/challenge?...` | SEP-10 runtime configuration | R1 returned 503 with problem type `https://api.ratesengine.net/errors/sep10-unavailable` and title `SEP-10 not configured`; no signing seed/validator is wired on R1. | request account was a public G-address test value; no secrets | EV-0027 |
| R1-0013 | 2026-05-12 | `ratesengine-archival-r1` | `sudo -u postgres psql -d ratesengine -tA -c "SELECT version, dirty FROM schema_migrations ORDER BY version DESC LIMIT 1"; ls /usr/local/share/ratesengine/migrations; cat /var/lib/ratesengine/deployed-versions/*` | Schema/deployed-version coupling | Database schema reported `28|f`. Deployed-version sidecars reported API, aggregator, and indexer at `v0.5.0-rc.47`; ops at `v0.5.0-rc.29`; sla-probe at `v0.5.0-rc.17`. `/usr/local/share/ratesengine/migrations` produced no migration-file listing in this probe. | no DSN or secret values printed | EV-0031; F-1220 |
| R1-0014 | 2026-05-12 | `ratesengine-archival-r1` and external workstation | `grep ... /etc/ratesengine.toml`; `sed -n '1,135p' /etc/caddy/Caddyfile`; `curl -skI https://api.ratesengine.net/metrics`; `curl -sk https://api.ratesengine.net/metrics` | Live Caddy/API request-boundary posture | R1 API listens on `127.0.0.1:3000`, trusts proxy CIDR `127.0.0.1/32`, and uses `auth_mode=apikey_optional`. Live Caddy forwards `X-Forwarded-For {remote_host}` and has no Cloudflare trusted-proxy block or `/metrics` guard. Public `/metrics` returned HTTP 200 and Prometheus metric text. | Cloudflare API token reference appears as an env placeholder only; no token value captured | EV-0033; F-1223; F-1224 |
| R1-0015 | 2026-05-12 | external workstation | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?...'`; `curl -sk 'https://api.ratesengine.net/v1/chart?...'` | Historical XLM/USD fallback behavior | `native/fiat:USD` since-inception history returned `points: []`; `native/fiat:USD` chart for 1w/1d returned six populated daily points with `flags.triangulated=true`; direct `native/USDC-GA5ZSEJY...` since-inception history returned nine populated daily points. | no secrets | EV-0035; F-1225 |
| R1-0016 | 2026-05-12 | `ratesengine-archival-r1` | `timeout 45 curl -sN --max-time 42 http://127.0.0.1:3000/v1/price/tip/stream?asset=native&quote=fiat:USD ...` | Live SSE connection lifetime | The loopback stream emitted `:connected` and `tip_update` at elapsed 0s, then `tip_update` at 5s, 10s, 15s, 20s, and 25s, with a `:keepalive` at 15s. The stream ended at `elapsed 30`, matching the API server's 30s write timeout. | no secrets | EV-0038; F-1228 |
| R1-0017 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://127.0.0.1:3000/v1/history/since-inception?asset=native&quote=USDC-GA5Z...&granularity=1d | jq ...` | Direct since-inception coverage depth | The direct XLM/Circle-USDC daily history returned `count=9`, first point `2026-05-03T00:00:00Z`, last point `2026-05-11T00:00:00Z`, and `as_of=2026-05-12T14:09:49Z`. | no secrets | EV-0040; F-1230 |
| R1-0018 | 2026-05-12 | `ratesengine-archival-r1` | `curl /v1/price?...USDC...`; `curl /v1/price/batch?...USDC...`; `curl /v1/assets/USDC-GA5Z...` | Stablecoin USD price parity | Asset detail returned `price_usd=0.9999838427` for Circle USDC. Single `/v1/price` for the same asset/quote returned 404. GET and POST batch returned empty `data:[]`. | no secrets | EV-0042; F-1232 |
| R1-0019 | 2026-05-12 | external workstation + `ratesengine-archival-r1` | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=fiat:USD&granularity=1d'`; `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=USDC-GA5ZSEJY...&granularity=1d'`; `sed -n '52,64p' /etc/ratesengine.toml` | Refreshed XLM/USD since-inception fallback truth | Public `native/fiat:USD` still returned `points: []`. Direct `native/USDC-GA5ZSEJY...` returned nine populated daily points. Live config still has `enable_stablecoin_fiat_proxy = true` and `[trades].usd_pegged_classic_assets` containing Circle USDC. | no secrets | EV-0140; F-1225 |
| R1-0020 | 2026-05-12 | `ratesengine-archival-r1` | loopback Python wrapper around `curl -sN --http1.1 --max-time 68 'http://127.0.0.1:3000/v1/price/tip/stream?asset=native&quote=fiat:USD&window_seconds=60'` | Refreshed SSE lifetime proof after the source-side write-deadline reset landed | The stream emitted `:connected`, one initial `tip_update`, and a `:keepalive` at 15.0s, then terminated at elapsed `30.0` seconds with curl exit 18 despite a 68-second client ceiling. | no secrets | EV-0141; F-1228 |
| R1-0021 | 2026-05-12 | `ratesengine-archival-r1` | `grep -n "signup_require_email_verification" /etc/ratesengine.toml || true` | Live config posture for the new signup verification gate | No matching config line was present, so R1 has no explicit override for the new gate key. Combined with the code/config default of `false`, the email-verification gate is not proven enabled on R1. | no secrets | EV-0146; F-1218 |
| R1-0022 | 2026-05-12 | external workstation | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=fiat:USD&granularity=1d'`; `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=USDC-GA5ZSEJY...&granularity=1d'` | Fresh public-history parity check after wave 54 | `native/fiat:USD` still returned `points: []`; direct `native/USDC-GA5ZSEJY...` returned nine populated daily points from `2026-05-03` through `2026-05-11`. | no secrets | EV-0166; F-1225 |
| R1-0023 | 2026-05-12 | external workstation | `curl -skN --max-time 68 'https://api.ratesengine.net/v1/price/tip/stream?asset=native&quote=fiat:USD' -w 'ELAPSED:%{time_total} EXIT:%{exitcode}'` | Fresh public SSE lifetime check after wave 54 | Stream emitted `:connected`, multiple `tip_update` frames, and a keepalive, then ended at `ELAPSED:30.251033 EXIT:92` before the 68-second client ceiling. | no secrets | EV-0166; F-1228 |
| R1-0024 | 2026-05-13 | external workstation + `ratesengine-archival-r1` | `nc -vz -w2 136.243.90.96 11726`; SSH `ss -ltnp | grep ':11726 '`; SSH `grep -n '11726' /etc/nftables.conf` | Refreshed captive-core/public-ingress posture | Public `11726/tcp` still accepts; `stellar-core` still listens on `0.0.0.0:11726`; nftables still accepts `{11625,11626,11725,11726}`. | no secrets | EV-0172; F-1201 |
| R1-0025 | 2026-05-13 | `ratesengine-archival-r1` | SSH `systemctl is-enabled sla-probe.timer archive-completeness.timer verify-archive-tier-a.timer supply-snapshot.timer` | Refreshed evidence-timer rollout state | `sla-probe.timer=disabled`; `archive-completeness.timer=enabled`; `verify-archive-tier-a.timer=enabled`; `supply-snapshot.timer=enabled`. | no secrets | EV-0172; F-1205 |
| R1-0026 | 2026-05-13 | `ratesengine-archival-r1` | SSH `grep -n 'signup_require_email_verification' /etc/ratesengine.toml || true` | Refreshed signup verification live-config posture | No config key is present; the email-verification gate remains unproven live because config falls back to default `false`. | no secrets | EV-0172; F-1218 |
| R1-0027 | 2026-05-13 | external workstation | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=fiat:USD&granularity=1d'`; `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=USDC-GA5ZSEJY...&granularity=1d'` | Refreshed public history parity + deployed coverage depth | `native/fiat:USD` still returned `points: []` at `as_of=2026-05-13T06:33:58.949423013Z`; direct native/Circle-USDC still returned nine daily points from `2026-05-03` through `2026-05-11` at `as_of=2026-05-13T06:33:58.950387268Z`. | no secrets | EV-0173; F-1225; F-1230 |
| R1-0028 | 2026-05-13 | external workstation | `curl -skN --max-time 68 'https://api.ratesengine.net/v1/price/tip/stream?asset=native&quote=fiat:USD' -w 'ELAPSED:%{time_total} EXIT:%{exitcode}'` | Refreshed public SSE lifetime check | Stream emitted `:connected`, multiple `tip_update` frames, and a keepalive, then ended at `ELAPSED:30.443053 EXIT:92` before the 68-second client ceiling. | no secrets | EV-0173; F-1228 |
| R1-0029 | 2026-05-13 | `ratesengine-archival-r1` | SSH Prometheus `/api/v1/alerts` snapshot plus loopback `curl -sk http://127.0.0.1:3000/v1/status` | Refreshed source-health / customer-status truth | Prometheus shows firing anomaly/supply/cache/latency/memory alerts, firing ECB external-poller stale, and pending Redstone source-stopped. `/v1/status` reports `overall=degraded`, `active_sources=12`, `total_sources=17`, and eight active incidents. | no secrets | EV-0175; F-1208 |
| R1-0030 | 2026-05-13 | `ratesengine-archival-r1` | SSH `free -m`; `swapon --show`; `df -h / /var/lib/minio` | Refreshed host-capacity posture | Memory shows 181,943 MiB used / 192,822 MiB total; swap shows 20,451 MiB used / 20,474 MiB total; `/var/lib/minio` shows 4.9T used of 6.4T (78%). | no secrets | EV-0175; F-1209 |
| R1-0031 | 2026-05-13 | `ratesengine-archival-r1` | SSH `systemctl list-timers --all 'ratesengine-*'`; `systemctl is-enabled/is-active ratesengine-heartbeat@*.timer ratesengine-smoke.timer ratesengine-sla-probe.timer`; redacted `/etc/default/ratesengine-healthchecks`; recent Healthchecks journal | Refreshed Healthchecks timer and URL state | R1 lists four Rates Engine Healthchecks timers: heartbeat indexer/api/aggregator and smoke. `ratesengine-sla-probe.timer` is `not-found`/inactive, `/opt/ratesengine/healthchecks` contains only `heartbeat.sh`, `smoke.sh`, and `r1-smoke.sh`, and the redacted env file has indexer/aggregator/api/smoke URLs but no `HEALTHCHECKS_URL_SLA_PROBE` key. | Healthchecks URLs redacted; no secret values captured | EV-0278; F-1205; F-1304 |
| R1-0032 | 2026-05-13 | `ratesengine-archival-r1` | SSH `systemctl is-enabled/is-active ratesengine-sla-probe.timer`; `systemctl list-timers --all ratesengine-sla-probe.timer`; `ls -l` wrapper/binary/textfile; redacted `HEALTHCHECKS_URL_SLA_PROBE`; `tail` SLA textfile; recent journal | Verified SLA timer rollout and current SLA probe verdict | `ratesengine-sla-probe.timer` is enabled/active with a recent successful service run, `/opt/ratesengine/healthchecks/sla-probe.sh` and `/usr/local/bin/ratesengine-sla-probe` are executable, `HEALTHCHECKS_URL_SLA_PROBE` is present redacted, and `sla_probe.prom` exists. The same textfile reports `ratesengine_sla_probe_unit_failed 1` and `ratesengine_sla_probe_freshness_sec{endpoint="price"} 186.574`, above the documented 30s freshness target. | Healthchecks URL redacted; no secret values captured | EV-0280; F-1205; F-1305 |
| R1-0033 | 2026-05-13 | `ratesengine-archival-r1` | SSH loopback `curl` for `/v1/price`, `/v1/status`, API `/metrics`, and Prometheus instant queries for `ratesengine_price_staleness_seconds`, `ratesengine_price_staleness_seconds > 120`, and `up{job="ratesengine-api"}` | Price stale alert producer check | `/v1/price?asset=native&quote=fiat:USD` returned `flags.stale=true` with `observed_at=2026-05-13T12:00:00Z`; `/v1/status` was degraded but did not include `ratesengine_api_price_stale`; API `/metrics` exposed request metrics but no `ratesengine_price_staleness_seconds`; Prometheus returned empty vectors for the metric and alert expression while `up{job="ratesengine-api"}=1`. | no secrets | EV-0282; F-1306 |
| R1-0034 | 2026-05-13 | `ratesengine-archival-r1` | SSH Prometheus instant queries for `ratesengine_sla_probe_unit_failed` and `ratesengine_sla_probe_freshness_sec`; `ps -ef` for node_exporter; node_exporter `/metrics` grep for `node_textfile` and `ratesengine_sla_probe` | SLA textfile scrape path check | Prometheus returns empty vectors for SLA probe verdict/freshness metrics. Live node_exporter runs with `--collector.systemd --collector.processes` only, without `--collector.textfile.directory=/var/lib/node_exporter/textfile_collector`; node_exporter exposes `node_textfile_scrape_error 0` but no `ratesengine_sla_probe_*` samples. | no secrets | EV-0286; F-1307 |
| R1-0035 | 2026-05-13 | `ratesengine-archival-r1` | SSH live node_exporter flag check; node_exporter scrape for `ratesengine_sla_probe_*`; Prometheus instant queries for SLA probe verdict/freshness; Prometheus and aggregator metrics queries for `ratesengine_price_staleness_seconds` | Post-remediation monitoring-chain verification | node_exporter now runs with `--collector.textfile --collector.textfile.directory=/var/lib/node_exporter/textfile_collector`; node_exporter and Prometheus both expose `ratesengine_sla_probe_unit_failed=1` and `ratesengine_sla_probe_freshness_sec{endpoint="price"}=206.709`. Prometheus and live aggregator metrics still have no `ratesengine_price_staleness_seconds` series, so F-1306 remains source-fixed but not live-closed. | no secrets | EV-0287; F-1306; F-1307 |
| R1-0036 | 2026-05-13 | `ratesengine-archival-r1` | SSH `curl -skN --max-time 68` against loopback and public `/v1/price/tip/stream?asset=native&quote=fiat:USD` | Refreshed SSE lifetime verification | Both loopback and public streams held open until the 68-second client timeout (`EXIT:28`, `HTTP:200`) and emitted `tip_update` frames plus keepalives. Neither reproduced the earlier server-side reset at ~30.4s (`EXIT:92`). | no secrets | EV-0289; F-1228 |
| R1-0037 | 2026-05-13 | `ratesengine-archival-r1` | SSH aggregator `/metrics` grep for `ratesengine_price_staleness_seconds`; Prometheus instant queries for `ratesengine_price_staleness_seconds` and `ratesengine_price_staleness_seconds > 120` | Post-deploy price-staleness metric verification | Live aggregator `:9465/metrics` exposes `ratesengine_price_staleness_seconds` for `crypto:BTC`, `crypto:ETH`, `crypto:XLM`, and `native`, all at `0`. Prometheus returns the same four series under `job="ratesengine-aggregator"`; the `> 120` alert-threshold query is empty because current values are not stale, not because the series is absent. | no secrets | EV-0290; F-1306 |
| R1-0038 | 2026-05-13 | `ratesengine-archival-r1` | SSH loopback `curl` for `native/fiat:USD` and direct `native/USDC-GA5ZSEJY...` `/v1/history/since-inception?granularity=1d`, plus `/v1/healthz` | Post-deploy since-inception fallback verification | The USD fallback path and direct Circle-USDC query both return 10 matching daily buckets from `2026-05-03` through `2026-05-12`; the previously empty `native/fiat:USD` series no longer reproduces. `/v1/healthz` reports `status=ok`. | no secrets | EV-0292; F-1225 |
| R1-0039 | 2026-05-13 | `ratesengine-archival-r1` | SSH Prometheus SLA queries, SLA textfile tail, loopback `/v1/price`, `/v1/status`, Prometheus alerts, `free -m`, `swapon --show`, and `df -h / /var/lib/minio` | Refreshed SLA/capacity/status snapshot | The SLA probe still fails (`unit_failed=1`, price freshness `83.196s`, stale price response observed); `/v1/status` is degraded with eight active incidents; memory alert is firing at `96.65%`; swap is `18039/20474 MiB` used; MinIO is `4.9T/6.3T` (`78%`). | no secrets | EV-0294; F-1209; F-1305 |
| R1-0040 | 2026-05-13 | `ratesengine-archival-r1` | SSH loopback `/v1/price`, aggregator `:9465/metrics`, and Prometheus instant queries for `ratesengine_price_staleness_seconds` plus `> 30` threshold | Price-staleness metric truth check | `/v1/price?asset=native&quote=fiat:USD` returned `flags.stale=true` with `observed_at=2026-05-13T12:45:00Z`, while aggregator metrics and Prometheus both reported `ratesengine_price_staleness_seconds{asset="native"} 0`; the threshold query returned empty. | no secrets | EV-0296; F-1308 |
| R1-0041 | 2026-05-13 | `ratesengine-archival-r1` | SSH loopback history queries for `native/fiat:USD` and direct `native/USDC-GA5ZSEJY...`, summarized by Python count/first/last/gap scan | Refreshed historical-depth verification | Both USD fallback and direct Circle-USDC daily since-inception series return 84 points, first `2026-02-12`, last `2026-05-12`, with a gap `2026-04-26->2026-05-03`; coverage remains below the one-year/inception target. | no secrets | EV-0300; F-1230 |
| R1-0042 | 2026-05-13 | `ratesengine-archival-r1` | SSH `systemctl list-units --all '*sla-probe*'`; Redis key probes for documented and actual native/fiat:USD price cache keys | SLA freshness runbook command truth | R1 has `ratesengine-sla-probe.service` inactive/dead and `ratesengine-sla-probe.timer` active/waiting; legacy `sla-probe.service` is loaded failed. Redis `price:native:fiat:USD` is empty, while `vwap:native:fiat:USD:300`, `:3600`, and `:86400` exist. | no secrets | EV-0302; F-1309 |
| R1-0043 | 2026-05-13 | `ratesengine-archival-r1` | SSH aggregator `:9465/metrics` and Prometheus instant query for `ratesengine_price_staleness_seconds` after local source-side staleness-label patch was observed | F-1308 live recheck | R1 still reports `ratesengine_price_staleness_seconds{asset="native"} 0` in both aggregator metrics and Prometheus; no live closure evidence yet. | no secrets | EV-0304; F-1308 |
